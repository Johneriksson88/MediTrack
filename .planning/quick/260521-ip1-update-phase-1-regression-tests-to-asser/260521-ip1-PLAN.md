---
phase: quick-260521-ip1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/test/admin.ping.test.ts
  - apps/api/test/auth.flow.smoke.test.ts
  - apps/api/test/auth.me.test.ts
autonomous: true
requirements:
  - AUTH-04
  - AUTH-05
  - AUTH-06
  - MED-01
must_haves:
  truths:
    - "`pnpm --filter @meditrack/api exec vitest run` exits 0 against the live Docker Postgres stack at localhost:5432."
    - "All three updated regression tests assert the post-Phase-2 permissions[] arrays exactly as emitted by `actionsForRole(role)`."
    - "No non-test files are modified. The PERMISSIONS map, /me route, and userService remain byte-identical."
  artifacts:
    - path: apps/api/test/admin.ping.test.ts
      provides: "Updated /me permissions[] regression block — admin, sjuksköterska, apotekare each assert the post-Phase-2 array shape."
    - path: apps/api/test/auth.flow.smoke.test.ts
      provides: "Updated ROLE_MATRIX.expectedPermissions for all three roles."
    - path: apps/api/test/auth.me.test.ts
      provides: "Updated admin permissions array in the meResponse-shape assertion."
  key_links:
    - from: apps/api/test/*.test.ts
      to: apps/api/src/auth/permissions.ts
      via: "/me response → actionsForRole(role) → Object.entries(PERMISSIONS) declaration order"
      pattern: "expect\\(.*permissions\\)\\.toEqual\\("
---

<objective>
Update three Phase 1 regression tests so their `permissions[]` assertions match the post-Phase-2 PERMISSIONS map. Phase 2 (commit 33deb19) widened `apps/api/src/auth/permissions.ts` with four medication actions but the API vitest suite was not re-run, leaving Phase 1 regression assertions stale. The /me endpoint now returns medication permissions for every role, and the three named tests still assert the pre-Phase-2 shape.

Purpose: Restore the API vitest suite to green so the Phase 1 regression backstop continues to guard the auth flow.

Output: Three updated test files; a single atomic commit; `pnpm --filter @meditrack/api exec vitest run` exits 0.

Mechanical edits only. Do NOT change PERMISSIONS semantics, route behavior, the userService, or any non-test code.
</objective>

<execution_context>
@C:/Projekt/MediTrack/.claude/get-shit-done/workflows/execute-plan.md
@C:/Projekt/MediTrack/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@apps/api/src/auth/permissions.ts
@apps/api/test/admin.ping.test.ts
@apps/api/test/auth.flow.smoke.test.ts
@apps/api/test/auth.me.test.ts

<interfaces>
<!-- The single source of truth that drives every expected array below. -->
<!-- Executor: do NOT re-derive these arrays — copy them verbatim. -->

From apps/api/src/auth/permissions.ts (declaration order in PERMISSIONS):
```ts
'admin:ping':        ['admin']
'medication:read':   ['apotekare', 'sjukskoterska', 'admin']
'medication:create': ['apotekare', 'admin']
'medication:update': ['apotekare', 'admin']
'medication:delete': ['apotekare', 'admin']
```

`actionsForRole(role)` iterates `Object.entries(PERMISSIONS)` in declaration order and
filters by membership, yielding (verbatim — these are the only allowed values):

- `actionsForRole('apotekare')` →
  `['medication:read', 'medication:create', 'medication:update', 'medication:delete']`

- `actionsForRole('sjukskoterska')` →
  `['medication:read']`

- `actionsForRole('admin')` →
  `['admin:ping', 'medication:read', 'medication:create', 'medication:update', 'medication:delete']`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update the three regression test files to assert the post-Phase-2 permissions[] arrays</name>
  <files>apps/api/test/admin.ping.test.ts, apps/api/test/auth.flow.smoke.test.ts, apps/api/test/auth.me.test.ts</files>
  <action>
    Mechanical edits only — replace stale `permissions[]` literals with the post-Phase-2 arrays from the `<interfaces>` block above. Do NOT add new test cases, do NOT rename or restructure existing `it(...)` blocks, do NOT touch any non-test file, and do NOT modify `apps/api/src/auth/permissions.ts`, the /me route, or the userService.

    (A) `apps/api/test/admin.ping.test.ts` — the `describe('GET /api/me — permissions[] regression (D-18)', ...)` block (lines 120-153):

      1. Line 121 — update the `it(...)` title from
         `"returns permissions: ['admin:ping'] for an admin session"`
         to
         `"returns permissions: ['admin:ping', 'medication:read', 'medication:create', 'medication:update', 'medication:delete'] for an admin session"`.
         Then line 129 — change `expect(res.json().permissions).toEqual(['admin:ping']);` to
         `expect(res.json().permissions).toEqual(['admin:ping', 'medication:read', 'medication:create', 'medication:update', 'medication:delete']);`.

      2. Line 132 — update the `it(...)` title from
         `'returns permissions: [] for a sjuksköterska session'`
         to
         `"returns permissions: ['medication:read'] for a sjuksköterska session"`.
         Then line 140 — change `expect(res.json().permissions).toEqual([]);` to
         `expect(res.json().permissions).toEqual(['medication:read']);`.

      3. Line 143 — update the `it(...)` title from
         `'returns permissions: [] for an apotekare session'`
         to
         `"returns permissions: ['medication:read', 'medication:create', 'medication:update', 'medication:delete'] for an apotekare session"`.
         Then line 151 — change `expect(res.json().permissions).toEqual([]);` to
         `expect(res.json().permissions).toEqual(['medication:read', 'medication:create', 'medication:update', 'medication:delete']);`.

    (B) `apps/api/test/auth.flow.smoke.test.ts` — the `ROLE_MATRIX` (lines 38-60). Update only the three `expectedPermissions` lines:

      - Row `apotekare` (line 43): replace `expectedPermissions: [] as string[],` with
        `expectedPermissions: ['medication:read', 'medication:create', 'medication:update', 'medication:delete'] as string[],`

      - Row `sjukskoterska` (line 50): replace `expectedPermissions: [] as string[],` with
        `expectedPermissions: ['medication:read'] as string[],`

      - Row `admin` (line 57): replace `expectedPermissions: ['admin:ping'] as string[],` with
        `expectedPermissions: ['admin:ping', 'medication:read', 'medication:create', 'medication:update', 'medication:delete'] as string[],`

      Do not change the `label`, `user`, `expectedRole`, or `adminPingStatus` fields. The `as const` on the array itself stays.

    (C) `apps/api/test/auth.me.test.ts` — the meResponse-shape assertion (lines 62-72). Update line 71 only:

      - Replace `permissions: ['admin:ping'], // Plan 03 — admin role has admin:ping per PERMISSIONS map` with
        `permissions: ['admin:ping', 'medication:read', 'medication:create', 'medication:update', 'medication:delete'], // Phase 2 D-43 — admin gains medication:* via PERMISSIONS map`

      Do not change the surrounding object (`id`, `email`, `name`, `role`, `careUnit`), the negative `not.toHaveProperty('passwordHash')` check, or the unauthenticated/tampered-cookie cases.

    Line numbers above are reference points from the snapshot in `<context>` — match by surrounding text, not by line, in case formatters have shifted things.

    No other edits. The PERMISSIONS map and /me wiring are the source of truth; the tests are catching up to them.
  </action>
  <verify>
    <automated>pnpm --filter @meditrack/api exec vitest run</automated>
  </verify>
  <done>
    - All three test files updated with exactly the assertions listed above; no other file is modified.
    - `pnpm --filter @meditrack/api exec vitest run` (executed against the live Docker Postgres stack at localhost:5432) exits 0 with no failing assertions and no skipped suites.
    - `git diff --stat HEAD` shows changes confined to the three test files; `apps/api/src/auth/permissions.ts`, the /me route, and userService are untouched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Atomic commit covering all three test updates</name>
  <files>apps/api/test/admin.ping.test.ts, apps/api/test/auth.flow.smoke.test.ts, apps/api/test/auth.me.test.ts</files>
  <action>
    Stage exactly the three test files and create a single atomic commit. Do NOT use `git add -A` or `git add .`. Do NOT amend any prior commit.

    Stage:
      git add apps/api/test/admin.ping.test.ts apps/api/test/auth.flow.smoke.test.ts apps/api/test/auth.me.test.ts

    Commit message (single line subject, then body explaining the why):

      test(api): align Phase 1 permissions[] regressions with post-Phase-2 map

      Phase 2 (33deb19) widened apps/api/src/auth/permissions.ts with four
      medication:* actions; /me now returns them via actionsForRole(role) for
      every role. The Phase 1 regression suite still asserted the pre-Phase-2
      shape because the Phase 2 verification only ran the web vitest suite
      (.planning/phases/02-medication-catalog/02-VERIFICATION.md:139).

      Updates three test files — assertions only, no production code:
        - admin.ping.test.ts        /me permissions[] regression block (D-18)
        - auth.flow.smoke.test.ts   ROLE_MATRIX.expectedPermissions × 3
        - auth.me.test.ts           admin meResponse-shape assertion

      No semantic change to the PERMISSIONS map, /me route, or userService.

    Verify staging is clean before committing:
      git status --short
    Only the three test paths should appear, each as `M `.

    Use a HEREDOC for the message to preserve formatting. Do NOT push.
  </action>
  <verify>
    <automated>git log -1 --stat | grep -c "apps/api/test/" </automated>
  </verify>
  <done>
    - HEAD commit subject is `test(api): align Phase 1 permissions[] regressions with post-Phase-2 map`.
    - `git log -1 --stat` lists exactly the three test files (count = 3).
    - `git status --short` is clean (no untracked or unstaged remnants from this change).
    - No production code (no file under `apps/api/src/`) appears in the commit.
  </done>
</task>

</tasks>

<verification>
Phase-wide regression gate (run after Task 1, before Task 2 commits):

```
pnpm --filter @meditrack/api exec vitest run
```

Expected: all suites pass against the live Docker stack (Postgres at localhost:5432). Specifically:
  - `test/admin.ping.test.ts` — 4 RBAC matrix cases + 3 /me permissions[] cases all green.
  - `test/auth.flow.smoke.test.ts` — 3 role iterations + the session-table-empty assertion all green.
  - `test/auth.me.test.ts` — 3 /me cases (happy path + no-cookie + tampered cookie) all green.

If any assertion fails, the executor must re-read `apps/api/src/auth/permissions.ts` and re-confirm the declaration order matches the arrays in this plan — do not edit production code to make a test pass.

Negative check (proves scope discipline):

```
git diff --name-only HEAD~1 HEAD
```

Must list exactly:
  apps/api/test/admin.ping.test.ts
  apps/api/test/auth.flow.smoke.test.ts
  apps/api/test/auth.me.test.ts
</verification>

<success_criteria>
- `pnpm --filter @meditrack/api exec vitest run` exits 0 against the live Docker Postgres stack.
- All three updated test files assert the exact post-Phase-2 arrays from the `<interfaces>` block.
- A single `test(api):` commit on HEAD contains exactly the three test files and nothing else.
- `apps/api/src/auth/permissions.ts`, the /me route, and userService remain byte-identical to their pre-plan state.
- No new test cases added; no `it(...)` block deleted or renamed beyond the title updates spelled out in Task 1.
</success_criteria>

<output>
Create `.planning/quick/260521-ip1-update-phase-1-regression-tests-to-asser/260521-ip1-SUMMARY.md` when done, recording: files changed, vitest run result, the three expected arrays per role, and the commit hash.
</output>
