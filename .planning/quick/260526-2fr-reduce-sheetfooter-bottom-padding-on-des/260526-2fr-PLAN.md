---
phase: quick-260526-2fr
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/routes/lakemedel/MedicationSheet.tsx
autonomous: true
requirements:
  - QUICK-260526-2fr
must_haves:
  truths:
    - "On desktop viewports (≥768px), the SheetFooter padding-bottom collapses to the standard p-4 16px so the form scroll container does not waste ~72px of vertical space."
    - "On mobile viewports (<768px), the existing safe-area padding (`calc(1rem+56px+env(safe-area-inset-bottom))`) remains intact so Android nav bars and iOS keyboard insets are still cleared."
    - "When the AI suggestion block expands inside EditSheet/create-mode on desktop, the Slutgiltig klass combobox remains within the visible viewport (no longer pushed below the fold by oversized footer padding)."
    - "All three SheetFooter sites (EditSheet, ViewSheet, create-mode) receive the fix symmetrically — desktop padding behavior is consistent across view/edit/create."
  artifacts:
    - path: "apps/web/src/routes/lakemedel/MedicationSheet.tsx"
      provides: "Three SheetFooter elements with responsive md:pb-4 override appended to the existing className"
      contains: "md:pb-4"
  key_links:
    - from: "apps/web/src/routes/lakemedel/MedicationSheet.tsx:616 (EditSheet footer)"
      to: "Tailwind responsive utility"
      via: "className suffix `md:pb-4`"
      pattern: "pb-\\[calc\\(1rem\\+56px\\+env\\(safe-area-inset-bottom\\)\\)\\] md:pb-4"
    - from: "apps/web/src/routes/lakemedel/MedicationSheet.tsx:776 (ViewSheet footer)"
      to: "Tailwind responsive utility"
      via: "className suffix `md:pb-4`"
      pattern: "pb-\\[calc\\(1rem\\+56px\\+env\\(safe-area-inset-bottom\\)\\)\\] md:pb-4"
    - from: "apps/web/src/routes/lakemedel/MedicationSheet.tsx:1299 (create-mode footer)"
      to: "Tailwind responsive utility"
      via: "className suffix `md:pb-4`"
      pattern: "pb-\\[calc\\(1rem\\+56px\\+env\\(safe-area-inset-bottom\\)\\)\\] md:pb-4"
---

<objective>
Reduce SheetFooter bottom padding on desktop (≥768px) in `apps/web/src/routes/lakemedel/MedicationSheet.tsx` so the AI suggestion block expansion no longer pushes the `Slutgiltig klass` combobox below the visible viewport.

Purpose: The existing `pb-[calc(1rem+56px+env(safe-area-inset-bottom))]` is a mobile safe-area hack (Android nav bar + iOS keyboard inset). On desktop it adds ~72px of dead space at the bottom of the sheet, eating the room that the dynamically-expanding AI block needs. A single responsive `md:pb-4` override resolves it without touching the mobile contract.

Output: Three className edits in one file. No behavior change on mobile. On desktop, footer padding-bottom collapses from ~88px+ to the standard 16px from `p-4`.
</objective>

<execution_context>
@C:/Projekt/MediTrack/.claude/get-shit-done/workflows/execute-plan.md
@C:/Projekt/MediTrack/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@apps/web/src/routes/lakemedel/MedicationSheet.tsx

<interfaces>
<!-- Three SheetFooter sites in MedicationSheet.tsx — verified line numbers and exact className strings -->

Line 616 (EditSheet — has Ta bort destructive button on left, uses justify-between):
```tsx
<SheetFooter className="border-t border-border p-4 flex items-center justify-between gap-2 pb-[calc(1rem+56px+env(safe-area-inset-bottom))]">
```

Line 776 (ViewSheet — right-aligned actions only, uses justify-end):
```tsx
<SheetFooter className="border-t border-border p-4 flex items-center justify-end gap-2 pb-[calc(1rem+56px+env(safe-area-inset-bottom))]">
```

Line 1299 (create-mode — right-aligned actions only, uses justify-end):
```tsx
<SheetFooter className="border-t border-border p-4 flex items-center justify-end gap-2 pb-[calc(1rem+56px+env(safe-area-inset-bottom))]">
```

The two `justify-end` footers (776, 1299) are textually identical at the className level — the Edit tool requires `old_string`/`new_string` calls with enough surrounding context to disambiguate. Anchor each Edit on the preceding line (the closing `</form>`, `</fieldset>`, or `</div>` plus blank line before the SheetFooter) to ensure uniqueness.

Breakpoint reference: this file already uses `matchMedia('(min-width: 768px)')` to drive `isDesktop`, so the `md:` Tailwind prefix (default 768px) is the matching responsive boundary.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append `md:pb-4` to all three SheetFooter classNames in MedicationSheet.tsx</name>
  <files>apps/web/src/routes/lakemedel/MedicationSheet.tsx</files>
  <action>
    Make THREE separate Edit calls against `apps/web/src/routes/lakemedel/MedicationSheet.tsx`, one per SheetFooter site. For each, append ` md:pb-4` to the end of the existing className string (after the closing `)]` of the `pb-[calc(...)]` arbitrary value). Do not reorder or remove any other Tailwind utility. Do not change `justify-between` vs `justify-end` — the three sites genuinely differ there (EditSheet has a Ta bort button on the left, ViewSheet and create-mode have right-only action rows).

    Anchor each `old_string` with the preceding line (the closing `</form>`, `</fieldset>`, or `</div>` plus the blank line) so the two `justify-end` footers (lines 776 and 1299) are unambiguously distinct. The preceding lines are:
    - Line 616 footer: preceded by `          </form>` then blank line (closes the EditSheet form)
    - Line 776 footer: preceded by `        </fieldset>` then blank line (closes the ViewSheet detail fieldset)
    - Line 1299 footer: preceded by `        </div>` then blank line (closes the create-mode scroll container)

    Final className shape per site:
    - EditSheet (616): `border-t border-border p-4 flex items-center justify-between gap-2 pb-[calc(1rem+56px+env(safe-area-inset-bottom))] md:pb-4`
    - ViewSheet (776): `border-t border-border p-4 flex items-center justify-end gap-2 pb-[calc(1rem+56px+env(safe-area-inset-bottom))] md:pb-4`
    - create-mode (1299): `border-t border-border p-4 flex items-center justify-end gap-2 pb-[calc(1rem+56px+env(safe-area-inset-bottom))] md:pb-4`

    No other files. No new tests. No imports needed (Tailwind responsive prefixes are zero-runtime).
  </action>
  <verify>
    <automated>cd apps/web && pnpm typecheck 2>&1 | tail -20 && cd ../.. && pnpm -w lint 2>&1 | tail -20 && cd apps/web && pnpm test --run src/routes/lakemedel/MedicationSheet 2>&1 | tail -30</automated>
    <human-check>
      1. `pnpm -w dev` (or whatever dev command starts the web app) and open the medication list on a desktop viewport (≥768px width).
      2. Open EditSheet for any medication → expand the AI suggestion block → confirm the `Slutgiltig klass` combobox is visible without scrolling past the footer.
      3. Open ViewSheet → confirm footer hugs the bottom with standard 16px padding (no large gap).
      4. Open create-mode (new medication) → confirm footer behavior matches.
      5. Resize to <768px (or use DevTools mobile preset) → confirm the safe-area padding returns (footer sits higher to clear simulated mobile nav).
    </human-check>
  </verify>
  <done>
    - `grep -c "md:pb-4" apps/web/src/routes/lakemedel/MedicationSheet.tsx` returns exactly 3
    - `pnpm typecheck` passes with no new errors
    - `pnpm -w lint` passes with no new warnings/errors on the edited file
    - Existing `MedicationSheet.*.test.tsx` suites still pass
    - The three SheetFooter classNames each end with `pb-[calc(1rem+56px+env(safe-area-inset-bottom))] md:pb-4`
  </done>
</task>

</tasks>

<verification>
- File diff shows exactly three changes, all in `apps/web/src/routes/lakemedel/MedicationSheet.tsx`, each appending ` md:pb-4` to a SheetFooter className.
- `grep -n "SheetFooter className" apps/web/src/routes/lakemedel/MedicationSheet.tsx` shows the three expected sites and each ends with `md:pb-4`.
- Typecheck, lint, and the MedicationSheet test suites pass unchanged.
- Human UAT confirms desktop footer collapses to 16px bottom padding while mobile safe-area behavior is preserved.
</verification>

<success_criteria>
- Three className strings updated; no other source changes.
- On desktop (≥768px), the AI block can expand inside EditSheet/create-mode without pushing `Slutgiltig klass` below the viewport.
- On mobile (<768px), the existing `calc(1rem+56px+env(safe-area-inset-bottom))` padding is preserved so Android nav / iOS keyboard insets are still cleared.
- All automated checks (typecheck, lint, existing MedicationSheet tests) pass.
</success_criteria>

<output>
Create `.planning/quick/260526-2fr-reduce-sheetfooter-bottom-padding-on-des/260526-2fr-SUMMARY.md` when done, documenting:
- The three exact className diffs
- Confirmation that typecheck/lint/tests pass
- Result of human UAT on desktop and mobile viewports
</output>
