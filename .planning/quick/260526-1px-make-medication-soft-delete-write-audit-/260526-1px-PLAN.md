---
phase: quick-260526-1px
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/constants/auditAction.ts
  - apps/api/src/services/medication.service.ts
  - apps/web/src/components/AuditActionChip.tsx
autonomous: true
requirements:
  - QUICK-260526-1px
must_haves:
  truths:
    - "Deleting a CareUnitMedication writes an audit row with action = 'medication.softDelete' (not 'update')."
    - "The admin audit log renders that action as Swedish label 'Borttagen' inside an AuditActionChip styled with destructive (red) colours, matching the 'order.softDelete' chip."
    - "The set of audit actions exposed by @meditrack/shared includes 'medication.softDelete', so FE label-map / chip-class exhaustiveness covers it without falling through to the unknown-action fallback."
  artifacts:
    - path: "packages/shared/src/constants/auditAction.ts"
      provides: "AUDIT_ACTIONS list + AUDIT_ACTION_LABELS map extended with medication.softDelete → Borttagen"
      contains: "medication.softDelete"
    - path: "apps/api/src/services/medication.service.ts"
      provides: "softDeleteCareUnitMedication wraps the prisma.careUnitMedication.update in withActionOverride('medication.softDelete', ...)"
      contains: "withActionOverride('medication.softDelete'"
    - path: "apps/web/src/components/AuditActionChip.tsx"
      provides: "ACTION_CLASS map entry mapping medication.softDelete to bg-destructive/10 text-destructive"
      contains: "'medication.softDelete': 'bg-destructive/10 text-destructive'"
  key_links:
    - from: "apps/api/src/services/medication.service.ts"
      to: "apps/api/src/plugins/requestContext.ts"
      via: "withActionOverride() ALS frame consumed by audit $extends middleware"
      pattern: "withActionOverride\\('medication\\.softDelete'"
    - from: "apps/web/src/components/AuditActionChip.tsx"
      to: "packages/shared/src/constants/auditAction.ts"
      via: "AuditAction type / AUDIT_ACTION_LABELS — chip must compile against the literal union"
      pattern: "medication\\.softDelete"
---

<objective>
Make `softDeleteCareUnitMedication` in `apps/api/src/services/medication.service.ts` emit an audit row with the domain-rich action string `'medication.softDelete'` (Swedish label **Borttagen**) instead of the generic `'update'` produced by the default Prisma `$extends` middleware path.

This mirrors the existing `'order.softDelete'` pattern at `apps/api/src/services/order.service.ts:847` (Phase 5 / D-94 — wrap status-machine mutations with `withActionOverride` so the audit log reads as the action a domain user actually performed, not as the underlying Prisma verb).

Purpose: The admin audit log currently shows a soft-deleted medication as a generic "Uppdaterad" row, indistinguishable from a name/strength edit. After this change it renders as a red "Borttagen" chip — coherent with the existing order.softDelete styling and discoverable as a delete event.

Output: Three small surgical edits land atomically as one commit. Shared constant + producer + consumer ship together so neither the FE label-map exhaustiveness check nor the API audit writer can be in a half-broken state at any commit.
</objective>

<execution_context>
@C:/Projekt/MediTrack/.claude/get-shit-done/workflows/execute-plan.md
@C:/Projekt/MediTrack/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Projekt/MediTrack/.planning/STATE.md
@C:/Projekt/MediTrack/CLAUDE.md
@C:/Projekt/MediTrack/packages/shared/src/constants/auditAction.ts
@C:/Projekt/MediTrack/apps/api/src/services/medication.service.ts
@C:/Projekt/MediTrack/apps/api/src/services/order.service.ts
@C:/Projekt/MediTrack/apps/web/src/components/AuditActionChip.tsx
@C:/Projekt/MediTrack/apps/api/src/plugins/requestContext.ts

<interfaces>
<!-- Key signatures the executor needs. Extracted from codebase — no exploration required. -->

From apps/api/src/plugins/requestContext.ts (line 208):
```typescript
export async function withActionOverride<T>(
  action: string,
  fn: () => Promise<T>,
): Promise<T>;
```
Wrap any Prisma mutation whose audit row should carry a domain-rich action string. The wrapper sets `actionOverrideALS` for the duration of `fn()`'s async continuation; the audit `$extends` middleware reads it and writes that string into `audit_events.action`. If no `actorALS` frame exists (seed scripts, tests not wrapped in `actorALS.run`), the call still executes — just without the audit-row side effect.

From apps/api/src/services/order.service.ts (line 847 — reference pattern):
```typescript
const result = await withActionOverride('order.softDelete', () =>
  prisma.order.updateMany({
    where: { id: orderId, careUnitId, status: 'utkast', deletedAt: null },
    data: { deletedAt: new Date() },
  }),
);
```
Note: the order path uses `updateMany` for the CAS / status-precondition guard; medication soft-delete uses `update` (the `findUnique` precheck above it has already enforced not-found / cross-tenant), so the wrap is structurally simpler.

From apps/api/src/services/medication.service.ts (lines 594–614 — the function to edit):
```typescript
export async function softDeleteCareUnitMedication(
  careUnitId: string,
  careUnitMedicationId: string,
): Promise<void> {
  const existing = await prisma.careUnitMedication.findUnique({
    where: { id: careUnitMedicationId },
  });

  if (!existing || existing.deletedAt !== null) {
    throw new NotFoundError('Läkemedlet finns inte i din vårdenhet.');
  }

  if (existing.careUnitId !== careUnitId) {
    throw new NotFoundError('Läkemedlet finns inte i din vårdenhet.');
  }

  await prisma.careUnitMedication.update({
    where: { id: careUnitMedicationId },
    data: { deletedAt: new Date() },
  });
}
```
Only the trailing `await prisma.careUnitMedication.update({...})` call is wrapped. Do not change the precheck logic or the error semantics. The wrapped expression is still `await`ed and still returns `void` to callers.

From packages/shared/src/constants/auditAction.ts (existing shape — DO NOT REORDER):
- `AUDIT_ACTIONS` is an ordered tuple grouped by semantic category (see the file's header comment). The order.softDelete entry sits inside the "Phase 3 / 4 status-machine overrides" block at line 28.
- `AUDIT_ACTION_LABELS` is a `Record<AuditAction, string>`; adding a literal to `AUDIT_ACTIONS` REQUIRES adding a matching entry here or TypeScript will error on the missing key.

From apps/web/src/components/AuditActionChip.tsx (existing shape):
- `ACTION_CLASS` is `Record<AuditAction, string>`; same exhaustiveness requirement — every literal in `AUDIT_ACTIONS` must appear here or `tsc` fails.
- The `'order.softDelete': 'bg-destructive/10 text-destructive'` entry is the styling template to mirror.
- Fallback for unknown strings is `UNKNOWN_ACTION_CLASS` (neutral slate); we want the new action to hit the explicit destructive entry, not the fallback.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Emit medication.softDelete audit action — shared constant, service wrap, FE chip class</name>
  <files>packages/shared/src/constants/auditAction.ts, apps/api/src/services/medication.service.ts, apps/web/src/components/AuditActionChip.tsx</files>
  <action>
Make THREE coordinated edits so a soft-delete of a CareUnitMedication produces an audit row with `action = 'medication.softDelete'` (Swedish label "Borttagen"), styled as a destructive chip in the admin audit log. The three edits MUST land in a single commit — the FE `ACTION_CLASS` and `AUDIT_ACTION_LABELS` maps are typed `Record<AuditAction, string>`, so adding the literal to `AUDIT_ACTIONS` without adding both map entries fails `tsc`; conversely, wrapping the service call before the literal exists in the shared package also fails `tsc`. Edit order does not matter as long as all three are saved before running typecheck.

1) **`packages/shared/src/constants/auditAction.ts`** — extend the action vocabulary:
   - In the `AUDIT_ACTIONS` tuple, insert `'medication.softDelete',` IMMEDIATELY after the existing `'order.softDelete',` line (line 28 today). Add a short inline comment noting this mirrors the order.softDelete pattern for the medication CRUD path (D-94 — withActionOverride wrap in medication.service.ts). Keep the grouping comment block intact; this entry belongs in the same "Phase 3 / 4 status-machine overrides" semantic group because it shares the same withActionOverride mechanism even though the surface area (CRUD) differs from order lifecycle.
   - In the `AUDIT_ACTION_LABELS` record, insert `'medication.softDelete': 'Borttagen',` IMMEDIATELY after the existing `'order.softDelete': 'Borttagen (utkast)',` line (line 63 today). The label is the bare word "Borttagen" (no qualifier) because a medication soft-delete is the canonical removal action for the medication registry — there is no "draft vs sent" duality to disambiguate, unlike orders. This matches the generic `delete: 'Borttagen'` label semantically while the action key keeps it distinct in filtering.
   - Do NOT reorder any other entries. Do NOT touch `auditActionEnum` (it's derived from `AUDIT_ACTIONS` via `z.enum`, so it picks up the new literal automatically).

2) **`apps/api/src/services/medication.service.ts`** — wrap the soft-delete update:
   - Add a new import line `import { withActionOverride } from '../plugins/requestContext.js';` placed AFTER the `import { ConflictDuplicateMedicationError, NotFoundError, ForbiddenScopeError } from '../plugins/errorHandler.js';` block (lines 3–7 today) and BEFORE the `import type { MedicationListResponse, ... } from '@meditrack/shared';` block. This matches the import ordering convention used in `order.service.ts` (runtime plugin imports grouped before shared-type imports).
   - In `softDeleteCareUnitMedication` (lines 594–614), replace the trailing `await prisma.careUnitMedication.update({ where: { id: careUnitMedicationId }, data: { deletedAt: new Date() } });` with a `withActionOverride('medication.softDelete', () => prisma.careUnitMedication.update({ where: { id: careUnitMedicationId }, data: { deletedAt: new Date() } }))` wrap. Keep the outer `await`; the wrap returns `Promise<CareUnitMedication>` (same as the bare call) and the function still discards the result by returning `void`.
   - Add a short comment ABOVE the wrapped call explaining the wrap: cite Phase 5 D-94 (audit action vocabulary), point to the precedent at `apps/api/src/services/order.service.ts:847`, and note that without the wrap this mutation would record as the generic `'update'` action and be indistinguishable from a name/strength edit in the audit log.
   - Do NOT alter the `findUnique` precheck, the `NotFoundError` branches, or the function signature. Do NOT introduce a `$transaction` — the precheck + atomic update is acceptable here (the medication-registry write path has no concurrent-mutator risk equivalent to the order CAS scenario; the existing single-update semantics are preserved).

3) **`apps/web/src/components/AuditActionChip.tsx`** — add chip styling:
   - In the `ACTION_CLASS` record, insert `'medication.softDelete': 'bg-destructive/10 text-destructive',` IMMEDIATELY after the existing `'order.softDelete': 'bg-destructive/10 text-destructive',` line (line 28 today). Use the IDENTICAL Tailwind class string — the visual signal "this is a removal" is the destructive palette, and we want medication.softDelete to read as the same kind of event as order.softDelete and the generic `delete`.
   - Do NOT modify `UNKNOWN_ACTION_CLASS`, the component body, or the props interface.

No DB migration is required: `audit_events.action` is a free-text column (`z.string()` on the API boundary per D-104 footer); the open set means a new literal needs no schema change. No new tests are required: the existing `apps/api/test/audit.integration.test.ts` covers the audit-row write path, and the user is doing browser UAT on the chip rendering.
  </action>
  <verify>
    <automated>cd C:/Projekt/MediTrack &amp;&amp; pnpm -r typecheck</automated>
    <automated>cd C:/Projekt/MediTrack &amp;&amp; pnpm lint</automated>
    <automated>cd C:/Projekt/MediTrack &amp;&amp; pnpm --filter @meditrack/api test -- audit.integration</automated>
    <automated>grep -n "medication.softDelete" C:/Projekt/MediTrack/packages/shared/src/constants/auditAction.ts | grep -v '^[[:space:]]*//' | wc -l   # expect 2 (AUDIT_ACTIONS entry + AUDIT_ACTION_LABELS entry)</automated>
    <automated>grep -n "withActionOverride('medication.softDelete'" C:/Projekt/MediTrack/apps/api/src/services/medication.service.ts | wc -l   # expect 1</automated>
    <automated>grep -n "'medication.softDelete': 'bg-destructive/10 text-destructive'" C:/Projekt/MediTrack/apps/web/src/components/AuditActionChip.tsx | wc -l   # expect 1</automated>
  </verify>
  <done>
    - `pnpm -r typecheck` passes across all workspaces (proves the new `AuditAction` literal is exhaustively handled in both `AUDIT_ACTION_LABELS` and `ACTION_CLASS`, and that the new `withActionOverride` import resolves).
    - `pnpm lint` reports zero errors and zero warnings.
    - `pnpm --filter @meditrack/api test -- audit.integration` continues to pass (no audit-path regression).
    - `softDeleteCareUnitMedication` invokes `prisma.careUnitMedication.update` ONLY inside a `withActionOverride('medication.softDelete', ...)` wrapper; the bare un-wrapped call is gone.
    - `AUDIT_ACTIONS` contains `'medication.softDelete'` exactly once, placed adjacent to `'order.softDelete'`.
    - `AUDIT_ACTION_LABELS['medication.softDelete']` is the string `'Borttagen'`.
    - `ACTION_CLASS['medication.softDelete']` is the string `'bg-destructive/10 text-destructive'`.
    - No other files modified; no migrations created.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → API | Authenticated user (pharmacist/admin role gated by route preHandler) triggers DELETE /care-unit-medications/:id |
| API service → audit middleware | `withActionOverride` ALS frame is read by the Prisma `$extends` middleware; cross-frame propagation must be intact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick-260526-1px-01 | Repudiation | `audit_events.action` for medication deletes | mitigate | Replace the generic `'update'` action with the explicit `'medication.softDelete'` action so the audit log is unambiguously attributable as a removal event (not maskable as an edit). Existing audit-row writer guarantees actor/careUnitId/requestId persistence — this change only enriches the action label. |
| T-quick-260526-1px-02 | Tampering | npm/pip/cargo installs | accept | No new dependencies introduced; all three edits use existing imports (`withActionOverride` already present in `requestContext.ts`, `cn` and Tailwind classes already present in the web app). No package-manager invocations required. |
| T-quick-260526-1px-03 | Information disclosure | Audit label "Borttagen" vs. "Borttagen (utkast)" | accept | The label set is intentionally Swedish-domain wording exposed only to authenticated admin viewers behind the existing audit-log route auth gate. No PII or cross-tenant leak introduced; the action string is already openly enumerable by the FE label map. |
</threat_model>

<verification>
- `pnpm -r typecheck` is green across `apps/api`, `apps/web`, and `packages/shared` (proves the exhaustive `Record<AuditAction, string>` maps cover the new literal and that import paths resolve).
- `pnpm lint` is clean.
- `pnpm --filter @meditrack/api test -- audit.integration` still passes (no audit path regression introduced).
- Manual browser UAT (out of scope for this plan's automation): pharmacist soft-deletes a medication; admin loads `/admin/audit`; the corresponding row renders a red "Borttagen" chip rather than a slate "Uppdaterad" chip.
</verification>

<success_criteria>
- Soft-deleting a CareUnitMedication produces exactly one `audit_events` row with `action = 'medication.softDelete'` and `entityType = 'CareUnitMedication'`.
- The shared package exports `'medication.softDelete'` as a member of `AuditAction`, with `'Borttagen'` as its Swedish label.
- The audit-log chip primitive renders that action with the destructive (red) Tailwind palette identical to `'order.softDelete'`.
- Full repo typecheck and lint pass.
- Existing audit integration test suite passes unmodified.
- Single git commit covers all three files (atomic landing — the shared/api/web maps stay exhaustively in sync at every commit boundary).
</success_criteria>

<output>
Create `.planning/quick/260526-1px-make-medication-soft-delete-write-audit-/260526-1px-SUMMARY.md` when done. Summary should record:
  - Files actually modified (paths + brief diff summary)
  - Output of the verify commands (PASS/FAIL)
  - Confirmation that no other audit-action literals were touched
  - Note for the user: browser UAT step is to soft-delete a medication and check the admin audit log shows the red "Borttagen" chip.
</output>
