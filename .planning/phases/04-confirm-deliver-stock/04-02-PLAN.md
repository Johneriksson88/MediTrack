---
phase: 04-confirm-deliver-stock
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - apps/api/src/services/order.service.ts
  - apps/api/src/routes/orders/deliver.ts
  - apps/api/src/routes/orders/index.ts
  - apps/web/src/features/orders/useOrderMutations.ts
  - apps/web/src/routes/bestallningar/DeliverConfirmDialog.tsx
  - apps/web/src/routes/bestallningar/ComposeOrderPage.tsx
  - apps/web/src/routes/bestallningar/OrderActorTrail.tsx
  - apps/api/test/orders.deliver.integration.test.ts
autonomous: true
requirements: [ORD-05, ORD-06, STK-01, STK-02, OPS-03]
must_haves:
  truths:
    - "Apotekare/admin can deliver a Bekräftad order; status flips to Levererad and each line's quantity is added to its CareUnitMedication.currentStock inside a single Postgres transaction (T1, T4, STK-01)."
    - "Deliver tx locks the Order row with `$queryRaw … FOR UPDATE`, then locks all affected CUM rows with `SELECT … WHERE id = ANY($1) ORDER BY id FOR UPDATE` BEFORE any UPDATE (T2, STK-02)."
    - "Same-CUM lines are aggregated; one UPDATE per distinct CUM (T3, D-79)."
    - "Two concurrent deliveries on the same Bekraftad order: exactly one commits, the other receives 409 `order_transition_invalid`, stock incremented exactly once. Proved by a deliberate pg_locks-instrumented test (OPS-03, T2)."
    - "Cross-careUnit deliver returns 404 not 403 (T5)."
    - "Sjuksköterska calling /deliver receives 403; FE Mode D button hidden via `<Can action=\"order:deliver\">` (T4, T10)."
    - "Soft-deleted CUM at deliver time → 422 `validation_failed` with reason `'medication_removed'`, no stock change (T7)."
    - "Levererad is terminal — no reversal endpoints exist; Mode E renders read-only with full actor trail (T8)."
    - "Deliver mutation's onSuccess: setQueryData(['order', id], response) + invalidate ['orders', …] + invalidate ['medications', …] (T9, Phase 6 NTF-02 hook)."
  artifacts:
    - path: "apps/api/src/services/order.service.ts"
      provides: "deliverOrder(careUnitId, orderId, actorUserId): Promise<OrderResponse> with CUM batch lock + atomic Order UPDATE + per-CUM stock increment"
    - path: "apps/api/src/routes/orders/deliver.ts"
      provides: "POST /api/orders/:id/deliver with [requireSession, requirePermission('order:deliver')]"
    - path: "apps/web/src/features/orders/useOrderMutations.ts"
      provides: "useDeliverOrder mutation with pessimistic cache hydration + ['medications'] invalidation"
    - path: "apps/web/src/routes/bestallningar/DeliverConfirmDialog.tsx"
      provides: "AlertDialog wrapping the deliver mutation (mirrors DiscardDraftDialog)"
    - path: "apps/web/src/routes/bestallningar/OrderActorTrail.tsx"
      provides: "Shared actor + timestamp trail line; rendered in Mode D (partial) and Mode E (full)"
    - path: "apps/web/src/routes/bestallningar/ComposeOrderPage.tsx"
      provides: "Mode D (Bekräftad + apotekare → Markera som levererad button + actor trail) and Mode E (Levererad terminal banner + full actor trail)"
    - path: "apps/api/test/orders.deliver.integration.test.ts"
      provides: "(1) full pipeline create→submit→confirm→deliver asserting stock + actor trail; (2) two-phase barrier concurrency test with pg_locks proof"
  key_links:
    - from: "apps/api/src/services/order.service.ts deliverOrder"
      to: "Postgres CareUnitMedication row locks"
      via: "$queryRaw SELECT … FROM CareUnitMedication WHERE id = ANY($1) ORDER BY id FOR UPDATE"
      pattern: "FOR UPDATE"
    - from: "apps/web/src/features/orders/useOrderMutations.ts useDeliverOrder"
      to: "TanStack Query medications cache (Phase 6 NTF-01 hook)"
      via: "queryClient.invalidateQueries({queryKey: ['medications']})"
      pattern: "invalidateQueries.*medications"
    - from: "apps/web/src/routes/bestallningar/DeliverConfirmDialog.tsx"
      to: "useDeliverOrder mutation"
      via: "onConfirm callback fires mutateAsync"
      pattern: "useDeliverOrder"
    - from: "apps/api/test/orders.deliver.integration.test.ts concurrency block"
      to: "pg_locks system view"
      via: "SELECT mode, granted FROM pg_locks polling during tx-B blocked window"
      pattern: "pg_locks"
---

<objective>
Slice B — ship the **deliver** half of the order lifecycle end-to-end including the stock-update transaction, the deliberate concurrency proof, and the FE Mode D/E surfaces. This is the most technically dense slice of Phase 4: the CUM-batch lock pattern (D-79) is novel in this codebase, and the pg_locks-instrumented concurrency test (D-88) is the §6 interview proof.

Purpose: implement STK-01 (atomic stock increment), STK-02 (row-level locking with `SELECT … FOR UPDATE`), OPS-03 (deliberate concurrency test covering the full pipeline), and ORD-05 (deliver transition). Close the FE loop with `<DeliverConfirmDialog>` (D-83), Mode D/E rendering, and the actor-trail shared component.
Output: working `POST /api/orders/:id/deliver` with atomic stock update; passing integration test covering the full create→submit→confirm→deliver pipeline AND the same-order concurrency race; FE Mode D/E surfaces with AlertDialog gate and actor trail.
</objective>

<execution_context>
@C:/Projekt/MediTrack/.claude/get-shit-done/workflows/execute-plan.md
@C:/Projekt/MediTrack/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-confirm-deliver-stock/04-CONTEXT.md
@.planning/phases/04-confirm-deliver-stock/04-PATTERNS.md
@.planning/phases/04-confirm-deliver-stock/04-UI-SPEC.md
@.planning/phases/04-confirm-deliver-stock/04-01-SUMMARY.md
@.planning/phases/03-draft-orders/03-CONTEXT.md
@.planning/phases/02-medication-catalog/02-CONTEXT.md

<interfaces>
<!-- Key contracts the executor needs. -->

From Slice A (04-01, already complete when this plan runs):
- `confirmOrder` exists with the Order-row lock pattern — `deliverOrder` reuses step 0 verbatim.
- `OrderTransitionError` is registered in the error handler.
- `ValidationFailedError.details.reason` accepts `'medication_removed'` + optional `medicationName?: string`.
- `toOrderResponse` already maps confirmedBy + deliveredBy; every include block in `order.service.ts` already widens to include those two relations.
- Shared `orderResponse` schema includes the deliveredAt/By/By-nullable trio.
- `'order:deliver'` is in `ACTION_KEYS` and `PERMISSIONS['order:deliver'] = ['apotekare', 'admin']`.
- `ApotekareActionFooter` component exists with `{label, onClick, isPending, loadingLabel?}` props — Slice B reuses it for Mode D.

From `apps/api/src/services/order.service.ts` (existing — Slice A widened it):
- `submitOrder` is the literal template for the Order-row lock step.
- `toOrderResponse` already returns the deliveredAt/By/By trio (mapped from the widened include).

From `apps/web/src/routes/bestallningar/DiscardDraftDialog.tsx` (Phase 3):
- The literal template for `DeliverConfirmDialog`. Same shadcn `<AlertDialog>` shape; Cancel rendered FIRST for focus management; `e.preventDefault()` on Action to prevent auto-dismiss; `disabled={isPending}` during mutation.

From `apps/api/test/orders.confirm.integration.test.ts` (Slice A):
- The lifecycle hooks + helpers (loginAs, captureSessionCookie, createEmptyOrder, findTestCareUnitMedication) are reused verbatim.
- Slice B adds a `progressOrderToBekraftad` composite helper (PATTERNS.md D-87) that strings together: line-add as nurse → submit as nurse → confirm as apotekare.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: deliverOrder service + POST /api/orders/:id/deliver route + happy-path integration test</name>
  <files>apps/api/src/services/order.service.ts, apps/api/src/routes/orders/deliver.ts, apps/api/src/routes/orders/index.ts, apps/api/test/orders.deliver.integration.test.ts</files>
  <behavior>
    - Test 1 (full pipeline happy path): sjuksköterska creates draft → adds 2 lines for the same CUM (qty 2 + qty 3, total 5 against one CUM) AND 1 line for a different CUM (qty 4) → submits. Apotekare confirms. Apotekare delivers. Response: 200, body has status='levererad', deliveredAt is recent ISO, deliveredByUserId equals apotekare.id, deliveredBy.name equals apotekare.name; confirmedBy + submittedBy + createdBy still populated. After delivery, CUM-1 stock incremented by 5 (aggregated 2+3), CUM-2 stock incremented by 4.
    - Test 2 (wrong-status 409): apotekare attempts /deliver against a Skickad order (no confirm). Response: 409 with code `order_transition_invalid`, details.from='skickad', details.expected='bekraftad'.
    - Test 3 (double-deliver 409): deliver once (200) → deliver again. Second call: 409 with details.expected='bekraftad'. Stock incremented exactly once (verify by reloading CUMs after both calls).
    - Test 4 (cross-careUnit 404): apotekare from careUnit B delivers against a Bekräftad order in careUnit A. Response: 404 with code `'not_found'`. Stock unchanged on the CUM.
    - Test 5 (sjuksköterska 403): sjuksköterska POSTs /deliver against a Bekräftad order. Response: 403. Order status unchanged.
    - Test 6 (soft-deleted CUM → 422 medication_removed): seed a Bekräftad order, then soft-delete one of its CUMs via direct prisma update (`deletedAt: new Date()`). Apotekare POSTs /deliver. Response: 422 with code `'validation_failed'`, details.reason='medication_removed', details.medicationName equals the soft-deleted med's name. Stock unchanged on ALL CUMs (the entire tx rolls back).
    - Test 7 (line aggregation): submit + confirm an order with the same CUM on 3 lines (qty 1, 1, 1). After deliver, that CUM's currentStock incremented by 3 (one aggregated UPDATE per distinct CUM).
  </behavior>
  <read_first>
    - apps/api/src/services/order.service.ts (post-Slice-A state — confirmOrder exists; toOrderResponse widened; include blocks widened)
    - apps/api/src/routes/orders/submit.ts (route template — clone for deliver.ts)
    - apps/api/src/routes/orders/index.ts (post-Slice-A; confirmOrderRoute registered; deliver registers after it)
    - apps/api/test/orders.confirm.integration.test.ts (Slice A test — copy lifecycle hooks + local helpers verbatim)
    - apps/api/test/orders.integration.test.ts (Phase 3 D-73 suite — reference for cross-careUnit and 403 patterns)
    - apps/api/test/helpers/buildTestApp.ts (verify TEST_APOTEKARE export + helpers)
    - .planning/phases/04-confirm-deliver-stock/04-PATTERNS.md (sections: deliverOrder — CUM-batch lock D-79 6 numbered steps; routes/orders/deliver.ts token map; orders.deliver.integration.test.ts test block 1)
    - .planning/phases/04-confirm-deliver-stock/04-CONTEXT.md (D-78 add semantics, D-79 6-step tx, D-80 live-join, D-81 medication_removed 422, D-89 race shape)
    - .planning/phases/02-medication-catalog/02-CONTEXT.md (D-28 CareUnitMedication model, D-30 soft-delete pattern, D-33 always-soft-delete invariant)
  </read_first>
  <action>
    Implement `deliverOrder` end-to-end with TDD. The CUM-batch lock is the novel piece — every other half is a faithful copy of the confirm pattern from Slice A.

    Step A — write the test file FIRST with the 7 `it` blocks above (RED). Reuse lifecycle hooks + local helpers from Slice A's `orders.confirm.integration.test.ts`. Add the `progressOrderToBekraftad(nurseCookie, apotekareCookie, orderId, lineSpecs)` composite helper per PATTERNS.md (it issues line-adds, submits, then confirms). Run the suite — all 7 MUST FAIL because `deliverOrder` and the route do not yet exist.

    Step B — implement `deliverOrder` in `apps/api/src/services/order.service.ts` following D-79's six numbered steps verbatim. Signature: `export async function deliverOrder(careUnitId: string, orderId: string, actorUserId: string): Promise<OrderResponse>`. Inside `prisma.$transaction(async (tx) => { … })` execute:
    (1) Order-row lock — `await tx.$queryRaw\`SELECT id FROM "Order" WHERE id = ${orderId} FOR UPDATE\`;` (verbatim from submitOrder step 0 / confirmOrder).
    (2) Load order + lines + nested CUMs + medication (need CUM deletedAt for D-81 and medication.name for the toast slot) — `tx.order.findUnique({ where: {id: orderId}, include: { lines: { include: { careUnitMedication: { include: { medication: true } } } }, createdBy:{select:{id,name}}, submittedBy:{select:{id,name}}, confirmedBy:{select:{id,name}}, deliveredBy:{select:{id,name}} } })`.
    (3) Existence + scope check — throw NotFoundError if `!order || order.deletedAt !== null || order.careUnitId !== careUnitId` (D-73).
    (4) Status precondition — if `order.status !== 'bekraftad'` throw `new OrderTransitionError({ from: order.status, to: 'levererad', expected: 'bekraftad' })` (D-76).
    (5) D-81 soft-deleted CUM check — iterate `order.lines`; if any `line.careUnitMedication.deletedAt !== null` throw `new ValidationFailedError('Läkemedlet har tagits bort.', { reason: 'medication_removed', medicationName: line.careUnitMedication.medication.name });` BEFORE any UPDATE.
    (6) D-79 aggregate same-CUM lines — build `const byCum = new Map<string, number>()`; for each line `byCum.set(line.careUnitMedicationId, (byCum.get(line.careUnitMedicationId) ?? 0) + line.quantity);`. Then `const sortedCumIds = [...byCum.keys()].sort();` (sorted-id ordering = deadlock prevention).
    (7) CUM batch lock — `await tx.$queryRaw\`SELECT id FROM "CareUnitMedication" WHERE id = ANY(${sortedCumIds}::text[]) ORDER BY id FOR UPDATE\`;` (the `::text[]` cast is REQUIRED because Prisma interpolates as `text[]` not `String[]`).
    (8) Per-CUM stock increment — for each `[cumId, qty]` of `byCum`: `await tx.careUnitMedication.update({ where: { id: cumId }, data: { currentStock: { increment: qty } } });` (Prisma's `{increment: N}` generates `SET "currentStock" = "currentStock" + $1`).
    (9) Atomic Order UPDATE with status precondition — `const updated = await tx.order.updateMany({ where: { id: orderId, careUnitId, status: 'bekraftad', deletedAt: null }, data: { status: 'levererad', deliveredAt: new Date(), deliveredByUserId: actorUserId } });`. If `updated.count === 0` throw `new OrderTransitionError({ from: 'levererad', to: 'levererad', expected: 'bekraftad' });`.
    (10) Reload + return with widened include; return `toOrderResponse(reloaded!)`.

    Step C — create `apps/api/src/routes/orders/deliver.ts` per PATTERNS.md token map: clone `submit.ts`, change `submitOrder` → `deliverOrder`, `/submit` → `/deliver`, `'order:submit'` → `'order:deliver'`, `submitOrderRoute` → `deliverOrderRoute`. preHandler: `[requireSession, requirePermission('order:deliver')]`. Schema: `params: z.object({ id: z.string().min(1) })`, `response: { 200: orderResponse }`. Handler: `return deliverOrder(req.user!.careUnitId, req.params.id, req.user!.id);`. Update docblock to reference D-78/D-79/D-81/D-89.

    Step D — register in `apps/api/src/routes/orders/index.ts` after `confirmOrderRoute`.

    Step E — GREEN. Run `pnpm --filter @meditrack/api test -- orders.deliver.integration`. All 7 MUST PASS. Common pitfalls if they don't: `::text[]` cast omitted; aggregation map keyed on `line.id` instead of `careUnitMedicationId`; `deletedAt` check happening AFTER the UPDATE; `findFirst` vs `findUnique` in reload.

    Step F — full BE suite passes — `pnpm --filter @meditrack/api test`. Slice A's confirm suite + Phase 3's D-73 suite MUST still pass.
  </action>
  <verify>
    <automated>pnpm --filter @meditrack/api test -- orders.deliver.integration &amp;&amp; pnpm --filter @meditrack/api test</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/src/services/order.service.ts` exports `async function deliverOrder(careUnitId: string, orderId: string, actorUserId: string): Promise<OrderResponse>`.
    - `deliverOrder`'s body contains TWO `$queryRaw` calls with `FOR UPDATE`: one against `"Order"` (single row) and one against `"CareUnitMedication"` (batch with `WHERE id = ANY(...)`).
    - The CUM batch lock query includes `ORDER BY id` before `FOR UPDATE` (sorted-id deadlock prevention per D-79).
    - The CUM batch lock query uses `::text[]` cast on the interpolated array parameter.
    - `deliverOrder` aggregates same-CUM lines via `Map<string, number>` keyed on `careUnitMedicationId` BEFORE issuing any UPDATE (verify by reading the function body — there should be a `for…of order.lines` building the map, then a separate loop issuing `tx.careUnitMedication.update` per CUM).
    - `deliverOrder` issues `tx.careUnitMedication.update({ data: { currentStock: { increment: qty } } })` (NOT `set:`, NOT raw arithmetic outside Prisma — must use `{ increment }`).
    - `deliverOrder` checks `careUnitMedication.deletedAt !== null` BEFORE any stock UPDATE and throws `ValidationFailedError({ reason: 'medication_removed', medicationName })`.
    - `deliverOrder` final UPDATE uses `tx.order.updateMany({ where: { ..., status: 'bekraftad', deletedAt: null }, data: { status: 'levererad', deliveredAt, deliveredByUserId } })` with `OrderTransitionError` on `count === 0`.
    - `apps/api/src/routes/orders/deliver.ts` exports `deliverOrderRoute(app)` and registers `POST /api/orders/:id/deliver` with `[requireSession, requirePermission('order:deliver')]`.
    - `apps/api/src/routes/orders/index.ts` calls `await app.register(deliverOrderRoute);` after `confirmOrderRoute`.
    - `apps/api/test/orders.deliver.integration.test.ts` exists with 7 `it` blocks covering happy path, wrong-status, double-deliver, cross-careUnit, sjuksköterska 403, medication_removed 422, line aggregation.
    - `pnpm --filter @meditrack/api test -- orders.deliver.integration` exits 0 with all 7 tests passing.
    - `pnpm --filter @meditrack/api test` (full suite) exits 0 — no regressions on Slice A's confirm suite or Phase 3's D-73 suite.
  </acceptance_criteria>
  <done>`deliverOrder` service implements the full 10-step D-79 transaction including the CUM batch lock + per-CUM aggregated stock increment + atomic Order UPDATE. Route registered. 7-case integration test passes including the 422 medication_removed branch and the per-CUM line aggregation case. No regressions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Deliberate concurrency test (OPS-03) — pg_locks-instrumented two-phase barrier</name>
  <files>apps/api/test/orders.deliver.integration.test.ts</files>
  <behavior>
    - Test 8 (concurrency): seed a Bekräftad order with 1 line (qty 5) against a known CUM. Snapshot the CUM's starting `currentStock`. Open Tx-A as a `prisma.$transaction` that acquires the Order-row `FOR UPDATE` and then awaits an externally-resolvable gate (an unresolved Promise). After a 50ms `setTimeout`, fire Tx-B by calling `deliverOrder(careUnitId, orderId, apotekare.id)` directly (NOT via app.inject — D-86). Poll `pg_locks` joined to `pg_stat_activity` filtered to `wait_event_type = 'Lock'` and assert that Tx-B is observed in a blocked state at least once during the barrier window. Resolve Tx-A's gate (Tx-A then commits its own `deliverOrder`-equivalent UPDATE — or simply releases). Await Tx-B; expect it to reject with `OrderTransitionError` whose `code === 'order_transition_invalid'` AND `details.expected === 'bekraftad'` (because by the time Tx-B's lock is granted, Tx-A has already committed status='levererad' and Tx-B's `updateMany WHERE status='bekraftad'` returns count===0). Reload the CUM. Assert `currentStock` incremented by EXACTLY 5 (not 10 — proves the race was won by exactly one transaction). Total test duration target: <500 ms.
  </behavior>
  <read_first>
    - apps/api/test/orders.deliver.integration.test.ts (Task 1 state — has the lifecycle hooks + helpers + 7 happy-path blocks; add the 8th block here)
    - apps/api/src/services/order.service.ts (deliverOrder body — Task 2 imports the function directly per D-86)
    - apps/api/src/db/client.ts (verify the exported `prisma` client used by both the test and the service is the same instance — required for tx isolation observability)
    - .planning/phases/04-confirm-deliver-stock/04-PATTERNS.md (orders.deliver.integration.test.ts test block 2 — full pg_locks polling query example)
    - .planning/phases/04-confirm-deliver-stock/04-CONTEXT.md (D-86 real prisma tx not inject, D-87 single file two it blocks, D-88 two-phase barrier with pg_locks proof step-by-step, D-89 race shape)
    - .planning/PROJECT.md (Key Decisions row: "Stock decrement uses Postgres transaction + SELECT … FOR UPDATE on medication row" — this test is the proof for that decision and answers brief §6)
  </read_first>
  <action>
    Add the 8th `it` block to `apps/api/test/orders.deliver.integration.test.ts` implementing the deliberate concurrency test per D-88. Concretely:

    Step A — import `deliverOrder` directly at the top of the test file: `import { deliverOrder } from '../src/services/order.service.js';`. D-86 mandates calling the service function directly (NOT via `app.inject`) so two concurrent calls land on parallel DB connections rather than serializing on Fastify's single event loop.

    Step B — write the `it('two concurrent deliveries on same Bekraftad order: one commits, other gets 409, stock incremented exactly once', async () => { … })` block. The structure:
    (1) Seed a Bekräftad order with one known CUM and one line of `quantity: 5` using `progressOrderToBekraftad`.
    (2) Snapshot `before = await prisma.careUnitMedication.findUnique({ where: { id: cum.id } })`.
    (3) Create a gate: `let releaseTxA: () => void; const gate = new Promise<void>((resolve) => { releaseTxA = resolve; });`.
    (4) Tx-A — start a `prisma.$transaction(async (tx) => { await tx.$queryRaw\`SELECT id FROM "Order" WHERE id = ${orderId} FOR UPDATE\`; await gate; /* now release lock by completing the tx normally */ })` as a Promise stored in `txAPromise`. This holds the Order-row lock until the gate resolves.
    (5) After `await new Promise(r => setTimeout(r, 50))` to ensure Tx-A has acquired its lock, fire Tx-B as `const txBPromise = deliverOrder(careUnitId, orderId, apotekare.id);` (a real call into the service — internally it will issue its own `$queryRaw FOR UPDATE` against the Order row and BLOCK).
    (6) Wait a tiny moment (50ms) and poll `pg_locks`: `const blockedRows = await prisma.$queryRaw\`SELECT pid, mode, granted FROM pg_locks JOIN pg_stat_activity USING (pid) WHERE wait_event_type = 'Lock' AND query LIKE '%Order%'\`;` — repeat in a tight loop up to ~200ms total to handle test flakiness. Assert at least one row was observed where `granted = false` (proves Tx-B was actually blocked at the DB level, not a JS race).
    (7) `releaseTxA()` to let Tx-A commit. Note: Tx-A in this test does NOT itself execute the full deliver logic — it just holds the Order-row lock. So after Tx-A commits, the order is still in `'bekraftad'` state. This means when Tx-B unblocks and runs its full `deliverOrder`, it WILL succeed (which contradicts our race story). Adjust: Tx-A SHOULD itself complete a real deliver. Use this shape instead: Tx-A is `deliverOrder(careUnitId, orderId, apotekare.id)` invoked normally, but wrap deliverOrder with an instrumentation hook OR (simpler) implement Tx-A as a parallel `deliverOrder` call started 50ms BEFORE Tx-B: `const txAPromise = deliverOrder(careUnitId, orderId, apotekare.id); await new Promise(r => setTimeout(r, 50)); const txBPromise = deliverOrder(careUnitId, orderId, apotekare.id);` and then `const [aResult, bResult] = await Promise.allSettled([txAPromise, txBPromise]);`. The DB-level `FOR UPDATE` serializes them; the `updateMany WHERE status='bekraftad'` precondition makes the second one fail. The pg_locks poll happens inside the test's main `await` window — between starting Tx-A and starting Tx-B, the test should poll once to observe that Tx-A is holding the lock. Between Tx-B's start and the resolution of `Promise.allSettled`, the test polls again to observe Tx-B is blocked (granted=false). This is the simpler, more honest shape and matches PATTERNS.md's test block 2 sketch.
    (8) Assertions: `expect(aResult.status).toBe('fulfilled')` AND `expect(bResult.status).toBe('rejected')`. `expect((bResult.reason as OrderTransitionError).code).toBe('order_transition_invalid')`. `expect((bResult.reason as OrderTransitionError).details.expected).toBe('bekraftad')`. Reload CUM: `const after = await prisma.careUnitMedication.findUnique({ where: { id: cum.id } });` and `expect(after!.currentStock).toBe(before!.currentStock + 5)` (exactly one increment, not two).
    (9) The pg_locks evidence: store the polled rows in a variable and `expect(blockedRows.length).toBeGreaterThan(0)` with at least one row where `granted === false` (proves DB-level lock contention occurred — without this assertion the test could false-pass under a sequential implementation).

    Step B follow-up if D-88's exact two-phase barrier (with the manual gate) is preferred for cleaner pg_locks observation: keep the manual gate approach BUT have Tx-A's body include the actual `updateMany` step inline (replicating step 9 of `deliverOrder`'s tx) rather than calling the function. This is more verbose but gives stable observation of the "Tx-B blocked while Tx-A holds lock" window. Either shape (Promise.allSettled simpler OR manual gate cleaner) is acceptable as long as: (a) Tx-B is observed as `granted = false` in `pg_locks` at least once, (b) exactly one tx commits the levererad transition, (c) stock incremented exactly once. Pick the shape that produces stable test runs and document the choice in a code comment referencing D-88.

    Step C — verify timing budget. Total it() duration target: <500ms per CONTEXT D-88. If polling intervals push it over, reduce poll frequency to every 10ms with a 200ms hard ceiling.

    Step D — verify the full suite still passes including the 8 tests: `pnpm --filter @meditrack/api test`.
  </action>
  <verify>
    <automated>pnpm --filter @meditrack/api test -- orders.deliver.integration &amp;&amp; pnpm --filter @meditrack/api test</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/test/orders.deliver.integration.test.ts` contains an 8th `it` block whose name includes the words "concurrent" and "exactly once" (or equivalently descriptive — the assertion is what matters).
    - The concurrency `it` block imports `deliverOrder` directly from `'../src/services/order.service.js'` (NOT via `app.inject`).
    - The block issues at least one `$queryRaw` query against `pg_locks` joined to `pg_stat_activity` (verify by grep on the test file for `pg_locks`).
    - The block asserts at least one polled row had `granted === false` (or equivalently — uses the polled data to prove DB-level contention).
    - The block asserts exactly one of the two delivery attempts succeeded and the other rejected with `OrderTransitionError` whose `code === 'order_transition_invalid'`.
    - The block asserts the CUM's `currentStock` was incremented by EXACTLY the line quantity once (e.g., `before + 5`, not `before + 10`).
    - `pnpm --filter @meditrack/api test -- orders.deliver.integration` exits 0 with all 8 tests passing.
    - The concurrency test's runtime is bounded — the test does not hang. (CI safety: `it.concurrent` is NOT used; vitest default per-test timeout is 5s which is more than enough headroom for the <500ms target.)
    - `pnpm --filter @meditrack/api test` (full suite) exits 0.
  </acceptance_criteria>
  <done>The deliberate concurrency test exists, runs in <500ms, proves at the DB level (via pg_locks observation) that two parallel `deliverOrder` calls against the same Bekräftad order serialize on the Order-row `FOR UPDATE` lock, exactly one commits, the other receives `order_transition_invalid`, and stock is incremented exactly once. This is the §6 interview proof.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: useDeliverOrder hook + DeliverConfirmDialog + Mode D/E on ComposeOrderPage + OrderActorTrail</name>
  <files>apps/web/src/features/orders/useOrderMutations.ts, apps/web/src/routes/bestallningar/DeliverConfirmDialog.tsx, apps/web/src/routes/bestallningar/OrderActorTrail.tsx, apps/web/src/routes/bestallningar/ComposeOrderPage.tsx</files>
  <read_first>
    - apps/web/src/features/orders/useOrderMutations.ts (post-Slice-A — has useConfirmOrder as the literal template)
    - apps/web/src/routes/bestallningar/DiscardDraftDialog.tsx (Phase 3 — literal template for DeliverConfirmDialog including the Cancel-first focus management + `e.preventDefault()` on Action)
    - apps/web/src/routes/bestallningar/ComposeOrderPage.tsx (post-Slice-A state — Mode A/B/C branches; Mode C uses ApotekareActionFooter from Slice A)
    - apps/web/src/routes/bestallningar/ApotekareActionFooter.tsx (Slice A — reused for Mode D button)
    - apps/web/src/components/Can.tsx + useAuth.ts (verify `<Can action="order:deliver">`)
    - packages/shared/src/constants/orderStatus.ts (ORDER_STATUS_LABELS — used in deliver error toast)
    - .planning/phases/04-confirm-deliver-stock/04-PATTERNS.md (useOrderMutations.ts useDeliverOrder section; DeliverConfirmDialog token map; ComposeOrderPage Mode D/E sections)
    - .planning/phases/04-confirm-deliver-stock/04-UI-SPEC.md (§Components 2/3/4/5 — Mode D + Mode E + DeliverConfirmDialog + Status Banners + Actor Trail; §Toast Feedback; §Copywriting Contract — every Swedish string is verbatim)
    - .planning/phases/04-confirm-deliver-stock/04-CONTEXT.md (D-83 Mode D/E + AlertDialog gate, D-81 422 medication_removed toast wording, D-89 race shape)
  </read_first>
  <action>
    Wire the FE deliver flow end-to-end, including the actor-trail shared component used by both Mode D and Mode E. Concretely:

    A. **`useOrderMutations.ts` — add `useDeliverOrder`:** Clone `useConfirmOrder` (Slice A). Change URL to `/api/orders/${orderId}/deliver`. `onSuccess`: `queryClient.setQueryData(['order', vars.orderId], response)` + invalidate `['orders', { status: 'bekraftad' }]` + invalidate `['orders', { status: 'levererad' }]` + **invalidate `['medications']`** (this is the Phase 6 NTF-01 hook per D-83 — broad key match so all medication list queries refetch). Then `toast.success('Levererad — lagret uppdaterat')`. `onError`: branch on `err.envelope.error.code`:
       - `'order_transition_invalid'` → `toast.error(\`Beställningen har redan ${ORDER_STATUS_LABELS[err.envelope.error.details.from]}.\`)` + invalidate `['order', vars.orderId]`.
       - `'validation_failed'` with `details.reason === 'medication_removed'` → `toast.error(\`${err.envelope.error.details.medicationName} har tagits bort — återställ läkemedlet i registret innan leverans.\`)`.
       - `'not_found'` → `toast.error('Beställning hittades inte.')`.
       - default → `toast.error('Kunde inte spara — försök igen.')`.

    B. **New file `apps/web/src/routes/bestallningar/DeliverConfirmDialog.tsx`:** Clone `DiscardDraftDialog.tsx` per PATTERNS.md token map: Title `Markera som levererad?`, Description `Stocken uppdateras direkt. Detta kan inte ångras.`, Cancel `Avbryt`, Action `Markera levererad` / loading `Levererar…`. Prop type: `{ open: boolean; onOpenChange: (open: boolean) => void; onConfirm: () => void; isDelivering: boolean }`. Rules: Cancel rendered FIRST (focus management). `e.preventDefault()` on Action `onClick` to prevent auto-dismiss before mutation resolves. `disabled={isDelivering}` on BOTH Cancel and Action. Per UI-SPEC §4 the Action uses default (NOT destructive) styling — stock additions are not deletions; the body copy is the warning signal.

    C. **New file `apps/web/src/routes/bestallningar/OrderActorTrail.tsx`:** Per UI-SPEC §5. Props: `{ createdBy, createdAt, submittedBy, submittedAt, confirmedBy, confirmedAt, deliveredBy, deliveredAt }`. Renders `<p className="text-xs text-muted-foreground flex flex-wrap gap-x-1 gap-y-1 mt-4">` with conditional segments: always `Skapad av {createdBy.name}`; conditionally `· Skickad av {submittedBy.name} {HH:mm}` if submittedBy; conditionally `· Bekräftad av {confirmedBy.name} {HH:mm}` if confirmedBy; conditionally `· Levererad av {deliveredBy.name} {HH:mm}` if deliveredBy. Time format `HH:mm` via `format(new Date(timestamp), 'HH:mm')` using `date-fns`. Separator: ` · ` (space + U+00B7 + space).

    D. **`ComposeOrderPage.tsx` — add Mode D (Bekräftad + apotekare) and Mode E (Levererad):**
       - Add `isBekraftad`, `isLevererad` booleans (Slice A already added `isSkickad`).
       - Add `useCan('order:deliver')` → `canDeliver`.
       - Add local state `const [deliverDialogOpen, setDeliverDialogOpen] = useState(false);` and `const deliverMutation = useDeliverOrder();`.
       - **Mode D rendering** (when `isBekraftad`): heading `Beställning · Bekräftad`; `<OrderStatusPill status="bekraftad" />`; Mode D banner per UI-SPEC §Components 2 (Clock icon, blue tint, copy `Beställningen är bekräftad — väntar på leverans.`); read-only line list (reuse Phase 3 `OrderLineTable`/`OrderLineCardList` with `isLocked={true}` if those props exist; otherwise render the lines in read-only mode following the Mode B pattern). When `canDeliver`, render `<Can action="order:deliver"><ApotekareActionFooter label="Markera som levererad" onClick={() => setDeliverDialogOpen(true)} isPending={deliverMutation.isPending} loadingLabel="Levererar…" /></Can>`. Render `<OrderActorTrail …all 4 trios from order…/>` (deliveredBy will be null in Mode D, the component will skip that segment).
       - **`<DeliverConfirmDialog>` mount** with `open={deliverDialogOpen}`, `onOpenChange={setDeliverDialogOpen}`, `isDelivering={deliverMutation.isPending}`, `onConfirm={async () => { try { await deliverMutation.mutateAsync({ orderId: order.id }); setDeliverDialogOpen(false); } catch { /* mutation hook owns toast; dialog stays open on generic failure per UI-SPEC §3 step 5 */ } }}`. For 409/422 errors the mutation's onError already fires the toast; close the dialog there by setting `deliverDialogOpen(false)` after the toast (the simplest way: in the `onConfirm`, ALWAYS close the dialog after `mutateAsync` resolves OR rejects with `order_transition_invalid`/`validation_failed`/`not_found`, but keep it open for generic errors). UI-SPEC §3 step 2-5 prescribes: success → dialog closes; 409/422 → dialog closes + toast; generic error → dialog stays open + toast + action re-enables. Implement that branching cleanly.
       - **Mode E rendering** (when `isLevererad`): heading `Beställning · Levererad`; `<OrderStatusPill status="levererad" />`; Mode E banner (CheckCircle2 icon, emerald palette, copy `Beställningen är levererad — lagret uppdaterat.`); read-only line list; full `<OrderActorTrail …/>` with all 4 trios populated. NO action button, NO sticky footer (Mode E is terminal — D-76, D-83).
       - **Sjuksköterska view** for bekraftad/levererad: when `canDeliver === false`, render the Mode D banner + lines + actor trail BUT no action button + no DeliverConfirmDialog mount (per UI-SPEC §Sjuksköterska View). For levererad it's identical for all roles since no action button exists anyway.
       - Widen `useDocumentTitle` to set `Beställning · Bekräftad — MediTrack` and `Beställning · Levererad — MediTrack` per UI-SPEC §Copywriting.

    E. **Slice A's Mode C stub for bekraftad** — Slice A rendered a minimal fallback for Bekräftad. Replace that fallback with the proper Mode D implementation above. Same for the Levererad stub if Slice A added one.

    F. Manual self-verification (smoke): with the seed extension from Slice C not yet present, create a test order via the API (or wait for Slice C's seeded Bekräftad order). Log in as apotekare, navigate to the order, verify Mode D renders. Click `Markera som levererad`, verify AlertDialog opens. Click `Markera levererad`, verify spinner + dialog disabled, then dialog closes + toast `Levererad — lagret uppdaterat` + page re-renders in Mode E + actor trail shows all four segments. Navigate to `/lakemedel` (use the existing Phase 2 page); verify the relevant CUM's stock count is updated (the `['medications']` invalidation triggers refetch).
  </action>
  <verify>
    <automated>pnpm --filter @meditrack/web typecheck &amp;&amp; pnpm --filter @meditrack/web build &amp;&amp; pnpm --filter @meditrack/web test</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/features/orders/useOrderMutations.ts` exports `useDeliverOrder` returning a `useMutation` against `POST /api/orders/${orderId}/deliver`.
    - `useDeliverOrder.onSuccess` invalidates `['orders', { status: 'bekraftad' }]`, `['orders', { status: 'levererad' }]`, AND `['medications']` (verify by grep — all three invalidation calls present).
    - `useDeliverOrder.onError` has branches for `'order_transition_invalid'`, `'validation_failed'` (with `reason === 'medication_removed'`), `'not_found'`, and a default fallback — each firing the exact Swedish toast strings from UI-SPEC §Toast Feedback.
    - The `medication_removed` toast extracts `err.envelope.error.details.medicationName` for the `{med.name}` slot.
    - `apps/web/src/routes/bestallningar/DeliverConfirmDialog.tsx` exists, exports `DeliverConfirmDialog` with the exact props `{ open, onOpenChange, onConfirm, isDelivering }`.
    - DeliverConfirmDialog title is `Markera som levererad?` (verify by grep) and description is `Stocken uppdateras direkt. Detta kan inte ångras.`.
    - DeliverConfirmDialog renders `<AlertDialogCancel>` BEFORE `<AlertDialogAction>` (Cancel-first focus management per Phase 3 D-67 precedent + UI-SPEC §3).
    - DeliverConfirmDialog Action's onClick calls `e.preventDefault()` before invoking `onConfirm()` (prevents Radix auto-dismiss before mutation resolves).
    - `apps/web/src/routes/bestallningar/OrderActorTrail.tsx` exists, exports `OrderActorTrail` and conditionally renders only the segments whose corresponding `*By` prop is non-null. Time formatted as `HH:mm` (verify by grep for `'HH:mm'` or the date-fns format call).
    - `apps/web/src/routes/bestallningar/ComposeOrderPage.tsx` renders Mode D when `order.status === 'bekraftad'` AND `canDeliver`: heading `Beställning · Bekräftad`, Mode D banner with copy `Beställningen är bekräftad — väntar på leverans.`, action button via `<ApotekareActionFooter label="Markera som levererad" …/>` wrapped in `<Can action="order:deliver">`, and `<OrderActorTrail …/>`.
    - `ComposeOrderPage.tsx` renders Mode E when `order.status === 'levererad'`: heading `Beställning · Levererad`, Mode E banner with copy `Beställningen är levererad — lagret uppdaterat.`, full `<OrderActorTrail/>`, NO action button (verify by grep — no `ApotekareActionFooter` inside the `isLevererad` branch).
    - For sjuksköterska viewer (canDeliver=false) on a Bekräftad order, NO `Markera som levererad` button is rendered AND NO `<DeliverConfirmDialog>` is mounted (verify via the `<Can>` wrapper or explicit conditional).
    - `useDocumentTitle` sets `Beställning · Bekräftad — MediTrack` for bekraftad and `Beställning · Levererad — MediTrack` for levererad.
    - `pnpm --filter @meditrack/web typecheck` exits 0.
    - `pnpm --filter @meditrack/web build` exits 0.
    - `pnpm --filter @meditrack/web test` exits 0.
  </acceptance_criteria>
  <done>FE deliver flow shipped end-to-end: useDeliverOrder hook with medications cache invalidation (Phase 6 NTF-02 ready); DeliverConfirmDialog AlertDialog gate; Mode D renders apotekare action button + banner + partial actor trail; Mode E renders terminal banner + full actor trail; sjuksköterska sees read-only view with no button. All Swedish copy verbatim from UI-SPEC.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API | Untrusted POST /deliver request from FE; cookie session |
| API → DB | $queryRaw FOR UPDATE locks the Order row + CUM rows in sorted order; per-CUM increments via Prisma {increment: n} |
| Cross-careUnit | Apotekare A from careUnit X delivering an order from careUnit Y |
| Cross-role | Sjuksköterska invoking /deliver directly |
| Concurrency | Two apotekare delivering the same Bekräftad order simultaneously |
| Catalog mutation race | Apotekare soft-deletes a CUM at the moment another apotekare is delivering an order referencing it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | Tampering (race) | Two concurrent deliveries on same Bekräftad order | mitigate | `$queryRaw … FOR UPDATE` on Order row at tx start serializes; `updateMany WHERE status='bekraftad'` precondition makes the second deliver return `count === 0` → `OrderTransitionError(409)`. Task 2 PROVES this with a pg_locks-instrumented test against real Postgres (D-88). |
| T-04-08 | Tampering (deadlock) | Two deliveries across different orders that share CUMs (e.g., order O1 delivers CUMs {A,B}; order O2 delivers CUMs {B,A}) | mitigate | Sorted-id ordering on the CUM batch lock (D-79 step 7): `WHERE id = ANY($1) ORDER BY id FOR UPDATE` ensures both tx attempt locks in identical order — Postgres serializes, never deadlocks. Phase 4 ships the same-order test; the cross-order case is deferred to Phase 7 README §6 polish (D-89). The sorted ordering is the correctness story regardless. |
| T-04-09 | Tampering / Integrity | Soft-deleted CUM at deliver time → stock corruption | mitigate | D-81 — step 5 of deliverOrder checks `careUnitMedication.deletedAt !== null` BEFORE any UPDATE; throws `ValidationFailedError({reason: 'medication_removed'})` which rolls back the tx. Test 6 in Task 1 proves no stock change on this branch. |
| T-04-10 | Information Disclosure / Tampering | Cross-tenant /deliver | mitigate | Service first-arg is `careUnitId`; existence+scope check throws `NotFoundError` (404, not 403, per D-73). Test 4 in Task 1 proves. |
| T-04-11 | Elevation of Privilege | Sjuksköterska calling /deliver | mitigate | `requirePermission('order:deliver')` preHandler rejects with 403 before the service ever runs (D-15). `<Can action="order:deliver">` hides the FE button (UX gate). Test 5 in Task 1 proves the 403. |
| T-04-12 | Repudiation | Apotekare denies delivering an order | mitigate | `deliveredByUserId` FK to User stamped inside the same tx as the status flip + stock increment. `onDelete: Restrict` preserves the FK if a user is deleted. Phase 5 audit-log middleware will record actor + before/after diff atop this. |
| T-04-13 | DoS / Resource exhaustion | Long-running CUM batch lock blocks catalog reads | accept | Read paths (Phase 2 `/api/medications`) use plain SELECTs (no `FOR UPDATE`); they observe the OLD `currentStock` until the deliver tx commits (Postgres READ COMMITTED default). Reads do not block; only competing writes (other deliveries, manual stock edits) wait. Phase 2's manual stock edits are single-row updates — minimal contention window. |
| T-04-14 | Integer overflow | `currentStock` is `Int` (signed 32-bit, ~2.1B cap) | accept | Documented in CONTEXT.md security threat surfaces as accepted v1 risk. 2.1B units per medication per care unit is multiple orders of magnitude beyond any realistic healthcare unit's storage. v2 idea: add a cap check + error code, or migrate to BigInt. Not in Phase 4 scope. |
| T-04-15 | Replay / Idempotency | Apotekare hits /deliver twice rapidly (network retry, double-click) | mitigate | The `updateMany WHERE status='bekraftad'` precondition makes /deliver naturally idempotent — the second call returns `count === 0` → 409, no double-increment. FE `disabled={isPending}` on the dialog Action button + AlertDialog `isDelivering` blocks the obvious double-click case at the UI. |
| T-04-16 | UI gate bypass | Sjuksköterska crafts a curl POST to /deliver | mitigate | UI `<Can>` is NOT the security boundary (D-17). `requirePermission` preHandler is. Test 5 proves the 403. |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | Slice B introduces NO new npm packages — uses existing date-fns, sonner, shadcn AlertDialog, lucide-react Clock/CheckCircle2. No `[ASSUMED]`/`[SUS]` gating needed. |
</threat_model>

<verification>
- All three Tasks' `<verify>` commands exit 0.
- `pnpm --filter @meditrack/api test` (full BE suite) passes — Slice A's confirm test + Phase 3's D-73 suite still pass.
- `pnpm --filter @meditrack/web typecheck && pnpm --filter @meditrack/web build && pnpm --filter @meditrack/web test` pass.
- Manual smoke (if a Bekräftad order is reachable — Slice C ships the seed; for Slice B-only verification, advance an order through API): log in as apotekare, open the Bekräftad order, click `Markera som levererad`, confirm in the AlertDialog, see the page flip to Mode E with the full actor trail, then navigate to `/lakemedel` and observe the relevant medications' currentStock incremented.
- Manual smoke (sjuksköterska): log in as sjukskoterska, open a Bekräftad order, verify Mode D banner + read-only lines + partial actor trail render WITHOUT a Markera-som-levererad button.
- Manual smoke (concurrency): not feasible by hand; covered by Task 2's automated test.
</verification>

<success_criteria>
- ORD-05 satisfied: apotekare advances Bekräftad → Levererad via POST /deliver.
- ORD-06 completed (was partial in Slice A): all invalid forward jumps return 409 `order_transition_invalid` from both /confirm and /deliver; backward jumps don't exist (D-76).
- STK-01 satisfied: stock increment happens atomically inside the deliver tx; soft-deleted CUM aborts with 422 and no stock change.
- STK-02 satisfied: `SELECT … FOR UPDATE` on Order row AND on CUM batch (sorted-id ordering); test 8 PROVES at the DB level via pg_locks observation.
- OPS-03 satisfied: integration test covers full pipeline create→submit→confirm→deliver AND the deliberate concurrency case.
- FE: `useDeliverOrder` hook hydrates cache + invalidates orders + medications (Phase 6 NTF-02 ready); `<DeliverConfirmDialog>` mounted on Mode D; Mode E renders terminal state with full actor trail; sjuksköterska sees read-only without action buttons.
- Workspace builds + typechecks + all tests pass.
</success_criteria>

<output>
Create `.planning/phases/04-confirm-deliver-stock/04-02-SUMMARY.md` when done.
</output>
