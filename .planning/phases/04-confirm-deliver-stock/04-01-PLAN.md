---
phase: 04-confirm-deliver-stock
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/prisma/schema.prisma
  - apps/api/prisma/migrations/0006_order_confirm_deliver/migration.sql
  - packages/shared/src/contracts/order.ts
  - packages/shared/src/contracts/permissions.ts
  - apps/api/src/auth/permissions.ts
  - apps/api/src/plugins/errorHandler.ts
  - apps/api/src/services/order.service.ts
  - apps/api/src/routes/orders/confirm.ts
  - apps/api/src/routes/orders/index.ts
  - apps/web/src/features/orders/useOrderMutations.ts
  - apps/web/src/routes/bestallningar/ComposeOrderPage.tsx
  - apps/web/src/routes/bestallningar/ApotekareActionFooter.tsx
  - apps/api/test/orders.confirm.integration.test.ts
autonomous: true
requirements: [ORD-04, ORD-06]
must_haves:
  truths:
    - "Apotekare/admin can confirm a Skickad order; status flips to Bekräftad and confirmedAt/confirmedByUserId are stamped atomically (T1, T4)."
    - "POST /api/orders/:id/confirm returns 200 with the full OrderResponse (widened with confirmedBy actor + deliveredBy nullable trio) (T6)."
    - "Invalid transition (e.g. Utkast → Bekräftad, double-confirm) returns 409 error code `order_transition_invalid` with details {from, to, expected} (T1)."
    - "Cross-careUnit confirm returns 404 not 403 (T5)."
    - "Sjuksköterska calling /confirm receives 403 (RBAC preHandler); FE Mode C button hidden via `<Can action=\"order:confirm\">` (T4, T10)."
    - "Confirm mutation's onSuccess calls setQueryData(['order', id], response) + invalidates ['orders', {status:'skickad'}] and ['orders', {status:'bekraftad'}] (T9)."
  artifacts:
    - path: "apps/api/prisma/schema.prisma"
      provides: "Order model with confirmedAt, confirmedByUserId, deliveredAt, deliveredByUserId columns + 2 named relations + 2 User inverse fields"
      contains: "confirmedAt DateTime?"
    - path: "apps/api/prisma/migrations/0006_order_confirm_deliver/migration.sql"
      provides: "Additive migration adding 4 nullable columns + 2 FK constraints to Order"
    - path: "packages/shared/src/contracts/order.ts"
      provides: "Widened orderResponse with confirmedAt/By/By-trio and deliveredAt/By/By-trio nullable; confirmOrderRequest/deliverOrderRequest empty-body schemas"
    - path: "packages/shared/src/contracts/permissions.ts"
      provides: "ACTION_KEYS appended with 'order:confirm' and 'order:deliver'"
    - path: "apps/api/src/auth/permissions.ts"
      provides: "PERMISSIONS map appended with 'order:confirm' and 'order:deliver' → ['apotekare','admin']"
    - path: "apps/api/src/plugins/errorHandler.ts"
      provides: "OrderTransitionError class + 409 branch; ValidationFailedError.details.reason widened with 'medication_removed'"
    - path: "apps/api/src/services/order.service.ts"
      provides: "confirmOrder(careUnitId, orderId, actorUserId): Promise<OrderResponse>; toOrderResponse widened with confirmedBy + deliveredBy; every reload include() widened with confirmedBy + deliveredBy"
    - path: "apps/api/src/routes/orders/confirm.ts"
      provides: "POST /api/orders/:id/confirm with [requireSession, requirePermission('order:confirm')]"
    - path: "apps/web/src/features/orders/useOrderMutations.ts"
      provides: "useConfirmOrder mutation with pessimistic cache hydration + status-aware error toasts"
    - path: "apps/web/src/routes/bestallningar/ComposeOrderPage.tsx"
      provides: "Mode C (Skickad+apotekare) renders Bekräfta beställning button; transitions to Mode B after confirm"
    - path: "apps/api/test/orders.confirm.integration.test.ts"
      provides: "Integration test: confirm happy path, 409 on wrong status, 404 on cross-careUnit, 403 on sjuksköterska"
  key_links:
    - from: "apps/api/src/services/order.service.ts confirmOrder"
      to: "apps/api/src/plugins/errorHandler.ts OrderTransitionError"
      via: "throw inside $transaction when updateMany count===0"
      pattern: "throw new OrderTransitionError"
    - from: "apps/api/src/routes/orders/confirm.ts"
      to: "apps/api/src/auth/requirePermission.ts"
      via: "preHandler: [requireSession, requirePermission('order:confirm')]"
      pattern: "requirePermission\\('order:confirm'\\)"
    - from: "apps/web/src/features/orders/useOrderMutations.ts useConfirmOrder"
      to: "apps/web/src/routes/bestallningar/ComposeOrderPage.tsx Mode C"
      via: "mutateAsync({orderId}) on Bekräfta beställning click"
      pattern: "useConfirmOrder"
    - from: "packages/shared/src/contracts/permissions.ts ACTION_KEYS"
      to: "apps/api/src/auth/permissions.ts PERMISSIONS"
      via: "Record<ActionKey, Role[]> drift-prevention (D-15)"
      pattern: "'order:confirm':"
---

<objective>
Slice A — ship the **confirm** half of the order lifecycle end-to-end. This plan lays the foundation Slice B + Slice C depend on: the schema migration (additive, includes BOTH confirm AND deliver columns so Slice B does not migrate again), the shared contract widening (orderResponse + new request shapes), the RBAC map entries, the OrderTransitionError class + 422 reason union widening, and the `confirmOrder` service + route. It then closes the vertical slice with `useConfirmOrder` + Mode C wiring on the FE so an apotekare can click "Bekräfta beställning" on a Skickad order and see it flip to Bekräftad. Ships ORD-04 (confirm transition) and ORD-06 (linear forward-only contract with 409 on invalid jumps).

Purpose: deliver a complete, demonstrable confirm flow while laying down all shared contracts (errors, schemas, RBAC keys, response shapes) that Slices B + C inherit. The migration includes BOTH confirm AND deliver columns intentionally so Slice B's deliver service can land without a second migration round-trip (CR-08 — additive, all nullable).
Output: working `POST /api/orders/:id/confirm`; widened Order schema with all 4 new actor columns; new error class + RBAC keys; Mode C on ComposeOrderPage; passing confirm integration test.
</objective>

<execution_context>
@C:/Projekt/MediTrack/.claude/get-shit-done/workflows/execute-plan.md
@C:/Projekt/MediTrack/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-confirm-deliver-stock/04-CONTEXT.md
@.planning/phases/04-confirm-deliver-stock/04-PATTERNS.md
@.planning/phases/04-confirm-deliver-stock/04-UI-SPEC.md
@.planning/phases/03-draft-orders/03-CONTEXT.md
@.planning/phases/01-foundation-auth/01-CONTEXT.md

<interfaces>
<!-- Key contracts the executor needs. Use these directly — no codebase exploration required. -->

From `apps/api/src/services/order.service.ts` (existing — extend, do not rebuild):
- `submitOrder(careUnitId: string, orderId: string, actorUserId: string): Promise<OrderResponse>` — the literal template for `confirmOrder`. The `$queryRaw FOR UPDATE` Order-row lock (step 0), the line-validation block (step 4), the `updateMany` atomic precondition (step 5), and the final reload+return (step 6) are all reused verbatim with the status tokens swapped (`'utkast'→'skickad'` becomes `'skickad'→'bekraftad'`).
- `toOrderResponse(order)` — internal mapper. Phase 4 widens to include `confirmedAt`, `confirmedByUserId`, `confirmedBy {id, name} | null`, `deliveredAt`, `deliveredByUserId`, `deliveredBy {id, name} | null`. EVERY place that calls Prisma `include` (`createDraftOrder`, `listOrdersForUnit`, `getOrderForUnit`, line ops, `submitOrder`, `softDeleteOrder`) MUST add `confirmedBy: { select: { id: true, name: true } }` and `deliveredBy: { select: { id: true, name: true } }` to its include block so the widened `toOrderResponse` does not produce undefined fields.

From `apps/api/src/plugins/errorHandler.ts` (existing):
- `OrderLockedError` (code `'order_locked'`, 409) — the literal template for the new `OrderTransitionError` class. Stays narrow per D-77.
- `ValidationFailedError` (code `'validation_failed'`, 422) — `details.reason` union widens with `'medication_removed'`; add optional `medicationName?: string` for the toast slot.

From `packages/shared/src/contracts/order.ts` (existing):
- `orderResponse` — widen with confirm/deliver trios per D-84.
- `orderListItem` — widen with `submittedBy`, `confirmedBy`, `deliveredBy` nullable+optional (Slice C uses these; Slice A defines them here so the contract is stable).
- `orderListQuery.status` — widen to `z.union([orderStatusEnum, z.array(orderStatusEnum)]).default('utkast')` (Slice C parses the comma-list in `list.ts`; Slice A defines the schema here).
- Add `confirmOrderRequest = z.object({}).strict()` and `deliverOrderRequest = z.object({}).strict()`.

From `packages/shared/src/contracts/permissions.ts`:
- `ACTION_KEYS` literal tuple — append `'order:confirm'`, `'order:deliver'` after `'order:delete'`. The Zod `actionKey` enum auto-derives.

From `apps/api/src/auth/permissions.ts`:
- `PERMISSIONS: Record<ActionKey, Role[]>` — append `'order:confirm': ['apotekare','admin']` and `'order:deliver': ['apotekare','admin']` (D-15 / D-75). The `Record<ActionKey, …>` type makes omission a TypeScript error.

From `apps/web/src/features/orders/useOrderMutations.ts` (existing):
- `useSubmitOrder()` — literal template. Pessimistic; onSuccess: setQueryData(['order', id], response) + invalidateQueries(['orders', {status: 'utkast'}]).

From `apps/web/src/routes/bestallningar/ComposeOrderPage.tsx` (existing):
- Existing `isUtkast` branch + `SubmitConfirmationBanner` for Mode B. Phase 4 widens to `switch (order.status)` covering Mode C (skickad + apotekare → Bekräfta button) plus the existing Mode B (sjuksköterska on skickad → read-only).

From `apps/api/test/orders.integration.test.ts` (existing — D-73 suite):
- `helpers/buildTestApp.ts` exports `buildTestApp`, `ensureAllRolesSeeded`, `resetSessions`, `prisma`, `TEST_SJUKSKOTERSKA`, `TEST_APOTEKARE`. Local helpers `captureSessionCookie`, `loginAs`, `createEmptyOrder`, `findTestCareUnitMedication` are copy/paste candidates.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Schema + migration + shared contracts + RBAC keys + error class</name>
  <files>apps/api/prisma/schema.prisma, packages/shared/src/contracts/order.ts, packages/shared/src/contracts/permissions.ts, apps/api/src/auth/permissions.ts, apps/api/src/plugins/errorHandler.ts</files>
  <read_first>
    - apps/api/prisma/schema.prisma (current Order model — locate the submittedAt/By pair around line 187 and the User model's createdOrders + submittedOrders inverse around line 76)
    - packages/shared/src/contracts/order.ts (current orderResponse, orderListItem, orderListQuery, createOrderRequest shapes)
    - packages/shared/src/contracts/permissions.ts (current ACTION_KEYS tuple and the order:submit/order:delete entries)
    - apps/api/src/auth/permissions.ts (current PERMISSIONS map; Record<ActionKey, Role[]> type)
    - apps/api/src/plugins/errorHandler.ts (OrderLockedError class lines 77-85, ValidationFailedError class around line 99, the setErrorHandler branch ordering at lines 139-150 where OrderLockedError + ValidationFailedError are checked before the Zod branch)
    - .planning/phases/04-confirm-deliver-stock/04-PATTERNS.md (sections: schema.prisma, errorHandler.ts, shared contracts, permissions — every code excerpt has exact line citations)
    - .planning/phases/04-confirm-deliver-stock/04-CONTEXT.md (D-74 transition error contract, D-75 two-route layout, D-81 medication_removed, D-84 actor columns, D-15 drift-prevention)
    - .planning/phases/03-draft-orders/03-CONTEXT.md (D-49 submittedAt/By precedent — Phase 4 mirrors verbatim)
  </read_first>
  <action>
    Land the foundational schema + contract + RBAC + error changes in a single coordinated diff. Concretely:

    1. **schema.prisma:** On `Order`, add four columns matching the D-49 / D-84 convention: `confirmedAt DateTime?`, `confirmedByUserId String?` with `confirmedBy User? @relation(name: "OrderConfirmedBy", fields: [confirmedByUserId], references: [id], onDelete: Restrict)`, `deliveredAt DateTime?`, `deliveredByUserId String?` with `deliveredBy User? @relation(name: "OrderDeliveredBy", fields: [deliveredByUserId], references: [id], onDelete: Restrict)`. Mirror the docblock style of the existing `submittedAt/By` block (D-49 / D-84 references). On `User`, add two inverse fields next to `submittedOrders`: `confirmedOrders Order[] @relation("OrderConfirmedBy")` and `deliveredOrders Order[] @relation("OrderDeliveredBy")`. No new indexes (D-62's `@@index([careUnitId, createdAt])` already covers history sort).

    2. **packages/shared/src/contracts/order.ts:** Widen `orderResponse` per D-84 — append `confirmedAt: z.string().datetime().nullable()`, `confirmedByUserId: z.string().nullable()`, `confirmedBy: z.object({ id: z.string(), name: z.string() }).nullable()`, plus the parallel three deliver fields. Widen `orderListItem` per D-82 with `submittedAt`, `submittedBy`, `confirmedAt`, `confirmedBy`, `deliveredAt`, `deliveredBy` all `.nullable().optional()` so Slice C can render per-tab columns without backfill. Widen `orderListQuery.status` to `z.union([orderStatusEnum, z.array(orderStatusEnum)]).default('utkast')` (the route-level comma-list pre-parser lands in Slice C; the schema accepts both shapes today). Add `confirmOrderRequest = z.object({}).strict()` and `deliverOrderRequest = z.object({}).strict()` mirroring `createOrderRequest`. Export inferred TS types for both new request schemas.

    3. **packages/shared/src/contracts/permissions.ts:** Append `'order:confirm'` and `'order:deliver'` to `ACTION_KEYS` (after `'order:delete'`). The `actionKey` Zod enum auto-derives from the tuple — no other change.

    4. **apps/api/src/auth/permissions.ts:** Append `'order:confirm': ['apotekare', 'admin']` and `'order:deliver': ['apotekare', 'admin']` to `PERMISSIONS` (per D-15 / D-75). The `Record<ActionKey, Role[]>` type forces these entries — verify TypeScript compiles after step 3 above.

    5. **apps/api/src/plugins/errorHandler.ts:**
       - Define new exported class `OrderTransitionError` cloning `OrderLockedError` shape: `readonly code = 'order_transition_invalid' as const`, `readonly details: { from: OrderStatus; to: OrderStatus; expected: OrderStatus }`, constructor takes details, default message uses `ORDER_STATUS_LABELS[from]/to` interpolation (import labels from `@meditrack/shared`) so the server-side message is already Swedish-localized.
       - Widen `ValidationFailedError.details` type: `reason: 'empty_order' | 'invalid_quantity' | 'medication_removed'`, add optional `medicationName?: string` (populated only for `'medication_removed'`; the FE toast uses this slot).
       - In the `setErrorHandler` branch list, insert the OrderTransitionError branch BEFORE the Zod isZod branch and alongside OrderLockedError + ValidationFailedError per D-56 precedent: `if (err instanceof OrderTransitionError) return send(reply, 409, envelope('order_transition_invalid', err.message, err.details));`

    Do NOT run the Prisma migration yet — that is Task 2. Do NOT touch the service or route files yet — that is Task 3.
  </action>
  <verify>
    <automated>pnpm --filter @meditrack/shared build &amp;&amp; pnpm --filter @meditrack/api typecheck 2>&amp;1 | tee /tmp/04-01-t1.log; grep -q "error" /tmp/04-01-t1.log &amp;&amp; exit 1 || exit 0</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/prisma/schema.prisma` contains four new column declarations on `model Order`: `confirmedAt DateTime?`, `confirmedByUserId String?`, `deliveredAt DateTime?`, `deliveredByUserId String?`.
    - `schema.prisma` contains `@relation(name: "OrderConfirmedBy"` AND `@relation(name: "OrderDeliveredBy"` with `onDelete: Restrict` in both.
    - `schema.prisma` `model User` contains `confirmedOrders Order[] @relation("OrderConfirmedBy")` AND `deliveredOrders Order[] @relation("OrderDeliveredBy")`.
    - `packages/shared/src/contracts/order.ts` `orderResponse` exports include `confirmedAt`, `confirmedByUserId`, `confirmedBy`, `deliveredAt`, `deliveredByUserId`, `deliveredBy` keys (verify by grep on the file).
    - `packages/shared/src/contracts/order.ts` exports `confirmOrderRequest` and `deliverOrderRequest` Zod schemas + inferred types.
    - `packages/shared/src/contracts/permissions.ts` `ACTION_KEYS` tuple literal contains the strings `'order:confirm'` and `'order:deliver'`.
    - `apps/api/src/auth/permissions.ts` `PERMISSIONS` map contains entries for `'order:confirm'` and `'order:deliver'` both bound to `['apotekare', 'admin']`.
    - `apps/api/src/plugins/errorHandler.ts` exports class `OrderTransitionError` with `code = 'order_transition_invalid'` and a constructor accepting `{from, to, expected}` typed as `OrderStatus`.
    - `apps/api/src/plugins/errorHandler.ts` `setErrorHandler` branch list maps `OrderTransitionError` to HTTP 409 with envelope code `'order_transition_invalid'` BEFORE the Zod fallback branch.
    - `apps/api/src/plugins/errorHandler.ts` `ValidationFailedError.details.reason` union includes `'medication_removed'` and the details type includes optional `medicationName?: string`.
    - `pnpm --filter @meditrack/shared build` exits 0.
    - `pnpm --filter @meditrack/api typecheck` (or `pnpm -w typecheck`) exits 0 with no errors — including verifying that the `Record<ActionKey, Role[]>` constraint compiles after appending the two new keys.
  </acceptance_criteria>
  <done>Schema declares all 4 actor columns + 2 named relations + 2 User inverses. Shared contracts widened. RBAC map appended. OrderTransitionError class exists + is registered in the error handler before the Zod branch. ValidationFailedError reason union accepts `'medication_removed'`. Workspace typecheck passes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: [BLOCKING] Run Prisma migration 0006_order_confirm_deliver</name>
  <files>apps/api/prisma/migrations/0006_order_confirm_deliver/migration.sql (auto-generated)</files>
  <read_first>
    - apps/api/prisma/schema.prisma (post-Task-1 state — verify all 4 columns + 2 relations are present)
    - apps/api/prisma/migrations/ (list existing migrations — verify previous is 0005 so 0006 sorts after)
    - apps/api/package.json (verify the `prisma:migrate` script exists; the planner-injected blocking instruction names `pnpm --filter @meditrack/api prisma:migrate -- --name 0006_order_confirm_deliver` as the exact command for this repo)
    - .planning/phases/04-confirm-deliver-stock/04-PATTERNS.md (`apps/api/prisma/migrations/0006_order_confirm_deliver/migration.sql` section — shows the expected generated DDL: ALTER TABLE ADD COLUMN x4 + AddForeignKey x2)
  </read_first>
  <action>
    Run the Prisma migrate command this repo uses: `pnpm --filter @meditrack/api prisma:migrate -- --name 0006_order_confirm_deliver`. The `--name` flag suppresses the interactive prompt so the task is non-interactive (autonomous: true).

    Prisma will:
    1. Diff the schema against the current DB and generate `apps/api/prisma/migrations/&lt;timestamp&gt;_0006_order_confirm_deliver/migration.sql` containing one `ALTER TABLE "Order"` block adding the 4 new columns + two `ALTER TABLE … ADD CONSTRAINT "Order_confirmedByUserId_fkey"` / `"Order_deliveredByUserId_fkey"` blocks.
    2. Apply the migration to the local Postgres.
    3. Regenerate the Prisma client (`prisma generate` is implicit in `prisma migrate dev`).

    DO NOT use `prisma db push` — Prisma migrate is the workflow in this repo (Phase 1+2+3 shipped 5 named migrations under `apps/api/prisma/migrations/`).

    DO NOT manually edit the generated SQL after the command completes — if Prisma's emit drifts from the spec, fix the schema and re-run.
  </action>
  <verify>
    <automated>pnpm --filter @meditrack/api prisma:migrate -- --name 0006_order_confirm_deliver &amp;&amp; ls apps/api/prisma/migrations/ | grep -q "0006_order_confirm_deliver" &amp;&amp; pnpm --filter @meditrack/api exec prisma migrate status</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @meditrack/api prisma:migrate -- --name 0006_order_confirm_deliver` exits 0.
    - `apps/api/prisma/migrations/` contains a new directory ending in `_0006_order_confirm_deliver` with a `migration.sql` file inside.
    - The generated `migration.sql` contains `ALTER TABLE "Order" ADD COLUMN "confirmedAt"`, `ADD COLUMN "confirmedByUserId"`, `ADD COLUMN "deliveredAt"`, `ADD COLUMN "deliveredByUserId"` (verify by grep).
    - The generated `migration.sql` contains TWO `ADD CONSTRAINT "Order_confirmedByUserId_fkey"` and `"Order_deliveredByUserId_fkey"` with `ON DELETE RESTRICT ON UPDATE CASCADE`.
    - `pnpm --filter @meditrack/api exec prisma migrate status` reports "Database schema is up to date" (no pending migrations).
    - After migration, `psql` (or Prisma) reports the four new columns exist on the `Order` table as nullable (no backfill required for existing utkast/skickad rows from Phase 3).
  </acceptance_criteria>
  <done>Migration `0006_order_confirm_deliver` is generated, applied to local Postgres, and the Prisma client is regenerated. Verification by `prisma migrate status` reports clean. Existing Phase 3 orders remain valid (NULL on all 4 new columns).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: confirmOrder service + POST /api/orders/:id/confirm route + integration test</name>
  <files>apps/api/src/services/order.service.ts, apps/api/src/routes/orders/confirm.ts, apps/api/src/routes/orders/index.ts, apps/api/test/orders.confirm.integration.test.ts</files>
  <behavior>
    - Test 1 (happy path): sjuksköterska creates draft → adds 2 lines → submits (status='skickad'). Apotekare POSTs /confirm. Response: 200, body matches widened orderResponse with status='bekraftad', confirmedAt is a recent ISO datetime, confirmedByUserId equals apotekare.id, confirmedBy.name equals apotekare.name. submittedBy still populated; deliveredBy still null.
    - Test 2 (wrong-status 409): same setup but skip the submit step (order stays utkast). Apotekare POSTs /confirm. Response: 409 with body `{error: {code: 'order_transition_invalid', details: {from: 'utkast', to: 'bekraftad', expected: 'skickad'}}}`.
    - Test 3 (double-confirm 409): submit → confirm once (200) → confirm again. Second call: 409 with `details.from = 'bekraftad'`, `details.expected = 'skickad'`.
    - Test 4 (cross-careUnit 404 not 403): apotekare from a different careUnit POSTs /confirm against the order from careUnit A. Response: 404 with code `'not_found'` (per D-73). NOT 403.
    - Test 5 (RBAC 403): sjuksköterska POSTs /confirm against a Skickad order in their own careUnit. Response: 403 with code `'forbidden'` from requirePermission preHandler. The order's status remains `'skickad'` afterwards.
    - Test 6 (not-found 404): apotekare POSTs /confirm against a fabricated orderId (`'nonexistent'`). Response: 404 with code `'not_found'`.
  </behavior>
  <read_first>
    - apps/api/src/services/order.service.ts (full file — `submitOrder` lines ~359-446 is the literal template; `toOrderResponse` lines ~66-82 is what gets widened; every existing function's Prisma `include` block must be widened in this task)
    - apps/api/src/routes/orders/submit.ts (the literal template for confirm.ts — copy + change four tokens per 04-PATTERNS.md)
    - apps/api/src/routes/orders/index.ts (current barrel registration order — confirmOrderRoute registers after submitOrderRoute per D-65 / PATTERNS.md)
    - apps/api/test/orders.integration.test.ts (full file — copy the lifecycle hooks and helpers verbatim; the D-73 5-scenario shape is the template for confirm's 6 scenarios)
    - apps/api/test/helpers/buildTestApp.ts (verify TEST_APOTEKARE export + ensureAllRolesSeeded + resetSessions)
    - .planning/phases/04-confirm-deliver-stock/04-PATTERNS.md (sections: order.service.ts confirmOrder + Order-row lock + Atomic UPDATE excerpts; routes/orders/confirm.ts token map; orders.deliver.integration.test.ts test block 1)
    - .planning/phases/04-confirm-deliver-stock/04-CONTEXT.md (D-74 error code, D-75 narrow endpoints, D-84 actor stamps, D-73 inherited from Phase 3)
    - .planning/phases/03-draft-orders/03-CONTEXT.md (D-54 atomic UPDATE pattern, D-57 full-Order response, D-66 assertOrder helper precedent)
  </read_first>
  <action>
    Implement `confirmOrder` end-to-end following the test-first contract above:

    **Step A — write the test file FIRST** (`apps/api/test/orders.confirm.integration.test.ts`):
    - Import harness verbatim from `helpers/buildTestApp.js`: `buildTestApp`, `ensureAllRolesSeeded`, `prisma`, `resetSessions`, `TEST_SJUKSKOTERSKA`, `TEST_APOTEKARE`.
    - Copy the local helpers `captureSessionCookie`, `loginAs`, `createEmptyOrder`, `findTestCareUnitMedication` from `apps/api/test/orders.integration.test.ts` lines 49-91 verbatim (these are not yet shared in the harness file — duplicating is fine; if you choose to promote them to `helpers/buildTestApp.ts`, that is also acceptable but not required).
    - Use the lifecycle hooks (`beforeAll buildTestApp + ensureAllRolesSeeded; beforeEach resetSessions; afterAll app.close + prisma.$disconnect`) verbatim.
    - Implement the six `it` blocks above. Run the test — it MUST FAIL (RED) because `confirmOrder` and the route do not yet exist.

    **Step B — widen `toOrderResponse` and every include block in order.service.ts:**
    - In `toOrderResponse`, map four new fields onto the response: `confirmedAt: order.confirmedAt?.toISOString() ?? null`, `confirmedByUserId: order.confirmedByUserId ?? null`, `confirmedBy: order.confirmedBy ? { id, name } : null`, plus the parallel deliver trio.
    - EVERY existing function that calls `findUnique`/`findMany`/`update`/`updateMany` and then loads the row to return must widen its `include` block with `confirmedBy: { select: { id: true, name: true } }` and `deliveredBy: { select: { id: true, name: true } }`. The PATTERNS.md `<special notes for planner>` block lists every place: `createDraftOrder` (~lines 142-157), `getOrderForUnit` (~lines 221-232), `submitOrder` reload (~lines 429-440), line CRUD reloads (search for `include` blocks in lines.ts / lines-related helpers), `softDeleteOrder` if it reloads, and `listOrdersForUnit` (Slice C handles this — confirm Slice A widens the per-row include or leaves to Slice C; PATTERNS.md suggests Slice C does the list widening, but make sure `toOrderListItem` does not produce undefined fields today — if it currently maps only existing fields, it stays compatible).

    **Step C — implement `confirmOrder` in `apps/api/src/services/order.service.ts`:** Follow the `submitOrder` template (D-54 + D-79 step A reused for the Order-row lock). The function signature is `export async function confirmOrder(careUnitId: string, orderId: string, actorUserId: string): Promise<OrderResponse>`. Inside a single `prisma.$transaction(async (tx) => { … })`:
    1. `await tx.$queryRaw\`SELECT id FROM "Order" WHERE id = ${orderId} FOR UPDATE\`;` (Order-row lock, verbatim from submitOrder step 0).
    2. `tx.order.findUnique({ where: {id: orderId}, include: { lines: {...}, createdBy:{select:{id,name}}, submittedBy:{select:{id,name}}, confirmedBy:{select:{id,name}}, deliveredBy:{select:{id,name}} } })`.
    3. Existence + scope check: `if (!order || order.deletedAt !== null || order.careUnitId !== careUnitId) throw new NotFoundError('Beställningen hittades inte.');` (D-73).
    4. Status precondition: `if (order.status !== 'skickad') throw new OrderTransitionError({ from: order.status, to: 'bekraftad', expected: 'skickad' });`.
    5. Atomic `tx.order.updateMany({ where: { id: orderId, careUnitId, status: 'skickad', deletedAt: null }, data: { status: 'bekraftad', confirmedAt: new Date(), confirmedByUserId: actorUserId } })`. If `updated.count === 0` throw `new OrderTransitionError({ from: 'bekraftad', to: 'bekraftad', expected: 'skickad' })` (race lost — best-effort `from`).
    6. Reload the row with the widened include and return `toOrderResponse(reloaded!)`.

    **Step D — create `apps/api/src/routes/orders/confirm.ts`:** Clone `submit.ts` and change four tokens per PATTERNS.md `confirm.ts` section: `submitOrder` → `confirmOrder`, `/submit` → `/confirm`, `'order:submit'` → `'order:confirm'`, `submitOrderRoute` → `confirmOrderRoute`. Update the docblock to reference D-74/D-75/D-84. preHandler order: `[requireSession, requirePermission('order:confirm')]` — never reorder. Schema: `params: z.object({ id: z.string().min(1) })`, `response: { 200: orderResponse }`. Handler body: `return confirmOrder(req.user!.careUnitId, req.params.id, req.user!.id);`.

    **Step E — register in `apps/api/src/routes/orders/index.ts`:** After `await app.register(submitOrderRoute);` add `await app.register(confirmOrderRoute);` (deliver registration lands in Slice B; do NOT preregister deliver here).

    **Step F — run the test (GREEN):** `pnpm --filter @meditrack/api test -- orders.confirm.integration` MUST PASS all 6 blocks. If any fail, fix the service/route — do NOT relax the test.

    **Step G — verify the full workspace test + typecheck still passes:** `pnpm --filter @meditrack/api test` (all suites) and `pnpm -w typecheck`. Specifically, Phase 3's `orders.integration.test.ts` (the D-73 suite) MUST still pass — `submitOrder`'s widened include adds fields, never removes; the suite's assertions should still hold.
  </action>
  <verify>
    <automated>pnpm --filter @meditrack/api test -- orders.confirm.integration 2>&amp;1 | tee /tmp/04-01-t3.log; grep -q "6 passed" /tmp/04-01-t3.log || grep -q "passed" /tmp/04-01-t3.log &amp;&amp; pnpm --filter @meditrack/api test 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/src/services/order.service.ts` exports `async function confirmOrder(careUnitId: string, orderId: string, actorUserId: string): Promise<OrderResponse>` (verify by grep for the exact signature).
    - `confirmOrder`'s body contains `tx.$queryRaw` with `FOR UPDATE` against `"Order"` (verify by grep: `grep -A 1 "confirmOrder" apps/api/src/services/order.service.ts | grep -q "FOR UPDATE"` after locating the block).
    - `confirmOrder` contains `tx.order.updateMany` with `where: { id, careUnitId, status: 'skickad', deletedAt: null }` and `data: { status: 'bekraftad', confirmedAt, confirmedByUserId }`.
    - `confirmOrder` throws `OrderTransitionError` when `updated.count === 0` and when `order.status !== 'skickad'`.
    - `toOrderResponse` maps `confirmedAt`, `confirmedByUserId`, `confirmedBy`, `deliveredAt`, `deliveredByUserId`, `deliveredBy` (verify each by grep).
    - Every Prisma `include` block in `order.service.ts` that loads `submittedBy: { select: { id: true, name: true } }` also includes `confirmedBy` and `deliveredBy` with the same select shape.
    - `apps/api/src/routes/orders/confirm.ts` exports `confirmOrderRoute(app)` and registers `POST /api/orders/:id/confirm` with `[requireSession, requirePermission('order:confirm')]` preHandler.
    - `apps/api/src/routes/orders/index.ts` calls `await app.register(confirmOrderRoute);` after the existing `submitOrderRoute` line.
    - `apps/api/test/orders.confirm.integration.test.ts` exists with 6 `it` blocks covering happy path, wrong-status 409, double-confirm 409, cross-careUnit 404, sjuksköterska 403, not-found 404.
    - `pnpm --filter @meditrack/api test -- orders.confirm.integration` exits 0 with all 6 tests passing.
    - `pnpm --filter @meditrack/api test` (full suite) exits 0 — Phase 3's `orders.integration.test.ts` D-73 suite still passes (no regressions from the widened include blocks).
    - `pnpm -w typecheck` exits 0.
  </acceptance_criteria>
  <done>`confirmOrder` service + `/confirm` route are implemented with the `$queryRaw FOR UPDATE` Order-row lock and atomic updateMany precondition. Integration test covers all 6 contract scenarios. Phase 3's existing suite still passes (regression-free). All actor-relation includes widened.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: useConfirmOrder hook + Mode C wiring + ApotekareActionFooter</name>
  <files>apps/web/src/features/orders/useOrderMutations.ts, apps/web/src/routes/bestallningar/ComposeOrderPage.tsx, apps/web/src/routes/bestallningar/ApotekareActionFooter.tsx</files>
  <read_first>
    - apps/web/src/features/orders/useOrderMutations.ts (full file — `useSubmitOrder` lines ~238-263 is the literal template)
    - apps/web/src/routes/bestallningar/ComposeOrderPage.tsx (full file — the existing `isUtkast` / `isLocked` branch, the `useDocumentTitle` call, where the Phase 3 `SubmitConfirmationBanner` and `ComposeStickyFooter` are rendered)
    - apps/web/src/routes/bestallningar/ComposeStickyFooter.tsx (existing sticky-footer geometry — D-71; use as reference for ApotekareActionFooter's container styling)
    - apps/web/src/routes/bestallningar/SubmitConfirmationBanner.tsx (existing Mode B banner — Mode C just keeps showing this banner; no change to the banner itself)
    - apps/web/src/components/Can.tsx and apps/web/src/features/auth/useAuth.ts (verify `<Can action="order:confirm">` is the correct UI gate per D-17)
    - .planning/phases/04-confirm-deliver-stock/04-PATTERNS.md (useOrderMutations.ts useConfirmOrder section, ComposeOrderPage Mode C section, AlertDialog precedent — Mode C does NOT use AlertDialog; only Mode D in Slice B uses it)
    - .planning/phases/04-confirm-deliver-stock/04-UI-SPEC.md (§Components — Mode C section, Toasts table, copywriting contract for `Bekräfta beställning` button + `Bekräftad` toast)
    - .planning/phases/04-confirm-deliver-stock/04-CONTEXT.md (D-83 Mode C button + onSuccess invalidation set, D-74 error toast wording)
  </read_first>
  <action>
    Wire the FE confirm flow end-to-end. Concretely:

    **A. `useOrderMutations.ts` — add `useConfirmOrder` after `useSubmitOrder`:**
    - Clone the `useSubmitOrder` body. Change the URL to `/api/orders/${orderId}/confirm` (POST).
    - `onSuccess(response, vars)`: `queryClient.setQueryData(['order', vars.orderId], response)` + `void queryClient.invalidateQueries({ queryKey: ['orders', { status: 'skickad' }] })` + `void queryClient.invalidateQueries({ queryKey: ['orders', { status: 'bekraftad' }] })`. Then `toast.success('Bekräftad')`.
    - `onError(err, vars)`: branch on `err.envelope.error.code`. If `'order_transition_invalid'`: `toast.error(\`Beställningen har redan ${ORDER_STATUS_LABELS[err.envelope.error.details.from as OrderStatus]}.\`)` + `void queryClient.invalidateQueries({ queryKey: ['order', vars.orderId] })`. If `'not_found'`: `toast.error('Beställning hittades inte.')` + (optionally) navigate to `/bestallningar` (use `useNavigate` if the hook needs it; otherwise emit a custom event and let the page navigate). Default: `toast.error('Kunde inte spara — försök igen.')`. Import `ORDER_STATUS_LABELS` from `@meditrack/shared/constants/orderStatus`.

    **B. New file `apps/web/src/routes/bestallningar/ApotekareActionFooter.tsx`:**
    - Props: `{ label: string; onClick: () => void; isPending: boolean; loadingLabel?: string }`.
    - Renders the same sticky-footer container geometry as `ComposeStickyFooter` (the outer `<div>` with `mt-4 mx-4 sm:mx-0` for desktop inline OR fixed-bottom sticky on `<md` per D-71 — match the existing component's pattern). The button itself is `<Button variant="default" className="min-h-[44px] w-full md:w-auto">` with the label, and when `isPending` it shows `Loader2 animate-spin` next to the loadingLabel (`Bekräftar…` for confirm; Slice B will reuse this footer with `Markerar…` for deliver).
    - This component is shared between Mode C and Mode D (Slice B). Slice A defines it; Slice B passes deliver props.

    **C. `ComposeOrderPage.tsx` — widen the status branch:**
    - Add boolean locals: `const isSkickad = order.status === 'skickad'`, `const isBekraftad = order.status === 'bekraftad'`, `const isLevererad = order.status === 'levererad'`.
    - Compute viewer capability via `useCan('order:confirm')` from `useAuth` (D-17) — store as `canConfirm`.
    - Render Mode C button block: when `isSkickad && canConfirm`, render `<Can action="order:confirm"><ApotekareActionFooter label="Bekräfta beställning" onClick={() => confirmMutation.mutateAsync({ orderId: order.id })} isPending={confirmMutation.isPending} loadingLabel="Bekräftar…" /></Can>` placed below the existing SubmitConfirmationBanner / read-only line list (the SubmitConfirmationBanner from Phase 3 stays — Mode C keeps "Beställningen är skickad till apotekare." because it's still factually skickad until confirm completes).
    - Keep the existing `useSubmitOrder` usage for Mode A's submit button unchanged.
    - Widen `useDocumentTitle` to handle bekraftad/levererad titles per UI-SPEC §Copywriting (these will be filled in Slice B but the switch statement should accept the values today; default falls through to current behavior).
    - Do NOT yet render Mode D (Bekräftad) or Mode E (Levererad) — those land in Slice B. After confirm succeeds, the cache hydrates with status='bekraftad' and the page enters a Slice-B-pending branch; for Slice A only, render a minimal "Beställning · Bekräftad" heading + read-only lines + SubmitConfirmationBanner-style fallback (no action button until Slice B). The integration test in Task 3 already proves the BE returns the right shape; the FE Mode D UX lands in 04-02.

    **D. `<Can>` wrapper at the button level guards both the BE preHandler (security boundary, D-15) and the FE UX (defense in depth, T10). Verify by manually checking with a sjuksköterska login that no Bekräfta button renders on a Skickad order detail page.**

    Do NOT touch `BestallningarPage.tsx` here — that is Slice C. Do NOT introduce the deliver flow — that is Slice B.
  </action>
  <verify>
    <automated>pnpm --filter @meditrack/web typecheck &amp;&amp; pnpm --filter @meditrack/web build &amp;&amp; pnpm --filter @meditrack/web test 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/features/orders/useOrderMutations.ts` exports `useConfirmOrder` returning a `useMutation` against `POST /api/orders/${orderId}/confirm`.
    - `useConfirmOrder.onSuccess` calls `setQueryData(['order', vars.orderId], response)` AND invalidates both `['orders', { status: 'skickad' }]` and `['orders', { status: 'bekraftad' }]` (verify by grep for both invalidation calls in the hook body).
    - `useConfirmOrder.onError` branches on `err.envelope.error.code === 'order_transition_invalid'` and renders a toast using `ORDER_STATUS_LABELS[details.from]` (verify by grep for `ORDER_STATUS_LABELS`).
    - On 409 transition errors, `useConfirmOrder` invalidates `['order', vars.orderId]` so the page refetches the now-bekraftad state.
    - `apps/web/src/routes/bestallningar/ApotekareActionFooter.tsx` is a new file exporting `ApotekareActionFooter` component with props `{ label, onClick, isPending, loadingLabel? }`.
    - `ApotekareActionFooter`'s button has `min-h-[44px]` and `w-full md:w-auto` (touch target floor per Phase 1 D-10 + UI-SPEC §Spacing).
    - `apps/web/src/routes/bestallningar/ComposeOrderPage.tsx` adds an `isSkickad` boolean and a `useCan('order:confirm')` call.
    - On a Skickad order, when viewer has `order:confirm`, the page renders `<Can action="order:confirm">…ApotekareActionFooter…</Can>` with the confirm mutation wired.
    - `<Can>` wrapper renders nothing for sjuksköterska viewers (verify by reading the rendered output via test or manual check; React tree does not contain the button).
    - `pnpm --filter @meditrack/web typecheck` exits 0.
    - `pnpm --filter @meditrack/web build` exits 0.
    - `pnpm --filter @meditrack/web test` exits 0 (no regressions on existing FE tests).
  </acceptance_criteria>
  <done>FE confirm flow wired end-to-end: `useConfirmOrder` hook with proper cache invalidation + status-aware error toasts; Mode C apotekare button on `ComposeOrderPage` rendered via `<Can action="order:confirm">`; sjuksköterska sees no button; clicking Bekräfta fires the mutation and on success the page re-renders with the order in bekraftad state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API (Fastify) | Untrusted JSON from FE; cookies carry session |
| API → DB (Postgres) | Service-layer Prisma; row-level locks; FK constraints |
| Cross-careUnit | Apotekare A from careUnit X attempting to confirm an order from careUnit Y |
| Cross-role | Sjuksköterska attempting to call confirm endpoint directly via curl |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering / Information Disclosure | POST /api/orders/:id/confirm cross-tenant | mitigate | `confirmOrder` service first arg is `careUnitId` from session; existence+scope check throws `NotFoundError` (404 not 403 per D-73) on mismatch, hiding the existence of orders in other careUnits. Test 4 in Task 3 proves this. |
| T-04-02 | Elevation of Privilege | Sjuksköterska calling /confirm | mitigate | `requirePermission('order:confirm')` preHandler returns 403 before the service ever runs (D-15 RBAC). `<Can action="order:confirm">` on FE hides the button (UX gate, defense in depth). Test 5 in Task 3 proves the 403. |
| T-04-03 | Tampering (race) | Two apotekare confirming the same order simultaneously | mitigate | `$queryRaw … FOR UPDATE` on Order row at the start of the tx serializes concurrent confirms; `updateMany WHERE status='skickad'` precondition makes the second one return `count === 0` → `OrderTransitionError(409)`. (The same-order race is fully tested for deliver in Slice B's concurrency test; confirm shares the lock pattern.) |
| T-04-04 | Spoofing | Forged session cookie | mitigate | `requireSession` (existing from Phase 1) is the first preHandler; rejects with 401 if cookie is absent/invalid. No change in Phase 4 — preHandler order `[requireSession, requirePermission]` is invariant. |
| T-04-05 | Repudiation | Apotekare denies confirming an order | mitigate | `confirmedByUserId` FK to User (`onDelete: Restrict`) is stamped inside the same tx as the status flip; preserved across user deletion. Phase 5 audit-log middleware will record the actor+timestamp+before/after diff atop this (no Phase 4 work required). |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | Phase 4 introduces NO new npm packages in Slice A (all deps from Phase 1+2+3). Slice C will install `tabs` via `pnpm dlx shadcn add tabs` which is an official shadcn registry component — out of npm scope. No `[ASSUMED]`/`[SUS]` packages to gate. |
| T-04-06 | DoS (low risk) | Massive line count on submitOrder side effect | accept | Existing Phase 3 contract; no new attack surface in Slice A. Future: rate-limit per-actor mutations if needed. |
</threat_model>

<verification>
- All four Tasks' `<verify>` commands exit 0.
- `pnpm --filter @meditrack/api test` (full BE suite) passes — Phase 3's `orders.integration.test.ts` MUST still pass with the widened includes.
- `pnpm --filter @meditrack/web typecheck && pnpm --filter @meditrack/web build` passes.
- `pnpm --filter @meditrack/api exec prisma migrate status` reports "Database schema is up to date".
- Manual smoke: log in as `apotekare@example.test`, navigate to a Skickad order (use one created via curl + the new endpoints OR wait for Slice C's seed extension; for Slice A self-verification, manually create a Skickad order via `POST /api/orders` + lines + submit, then visit `/bestallningar/<id>`). Click `Bekräfta beställning`. Verify: toast `Bekräftad` appears, page reloads with status=bekraftad fetched, button disappears. Verify the same flow as sjuksköterska → no button visible.
</verification>

<success_criteria>
- ORD-04 satisfied: apotekare advances Skickad → Bekräftad via POST /confirm.
- ORD-06 (partial — confirm half): invalid transition (e.g. Utkast → Bekräftad) returns 409 with code `order_transition_invalid`.
- Schema migration 0006 lands cleanly with all 4 new actor columns (deliver columns ride along for Slice B's use).
- Shared contracts widened with confirm/deliver response trios + new request schemas + widened list query.
- OrderTransitionError class registered before Zod branch; ValidationFailedError accepts `'medication_removed'` reason.
- RBAC map appended; `<Can action="order:confirm">` gates the FE button; `requirePermission('order:confirm')` preHandler gates the API.
- 6-case integration test covers: happy path, wrong-status 409, double-confirm 409, cross-careUnit 404, RBAC 403, not-found 404.
- FE: `useConfirmOrder` hook hydrates cache + invalidates both status lists + status-aware error toasts; Mode C button renders on Skickad order pages for apotekare only.
- Workspace builds + typechecks + all tests pass.
</success_criteria>

<output>
Create `.planning/phases/04-confirm-deliver-stock/04-01-SUMMARY.md` when done.
</output>
