---
phase: 04-confirm-deliver-stock
plan: 03
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - apps/api/src/services/order.service.ts
  - apps/api/src/routes/orders/list.ts
  - apps/web/src/components/ui/tabs.tsx
  - apps/web/src/features/orders/useOrderQueries.ts
  - apps/web/src/routes/bestallningar/BestallningarPage.tsx
  - apps/web/src/routes/bestallningar/OrdersTable.tsx
  - apps/web/src/routes/bestallningar/OrdersCardList.tsx
  - apps/api/prisma/seed.ts
  - apps/api/test/orders.list.integration.test.ts
autonomous: true
requirements: [ORD-07]
must_haves:
  truths:
    - "Apotekare/admin/sjuksköterska can view per-vårdenhet order history at /bestallningar with status tabs (Utkast | Skickade | Bekräftade | Levererade | Alla). Status tab is URL-deep-linkable via ?status= param (ORD-07)."
    - "Each non-Utkast tab shows status-specific columns (Skickad av + Skickad timestamp; Bekräftad av + Bekräftad timestamp; Levererad av + Levererad timestamp). 'Alla' tab adds a status pill column. (ORD-07)"
    - "GET /api/orders accepts ?status= as a single value, a comma-list (e.g. 'skickad,bekraftad'), or 'alla' (server treats as all four). Default remains ?status=utkast for Phase 3 back-compat."
    - "Cross-careUnit isolation preserved: list query filters by req.user.careUnitId; users only see their own vårdenhet's orders (T5, AUTH-07 inherited)."
    - "Seed extends to one order per status (Utkast, Skickad, Bekräftad, Levererad) for the demo career-unit, idempotent. The Levererad seed includes a post-step stock UPDATE so the demo on /lakemedel reflects the historic delivery (D-85)."
    - "Empty states per tab use the exact Swedish copy from UI-SPEC §Empty States."
  artifacts:
    - path: "apps/api/src/services/order.service.ts"
      provides: "listOrdersForUnit widened: status param accepts string | string[]; include block widens with submittedBy + confirmedBy + deliveredBy; toOrderListItem maps the three optional actor trios"
    - path: "apps/api/src/routes/orders/list.ts"
      provides: "Route-level pre-parser: ?status=alla → all four statuses; ?status=a,b,c → array; ?status=x → single (back-compat)"
    - path: "apps/web/src/components/ui/tabs.tsx"
      provides: "shadcn Tabs primitive installed via `pnpm dlx shadcn add tabs`"
    - path: "apps/web/src/features/orders/useOrderQueries.ts"
      provides: "useOrdersByStatusQuery(status: OrderStatus | OrderStatus[] | 'alla') parallel to useDraftsQuery"
    - path: "apps/web/src/routes/bestallningar/BestallningarPage.tsx"
      provides: "URL-backed status-tab filter; per-tab query branching; per-tab empty states"
    - path: "apps/web/src/routes/bestallningar/OrdersTable.tsx"
      provides: "≥md table for non-Utkast tabs with status-specific columns"
    - path: "apps/web/src/routes/bestallningar/OrdersCardList.tsx"
      provides: "<md card list for non-Utkast tabs"
    - path: "apps/api/prisma/seed.ts"
      provides: "seedDemoOrders fan-out: 1 Utkast + 1 Skickad + 1 Bekräftad + 1 Levererad; idempotent per status; Levererad post-step stock UPDATE"
    - path: "apps/api/test/orders.list.integration.test.ts"
      provides: "Integration tests covering status comma-list, single status, alla, default utkast back-compat, cross-careUnit isolation"
  key_links:
    - from: "apps/web/src/routes/bestallningar/BestallningarPage.tsx"
      to: "react-router-dom useSearchParams"
      via: "?status= URL param drives Tabs value + query key"
      pattern: "useSearchParams"
    - from: "apps/web/src/routes/bestallningar/BestallningarPage.tsx Tabs"
      to: "apps/web/src/features/orders/useOrderQueries.ts useOrdersByStatusQuery"
      via: "status param threaded from URL → query key → fetchJson URL"
      pattern: "useOrdersByStatusQuery"
    - from: "apps/api/src/routes/orders/list.ts pre-parser"
      to: "apps/api/src/services/order.service.ts listOrdersForUnit"
      via: "parsed status[] passed to filters.status"
      pattern: "status.*Array"
    - from: "apps/api/prisma/seed.ts seedDemoOrders Levererad"
      to: "CareUnitMedication.currentStock"
      via: "post-step UPDATE per line; mirrors deliverOrder's increment shape"
      pattern: "currentStock.*increment"
---

<objective>
Slice C — ship the **history surface** end-to-end so apotekare and sjuksköterska can browse every order in their vårdenhet by status, see who advanced which order when, and demo the lifecycle visibly. Closes ORD-07 (order history with status, lines, timestamps, and actor per transition) and lays the seed-data so the first `docker compose up` after Phase 4 lands shows all four statuses live (D-85).

Purpose: extend the existing drafts-only `/bestallningar` page into a per-vårdenhet history with five status tabs (Utkast | Skickade | Bekräftade | Levererade | Alla); widen the list API to accept comma-list / "alla" status filtering; install shadcn `<Tabs>`; and extend the seed to produce one demo order per status (with the Levererad order's stock post-step applied so `/lakemedel` reflects the historic delivery). Independent of Slice B's deliver wiring — both can run in parallel in Wave 2.
Output: working status-tab navigation on `/bestallningar`; widened API + service; new shadcn primitive; extended seed; integration tests for the list endpoint covering single, comma-list, and "alla" filters.
</objective>

<execution_context>
@C:/Projekt/MediTrack/.claude/get-shit-done/workflows/execute-plan.md
@C:/Projekt/MediTrack/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-confirm-deliver-stock/04-CONTEXT.md
@.planning/phases/04-confirm-deliver-stock/04-PATTERNS.md
@.planning/phases/04-confirm-deliver-stock/04-UI-SPEC.md
@.planning/phases/04-confirm-deliver-stock/04-01-SUMMARY.md
@.planning/phases/03-draft-orders/03-CONTEXT.md
@.planning/phases/02-medication-catalog/02-CONTEXT.md
@.planning/phases/01-foundation-auth/01-CONTEXT.md

<interfaces>
<!-- Key contracts the executor needs. -->

From Slice A (04-01, already complete when this plan runs):
- `orderListItem` shared schema is already widened with `submittedAt/By`, `confirmedAt/By`, `deliveredAt/By` as `.nullable().optional()`. Slice C populates these fields from the service.
- `orderListQuery.status` is already `z.union([orderStatusEnum, z.array(orderStatusEnum)]).default('utkast')`. Slice C adds the route-level pre-parser to split comma-lists + the `'alla'` literal expansion.
- `toOrderResponse` includes confirmedBy + deliveredBy. `toOrderListItem` (slimmer projection) needs Slice C's widening.

From `apps/api/src/routes/orders/list.ts` (existing):
- Current shape parses `req.query` via Zod against `orderListQuery`. Slice C adds a pre-parser BEFORE Zod validation that turns `req.query.status === 'alla'` into all four ORDER_STATUSES, and `req.query.status === 'a,b,c'` into an array of three.

From `apps/api/src/services/order.service.ts` (existing):
- `listOrdersForUnit(careUnitId, filters)` — currently filters with `status: filters.status` (single value). Slice C widens to `status: Array.isArray(filters.status) ? { in: filters.status } : filters.status`.
- Existing include for list: `{ lines: {select: {id, quantity}}, createdBy: {select: {id, name}} }`. Slice C adds `submittedBy`, `confirmedBy`, `deliveredBy` with the same select.
- `toOrderListItem` widens to map the three new optional actor trios.

From `apps/web/src/routes/bestallningar/BestallningarPage.tsx` (existing):
- Currently renders a single `useDraftsQuery()` + `DraftsTable`/`DraftsCardList`. Slice C wraps in shadcn `<Tabs value={status}>` and branches the query.

From `apps/web/src/routes/bestallningar/DraftsTable.tsx` + `DraftsCardList.tsx` (existing Phase 3):
- Templates for the new `OrdersTable.tsx` / `OrdersCardList.tsx`. Same `<Table>` / card geometry; only the column set varies per tab.

From `apps/api/prisma/seed.ts` (existing):
- `seedDraftOrder` (~lines 340-423) is the literal template. Slice C generalizes to `seedDemoOrders` per D-85.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Backend list-API widening + integration tests (status comma-list, 'alla', back-compat)</name>
  <files>apps/api/src/services/order.service.ts, apps/api/src/routes/orders/list.ts, apps/api/test/orders.list.integration.test.ts</files>
  <behavior>
    - Test 1 (back-compat default): GET `/api/orders` (no query param) → 200, returns only Utkast orders for the requesting user's careUnit (Phase 3 contract preserved).
    - Test 2 (single status param): GET `/api/orders?status=skickad` → 200, returns only Skickad orders for the careUnit; submittedBy/submittedAt populated, other actor trios null.
    - Test 3 (comma-list): GET `/api/orders?status=skickad,bekraftad,levererad` → 200, returns all three statuses for the careUnit. Each row's actor trio for the matching status is populated.
    - Test 4 ('alla' literal): GET `/api/orders?status=alla` → 200, returns all four statuses (utkast + skickad + bekraftad + levererad) for the careUnit. Counts match the sum of each status's per-status query.
    - Test 5 (cross-careUnit isolation): seed an order in careUnit B; apotekare from careUnit A queries `?status=alla`; the careUnit-B order is NOT in the response (filter by req.user.careUnitId only). Verifies T5 inherited.
    - Test 6 (invalid status rejected): GET `/api/orders?status=foo` → 400 (Zod validation failure; the pre-parser parses tokens against `orderStatusEnum`). GET `/api/orders?status=skickad,foo` → 400 (one bad token rejects the whole list).
    - Test 7 (orderListItem actor fields shape): A bekraftad order in the response has `confirmedBy: {id, name}` populated AND `submittedBy: {id, name}` populated AND `deliveredBy: null` (only the up-to-status actors are populated; deferred future steps are null). Verify the field shapes match the widened orderListItem schema.
  </behavior>
  <read_first>
    - apps/api/src/services/order.service.ts (post-Slice-A — listOrdersForUnit currently at ~lines 181-202; toOrderListItem at ~lines 112-121; include block at ~lines 192-194)
    - apps/api/src/routes/orders/list.ts (current shape — parses ?status via Zod orderListQuery)
    - packages/shared/src/contracts/order.ts (post-Slice-A — orderListItem widened with the three optional actor trios; orderListQuery.status is union of single + array)
    - packages/shared/src/constants/orderStatus.ts (ORDER_STATUSES tuple — used to expand 'alla')
    - apps/api/test/orders.integration.test.ts (Phase 3 list scenarios — reference for list-test shape + harness)
    - apps/api/test/orders.confirm.integration.test.ts (Slice A — for additional helper patterns if needed)
    - apps/api/test/helpers/buildTestApp.ts (TEST_SJUKSKOTERSKA + TEST_APOTEKARE + ensureAllRolesSeeded)
    - .planning/phases/04-confirm-deliver-stock/04-PATTERNS.md (listOrdersForUnit widening section; route-level pre-parser explanation)
    - .planning/phases/04-confirm-deliver-stock/04-CONTEXT.md (D-82 status-tab + URL convention; D-Specifics 'Alla' = all four statuses)
  </read_first>
  <action>
    Implement the list-API widening with TDD: write the failing tests, then widen the route + service.

    Step A — write `apps/api/test/orders.list.integration.test.ts` with the 7 `it` blocks above (RED). Use the same lifecycle hooks + helpers pattern as Slice A's confirm test (build app, ensure roles seeded, reset sessions, close). Helpers needed: `loginAs` (shared), `createOrderWithStatus(cookie, status, lines)` — a small composite that creates the draft, optionally adds lines + submits + confirms + delivers to reach the target status (call deliverOrder service directly when it's not available via inject; or for Slice C's purposes, write the orders directly via `prisma.order.create({...status: 'bekraftad', submittedAt, ...})` — this is acceptable for *list-endpoint* tests because the list endpoint doesn't validate transitions, only reads). Direct prisma fixtures are faster and avoid coupling Slice C's list tests to Slice B's deliver implementation.

    Step B — widen `listOrdersForUnit` in `apps/api/src/services/order.service.ts`:
    1. Change the where clause from `status: filters.status` to `status: Array.isArray(filters.status) ? { in: filters.status } : filters.status`.
    2. Widen the include block to add `submittedBy: { select: { id: true, name: true } }, confirmedBy: { select: { id: true, name: true } }, deliveredBy: { select: { id: true, name: true } }` next to the existing `createdBy` select.
    3. Widen `toOrderListItem` to map the three optional actor trios + their timestamps from the loaded row to the response shape. Each is `nullable+optional` per the shared contract.

    Step C — add the pre-parser to `apps/api/src/routes/orders/list.ts`:
    1. Before Zod validation, intercept `req.query.status`. If it equals the string `'alla'`, replace with `ORDER_STATUSES` (import from `@meditrack/shared`). If it contains a comma (e.g. `'skickad,bekraftad'`), split on `,`, `.trim()` each token, and replace with the resulting array.
    2. Let Zod (`orderListQuery`) then validate the result against the `union(orderStatusEnum, array(orderStatusEnum))` shape — invalid tokens will reject with 400.
    3. Pass the parsed `filters` to `listOrdersForUnit(req.user.careUnitId, filters)` unchanged.

    Step D — GREEN. Run `pnpm --filter @meditrack/api test -- orders.list.integration`. All 7 MUST PASS.

    Step E — full BE suite passes — `pnpm --filter @meditrack/api test`. Slice A's confirm test + Phase 3's D-73 suite MUST still pass.
  </action>
  <verify>
    <automated>pnpm --filter @meditrack/api test -- orders.list.integration &amp;&amp; pnpm --filter @meditrack/api test</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/src/services/order.service.ts` `listOrdersForUnit` where clause uses `Array.isArray(filters.status) ? { in: filters.status } : filters.status` (verify by grep).
    - `apps/api/src/services/order.service.ts` `listOrdersForUnit` include block contains `submittedBy: { select: { id: true, name: true } }`, `confirmedBy: { select: { id: true, name: true } }`, `deliveredBy: { select: { id: true, name: true } }` next to the existing `createdBy` select.
    - `toOrderListItem` maps `submittedAt`, `submittedBy`, `confirmedAt`, `confirmedBy`, `deliveredAt`, `deliveredBy` from the loaded row.
    - `apps/api/src/routes/orders/list.ts` contains a pre-parser that intercepts `req.query.status === 'alla'` and replaces with `ORDER_STATUSES` array (verify by grep for the literal `'alla'` string + `ORDER_STATUSES` import).
    - The pre-parser splits comma-list strings (e.g., `'skickad,bekraftad'`) into an array before Zod validation.
    - GET `/api/orders` (no param) returns only utkast (Phase 3 back-compat preserved).
    - GET `/api/orders?status=alla` returns all four statuses (count matches sum of per-status counts).
    - GET `/api/orders?status=foo` returns 400.
    - GET `/api/orders?status=skickad,bekraftad` returns both statuses' orders.
    - Cross-careUnit isolation preserved: a careUnit-B order does not appear in careUnit-A apotekare's `?status=alla` response.
    - `apps/api/test/orders.list.integration.test.ts` exists with 7 `it` blocks.
    - `pnpm --filter @meditrack/api test -- orders.list.integration` exits 0 with all 7 tests passing.
    - `pnpm --filter @meditrack/api test` (full suite) exits 0 — no regressions on Slice A or Phase 3 tests.
  </acceptance_criteria>
  <done>List API widened to accept comma-list and 'alla' status; service widened with actor trio includes; 7 integration tests pass including back-compat default, comma-list, 'alla', invalid-token 400, and cross-careUnit isolation. No regressions.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Install shadcn Tabs + useOrdersByStatusQuery + BestallningarPage tabbed history</name>
  <files>apps/web/src/components/ui/tabs.tsx, apps/web/src/features/orders/useOrderQueries.ts, apps/web/src/routes/bestallningar/BestallningarPage.tsx, apps/web/src/routes/bestallningar/OrdersTable.tsx, apps/web/src/routes/bestallningar/OrdersCardList.tsx</files>
  <read_first>
    - apps/web/src/components/ui/ (verify tabs.tsx is NOT present today — per UI-SPEC §Design System)
    - apps/web/src/components/ui/alert-dialog.tsx (precedent for what shadcn-installed files look like — verify the conventions match what `pnpm dlx shadcn add tabs` will emit)
    - apps/web/components.json (shadcn config — confirms style=new-york, baseColor=slate; should not need modification)
    - apps/web/src/features/orders/useOrderQueries.ts (current `useDraftsQuery` shape — template for new `useOrdersByStatusQuery`)
    - apps/web/src/routes/bestallningar/BestallningarPage.tsx (current shape — drafts-only listing with table/card pair)
    - apps/web/src/routes/bestallningar/DraftsTable.tsx + DraftsCardList.tsx (templates for OrdersTable + OrdersCardList; geometry is identical, column set varies)
    - packages/shared/src/constants/orderStatus.ts (ORDER_STATUSES + ORDER_STATUS_LABELS — used for tab labels)
    - .planning/phases/04-confirm-deliver-stock/04-UI-SPEC.md (§Components 1 — full Tab Strip + per-tab column spec + empty states + mobile responsive rules; §Copywriting — exact tab labels and empty-state copy)
    - .planning/phases/04-confirm-deliver-stock/04-CONTEXT.md (D-82 status-tab refactor; D-Specifics empty-state copy)
    - .planning/phases/02-medication-catalog/02-CONTEXT.md (D-39 / D-42 URL-as-state pattern — react-router-dom v7 useSearchParams precedent)
  </read_first>
  <action>
    Wire the FE history surface end-to-end. Concretely:

    A. **Install shadcn Tabs primitive:** Run `pnpm dlx shadcn add tabs` from the `apps/web/` directory. This will:
       1. Read `apps/web/components.json` (style=new-york, baseColor=slate).
       2. Add `@radix-ui/react-tabs` to `apps/web/package.json` if not already present.
       3. Write `apps/web/src/components/ui/tabs.tsx` exporting `Tabs`, `TabsList`, `TabsTrigger`, `TabsContent`.
       Verify by checking `apps/web/src/components/ui/tabs.tsx` exists and exports those four components. If the install prompts for any choices, accept defaults. Do not modify the generated file beyond what shadcn emits (any style overrides happen via Tailwind classes at usage sites, not via editing the primitive).

    B. **Add `useOrdersByStatusQuery` to `useOrderQueries.ts`:** Clone `useDraftsQuery` (Option A from PATTERNS.md). Signature: `export function useOrdersByStatusQuery(status: OrderStatus | OrderStatus[] | 'alla') { … }`. Body: compute `statusKey = Array.isArray(status) ? status.join(',') : status;` and `queryKey: ['orders', { status: statusKey }]`, `queryFn: () => fetchJson<OrderListResponse>(\`/api/orders?status=${statusKey}\`)`, `placeholderData: keepPreviousData`. The `'alla'` literal is sent verbatim to the API (the BE pre-parser expands it server-side). `useDraftsQuery` stays unchanged for the Utkast tab (it shares the cache key `['orders', { status: 'utkast' }]`).

    C. **New file `apps/web/src/routes/bestallningar/OrdersTable.tsx`** (≥md table for non-Utkast tabs). Per UI-SPEC §Components 1 Column Spec. Props: `{ rows: OrderListItem[]; tab: 'skickad'|'bekraftad'|'levererad'|'alla' }`. Render shadcn `<Table>` with columns determined by `tab`:
       - skickad: `Skickad` (formatRelative(submittedAt)) / `Rader` / `Total` / `Skickad av` (submittedBy.name) / `Öppna` (ChevronRight).
       - bekraftad: `Bekräftad` (formatRelative(confirmedAt)) / `Rader` / `Total` / `Bekräftad av` (confirmedBy.name) / `Öppna`.
       - levererad: `Levererad` (formatRelative(deliveredAt)) / `Rader` / `Total` / `Levererad av` (deliveredBy.name) / `Öppna`.
       - alla: `Skapad` (formatRelative(createdAt)) / `Status` (<OrderStatusPill>) / `Rader` / `Skapad av` / `Öppna`.
       Entire `<TableRow>` is `cursor-pointer hover:bg-muted/50` + onClick → `navigate('/bestallningar/${row.id}')`. `aria-label="Öppna beställning från {formatRelative(relevantAt)}"`. Header row: `text-xs font-semibold text-muted-foreground uppercase tracking-wide bg-muted/50` (matches Phase 2/3 tables). Reuse `formatRelative` from `DraftsTable` (or import from wherever Phase 3 inlined it; if it's inline in DraftsTable.tsx, extract to a small shared util `apps/web/src/lib/formatRelative.ts` ONLY if both files genuinely use it; otherwise copy the function — Phase 3 D-15 `<critical_rules>` prefers extraction for two consumers).

    D. **New file `apps/web/src/routes/bestallningar/OrdersCardList.tsx`** (<md card list for non-Utkast tabs). Per UI-SPEC §Components 1 Mobile Card Variants. Props: `{ rows: OrderListItem[]; tab: 'skickad'|'bekraftad'|'levererad'|'alla' }`. Render one `<div>` per row matching the card anatomy from UI-SPEC: top line with relevant timestamp + ChevronRight (+ OrderStatusPill on Alla tab); second line with `<status> av {name}` (skickad/bekraftad/levererad) or `Skapad av {createdBy.name}` (alla); third line `{N} rader · totalt {sum}` (omit `totalt {sum}` for the Alla tab per UI-SPEC). Each card is a `<button type="button">` with `aria-label="Öppna beställning från {formatRelative(relevantAt)}"` and `onClick` navigates. Styling: `bg-card border border-border rounded-lg p-4 shadow-sm` (same as Phase 3 DraftCard).

    E. **`BestallningarPage.tsx` — extend per UI-SPEC §Components 1:**
       1. Replace the single `useDraftsQuery()` with a URL-backed status state: `const [searchParams, setSearchParams] = useSearchParams(); const status = (searchParams.get('status') ?? 'utkast') as 'utkast'|'skickad'|'bekraftad'|'levererad'|'alla';`.
       2. Render `<Tabs value={status} onValueChange={(v) => setSearchParams({ status: v })}>` above the list. `<TabsList>` with className overrides per UI-SPEC §Components 1 ("underlined tabs"): `className="bg-transparent border-b border-border rounded-none p-0 overflow-x-auto flex-nowrap"` + `aria-label="Beställningsstatus"`. Each `<TabsTrigger value="…">` uses the className from UI-SPEC: `className="rounded-none border-b-2 border-transparent data-[state=active]:border-primary data-[state=active]:text-foreground font-semibold text-xs text-muted-foreground pb-2 flex-shrink-0 whitespace-nowrap"`. Tab labels exact: `Utkast` / `Skickade` / `Bekräftade` / `Levererade` / `Alla`.
       3. Branch the query: `const ordersQuery = status === 'utkast' ? useDraftsQuery() : useOrdersByStatusQuery(status === 'alla' ? 'alla' : status);` (note: React Hook rules require both hooks to be called unconditionally — use a single `useOrdersByStatusQuery(status === 'utkast' ? 'utkast' : status === 'alla' ? 'alla' : status)` instead, which subsumes both code paths; the back-compat cache key `['orders', { status: 'utkast' }]` is preserved because the statusKey computation produces `'utkast'` for that input).
       4. Render the rows: for `status === 'utkast'` render the existing `<DraftsTable>` (≥md) / `<DraftsCardList>` (<md); for any other tab render `<OrdersTable rows={data} tab={status} />` / `<OrdersCardList rows={data} tab={status} />`.
       5. Per-tab empty states (UI-SPEC §Empty States): branch on `status` for the empty-state copy. Utkast keeps the existing `<EmptyStateCard>` with the `Ny beställning` CTA. Skickade: `Inga skickade beställningar.`. Bekräftade: `Inga bekräftade beställningar.`. Levererade: `Inga levererade beställningar ännu.`. Alla: same as utkast EmptyStateCard. Non-Utkast empty states render as `<p className="text-sm text-muted-foreground py-12 text-center">…</p>` per UI-SPEC.
       6. Loading state (tab switch): skeleton rows replace the list; tab strip remains visible + interactive (`placeholderData: keepPreviousData` already handles this — verify visually).

    F. **No nav changes.** The `Beställningar` nav link in the bottom tab bar / sidebar already exists; tab navigation is intra-page only.
  </action>
  <verify>
    <automated>cd apps/web &amp;&amp; pnpm dlx shadcn add tabs --yes 2>/dev/null || true; cd ../.. &amp;&amp; ls apps/web/src/components/ui/tabs.tsx &amp;&amp; pnpm --filter @meditrack/web typecheck &amp;&amp; pnpm --filter @meditrack/web build &amp;&amp; pnpm --filter @meditrack/web test</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/components/ui/tabs.tsx` exists and exports `Tabs`, `TabsList`, `TabsTrigger`, `TabsContent` (verify by grep on the file for the four exports).
    - `apps/web/package.json` lists `@radix-ui/react-tabs` as a dependency.
    - `apps/web/src/features/orders/useOrderQueries.ts` exports `useOrdersByStatusQuery` accepting `OrderStatus | OrderStatus[] | 'alla'` and producing query key `['orders', { status: statusKey }]` where `statusKey` is the joined-comma string for arrays.
    - `apps/web/src/routes/bestallningar/OrdersTable.tsx` exists, exports `OrdersTable` and branches columns on the `tab` prop with the exact column sets from UI-SPEC §Components 1 Column Spec.
    - `apps/web/src/routes/bestallningar/OrdersCardList.tsx` exists with per-tab card layout matching UI-SPEC §Components 1 Mobile Card Variants.
    - `BestallningarPage.tsx` uses `useSearchParams` to read the `?status` URL param (default `'utkast'`) and renders shadcn `<Tabs value={status} onValueChange={…setSearchParams…}>`.
    - `<TabsList>` `aria-label` is `Beställningsstatus`.
    - All 5 `<TabsTrigger>` values are exactly: `utkast`, `skickad`, `bekraftad`, `levererad`, `alla`. Their labels are exactly: `Utkast`, `Skickade`, `Bekräftade`, `Levererade`, `Alla`.
    - For each non-Utkast tab, the empty state copy matches UI-SPEC §Empty States verbatim.
    - The Utkast tab renders `<DraftsTable>` / `<DraftsCardList>` (Phase 3 components unchanged).
    - For non-Utkast tabs, `<OrdersTable>` / `<OrdersCardList>` render with the tab-specific columns.
    - Switching tabs updates the URL (verify by clicking a tab + reading `window.location.search`).
    - `pnpm --filter @meditrack/web typecheck` exits 0.
    - `pnpm --filter @meditrack/web build` exits 0.
    - `pnpm --filter @meditrack/web test` exits 0.
  </acceptance_criteria>
  <done>shadcn Tabs installed. `useOrdersByStatusQuery` parallel to useDraftsQuery. `BestallningarPage` renders all 5 status tabs URL-backed with per-tab column sets + per-tab empty states. Mobile tab strip horizontally scrollable. No regressions on the Utkast tab (Phase 3 components reused).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Seed extension — seedDemoOrders with all four statuses + Levererad post-step stock</name>
  <files>apps/api/prisma/seed.ts</files>
  <read_first>
    - apps/api/prisma/seed.ts (full file — `seedDraftOrder` ~lines 340-423 is the literal template; locate the apotekare user lookup pattern + the sjukskoterska lookup at ~lines 341-343)
    - apps/api/src/services/order.service.ts (deliverOrder body — Slice B — for the exact shape of the post-step stock UPDATE per D-85)
    - .planning/phases/04-confirm-deliver-stock/04-PATTERNS.md (seed.ts seedDraftOrder → seedDemoOrders fan-out section — covers idempotency-check pattern + post-step stock options)
    - .planning/phases/04-confirm-deliver-stock/04-CONTEXT.md (D-85 seed fan-out + 30-second demo path)
    - .planning/phases/03-draft-orders/03-CONTEXT.md (D-23 / D-72 existing draft-seed behavior, low-stock CUM picker pattern)
  </read_first>
  <action>
    Extend `apps/api/prisma/seed.ts` to seed one order per status, idempotently. Concretely:

    A. **Rename `seedDraftOrder` → `seedDemoOrders`** (or add `seedDemoOrders` and have it internally call four per-status helpers). Either shape is acceptable per D-85. Recommend: keep `seedDraftOrder` as a Utkast-specific seeder (back-compat — other places may reference it) and add `seedDemoOrders(prisma)` that orchestrates four calls: `seedOrderInStatus(prisma, 'utkast', sjukskoterska)`, `seedOrderInStatus(prisma, 'skickad', sjukskoterska)`, `seedOrderInStatus(prisma, 'bekraftad', sjukskoterska, apotekare)`, `seedOrderInStatus(prisma, 'levererad', sjukskoterska, apotekare)`. Update the seed's main entry to invoke `seedDemoOrders(prisma)` in place of `seedDraftOrder(prisma)`.

    B. **Idempotency-check pattern per status** (PATTERNS.md): for each status, `findFirst` keyed on `(careUnitId, createdByUserId, status, deletedAt: null)`. If found, log `[seed] {statusLabel} order already exists — skipping (idempotent).` and skip. Per D-85, this is per-status, NOT keyed on the entire 4-order group — so re-running seed after a partial run still tops up the missing statuses.

    C. **Per-status order body** (D-85):
       - Utkast: `status: 'utkast'`. No actor stamps beyond `createdByUserId: sjukskoterska.id`. Add 3 lines using the existing low-stock CUM picker.
       - Skickad: `status: 'skickad'`. Stamps: `createdByUserId: sjukskoterska.id`, `submittedAt: new Date()`, `submittedByUserId: sjukskoterska.id`. Add 3 lines.
       - Bekräftad: status='bekraftad'. Stamps: createdByUserId + submittedAt/By + `confirmedAt: new Date(), confirmedByUserId: apotekare.id`. Add 3 lines. (Apotekare lookup mirrors sjukskoterska lookup — query the seeded apotekare user `apotekare@example.test`; if the lookup function does not exist, follow the same `findUniqueOrThrow({ where: { email } })` pattern used for sjukskoterska.)
       - Levererad: status='levererad'. Stamps: all of the above + `deliveredAt: new Date(), deliveredByUserId: apotekare.id`. Add 3 lines. **POST-STEP: After creating the Levererad order, for each line apply `prisma.careUnitMedication.update({ where: { id: cumId }, data: { currentStock: { increment: line.quantity } } })`** so the demo on `/lakemedel` reflects the historic delivery (D-85 — Claude's discretion was on post-step-vs-PRNG; PATTERNS.md and CONTEXT.md prefer post-step for transparency; choose post-step).

    D. **Idempotency for the Levererad post-step:** If the Levererad order was already seeded on a previous run, the post-step MUST NOT re-apply (would double-increment stock). The order's `findFirst` early-return guards the entire `seedOrderInStatus(prisma, 'levererad', ...)` block — both the order create AND the post-step UPDATE skip together when an existing Levererad order is found. Verify this by reading the helper's flow.

    E. **Reuse the existing low-stock CUM picker** (Phase 3 `seedDraftOrder` lines ~367-403): the same 3 meds appear in each of the four orders so the 30-second demo path is predictable. If the picker is currently inlined inside `seedDraftOrder`, extract it to a small helper `pickLowStockCumsFor(prisma, careUnitId, n)` that all four status-seeders call.

    F. **Console messages match existing format** (PATTERNS.md): `[seed] {statusLabel} order created.` on create; `[seed] {statusLabel} order already exists — skipping (idempotent).` on idempotent skip. Use `statusLabel` from `ORDER_STATUS_LABELS` for human-readable Swedish.

    G. **Run seed manually to verify:** `pnpm --filter @meditrack/api prisma:seed` (or whichever script the repo uses) — it MUST complete idempotently. Run it twice. The second run should log all four "skipping (idempotent)" lines and produce zero new rows + zero stock changes.

    H. **Verify the demo flow:** After seed (clean DB or post-2nd-run), apotekare@example.test should see one of each status when querying `/api/orders?status=alla`. The /lakemedel page should show the 3 demo CUMs with stock counts including the Levererad order's increments.
  </action>
  <verify>
    <automated>pnpm --filter @meditrack/api exec prisma migrate reset --force --skip-generate 2>&amp;1 | tail -10 &amp;&amp; pnpm --filter @meditrack/api prisma:seed 2>&amp;1 | tee /tmp/04-03-seed-1.log &amp;&amp; pnpm --filter @meditrack/api prisma:seed 2>&amp;1 | tee /tmp/04-03-seed-2.log &amp;&amp; grep -c "skipping (idempotent)" /tmp/04-03-seed-2.log</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/prisma/seed.ts` contains a function `seedDemoOrders` (or equivalent orchestrator) that fans out to four per-status seed operations.
    - Each per-status block uses `findFirst({ where: { careUnitId, createdByUserId: sjukskoterska.id, status, deletedAt: null } })` to check existence before creating.
    - Each per-status block creates an order with exactly the actor stamps prescribed by D-85: utkast (none), skickad (+submittedAt/By), bekraftad (+confirmedAt/By), levererad (+deliveredAt/By).
    - The Levererad block applies post-step `careUnitMedication.update({ data: { currentStock: { increment: qty } } })` per line.
    - The Levererad post-step is INSIDE the `findFirst`-guarded branch so re-running seed does NOT re-increment stock (idempotency).
    - Console output on a clean run reports 4 "created" messages.
    - Console output on a second run reports 4 "skipping (idempotent)" messages and NO new orders or stock changes (verify by reading DB row counts before/after).
    - The 3 demo CUMs are the same across all four status orders (predictable demo path per D-85).
    - `pnpm --filter @meditrack/api prisma:seed` exits 0 on both runs.
    - After seed, `GET /api/orders?status=alla` for the demo apotekare returns 4 orders (or more if Phase 3's seed also leaves draft orders — confirm the expected count includes only the seedDemoOrders rows + any pre-existing rows from older test runs).
    - The Levererad order's three CUMs show currentStock incremented by the matching line quantities when reading via `prisma.careUnitMedication.findMany`.
  </acceptance_criteria>
  <done>Seed extended to produce one demo order per status idempotently. Levererad post-step stock UPDATE applied once (transparent — `/lakemedel` shows the delivery effect). Re-running seed is a no-op. The 30-second demo path from CONTEXT.md D-85 (apotekare login → /bestallningar?status=skickad → confirm → deliver → /lakemedel shows updated stock) is now reproducible from `docker compose up` on a fresh clone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API GET /api/orders | Untrusted ?status query param |
| Cross-careUnit | Apotekare from careUnit A querying ?status=alla; must NOT see careUnit B's orders |
| Seed → DB | Local-only; not exposed in production, but seed runs on every `docker compose up` so idempotency matters |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-17 | Information Disclosure | Cross-tenant list query | mitigate | `listOrdersForUnit(careUnitId, …)` first arg is the session's careUnitId; where-clause forces `careUnitId: careUnitId`. No way for a request to override this — the route handler passes `req.user.careUnitId` directly to the service. Test 5 in Task 1 proves a careUnit-B order does NOT appear in careUnit-A's response. |
| T-04-18 | Tampering / Input | Crafted ?status=foo or ?status=skickad,foo | mitigate | The pre-parser passes the result to Zod validation. `orderListQuery.status` is a `z.union(orderStatusEnum, z.array(orderStatusEnum))` — invalid tokens trigger 400 via Fastify-Zod. Test 6 in Task 1 proves both shapes reject. |
| T-04-19 | DoS | ?status=alla returning unbounded orders | accept (with limit) | `orderListQuery.pageSize` already caps at 100 (Phase 3); default 50. A vårdenhet with >50 historical orders pages naturally. v2: cursor pagination if a vårdenhet's history grows beyond ~500 rows. |
| T-04-20 | Information Disclosure | Actor names in the response (privacy) | accept | This is internal staff-only data. Names are first-name + last-initial style per Phase 1 seed convention (e.g., `Anna A.`). No PII beyond name + role; all users are vårdenhet staff who already know each other. |
| T-04-21 | Tampering (seed) | Re-running seed creates duplicate stock increments | mitigate | Per-status `findFirst` guards the Levererad block; running seed N times produces stock incremented exactly once. Verify per acceptance criteria in Task 3. |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | One new package: `@radix-ui/react-tabs` (pulled in by `pnpm dlx shadcn add tabs`). This is installed via the official shadcn CLI which fetches from the curated shadcn registry → @radix-ui official packages. Treat as legitimate (mirrors how every prior shadcn primitive in this project was installed); no per-package legitimacy gate needed per RESEARCH.md exemption for shadcn-managed deps. |
</threat_model>

<verification>
- All three Tasks' `<verify>` commands exit 0.
- `pnpm --filter @meditrack/api test` (full BE suite) passes — Slice A + Phase 3 D-73 suite still pass.
- `pnpm --filter @meditrack/web typecheck && pnpm --filter @meditrack/web build && pnpm --filter @meditrack/web test` pass.
- `pnpm --filter @meditrack/api prisma:seed` run twice exits 0 both times; second run logs 4 "skipping (idempotent)" lines.
- Manual smoke: `docker compose up` on a fresh clone (or after `prisma migrate reset`); log in as apotekare@example.test; navigate to `/bestallningar`; verify Tabs render with 5 triggers; click each tab; verify URL updates (`?status=skickad` etc.); verify per-tab columns match UI-SPEC; verify the seeded orders appear; click into a Bekräftad order; verify Mode D from Slice B renders (when Slices B and C both ship, the demo path is complete).
- Manual smoke (mobile): resize to 360px; verify tab strip horizontally scrolls; verify card layouts render; verify no horizontal scroll on the page itself (UX-01 inherited).
</verification>

<success_criteria>
- ORD-07 satisfied: order history per vårdenhet with status, lines, timestamps, and the user who made each transition is visible at `/bestallningar` with five URL-deep-linkable status tabs.
- Per-tab columns + empty states match UI-SPEC §Components 1 verbatim.
- API: `GET /api/orders` accepts single status, comma-list, or 'alla'; back-compat default (no param → utkast); 400 on invalid tokens; cross-careUnit isolation preserved.
- Seed extension produces one demo order per status; Levererad post-step stock UPDATE applied once and idempotent on re-run.
- shadcn `<Tabs>` installed via official CLI; no third-party primitives.
- Workspace builds + typechecks + all tests pass.
</success_criteria>

<output>
Create `.planning/phases/04-confirm-deliver-stock/04-03-SUMMARY.md` when done.
</output>
