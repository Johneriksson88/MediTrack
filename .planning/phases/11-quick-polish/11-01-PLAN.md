---
phase: 11-quick-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/routes/shell/TopBar.tsx
  - apps/web/src/routes/shell/UserPillPopover.tsx
  - apps/web/src/routes/shell/UserPill.tsx
  - apps/web/src/routes/konto/KontoPage.tsx
  - apps/web/test/KontoPage.test.tsx
  - apps/web/test/TopBar.test.tsx
  - docs/screenshots/sc04-360-lakemedel.png
  - docs/screenshots/sc04-360-bestallningsskapande.png
  - docs/screenshots/sc04-360-bestallningshistorik.png
  - docs/screenshots/sc04-360-audit.png
  - docs/screenshots/sc04-360-dashboard.png
autonomous: true
requirements: [UX-02, UX-03]
requirements_addressed: [UX-02, UX-03]
tags: [phase-11, frontend, ui-polish, logout, konto, sc04]

must_haves:
  truths:
    - "Mobile user (<768px) sees a 'Logga ut' icon-button in the top-right of TopBar on every authenticated page, without navigating to Konto (UX-02 mobile half — D-170/D-174)"
    - "Desktop user (>=768px) sees a 'Logga ut' icon+label button in the top-right of TopBar next to a static UserPill, without clicking the pill to open a popover (UX-02 desktop half — D-170/D-171/D-174)"
    - "Clicking either TopBar logout button (mobile icon or desktop icon+label) invokes useLogout().mutate() which DELETEs /api/auth/session, evicts the ['me'] cache, and navigates to /login (D-170, single useLogout source-of-truth)"
    - "UserPill (the renamed-from-UserPillPopover identity strip) renders as a non-interactive <div> showing {user.name} · RoleBadge · {user.careUnit.name} — no popover, no click handler, no role='button' (D-171)"
    - "Konto page non-admin gate note reads EXACTLY 'Ändringar kan endast göras av administratör.' (with trailing period) for sjukskoterska and apotekare roles (UX-03 — D-173)"
    - "Konto page destructive 'Logga ut' button at KontoPage.tsx:79-87 remains unchanged — same wiring, same copy, same variant (D-172 retention)"
    - "sc04 mobile-first verification at 360px still passes — scrollWidth <= innerWidth, [data-test='primary-nav'] still present on BottomTabBar.tsx:27 (Phase 7 SC#4 invariant — Phase 11 does not touch BottomTabBar or Sidebar)"
    - "Refreshed sc04-360-*.png screenshots for the 5 authenticated routes show the new mobile logout icon in the TopBar (login is shell-less, untouched)"
  artifacts:
    - path: "apps/web/src/routes/shell/TopBar.tsx"
      provides: "TopBar with three right-side elements: static UserPill (md+), desktop icon+label logout button (md+), mobile icon-only logout button (<md)"
      contains: "useLogout, LogOut, md:hidden, hidden md:flex, h-11 w-11, min-h-[44px]"
    - path: "apps/web/src/routes/shell/UserPill.tsx"
      provides: "Static identity display (renamed from UserPillPopover.tsx per D-175)"
      contains: "useAuth, RoleBadge, user.careUnit.name"
    - path: "apps/web/src/routes/konto/KontoPage.tsx"
      provides: "Konto page with updated gate note copy"
      contains: "Ändringar kan endast göras av administratör."
    - path: "apps/web/test/TopBar.test.tsx"
      provides: "New TopBar component test covering both logout variants + static UserPill + logo link"
      min_lines: 80
    - path: "apps/web/test/KontoPage.test.tsx"
      provides: "Updated KontoPage tests asserting the new verbatim gate note string"
      contains: "Ändringar kan endast göras av administratör."
    - path: "docs/screenshots/sc04-360-dashboard.png"
      provides: "Regenerated 360px screenshot showing the new mobile logout icon in TopBar"
  key_links:
    - from: "apps/web/src/routes/shell/TopBar.tsx"
      to: "apps/web/src/features/auth/useLogout.ts"
      via: "useLogout() hook called once, mutate() invoked from two onClick handlers"
      pattern: "useLogout\\(\\)"
    - from: "apps/web/src/routes/shell/TopBar.tsx"
      to: "apps/web/src/routes/shell/UserPill.tsx"
      via: "import { UserPill } from './UserPill'"
      pattern: "from '\\./UserPill'"
    - from: "apps/web/src/routes/shell/UserPill.tsx"
      to: "apps/web/src/auth/useAuth.ts"
      via: "const { user } = useAuth()"
      pattern: "useAuth\\(\\)"
    - from: "apps/web/test/TopBar.test.tsx"
      to: "apps/web/src/routes/shell/TopBar.tsx"
      via: "renderWithProviders(<TopBar />) + getAllByRole('button', { name: /Logga ut/i }) asserts length=2"
      pattern: "getAllByRole.*Logga ut"
---

<objective>
Two UX corrections in a single vertical slice — Phase 11 ships:

1. **UX-02 (D-170/D-171/D-174/D-175):** TopBar exposes "Logga ut" at every breakpoint. Mobile gets a 44×44 icon-only button; desktop gets an icon+label button next to a simplified static UserPill (the existing UserPillPopover loses its popover wrapper, becoming identity display only). Both new buttons wire to the existing useLogout() hook — single source of truth, no new endpoint.

2. **UX-03 (D-173):** Konto page gate note for non-admin roles swaps verbatim from "Denna åtgärd kräver adminrättigheter." → "Ändringar kan endast göras av administratör." (trailing period preserved per the canonical ROADMAP §"Phase 11" SC#2 quote).

Purpose: Closes the last two requirements (UX-02 + UX-03) in the v1 requirements matrix. Phase 11 is the final cuttable phase per Roadmap §Cuttability — these are pure FE diffs that ship even if everything else slips.

Output: Updated TopBar.tsx + renamed UserPill.tsx (was UserPillPopover.tsx) + Konto string swap + new TopBar.test.tsx + updated KontoPage.test.tsx + 5 regenerated sc04-360-*.png screenshots.

Security: Pure FE diff. No new endpoints, no schema, no migration, no audit allowlist change, no env vars. Three new UI callers wire to the existing `useLogout()` hook; logout flow is already CSRF-protected via the existing cookie/session machinery. Threat model: NO new attack surface introduced by this phase.
</objective>

<execution_context>
@C:/Projekt/MediTrack/.claude/get-shit-done/workflows/execute-plan.md
@C:/Projekt/MediTrack/.claude/get-shit-done/templates/summary.md
</execution_context>

## Phase Goal

**As a** logged-in MediTrack user on any device size, **I want to** log out from the global top navigation at every breakpoint (without first navigating to Konto on mobile or opening a popover on desktop), and as a non-admin user **I want to** see clear guidance that "Ändringar kan endast göras av administratör." on the Konto page, **so that** logout is always one tap/click away and the admin-gate copy reads exactly as the requirement specifies.

<context>
@C:/Projekt/MediTrack/.planning/PROJECT.md
@C:/Projekt/MediTrack/.planning/ROADMAP.md
@C:/Projekt/MediTrack/.planning/STATE.md
@C:/Projekt/MediTrack/.planning/REQUIREMENTS.md
@C:/Projekt/MediTrack/.planning/phases/11-quick-polish/11-CONTEXT.md
@C:/Projekt/MediTrack/.planning/phases/11-quick-polish/11-PATTERNS.md
@C:/Projekt/MediTrack/.planning/phases/01-foundation-auth/01-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->
<!-- Executor should use these directly — no codebase exploration needed. -->

From `apps/web/src/features/auth/useLogout.ts` (do NOT edit — read for understanding only):
```typescript
// Idempotent DELETE /api/auth/session + cache eviction (['me']) + navigate('/login', { replace: true })
export function useLogout(): UseMutationResult<void, Error, void> {
  // returns { mutate: () => void, isPending: boolean, ... }
}
```

From `apps/web/src/auth/useAuth.ts` (do NOT edit — read for understanding only):
```typescript
// AuthGate guarantees user is non-null when called from authenticated routes,
// but UserPill keeps a `if (!user) return null;` defense in depth.
export function useAuth(): {
  user: MeResponse | null;
  isLoading: boolean;
  can: (action: string) => boolean;
};
```

From `@meditrack/shared` (MeResponse — used in test makeUser helper):
```typescript
type MeResponse = {
  id: string;
  email: string;
  name: string;
  role: 'sjukskoterska' | 'apotekare' | 'admin';
  careUnit: { id: string; name: string };
  permissions: string[];
};
```

From `apps/web/src/components/RoleBadge.tsx` (do NOT edit — read for understanding):
```typescript
export function RoleBadge({ role }: { role: MeResponse['role'] }): JSX.Element;
// Renders the localized pill: "Sjuksköterska" / "Apotekare" / "Admin"
```

From `apps/web/src/routes/shell/TopBar.tsx` (CURRENT — will be widened):
```typescript
// Lines 1-4: imports — Stethoscope, Link, UserPillPopover
// Lines 23-30: <header> + <Link> logo block — KEEP VERBATIM
// Lines 32-34: <div className="hidden md:block"><UserPillPopover /></div> — REPLACED
```

From `apps/web/src/routes/shell/UserPillPopover.tsx` (CURRENT — will be simplified + renamed):
```typescript
// Lines 1-8: imports — useAuth, RoleBadge, Popover/PopoverContent/PopoverTrigger, useLogout — KEEP useAuth+RoleBadge ONLY
// Lines 24-30: function signature + `const { user } = useAuth()` + `if (!user) return null;` — KEEP, drop `const logout = useLogout()`
// Lines 32-66: <Popover>...</Popover> JSX — REPLACED with non-interactive <div> wrapping the 5 spans
// Lines 39-49: the 5 identity spans — KEEP VERBATIM inside the new <div>
```

From `apps/web/src/routes/konto/KontoPage.tsx` (CURRENT — line 115 only):
```typescript
// Line 113-117 block — only line 115 literal changes:
//   "Denna åtgärd kräver adminrättigheter." -> "Ändringar kan endast göras av administratör."
// Line 79-87 (destructive button) — UNCHANGED per D-172
```

From `apps/web/test/KontoPage.test.tsx` (CURRENT — 4 string sites swap):
```typescript
// Line 17 doc-comment: "  - Gate note: <STRING>"
// Line 82 it() description literal: "does NOT render the gate note '<STRING>'"
// Line 85: screen.queryByText('<STRING>')
// Line 110 it() description: "renders the verbatim gate note '<STRING>'"
// Line 114: screen.getByText('<STRING>')
// Line 142: screen.getByText('<STRING>') (apotekare branch)
```
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure FE diff — no new endpoints, no new request handlers, no new data sinks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering / DoS | New TopBar logout buttons (3 new caller sites of useLogout) | accept | All three callers (mobile TopBar icon, desktop TopBar icon+label, retained Konto button) invoke the SAME `useLogout()` hook. DELETE /api/auth/session is idempotent and CSRF-protected via the existing cookie-bound session machinery established in Phase 1. No new attack surface. The `disabled={logout.isPending}` guard prevents double-fire while in flight. |
| T-11-SC | Tampering | npm/pip/cargo installs | mitigate | **N/A — no package installs in Phase 11.** No new dev-deps, no new runtime deps. lucide-react (`<LogOut/>`) and react-router-dom (`<Link>`) already in `apps/web/package.json`. No legitimacy gate needed. |

</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TopBar split + UserPill simplification + TopBar.test.tsx (UX-02 vertical, commit 1 of 3)</name>
  <files>apps/web/src/routes/shell/TopBar.tsx, apps/web/src/routes/shell/UserPillPopover.tsx → apps/web/src/routes/shell/UserPill.tsx (git rename), apps/web/test/TopBar.test.tsx (NEW)</files>
  <read_first>
    - `apps/web/src/routes/shell/TopBar.tsx` (current 38-line file — preserve `<header>` + `<Link>` logo block at lines 23-30 verbatim; widen the right-side `<div className="hidden md:block">` block at lines 32-34)
    - `apps/web/src/routes/shell/UserPillPopover.tsx` (current 68-line file — keep imports `useAuth` + `RoleBadge`, drop Popover/PopoverContent/PopoverTrigger + useLogout imports; keep the 5 identity spans at lines 39-49 verbatim inside a new non-interactive `<div>`)
    - `apps/web/src/features/auth/useLogout.ts` (the hook contract — three new caller sites all use this; no behavioral diff)
    - `apps/web/src/auth/useAuth.ts` (read pattern only — `const { user } = useAuth()` + `if (!user) return null;` defense)
    - `apps/web/src/components/RoleBadge.tsx` (consumed by UserPill — read for prop signature `{ role }`)
    - `apps/web/src/routes/shell/BottomTabBar.tsx` (analog for `md:hidden` + `min-h-[44px]` patterns — UI-SPEC §3 44px floor)
    - `apps/web/test/KontoPage.test.tsx` (mock shape for `useLogout` + `useAuth` + `makeUser` helper — lines 23-34, 47-60; copy structurally, lift `mockMutate` to module scope so click assertions can `expect(mockMutate).toHaveBeenCalled()`)
    - `apps/web/test/BottomTabBar.test.tsx` (analog for describe/it/render structure — lines 41-127)
    - `apps/web/test/helpers/renderWithProviders.tsx` (the test harness — wraps in `MemoryRouter` + `QueryClientProvider`; no overrides needed for these tests)
    - `.planning/phases/01-foundation-auth/01-UI-SPEC.md` §3 (touch targets 44px), §Colors line 97 (`#DC2626` destructive), §Focus rings line 149 (`focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-[#2563EB] focus-visible:ring-offset-2`), §Top Bar lines 257-265 (h-14 header), §User Pill lines 388-403 (pre-D-171 spec, what is being collapsed), §Konto §385-386 (destructive logout button — UNCHANGED in Phase 11), §Logout flow §403 (DELETE /api/auth/session + cache clear + navigate)
    - `.planning/phases/11-quick-polish/11-CONTEXT.md` (the source of truth for D-170/D-171/D-174/D-175 — every CSS class, aria-label, and copy literal in this task derives from there)
    - `.planning/phases/11-quick-polish/11-PATTERNS.md` lines 19-107 (TopBar pattern composition), lines 110-179 (UserPill simplification), lines 256-397 (TopBar.test.tsx scaffold)
  </read_first>
  <behavior>
    - **TopBar mobile variant test:** `screen.getByRole('button', { name: 'Logga ut' })` returns a `<button>` with `className` matching `/md:hidden/`. The element is the icon-only mobile logout button.
    - **TopBar desktop variant test:** `screen.getAllByRole('button', { name: /Logga ut/i })` returns exactly 2 buttons (mobile icon-only + desktop icon+label). The desktop one sits inside a parent with `hidden md:flex`.
    - **Click invokes mutate (mobile):** Clicking the first `Logga ut`-named button (mobile, index 0) calls `mockMutate` exactly once.
    - **Click invokes mutate (desktop):** Clicking the second `Logga ut`-named button (desktop, index 1) calls `mockMutate` exactly once.
    - **UserPill is non-interactive:** `screen.getByText('sjukskoterska user')` is present, AND `screen.queryByRole('button', { name: 'sjukskoterska user' })` is `null` (the pill renders as a `<div>`, not a `<button>`).
    - **Logo Link to /dashboard unchanged:** `screen.getByRole('link', { name: /MediTrack/i })` has `href="/dashboard"`.
  </behavior>
  <action>
**Step 1.1 — Git rename UserPillPopover.tsx → UserPill.tsx (per D-175):**
Run `git mv apps/web/src/routes/shell/UserPillPopover.tsx apps/web/src/routes/shell/UserPill.tsx` BEFORE editing the file so git preserves the rename in the commit (rather than recording "delete A + add B"). If running git from inside the agent's shell, use the equivalent file system move + `git add`/`git rm` so the rename is detected.

**Step 1.2 — Rewrite `apps/web/src/routes/shell/UserPill.tsx` per D-171 (strip popover wrapper):**
- DROP imports: `Popover`, `PopoverContent`, `PopoverTrigger` from `@/components/ui/popover`; `useLogout` from `@/features/auth/useLogout`.
- KEEP imports: `useAuth` from `@/auth/useAuth`; `RoleBadge` from `@/components/RoleBadge`.
- RENAME exported function `UserPillPopover` → `UserPill`.
- KEEP the `if (!user) return null;` defense-in-depth guard (was UserPillPopover.tsx:28-30).
- REPLACE the entire `<Popover>...</Popover>` JSX (was UserPillPopover.tsx:32-66) with a single non-interactive `<div className="flex items-center">` containing the 5 identity spans VERBATIM from UserPillPopover.tsx:39-49 (name span, mx-2 dot, RoleBadge, mx-2 dot, careUnit.name span).
- Drop `rounded px-2 py-1`, `cursor-pointer`, `hover:bg-[#E2E8F0]/60`, focus-ring classes from the wrapping element — non-interactive node, no padding needed.
- Update the file's doc-comment to reflect the new contract (identity display only — no popover, no logout trigger; logout now lives in TopBar.tsx per D-170).

**Step 1.3 — Rewrite `apps/web/src/routes/shell/TopBar.tsx` per D-170 + D-174:**
- ADD imports: `LogOut` from `lucide-react` (extend the existing `import { Stethoscope }` line to `import { LogOut, Stethoscope }`); `useLogout` from `@/features/auth/useLogout`.
- CHANGE import: `import { UserPillPopover } from './UserPillPopover';` → `import { UserPill } from './UserPill';` (line 4-5).
- ADD `const logout = useLogout();` as the first line inside the `TopBar()` function body.
- KEEP the `<header>` element and `<Link>` logo block (lines 23-30) VERBATIM — same className `"h-14 bg-[#F1F5F9] border-b border-[#E2E8F0] flex items-center justify-between px-4 md:px-6"`, same logo focus ring, same `<Stethoscope className="h-5 w-5 text-[#2563EB]" aria-hidden="true" />`.
- REPLACE the right-side `<div className="hidden md:block"><UserPillPopover /></div>` (lines 32-34) with TWO siblings (both inside the same `<header>`):
  1. **Desktop right cluster** — `<div className="hidden md:flex md:items-center md:gap-3">` containing `<UserPill />` followed by the desktop logout `<button type="button" onClick={() => logout.mutate()} disabled={logout.isPending} className="inline-flex items-center gap-2 text-sm font-semibold text-[#DC2626] px-3 py-2 min-h-[44px] rounded hover:bg-slate-50 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-[#2563EB] focus-visible:ring-offset-2 disabled:opacity-50 disabled:cursor-not-allowed">` whose children are `<LogOut className="h-4 w-4" aria-hidden="true" />` then `{logout.isPending ? 'Loggar ut…' : 'Logga ut'}` (the ellipsis is U+2026, matches UserPillPopover.tsx:62 byte-for-byte).
  2. **Mobile right cluster** — `<button type="button" onClick={() => logout.mutate()} disabled={logout.isPending} aria-label="Logga ut" className="md:hidden inline-flex items-center justify-center h-11 w-11 text-[#DC2626] rounded hover:bg-slate-50 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-[#2563EB] focus-visible:ring-offset-2 disabled:opacity-50 disabled:cursor-not-allowed">` containing `<LogOut className="h-5 w-5" aria-hidden="true" />`.
- Update the TopBar.tsx doc-comment to reflect: mobile right cluster = icon-only logout (per D-170/D-174); desktop right cluster = static UserPill + icon+label logout (per D-171/D-174). Both share useLogout (D-170 single hook). Konto-page logout retained (D-172).

**Step 1.4 — Create `apps/web/test/TopBar.test.tsx` (NEW file, ~150 lines):**
Mirror the structure of `apps/web/test/KontoPage.test.tsx` + `apps/web/test/BottomTabBar.test.tsx`. Imports: `describe, it, expect, vi, beforeEach` from vitest; `screen` from @testing-library/react; `MeResponse` from `@meditrack/shared`; `renderWithProviders` from `./helpers/renderWithProviders`; `TopBar` from `@/routes/shell/TopBar`.

Doc-comment header (lines 1-15):
```
/**
 * UX-02 — TopBar logout reachability at every breakpoint
 * (apps/web/src/routes/shell/TopBar.tsx)
 *
 * Behavioral requirements (Phase 11 D-170 / D-171 / D-174):
 * - Mobile (<md): icon-only <LogOut/> button, 44×44, aria-label="Logga ut".
 * - Desktop (>=md): icon+label "Logga ut" button next to static UserPill.
 * - Both wire to useLogout().mutate() — same hook, single source of truth.
 * - UserPill renders as a non-interactive <div> (no role="button"). D-171.
 * - Logo Link to /dashboard unchanged.
 */
```

Mock setup (module scope, before describe):
- `const mockMutate = vi.fn();`
- `vi.mock('@/features/auth/useLogout', () => ({ useLogout: vi.fn(() => ({ mutate: mockMutate, isPending: false })) }));`
- `vi.mock('@/auth/useAuth', () => ({ useAuth: vi.fn(), fetchMe: vi.fn() }));`
- Import `useAuth` AFTER the mock; `const mockUseAuth = vi.mocked(useAuth);`
- `makeUser(role)` helper — copy verbatim from KontoPage.test.tsx:51-60.
- `beforeEach(() => { vi.clearAllMocks(); mockMutate.mockClear(); });`

Tests (`describe('TopBar', () => { ... })`):
1. `it('renders the mobile-only icon-button (aria-label "Logga ut", md:hidden class)')` — set mockUseAuth to sjukskoterska, render, query `screen.getAllByRole('button', { name: /Logga ut/i })`, expect length 2; the one with `className` matching `/md:hidden/` is the mobile button. Use `Array.prototype.find` to disambiguate.
2. `it('renders the desktop-only logout button (icon+label inside hidden md:flex cluster)')` — the desktop button is the one NOT matching `/md:hidden/`. Assert its parent (`.closest('div')`) has `className` matching `/hidden md:flex/`.
3. `it('clicking the mobile logout button invokes useLogout().mutate()')` — `const userEvent = (await import('@testing-library/user-event')).default.setup();` then click the mobile button; assert `expect(mockMutate).toHaveBeenCalledTimes(1)`.
4. `it('clicking the desktop logout button invokes useLogout().mutate()')` — same shape, click the desktop button instead; assert `expect(mockMutate).toHaveBeenCalledTimes(1)`.
5. `it('renders the static UserPill (identity display is NOT a button)')` — assert `screen.getByText('sjukskoterska user')` exists AND `screen.queryByRole('button', { name: 'sjukskoterska user' })` returns null. Per D-171 the pill is a `<div>`, not a `<button>`.
6. `it('keeps the existing logo Link to /dashboard')` — assert `screen.getByRole('link', { name: /MediTrack/i })` has `href="/dashboard"`.

Set `mockUseAuth.mockReturnValue({ user: makeUser('sjukskoterska'), isLoading: false, can: (_a) => false });` inside a top-level `beforeEach` in the `describe('TopBar', ...)` block so every test has a valid user.

**Step 1.5 — Verify integration:**
- Run `pnpm --filter @meditrack/web typecheck` to confirm TopBar.tsx imports compile (the `UserPill` named export must match the rename; the `useLogout` import path is `@/features/auth/useLogout`).
- Run `pnpm --filter @meditrack/web test -- TopBar.test.tsx` to confirm all 6 tests pass.
- Run `pnpm --filter @meditrack/web test -- KontoPage.test.tsx` to confirm the existing KontoPage tests still pass (no regression — this task doesn't touch KontoPage; that's Task 2).

**Step 1.6 — Commit (atomic, per CONTEXT.md `<specifics>` line 304):**
`feat(11-01): split TopBar logout into per-breakpoint variants (UX-02)` — message body should reference D-170 (per-breakpoint divergence) + D-171 (UserPill simplification) + D-174 (mobile icon-only + desktop icon+label) + D-175 (rename UserPillPopover → UserPill). Use HEREDOC.
  </action>
  <verify>
    <automated>pnpm --filter @meditrack/web test -- TopBar.test.tsx KontoPage.test.tsx BottomTabBar.test.tsx Sidebar.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Source: `apps/web/src/routes/shell/TopBar.tsx` contains both `md:hidden` AND `hidden md:flex` substrings (verifies both per-breakpoint variants per D-170)
    - Source: `apps/web/src/routes/shell/TopBar.tsx` contains `useLogout` AND `text-[#DC2626]` AND `h-11 w-11` AND `min-h-[44px]` AND `aria-label="Logga ut"` AND `focus-visible:ring-[#2563EB]` (verifies D-174 spec verbatim)
    - Source: `apps/web/src/routes/shell/TopBar.tsx` still contains the unchanged logo block: `Stethoscope` AND `to="/dashboard"` AND `MediTrack`
    - File: `apps/web/src/routes/shell/UserPill.tsx` EXISTS (created via git mv from UserPillPopover.tsx per D-175)
    - File: `apps/web/src/routes/shell/UserPillPopover.tsx` DOES NOT EXIST (renamed)
    - Source: `apps/web/src/routes/shell/UserPill.tsx` does NOT contain `Popover` OR `PopoverTrigger` OR `PopoverContent` OR `useLogout` (verifies popover-wrapper stripped per D-171)
    - Source: `apps/web/src/routes/shell/UserPill.tsx` contains `useAuth` AND `RoleBadge` AND `careUnit.name` AND `export function UserPill` (verifies renamed export)
    - Source: `apps/web/src/routes/shell/UserPill.tsx` contains `<div` (the wrapping element is a non-interactive div, per D-171)
    - Source: `apps/web/src/routes/shell/TopBar.tsx` imports `UserPill` from `./UserPill` (NOT `./UserPillPopover`)
    - File: `apps/web/test/TopBar.test.tsx` EXISTS and contains ≥6 `it(` blocks
    - Test command: `pnpm --filter @meditrack/web test -- TopBar.test.tsx` exits 0 (all 6 new tests pass)
    - Test command: `pnpm --filter @meditrack/web test -- KontoPage.test.tsx` exits 0 (no regression — Task 1 does not touch KontoPage; old strings still expected here, swapped in Task 2)
    - Test command: `pnpm --filter @meditrack/web test -- BottomTabBar.test.tsx Sidebar.test.tsx` exits 0 (no regression — Phase 11 does not touch nav surfaces)
    - Typecheck: `pnpm --filter @meditrack/web typecheck` exits 0
    - Behavior: `apps/web/test/TopBar.test.tsx` asserts `getAllByRole('button', { name: /Logga ut/i })` returns length 2 (mobile + desktop both present in DOM)
    - Behavior: `apps/web/test/TopBar.test.tsx` asserts `mockMutate` is called exactly once per logout button click
    - Behavior: `apps/web/test/TopBar.test.tsx` asserts the UserPill identity text is present AND has no `role="button"`
    - Git: rename detected — `git status` shows `renamed: apps/web/src/routes/shell/UserPillPopover.tsx -> apps/web/src/routes/shell/UserPill.tsx` (NOT a delete + add pair)
    - Commit: HEAD commit message starts with `feat(11-01): split TopBar logout into per-breakpoint variants`
  </acceptance_criteria>
  <done>
    - All 6 new TopBar tests pass; all 8 existing KontoPage tests still pass (using OLD strings — Task 2 swaps them); all 9 existing BottomTabBar + Sidebar tests still pass.
    - TopBar.tsx renders mobile icon-only logout AND desktop icon+label logout AND static UserPill on the same surface.
    - UserPillPopover.tsx is gone; UserPill.tsx is present and exports `UserPill` (not `UserPillPopover`).
    - Atomic commit landed: `feat(11-01): split TopBar logout into per-breakpoint variants (UX-02)`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: KontoPage gate-note copy swap + KontoPage.test.tsx assertion updates (UX-03 vertical, commit 2 of 3)</name>
  <files>apps/web/src/routes/konto/KontoPage.tsx, apps/web/test/KontoPage.test.tsx</files>
  <read_first>
    - `apps/web/src/routes/konto/KontoPage.tsx` (current 122-line file — ONLY line 115 changes; verify line 79-87 destructive button block is unchanged per D-172)
    - `apps/web/test/KontoPage.test.tsx` (current 152-line file — 4 string sites: line 17 doc-comment, line 82+85 admin-branch negative assertion, line 110+114 sjukskoterska assertion, line 139+142 apotekare assertion; plus update the `it(...)` description literals at lines 82, 110, 139 to mirror the new string)
    - `.planning/phases/11-quick-polish/11-CONTEXT.md` lines 16, 56-67, 263-286 (D-173 verbatim swap + test consequence + exact diff)
    - `.planning/phases/11-quick-polish/11-PATTERNS.md` lines 182-253 (the exact 4 diff sites)
    - `.planning/REQUIREMENTS.md` line 74 (UX-03 canonical source — "Ändringar kan endast göras av administratör" with trailing period per ROADMAP)
    - `.planning/ROADMAP.md` §"Phase 11" SC#2 (the "reads exactly" clause — trailing period preserved)
  </read_first>
  <behavior>
    - **Sjukskoterska sees new copy:** Render KontoPage with sjukskoterska user; `screen.getByText('Ändringar kan endast göras av administratör.')` returns the gate-note `<p>` element.
    - **Apotekare sees new copy:** Render KontoPage with apotekare user; same assertion passes.
    - **Admin does NOT see new copy:** Render KontoPage with admin user; `screen.queryByText('Ändringar kan endast göras av administratör.')` returns `null`.
    - **No occurrence of old string:** `grep -F "Denna åtgärd kräver adminrättigheter."` against `apps/web/src/routes/konto/KontoPage.tsx` AND `apps/web/test/KontoPage.test.tsx` returns 0 matches.
    - **KontoPage destructive logout button unchanged:** `apps/web/src/routes/konto/KontoPage.tsx` still contains `variant="destructive"` AND `mt-6 w-full` AND `Loggar ut…` AND `Logga ut` (lines 79-87 block intact per D-172).
  </behavior>
  <action>
**Step 2.1 — Edit `apps/web/src/routes/konto/KontoPage.tsx` line 115:**
Replace the literal `Denna åtgärd kräver adminrättigheter.` with `Ändringar kan endast göras av administratör.` (trailing period preserved per ROADMAP §"Phase 11" SC#2 canonical quote). Use the Edit tool — keep the wrapping `<p className="text-xs text-[#64748B]">...</p>` and the surrounding `{user.role !== 'admin' && ...}` conditional unchanged. NO other edits to this file.

**Step 2.2 — Edit `apps/web/test/KontoPage.test.tsx` — 4 string sites (per D-173 lockstep):**

a. **Line 17 (doc-comment):** `*   - Gate note: "Denna åtgärd kräver adminrättigheter."` → `*   - Gate note: "Ändringar kan endast göras av administratör."`

b. **Line 82 (admin-branch `it()` description):** `'does NOT render the gate note "Denna åtgärd kräver adminrättigheter."'` → `'does NOT render the gate note "Ändringar kan endast göras av administratör."'`

c. **Line 85 (admin-branch queryByText argument):** `'Denna åtgärd kräver adminrättigheter.'` → `'Ändringar kan endast göras av administratör.'`

d. **Line 110 (sjukskoterska-branch `it()` description):** `'renders the verbatim gate note "Denna åtgärd kräver adminrättigheter."'` → `'renders the verbatim gate note "Ändringar kan endast göras av administratör."'`

e. **Line 114 (sjukskoterska-branch getByText argument):** `'Denna åtgärd kräver adminrättigheter.'` → `'Ändringar kan endast göras av administratör.'`

f. **Line 142 (apotekare-branch getByText argument):** `'Denna åtgärd kräver adminrättigheter.'` → `'Ändringar kan endast göras av administratör.'`

(That is 6 individual string occurrences across 4 logical sites — admin negative assertion has 2 occurrences (it-description + queryByText), sjukskoterska has 2 (it-description + getByText), apotekare has 1 (getByText), doc-comment has 1. Total 6 swaps in `KontoPage.test.tsx` — count must match `grep -c` of the old string returning 6 BEFORE and 0 AFTER.)

NO structural changes to the test file. No new `describe`, no new `it`, no mock changes, no provider changes. The verbatim period is preserved at every site.

**Step 2.3 — Verify integration:**
- Run `pnpm --filter @meditrack/web typecheck` to confirm no TypeScript regression.
- Run `pnpm --filter @meditrack/web test -- KontoPage.test.tsx` to confirm all 9 existing tests now pass with the new string.
- Run `pnpm --filter @meditrack/web test -- TopBar.test.tsx` to confirm Task 1's tests still pass (Task 2 does not touch TopBar).
- Run `grep -F "Denna åtgärd kräver adminrättigheter" apps/web/src/routes/konto/KontoPage.tsx apps/web/test/KontoPage.test.tsx` and confirm exit code 1 (zero matches — old string is fully purged).

**Step 2.4 — Commit (atomic, per CONTEXT.md `<specifics>` line 305):**
`feat(11-01): update Konto gate-note copy (UX-03)` — message body should reference D-173 (verbatim swap "Denna åtgärd kräver adminrättigheter." → "Ändringar kan endast göras av administratör." with trailing period preserved per ROADMAP §"Phase 11" SC#2). Use HEREDOC.
  </action>
  <verify>
    <automated>pnpm --filter @meditrack/web test -- KontoPage.test.tsx TopBar.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Source: `apps/web/src/routes/konto/KontoPage.tsx` contains `Ändringar kan endast göras av administratör.` (with trailing period, exact unicode)
    - Source: `apps/web/src/routes/konto/KontoPage.tsx` does NOT contain `Denna åtgärd kräver adminrättigheter` (old string fully removed — verify with `grep -c "Denna åtgärd kräver adminrättigheter" apps/web/src/routes/konto/KontoPage.tsx` returning 0)
    - Source: `apps/web/test/KontoPage.test.tsx` contains `Ändringar kan endast göras av administratör.` at minimum 4 times (3 assertion sites + 1 doc-comment; `grep -c` ≥ 4 after filtering with `grep -v '^//'` to exclude any commented-out matches; bare count must be ≥ 4)
    - Source: `apps/web/test/KontoPage.test.tsx` does NOT contain `Denna åtgärd kräver adminrättigheter` (`grep -c` returns 0 against the file)
    - Source: `apps/web/src/routes/konto/KontoPage.tsx` STILL contains `variant="destructive"` AND `mt-6 w-full` AND `{logout.isPending ? 'Loggar ut…' : 'Logga ut'}` (D-172 retention — destructive logout button untouched at lines 79-87)
    - Behavior: clicking the Konto destructive logout button still invokes `useLogout().mutate()` (no behavioral diff — preserved from Phase 1)
    - Test command: `pnpm --filter @meditrack/web test -- KontoPage.test.tsx` exits 0 (all 9 tests pass with the new string)
    - Test command: `pnpm --filter @meditrack/web test -- TopBar.test.tsx` exits 0 (Task 1 tests still pass — Task 2 does not touch TopBar)
    - Typecheck: `pnpm --filter @meditrack/web typecheck` exits 0
    - Commit: HEAD commit message starts with `feat(11-01): update Konto gate-note copy`
  </acceptance_criteria>
  <done>
    - KontoPage.tsx renders the new gate note for non-admin roles.
    - KontoPage.test.tsx asserts the new string at all 3 assertion sites (admin negative + sjukskoterska positive + apotekare positive) + the doc-comment reflects the new copy.
    - Old string is fully purged from both files.
    - Konto-page destructive logout button untouched (D-172 retention verified).
    - Atomic commit landed: `feat(11-01): update Konto gate-note copy (UX-03)`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Regenerate sc04 mobile screenshots (commit 3 of 3)</name>
  <files>docs/screenshots/sc04-360-lakemedel.png, docs/screenshots/sc04-360-bestallningsskapande.png, docs/screenshots/sc04-360-bestallningshistorik.png, docs/screenshots/sc04-360-audit.png, docs/screenshots/sc04-360-dashboard.png</files>
  <read_first>
    - `apps/web/scripts/captureSc04Screenshots.ts` (the Playwright harness — exact command shape: `pnpm --filter @meditrack/web exec tsx scripts/captureSc04Screenshots.ts`; prereq: `docker compose up` running on api :3000 + web :5173; first-time setup: `pnpm exec playwright install chromium`)
    - `apps/web/src/routes/shell/BottomTabBar.tsx` line 27 (verify the `data-test="primary-nav"` attribute is untouched — the sc04 harness asserts this is present at 360px)
    - `.planning/phases/07-ops-submission-polish/07-CONTEXT.md` (the sc04 harness contract — scrollWidth <= innerWidth invariant + primary-nav selector contract; D-128)
    - `.planning/phases/10-order-numbers/10-CONTEXT.md` lines 118, 139 (the precedent — sc04 re-capture pattern from Phase 10; commit refreshed PNGs as part of slice closeout)
    - `.planning/phases/11-quick-polish/11-CONTEXT.md` lines 21, 118-122 (Phase 11's sc04 contract — 5 of 6 captures change because login is shell-less; the data-test="primary-nav" attribute must remain intact)
  </read_first>
  <action>
**Step 3.1 — Pre-flight verification (no edits expected):**
Verify Phase 11 has NOT touched the primary-nav contract. Run `grep -F 'data-test="primary-nav"' apps/web/src/routes/shell/BottomTabBar.tsx` and confirm a match exists at line 27 (Phase 7 D-128 — must not be regressed by Phase 11). Run `git diff HEAD~2 -- apps/web/src/routes/shell/BottomTabBar.tsx apps/web/src/routes/shell/Sidebar.tsx` and confirm empty output (Phase 11 commits do not touch these files).

**Step 3.2 — Ensure docker-compose stack is running:**
The harness requires the api :3000 and web :5173 services to be reachable. Either:
- (Preferred) Run `docker compose up -d` in the repo root and wait for the api healthcheck to clear.
- If a stack is already running (check via `docker compose ps`), reuse it.
If first-time setup is needed, run `pnpm --filter @meditrack/web exec playwright install chromium` to install the Playwright browser.

**Step 3.3 — Run the sc04 harness:**
Execute `pnpm --filter @meditrack/web exec tsx scripts/captureSc04Screenshots.ts` from the repo root. The harness logs in as admin, iterates 4 viewports × 6 routes (24 cells), asserts scrollWidth <= innerWidth + presence of `[data-test="primary-nav"]` for every cell, and captures `docs/screenshots/sc04-360-{slug}.png` for the 6 × 360 cells. Exit code 0 = mechanical SC#4 verification still passes.

**Step 3.4 — Review the 5 changed PNGs:**
After the harness exits 0:
- `docs/screenshots/sc04-360-login.png` should be byte-identical to its pre-Phase-11 version (login is shell-less; Phase 11 did not touch it). If it changed, investigate — likely a non-deterministic render difference (font fallback, anti-alias) that is acceptable; do not block on this.
- `docs/screenshots/sc04-360-{lakemedel,bestallningsskapande,bestallningshistorik,audit,dashboard}.png` MUST show the new mobile logout icon in the top-right of the TopBar (the small red `<LogOut/>` icon at 44×44 px). Visually inspect with `Read` to confirm the new icon is present on at least one of the 5 (sampling — the others will follow by construction since TopBar is shared shell).

**Step 3.5 — Stage and commit the regenerated PNGs:**
Stage with `git add docs/screenshots/sc04-360-lakemedel.png docs/screenshots/sc04-360-bestallningsskapande.png docs/screenshots/sc04-360-bestallningshistorik.png docs/screenshots/sc04-360-audit.png docs/screenshots/sc04-360-dashboard.png` (skip login.png if unchanged — `git status` will reveal that). Commit per CONTEXT.md `<specifics>` line 306:
`chore(11-01): regenerate sc04 mobile screenshots after TopBar update` — message body should reference Phase 7 D-128 contract still holding, Phase 11 D-170 mobile logout icon now visible on 5 of 6 authenticated routes (login is shell-less). Use HEREDOC.
  </action>
  <verify>
    <automated>pnpm --filter @meditrack/web exec tsx apps/web/scripts/captureSc04Screenshots.ts</automated>
  </verify>
  <acceptance_criteria>
    - CLI: `pnpm --filter @meditrack/web exec tsx scripts/captureSc04Screenshots.ts` exits 0 (Phase 7 SC#4 invariant — scrollWidth <= innerWidth at 360/768/1024/1440 × 6 routes still holds)
    - Source: `apps/web/src/routes/shell/BottomTabBar.tsx` still contains `data-test="primary-nav"` at line ~27 (Phase 7 D-128 untouched by Phase 11)
    - Git: `git diff HEAD~2..HEAD -- apps/web/src/routes/shell/BottomTabBar.tsx apps/web/src/routes/shell/Sidebar.tsx` returns empty (Phase 11 did not touch nav surfaces — sc04 harness's `[data-test="primary-nav"]` assertion remains unaffected by construction)
    - Files: 5 PNGs updated — `docs/screenshots/sc04-360-{lakemedel,bestallningsskapande,bestallningshistorik,audit,dashboard}.png` (`git status` shows them as modified)
    - File: `docs/screenshots/sc04-360-login.png` either unchanged or only-pixel-different (login is shell-less; if changed it is an acceptable render variance)
    - Visual: at least 1 of the 5 regenerated PNGs shows the new red `<LogOut/>` icon button in the top-right of the TopBar at 360px (verify via Read tool on one PNG — e.g., `docs/screenshots/sc04-360-dashboard.png`)
    - Commit: HEAD commit message starts with `chore(11-01): regenerate sc04 mobile screenshots`
  </acceptance_criteria>
  <done>
    - sc04 Playwright harness exits 0 — mobile-first verification still passes for all 24 viewport×route cells.
    - 5 regenerated PNGs committed showing the new TopBar mobile logout icon.
    - Atomic commit landed: `chore(11-01): regenerate sc04 mobile screenshots after TopBar update`.
  </done>
</task>

</tasks>

<verification>

## Phase-Level Verification

Once all 3 tasks complete, run these gates to prove Phase 11 satisfies ROADMAP §"Phase 11" Success Criteria #1 and #2:

1. **UX-02 SC#1 verification (TopBar logout reachable at every breakpoint):**
   - `pnpm --filter @meditrack/web test -- TopBar.test.tsx` exits 0 — all 6 tests pass (mobile button present, desktop button present, both wire to useLogout, UserPill is non-interactive, logo Link unchanged).
   - Visual confirmation in regenerated `docs/screenshots/sc04-360-dashboard.png` (or any of the 5 authenticated route screenshots) — the red `<LogOut/>` icon button is visible in the top-right of the TopBar at 360px width.

2. **UX-03 SC#2 verification (Konto gate-note reads exactly):**
   - `pnpm --filter @meditrack/web test -- KontoPage.test.tsx` exits 0 — all 9 tests pass with the new verbatim string `Ändringar kan endast göras av administratör.` (with trailing period).
   - `grep -c "Denna åtgärd kräver adminrättigheter" apps/web/src/routes/konto/KontoPage.tsx apps/web/test/KontoPage.test.tsx` returns 0 for both files (old string fully purged).

3. **Phase-7 SC#4 regression check (Phase 11 does not regress mobile-first contract):**
   - `pnpm --filter @meditrack/web exec tsx apps/web/scripts/captureSc04Screenshots.ts` exits 0 (scrollWidth <= innerWidth holds at 4 viewports × 6 routes; `[data-test="primary-nav"]` still present on BottomTabBar at line 27).

4. **Full repo test suite (no regression):**
   - `pnpm --filter @meditrack/web test` exits 0 — every existing FE test still passes.
   - `pnpm --filter @meditrack/web typecheck` exits 0 — no TypeScript regression from the UserPillPopover → UserPill rename.

5. **Git narrative check:**
   - `git log --oneline -5` shows the 3 phase-11 commits in order: (a) `feat(11-01): split TopBar logout into per-breakpoint variants`, (b) `feat(11-01): update Konto gate-note copy`, (c) `chore(11-01): regenerate sc04 mobile screenshots`. Plus the closeout `docs(phase-11): complete phase execution` after the SUMMARY lands.

6. **Source-of-truth audit:**
   - `git grep "from '@/components/ui/popover'"` — if zero results, the shadcn popover wrapper has no remaining users. Per CONTEXT.md Claude's Discretion line 77, DO NOT remove in Phase 11 — flag in `deferred-items.md` as a v2 cleanup quick-task candidate. If non-zero results, no action (the dep is still in use elsewhere — confirmed by PATTERNS.md metadata line 532: `AtcCodeCombobox`, `TherapeuticClassCombobox`, `AuditFilterBar` are remaining importers).

</verification>

<success_criteria>

Phase 11 is complete when:

- [ ] **UX-02:** TopBar exposes Logga ut at every breakpoint — mobile icon-only (`md:hidden`, `h-11 w-11`, `aria-label="Logga ut"`, `text-[#DC2626]`) and desktop icon+label (`hidden md:flex`, `min-h-[44px]`, inline `Logga ut` text, `text-[#DC2626]`). Both wire to `useLogout()`. ROADMAP §"Phase 11" SC#1 satisfied.
- [ ] **UX-03:** KontoPage non-admin gate note reads exactly `Ändringar kan endast göras av administratör.` (with trailing period). Old string purged from source + tests. ROADMAP §"Phase 11" SC#2 satisfied.
- [ ] **D-171:** UserPill (renamed from UserPillPopover) renders as static `<div>` identity display — no popover, no click handler, no `role="button"`.
- [ ] **D-172:** Konto-page destructive `Logga ut` button at KontoPage.tsx:79-87 retained verbatim — no visual or behavioral change.
- [ ] **D-175:** File rename `UserPillPopover.tsx` → `UserPill.tsx` landed via `git mv` (preserved in git history as a rename, not a delete+add pair). Import at TopBar.tsx updated.
- [ ] All 6 new TopBar.test.tsx tests pass.
- [ ] All 9 KontoPage.test.tsx tests pass with the new string.
- [ ] No regression: BottomTabBar.test.tsx, Sidebar.test.tsx still pass.
- [ ] sc04 Playwright harness exits 0 — Phase 7 mobile-first invariant preserved.
- [ ] 5 sc04-360-*.png screenshots regenerated and committed.
- [ ] 3 atomic feat/chore commits + 1 closeout docs commit follow the project's conventional-commits convention.
- [ ] No new endpoints, no new permissions, no new schema, no new env vars, no new deps, no Docker Compose changes (lightweight bias per CLAUDE.md confirmed).
- [ ] Threat model `T-11-01` disposition stands: pure FE diff, no new attack surface, all three callers (mobile TopBar, desktop TopBar, retained Konto) share the existing CSRF-protected `useLogout()` hook.

</success_criteria>

<output>
Create `.planning/phases/11-quick-polish/11-01-SUMMARY.md` when all 3 tasks complete and verification gates pass. Use template at `@C:/Projekt/MediTrack/.claude/get-shit-done/templates/summary.md`. Include:
- Files modified (6 total: TopBar.tsx, UserPill.tsx renamed-from-UserPillPopover.tsx, KontoPage.tsx, KontoPage.test.tsx, TopBar.test.tsx, plus 5 sc04 PNGs)
- Commits landed (3 atomic — feat/feat/chore — per CONTEXT.md `<specifics>` ordering)
- Decisions implemented (D-170, D-171, D-172, D-173, D-174, D-175 — all 6 surface here)
- Tests added (6 new in TopBar.test.tsx) + tests updated (4 string sites in KontoPage.test.tsx)
- Any deferred follow-ups (e.g., `@/components/ui/popover` cleanup if zero remaining importers — but per PATTERNS.md line 532 there ARE 3 remaining importers, so the dep stays).

After SUMMARY.md is committed (`docs(phase-11): complete phase execution`), the orchestrator runs `/gsd:checker` or proceeds to milestone close per workflow.
</output>
