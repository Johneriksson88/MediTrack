---
phase: 08-compose-catalog-ux
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - packages/shared/src/contracts/medication.ts
  - apps/api/src/services/medication.service.ts
  - apps/api/src/routes/medications/search.ts
  - apps/api/test/medications.searchEmptyStates.integration.test.ts
  - apps/web/src/routes/lakemedel/MedicationSheet.tsx
  - apps/web/src/routes/lakemedel/__tests__/MedicationSheet.emptyStates.test.tsx
autonomous: true
requirements:
  - CAT-10

must_haves:
  truths:
    - "GET /api/medications/search?q=… returns 200 with both `results` (post-D-45-exclusion) AND `globalCatalogMatchCount` (pre-D-45 count)."
    - "MedicationSheet empty-state branch renders Variant A copy (`Inget i NPL matchade »{q}«.`) when globalCatalogMatchCount === 0."
    - "MedicationSheet empty-state branch renders Variant B copy (`Alla träffar finns redan i din vårdenhet.`) when globalCatalogMatchCount > 0 AND results.length === 0."
    - "The inline 'skapa ett nytt läkemedel' link (lowercase) inside both variants still opens the user-create form (setShowCreateForm + setShowResults)."
  artifacts:
    - path: packages/shared/src/contracts/medication.ts
      provides: "medicationSearchResponse extended with globalCatalogMatchCount: number"
      contains: "globalCatalogMatchCount"
    - path: apps/api/test/medications.searchEmptyStates.integration.test.ts
      provides: "Integration coverage for both variants A and B"
    - path: apps/web/src/routes/lakemedel/__tests__/MedicationSheet.emptyStates.test.tsx
      provides: "FE branching coverage of the two variants"
  key_links:
    - from: apps/api/src/services/medication.service.ts
      to: "Medication.findMany count query"
      via: "second aggregate call inside searchGlobalMedications (D-139)"
      pattern: "globalCatalogMatchCount"
    - from: apps/api/src/routes/medications/search.ts
      to: "extended medicationSearchResponse"
      via: "schema response 200 returns {results, globalCatalogMatchCount}"
      pattern: "globalCatalogMatchCount"
    - from: apps/web/src/routes/lakemedel/MedicationSheet.tsx
      to: "searchQuery.data?.globalCatalogMatchCount"
      via: "branch on === 0 vs > 0 inside the empty-state render"
      pattern: "globalCatalogMatchCount"
---

<objective>
Ship CAT-10 end-to-end: extend the global medication search response with a `globalCatalogMatchCount` field (the pre-D-45-exclusion NPL match count), and split the MedicationSheet typeahead empty state into two verbatim D-140 variants so users can tell apart "NPL truly has no match" from "every match is already stocked at this vårdenhet".

Purpose: Close the second Phase 8 pain point — today's single "Inget läkemedel matchade" message is opaque about whether the cause is no NPL match or D-45 exclusion. Differentiating empty states makes the catalog Add flow self-explanatory and gives the reviewer a tight, demonstrable UI improvement.
Output: One contract field, one service aggregate query, one route response schema bump, one FE empty-state rewrite (lines 1034-1048 of MedicationSheet.tsx), one BE integration test file covering both variants, one FE component test file asserting both variants render the locked copy verbatim.
</objective>

<execution_context>
@C:/Projekt/MediTrack/.claude/get-shit-done/workflows/execute-plan.md
@C:/Projekt/MediTrack/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-compose-catalog-ux/08-CONTEXT.md
@.planning/phases/08-compose-catalog-ux/08-UI-SPEC.md

<!-- Wave 1 just landed the file-coordinator-critical pieces; reference its summary so the executor knows what's already on disk -->
@.planning/phases/08-compose-catalog-ux/08-01-SUMMARY.md

<interfaces>
<!-- Key types and patterns the executor needs. Extracted from codebase. -->

From packages/shared/src/contracts/medication.ts (Wave 1 left this file with `atcCodesResponse` appended; Plan 02 extends a different schema):
- Existing: `medicationSearchResponse = z.object({ results: z.array(medicationSearchResult) })`.
- Phase 8 Plan 02 extends to: `medicationSearchResponse = z.object({ results: z.array(medicationSearchResult), globalCatalogMatchCount: z.number().int().nonnegative() })`.
- `medicationSearchResult` itself is NOT touched — the new field lives on the envelope, not on each row.

From apps/api/src/services/medication.service.ts (current `searchGlobalMedications` signature):
- `export async function searchGlobalMedications(careUnitId: string, filters: { q: string; limit: number }): Promise<MedicationSearchResult[]>` returns just the rows.
- Phase 8 changes the return type to `Promise<{ results: MedicationSearchResult[]; globalCatalogMatchCount: number }>` so the route handler can pass it through verbatim.
- The existing `prisma.medication.findMany` uses `OR: [{ name: { contains: q, mode: 'insensitive' } }, { atcCode: { startsWith: q, mode: 'insensitive' } }]` PLUS the D-45 exclusion `careUnitMedications: { none: { careUnitId, deletedAt: null } }`. The NEW aggregate query in this plan strips the D-45 exclusion — counts ONLY by `OR: [...name..., ...atcCode...]`.

From apps/api/src/routes/medications/search.ts:
- Current response schema: `200: z.object({ results: z.array(medicationSearchResult) })`. Replace with `200: medicationSearchResponse` (the extended envelope from shared).
- The route handler currently does `return { results: rows }`. New behavior: `const out = await searchGlobalMedications(...); return out;` — service returns the full envelope, route is a pass-through.

From apps/web/src/routes/lakemedel/MedicationSheet.tsx (the change site — lines 1034-1048 verbatim copied from existing source):
```
{!searchQuery.isLoading && results.length === 0 && (
  <div className="p-3 text-sm text-muted-foreground flex items-center gap-1 flex-wrap">
    Inget läkemedel matchade.
    <Button
      variant="link"
      className="ml-1 p-0 h-auto text-sm"
      onClick={() => {
        setShowCreateForm(true);
        setShowResults(false);
      }}
      type="button"
    >
      Skapa nytt läkemedel
    </Button>
  </div>
)}
```
Phase 8 Plan 02 REPLACES this block with the two-variant branch documented in 08-UI-SPEC.md §Components &amp; States §5 (verbatim copy strings in §Copywriting Contract §CAT-10).

The `searchQuery` variable is the result of the existing typeahead `useQuery` hook in this component (search the file for `useQuery` + `medication-search` to confirm). The new field reads as `searchQuery.data?.globalCatalogMatchCount`. The variable `debouncedQ` is already in scope for the heading interpolation `»{debouncedQ}«`.

From .planning/phases/08-compose-catalog-ux/08-UI-SPEC.md §Copywriting Contract (verbatim — DO NOT paraphrase):
- Variant A heading: `Inget i NPL matchade »{q}«.`
- Variant A sub-line: `Kontrollera stavning eller [skapa ett nytt läkemedel].`
- Variant B heading: `Alla träffar finns redan i din vårdenhet.`
- Variant B sub-line: `Justera sökningen eller [skapa ett nytt läkemedel].`
- Inline link label (lowercase): `skapa ett nytt läkemedel` (note the period sits OUTSIDE the Button so the underline does not extend across it)
- Guillemets `»` and `«` are Unicode U+00BB and U+00AB — paste them directly into the JSX template; do NOT use HTML entities.
- Truncation rule: if `debouncedQ.length > 40`, render `${debouncedQ.slice(0, 40)}…` in the heading so the heading wraps at most one extra line on a 360px viewport.

Fallback behavior (UI-SPEC §5 Branch contract): when `searchQuery.data?.globalCatalogMatchCount` is undefined (first-load race or BE deploy skew where the field has not arrived yet), fall through to Variant A. NEVER crash the empty-state on a missing field.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend search response with globalCatalogMatchCount (contract + service + route + integration test)</name>
  <files>packages/shared/src/contracts/medication.ts, apps/api/src/services/medication.service.ts, apps/api/src/routes/medications/search.ts, apps/api/test/medications.searchEmptyStates.integration.test.ts</files>
  <read_first>
    - packages/shared/src/contracts/medication.ts (extension target — find `medicationSearchResponse` and replace the schema body)
    - apps/api/src/services/medication.service.ts lines 274-311 (current `searchGlobalMedications` — Phase 8 changes the return type and adds the second query)
    - apps/api/src/routes/medications/search.ts (response schema swap + handler simplification)
    - apps/api/test/dashboard.integration.test.ts (test bootstrap pattern to mirror)
    - apps/api/test/helpers/buildTestApp.ts (helper signatures including loginAs and TEST_APOTEKARE)
    - .planning/phases/08-compose-catalog-ux/08-CONTEXT.md §D-139 (decision rationale — single additional aggregate query, scope NOT cached)
  </read_first>
  <action>
    Implement D-139 BE end-to-end.

    1. **Contract extension:** In `packages/shared/src/contracts/medication.ts`, find the existing `export const medicationSearchResponse = z.object({ results: z.array(medicationSearchResult) });` declaration and replace it with:
       ```
       export const medicationSearchResponse = z.object({
         results: z.array(medicationSearchResult),
         globalCatalogMatchCount: z.number().int().nonnegative(),
       });
       ```
       Add a brief inline comment above the schema citing D-139 and explaining: "Pre-D-45-exclusion count of NPL Medication rows matching `q`; lets the FE distinguish 'NPL has no hit' vs 'every hit is already stocked at this vårdenhet' empty states (CAT-10)."

    2. **Service extension:** In `apps/api/src/services/medication.service.ts`, change `searchGlobalMedications` from `Promise<MedicationSearchResult[]>` to `Promise<{ results: MedicationSearchResult[]; globalCatalogMatchCount: number }>`. Implement per D-139:
       - Keep the existing `prisma.medication.findMany({ where: { OR: [...], careUnitMedications: { none: {...} } }, take: limit, orderBy, select })` query verbatim — this is still the post-D-45-exclusion result set.
       - Add a parallel `prisma.medication.count({ where: { OR: [{ name: { contains: q, mode: 'insensitive' } }, { atcCode: { startsWith: q, mode: 'insensitive' } }] } })` — note the `careUnitMedications` exclusion clause is DELIBERATELY OMITTED here so the count reflects the global NPL match count BEFORE D-45 strips already-stocked rows.
       - Wrap both in `Promise.all([...])` so the two queries run in parallel (the standard-path precedent at line 227 in this same file).
       - Return `{ results: results.map(...), globalCatalogMatchCount }`.
       - Update the JSDoc on the function to cite D-139 and document the deliberate asymmetry (count includes already-stocked, results exclude them).

    3. **Route handler:** In `apps/api/src/routes/medications/search.ts`, import `medicationSearchResponse` from `@meditrack/shared` (if not already imported). Change the route's response schema from `200: z.object({ results: z.array(medicationSearchResult) })` to `200: medicationSearchResponse`. Change the handler body from `return { results: rows };` to `return await searchGlobalMedications(req.user!.careUnitId, { q: req.query.q, limit: req.query.limit });` — the service now returns the envelope directly.

    4. **Integration test:** Create `apps/api/test/medications.searchEmptyStates.integration.test.ts` mirroring the bootstrap of `apps/api/test/dashboard.integration.test.ts` (buildTestApp + ensureAllRolesSeeded + loginAs as TEST_APOTEKARE). Three scenarios:
       - **Test A (Variant A — no NPL match):** Query with a guaranteed-no-match string like `q=qqqzzzimpossible123`. Assert response: `results.length === 0` AND `globalCatalogMatchCount === 0`.
       - **Test B (Variant B — D-45 excludes everything):** Pre-seed: pick a small set of identifiable medications already present at TEST_APOTEKARE's careUnit (use the existing helper `findTestCareUnitMedication` or query `careUnitMedication` join to find one). Use a search `q` that targets that medication's name (e.g. its first 4 letters) AND assert no NPL rows OTHER THAN the stocked one match. Then GET /api/medications/search?q=<chosen-prefix>. Assert: `results.length === 0` (the only match was D-45-excluded) AND `globalCatalogMatchCount === 1` (or whatever the stocked-row count is — assert `>= 1` for robustness).
       - **Test C (mixed — both fields present and ≥ 1 result):** Query with a common prefix that should return multiple unstocked rows like `q=para` (paracetamol et al.). Assert: `results.length > 0` AND `globalCatalogMatchCount >= results.length` (the count must be ≥ the post-exclusion result set since exclusion can only reduce).
       - Each test parses the body through `medicationSearchResponse.parse(...)` to enforce the contract shape end-to-end (the test fails if BE drops the field).
  </action>
  <verify>
    <automated>cd C:/Projekt/MediTrack &amp;&amp; pnpm --filter @meditrack/shared exec tsc --noEmit &amp;&amp; pnpm --filter @meditrack/api exec vitest run apps/api/test/medications.searchEmptyStates.integration.test.ts</automated>
  </verify>
  <done>
    - `grep -F "globalCatalogMatchCount" packages/shared/src/contracts/medication.ts` returns &gt;= 1 match.
    - `grep -F "globalCatalogMatchCount" apps/api/src/services/medication.service.ts apps/api/src/routes/medications/search.ts` returns &gt;= 1 match each.
    - 3/3 integration tests pass.
    - Existing tests touching `/api/medications/search` (look for them: `pnpm --filter @meditrack/api exec vitest run -t "search"` from apps/api/test/) still pass after the schema bump — if any test parses the response as `{results: [...]}` without the new field, update it to also expect `globalCatalogMatchCount`.
  </done>
</task>

<task type="auto">
  <name>Task 2: MedicationSheet empty-state branching with verbatim D-140 copy + lowercase inline link + FE test</name>
  <files>apps/web/src/routes/lakemedel/MedicationSheet.tsx, apps/web/src/routes/lakemedel/__tests__/MedicationSheet.emptyStates.test.tsx</files>
  <read_first>
    - apps/web/src/routes/lakemedel/MedicationSheet.tsx lines 1020-1065 (the empty-state branch IS the change site; surrounding `searchQuery`, `debouncedQ`, `setShowCreateForm`, `setShowResults` identifiers are referenced)
    - .planning/phases/08-compose-catalog-ux/08-UI-SPEC.md §Components &amp; States §5 (verbatim "After" JSX block to mirror)
    - .planning/phases/08-compose-catalog-ux/08-UI-SPEC.md §Copywriting Contract §CAT-10 (verbatim Swedish strings)
    - .planning/phases/08-compose-catalog-ux/08-CONTEXT.md §D-140 (variant rules + truncation guard)
    - apps/web/src/routes/dashboard/__tests__/DashboardLowStockCard.test.tsx (QueryClient harness pattern for component test)
    - apps/web/src/components/__tests__/AtcCodeCombobox.test.tsx (Plan 01 — pattern for mocking fetch + asserting copy strings)
  </read_first>
  <action>
    Implement D-140 FE end-to-end.

    1. **Replace the empty-state block in MedicationSheet.tsx** (existing lines 1034-1048 — verify the line range with a quick `grep -n "Inget läkemedel matchade"` before editing). The replacement JSX must match UI-SPEC §Components &amp; States §5 "After" verbatim:
       ```
       {!searchQuery.isLoading && results.length === 0 && (
         <div className="p-3 flex flex-col gap-1">
           {searchQuery.data?.globalCatalogMatchCount === 0 ? (
             <>
               <p className="text-sm font-semibold text-foreground">
                 Inget i NPL matchade »{debouncedQ.length > 40 ? `${debouncedQ.slice(0, 40)}…` : debouncedQ}«.
               </p>
               <p className="text-xs text-muted-foreground flex items-center gap-1 flex-wrap">
                 Kontrollera stavning eller
                 <Button
                   variant="link"
                   className="p-0 h-auto text-xs"
                   onClick={() => {
                     setShowCreateForm(true);
                     setShowResults(false);
                   }}
                   type="button"
                 >
                   skapa ett nytt läkemedel
                 </Button>
                 .
               </p>
             </>
           ) : (
             <>
               <p className="text-sm font-semibold text-foreground">
                 Alla träffar finns redan i din vårdenhet.
               </p>
               <p className="text-xs text-muted-foreground flex items-center gap-1 flex-wrap">
                 Justera sökningen eller
                 <Button
                   variant="link"
                   className="p-0 h-auto text-xs"
                   onClick={() => {
                     setShowCreateForm(true);
                     setShowResults(false);
                   }}
                   type="button"
                 >
                   skapa ett nytt läkemedel
                 </Button>
                 .
               </p>
             </>
           )}
         </div>
       )}
       ```
       Notes:
       - `globalCatalogMatchCount === 0` is the Variant A predicate. The `else` arm is Variant B (covers `globalCatalogMatchCount > 0` AND the safer-default `undefined` fallback per UI-SPEC §5 Branch contract). DO NOT replace the strict equality with `?? 0` — undefined must fall to Variant B's "Alla träffar..." copy per the locked contract.
       - Both inline link buttons use lowercase `skapa ett nytt läkemedel` (D-140 + UI-SPEC Notes: "lowercase first letter because the button appears inline within a sentence"). The original sentence-cased `Skapa nytt läkemedel` link is DELETED — it does not survive Phase 8.
       - The period `.` after each link sits OUTSIDE the `<Button>` so the underline does not extend across it (UI-SPEC §Copywriting Notes).
       - Guillemets `»` and `«` are pasted as Unicode (U+00BB / U+00AB), not as HTML entities.
       - The 40-char truncation guard inside the Variant A heading is the UI-SPEC §Copywriting Notes "Truncation rule".
       - Existing `setShowCreateForm` and `setShowResults` setters are already in scope (they're the same setters the original link called).

    2. **CAT-08 verification (negative test — no scope creep):** Confirm `grep -n "Skapa nytt" apps/web/src/routes/lakemedel/MedicationSheet.tsx` returns ZERO matches after the edit (the sentence-cased variant is gone). The only "skapa" references should be the two lowercase inline links inside the new empty-state branch. This guards against any executor accidentally promoting the link to an always-visible CTA (CAT-08 was dropped per scope_fence).

    3. **Component test:** Create `apps/web/src/routes/lakemedel/__tests__/MedicationSheet.emptyStates.test.tsx`. Test harness mirrors `apps/web/src/routes/dashboard/__tests__/DashboardLowStockCard.test.tsx` (QueryClientProvider wrapper + `vi.spyOn(global, 'fetch')` to stub responses). Set up the component in create-mode with the typeahead exposed (the user-create form is reached by opening the Sheet in add mode — read the existing component to find the prop or store flag that toggles create mode). Tests:
       - **Variant A — NPL no match:** Mock `/api/medications/search` to resolve `{ results: [], globalCatalogMatchCount: 0 }`. Type `qqqzzz` into the search input (use `userEvent.type`). Wait for the debounce. Assert: `screen.getByText(/Inget i NPL matchade »qqqzzz«\./)` is visible AND `screen.getByRole('button', { name: /skapa ett nytt läkemedel/ })` is visible.
       - **Variant A — truncation:** Same mock; type a 50-char string. Assert the rendered heading contains the first 40 chars + `…` and NOT the full 50.
       - **Variant B — D-45 exclusion:** Mock `/api/medications/search` to resolve `{ results: [], globalCatalogMatchCount: 3 }`. Type any prefix. Assert: `screen.getByText('Alla träffar finns redan i din vårdenhet.')` is visible.
       - **Variant B — undefined fallback:** Mock `/api/medications/search` to resolve `{ results: [] }` (NO globalCatalogMatchCount field — simulate the deploy-skew safe-default case). Assert: `screen.getByText('Alla träffar finns redan i din vårdenhet.')` is visible (the `=== 0` predicate is strict so undefined falls into the else arm). This test locks the UI-SPEC §5 Branch contract fallback rule.
       - **Link click opens create form:** Variant A mock. Type query, click the `skapa ett nytt läkemedel` link, assert `setShowCreateForm` effect (test that the create-form region is now rendered — query for a stable element from the user-create form like the `<Label>ATC-kod</Label>` Plan 01 wired into the form).
       - **Sentence-case guard:** Render either variant; assert `screen.queryByText('Skapa nytt läkemedel')` returns `null` (the original sentence-cased copy is gone — CAT-08 deferral guarded).
  </action>
  <verify>
    <automated>cd C:/Projekt/MediTrack &amp;&amp; pnpm --filter @meditrack/web exec tsc --noEmit &amp;&amp; pnpm --filter @meditrack/web exec vitest run src/routes/lakemedel/__tests__/MedicationSheet.emptyStates.test.tsx</automated>
  </verify>
  <done>
    - `grep -F "Skapa nytt läkemedel" apps/web/src/routes/lakemedel/MedicationSheet.tsx` returns ZERO matches (sentence-case form deleted).
    - `grep -F "skapa ett nytt läkemedel" apps/web/src/routes/lakemedel/MedicationSheet.tsx` returns exactly 2 matches (one per variant).
    - `grep -F "globalCatalogMatchCount === 0" apps/web/src/routes/lakemedel/MedicationSheet.tsx` returns &gt;= 1 match (strict-equality variant branch).
    - `grep -F "Inget i NPL matchade" apps/web/src/routes/lakemedel/MedicationSheet.tsx` returns 1 match.
    - `grep -F "Alla träffar finns redan i din vårdenhet" apps/web/src/routes/lakemedel/MedicationSheet.tsx` returns 1 match.
    - All component tests pass (6 assertions).
    - `pnpm --filter @meditrack/web exec tsc --noEmit` exits 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → GET /api/medications/search | Existing boundary; Phase 8 widens the response shape only. The new field is computed, not user-controlled. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | Information Disclosure | `globalCatalogMatchCount` field on /api/medications/search response | accept | The total count of NPL catalog matches for a given query is already implicitly exposed via the existing typeahead (a paginated `results` array of the same matches). Surfacing the pre-exclusion total is not sensitive; the NPL catalog itself is a public Swedish drug registry. No personal/clinical data crosses; only a non-negative integer. Scope assertion already enforced by the `requirePermission('medication:read')` preHandler (all three roles can read). |
| T-08-DoS-search | Denial of Service | Doubled query cost on /api/medications/search (count + findMany) | accept | The existing `medicationSearchQuery.q.min(1)` schema (CR-03 from Phase 2 gap closure) + the existing `pg_trgm` GIN index `Medication_name_trgm_idx` (Phase 2 CR-02 follow-up migration) keep both queries bitmap-scan-fast. Doubling a millisecond-scale query is acceptable; no rate-limit change needed. Per-IP rate limits already in place from Phase 5 Plan 09 cover abuse. |
| T-08-SC | Tampering | Package installs | mitigate | No new packages installed. All deps (zod, prisma, vitest, react, @tanstack/react-query) already present and audited in prior phases. |
</threat_model>

<verification>
After both tasks complete:
1. `pnpm verify` exits 0.
2. `pnpm --filter @meditrack/api exec vitest run apps/api/test/medications.searchEmptyStates.integration.test.ts` exits 0 (3/3 tests pass).
3. `pnpm --filter @meditrack/web exec vitest run src/routes/lakemedel/__tests__/MedicationSheet.emptyStates.test.tsx` exits 0 (6/6 tests pass).
4. Pre-existing tests that touch `/api/medications/search` continue to pass (run `pnpm --filter @meditrack/api exec vitest run -t "search"` to confirm).
5. Manual smoke (optional, in dev): `docker compose up`, navigate to `/lakemedel` → Lägg till läkemedel. Type `qqqqqqq` → heading `Inget i NPL matchade »qqqqqqq«.` appears. Type a name of a medication already stocked at the seed vårdenhet (e.g. `paracetamol` if seeded as a CareUnitMedication) → heading `Alla träffar finns redan i din vårdenhet.` appears.
</verification>

<success_criteria>
- Roadmap Phase 8 SC #2 (CAT-10) satisfied: empty-state copy differentiates "no NPL match" vs "D-45 exclusion".
- `medicationSearchResponse` carries `globalCatalogMatchCount: number` (zod-parsed at the route boundary).
- BE service runs the second aggregate query in parallel with the existing findMany; cost stays in milliseconds (T-08-DoS-search disposition).
- FE empty-state renders the verbatim D-140 copy for both variants; the inline link is lowercased; the period sits outside the Button.
- Plan does NOT touch LakemedelFilter, MedicationPickerSheet, or any picker-suggestions surfaces (those are Plan 01 / Plan 03 territory).
- Plan does NOT add an always-visible "Skapa nytt" CTA (CAT-08 deferred per scope_fence — guarded by the negative-grep `Skapa nytt läkemedel` assertion).
</success_criteria>

<output>
Create `.planning/phases/08-compose-catalog-ux/08-02-SUMMARY.md` when done, summarising:
- Files modified (5 paths: 3 source, 2 tests)
- Tests added (BE integration + FE component, 9 total assertions)
- Verification outcomes (pnpm verify + targeted vitest runs)
- Confirmation that CAT-08 negative-grep guard passes (sentence-cased "Skapa nytt läkemedel" string is no longer in MedicationSheet.tsx)
- Any deviations from the plan
</output>
</content>
</invoke>