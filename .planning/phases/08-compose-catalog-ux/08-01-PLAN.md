---
phase: 08-compose-catalog-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/services/medication.service.ts
  - apps/api/src/routes/medications/atcCodes.ts
  - apps/api/src/routes/medications/index.ts
  - apps/api/test/medications.atcCodes.integration.test.ts
  - packages/shared/src/contracts/medication.ts
  - packages/shared/src/index.ts
  - apps/web/src/components/AtcCodeCombobox.tsx
  - apps/web/src/features/medications/useAtcCodesQuery.ts
  - apps/web/src/features/medications/useMedicationMutations.ts
  - apps/web/src/routes/lakemedel/MedicationSheet.tsx
  - apps/web/src/routes/lakemedel/LakemedelFilter.tsx
  - apps/web/src/routes/lakemedel/LakemedelPage.tsx
  - apps/web/src/components/__tests__/AtcCodeCombobox.test.tsx
autonomous: true
requirements:
  - CAT-09

must_haves:
  truths:
    - "GET /api/medications/atc-codes returns 200 + { codes: string[] } sorted ascending; 401 without a session."
    - "MedicationSheet user-create form renders the AtcCodeCombobox (no free-text Input) and accepts both list picks and free-text fallback values (uppercased)."
    - "LakemedelFilter ATC control is the shared AtcCodeCombobox; URL still updates ?atc=<code> on pick and clears on the X button."
    - "Creating a new medication invalidates the ['atc-codes'] cache so the next combobox open includes the new code."
  artifacts:
    - path: apps/web/src/components/AtcCodeCombobox.tsx
      provides: "Shared Popover+Command typeahead for global ATC codes; free-text fallback row."
      min_lines: 90
    - path: apps/web/src/features/medications/useAtcCodesQuery.ts
      provides: "TanStack hook with staleTime: Infinity for ['atc-codes']."
      min_lines: 10
    - path: apps/api/src/routes/medications/atcCodes.ts
      provides: "GET /api/medications/atc-codes route registrar."
      exports: ["atcCodesRoute"]
    - path: apps/api/test/medications.atcCodes.integration.test.ts
      provides: "Integration coverage for shape, auth, and ascending sort."
  key_links:
    - from: apps/api/src/routes/medications/index.ts
      to: apps/api/src/routes/medications/atcCodes.ts
      via: "app.register(atcCodesRoute) in the medication barrel."
      pattern: "atcCodesRoute"
    - from: apps/web/src/features/medications/useMedicationMutations.ts
      to: queryKey ['atc-codes']
      via: "useCreateMedication.onSuccess sibling invalidation"
      pattern: "queryKey: \\['atc-codes'\\]"
    - from: apps/web/src/routes/lakemedel/LakemedelFilter.tsx
      to: apps/web/src/components/AtcCodeCombobox.tsx
      via: "Replace inline Popover+Command at lines 175-244 with shared component."
      pattern: "AtcCodeCombobox"
    - from: apps/web/src/routes/lakemedel/MedicationSheet.tsx
      to: apps/web/src/components/AtcCodeCombobox.tsx
      via: "Controller-wrapped combobox replaces the user-create ATC Input at lines 1156-1170."
      pattern: "AtcCodeCombobox"
---

<objective>
Ship CAT-09 end-to-end: replace free-text ATC input on the catalog Add-medication user-create form with a typeahead combobox sourced from the global NPL catalog, AND wire the same shared component into LakemedelFilter so both surfaces have one canonical implementation (D-134 anti-duplication). Read endpoint + shared contract + TanStack hook + two consumer rewires + integration + component tests.

Purpose: Deliver the first Phase 8 vertical slice (BE → contract → FE component → two consumers → tests) so the slice is demoable at commit time. Establishes the AtcCodeCombobox pattern that downstream CAT-10 / ORD-08 work depends on for combobox sizing/copy/aria conventions, even though those slices do not import the component.
Output: One new BE endpoint, one shared contract addition, one new shared component, one new TanStack hook, two route edits (Sheet + Filter), one mutation invalidation line, one integration test file, one component test file.
</objective>

<execution_context>
@C:/Projekt/MediTrack/.claude/get-shit-done/workflows/execute-plan.md
@C:/Projekt/MediTrack/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-compose-catalog-ux/08-CONTEXT.md
@.planning/phases/08-compose-catalog-ux/08-UI-SPEC.md

<!-- Phase 6 SUMMARYs establish the dashboard-service / shared-combobox / per-endpoint-cache-key patterns this plan reuses -->
@.planning/phases/06-ai-categorization-low-stock-notifications/06-01-SUMMARY.md
@.planning/phases/06-ai-categorization-low-stock-notifications/06-02-SUMMARY.md

<interfaces>
<!-- Key types and patterns the executor needs. Extracted from codebase. -->

From apps/web/src/components/TherapeuticClassCombobox.tsx (canonical Popover+Command pattern to mirror — D-134):
- Component owns its own `open` state internally; consumer owns only `value` / `onChange`.
- Clear affordance is a span (NOT a nested button — invalid HTML); `role="button"`, `tabIndex={0}`, `stopPropagation` on click.
- PopoverContent uses `w-[--radix-popover-trigger-width] min-w-[220px] p-0` — Phase 8 uses `min-w-[240px]` instead (UI-SPEC).
- CommandItem.value is the searchable string fed to cmdk.
- Difference from TherapeuticClassCombobox: ATC needs a FREE-TEXT FALLBACK row (rendered as an extra CommandItem when the typed query is non-empty and not in the served list) — see existing inline implementation in LakemedelFilter.tsx lines 210-222.

From packages/shared/src/contracts/medication.ts:
- Existing `medicationSearchResult`, `medicationSearchQuery`, `medicationSearchResponse`, `medicationListItem`, `medicationCreateUserRequest`, `medicationUpdateRequest` — all preserved unchanged in this plan (CAT-10's `globalCatalogMatchCount` is Plan 02).
- Phase 8 adds: `atcCodesResponse = z.object({ codes: z.array(z.string()) })` and `export type AtcCodesResponse = z.infer<typeof atcCodesResponse>`.

From apps/api/src/routes/medications/search.ts (existing route registrar pattern to copy for atcCodes.ts):
- `r.get('/api/medications/...', { preHandler: [requireSession, requirePermission(...)], schema: { ... } }, handler)`.
- For Phase 8 atc-codes: preHandler is `[requireSession]` ONLY — all roles can read the ATC list, no permission gate beyond auth (T-08-01).

From apps/api/src/routes/medications/index.ts (existing barrel — extend with one register call):
- Already calls `app.register(listMedicationsRoute)`, `searchMedicationsRoute`, `createMedicationRoute`, `updateMedicationRoute`, `deleteMedicationRoute`.
- Add: `await app.register(atcCodesRoute);` BEFORE the `:id` routes to avoid `atc-codes` being parsed as an :id param (D-65 precedent from pickerOptions.ts ordering).

From apps/web/src/features/medications/useMedicationMutations.ts:
- `useCreateMedication.onSuccess` already invalidates `['medications']`, `['medication-search']`, `['dashboard', 'low-stock']`. Phase 8 adds ONE line: `void queryClient.invalidateQueries({ queryKey: ['atc-codes'] });` (D-133).
- Do NOT add invalidation to useUpdateMedication / useDeleteMedication / useUpdateThresholdOptimistic — only create can introduce a new ATC code value to the global table.

From apps/web/src/routes/lakemedel/LakemedelFilter.tsx (rewire target — lines 122-129 + 175-244):
- DELETE: `localAtc` useState (line 124), `atcOpen` useState (line 123), `useEffect([atc])` sync (lines 127-129), `filteredSuggestions` derived array (lines 140-148), the entire inline Popover+Command block (lines 175-244 inclusive of the clear `×` Button).
- KEEP: the surrounding `<div className="flex flex-wrap items-center gap-2 py-3">`, the TherapeuticClassCombobox above the ATC slot, the Form Select after, the below-threshold Button.
- The `atcSuggestions?: string[]` prop (lines 69-72) is no longer read — drop from interface AND from LakemedelPage.tsx call site.
- onChange contract preserved: `onChange({ atc: <code>, page: 1 })` on pick, `onChange({ atc: '', page: 1 })` on clear.

From apps/web/src/routes/lakemedel/MedicationSheet.tsx (rewire target — lines 1156-1170):
- The user-create form uses `react-hook-form` (userForm). The existing ATC field is `<Input id="user-atc" {...userForm.register('atcCode')} disabled={isPending} className="mt-1" />`.
- Replace with `<Controller>` wrapping AtcCodeCombobox. The `react-hook-form` `Controller` is already imported elsewhere in this file (verify with grep before adding the import).
- Keep the `<Label htmlFor="user-atc">ATC-kod</Label>` and the `userForm.formState.errors.atcCode` error block below the field unchanged.
- The existing in-empty-state `<Button variant="link">Skapa nytt läkemedel</Button>` at lines 1037-1047 is NOT touched in this plan — Plan 02 (CAT-10) edits the entire empty-state branch including this link.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shared contract + BE endpoint + integration test for GET /api/medications/atc-codes</name>
  <files>packages/shared/src/contracts/medication.ts, packages/shared/src/index.ts, apps/api/src/services/medication.service.ts, apps/api/src/routes/medications/atcCodes.ts, apps/api/src/routes/medications/index.ts, apps/api/test/medications.atcCodes.integration.test.ts</files>
  <read_first>
    - packages/shared/src/contracts/medication.ts (existing schemas; add atcCodesResponse alongside medicationSearchResponse)
    - packages/shared/src/index.ts (existing exports; add the new schema + type re-export)
    - apps/api/src/services/medication.service.ts (existing `searchGlobalMedications` is the closest sibling; add the new `listGlobalAtcCodes(): Promise<string[]>` next to it)
    - apps/api/src/routes/medications/search.ts (verbatim registrar pattern to mirror)
    - apps/api/src/routes/medications/index.ts (barrel — extend with atcCodesRoute register call)
    - apps/api/test/dashboard.integration.test.ts (testing harness pattern — buildTestApp + loginAs)
    - apps/api/test/helpers/buildTestApp.ts (helper signatures referenced in the new test)
  </read_first>
  <action>
    Implement D-132 + D-134 backend foundation.

    1. **Shared contract:** In `packages/shared/src/contracts/medication.ts`, append a new Zod schema `atcCodesResponse = z.object({ codes: z.array(z.string()) })` immediately after `medicationSearchResponse`. Export `type AtcCodesResponse = z.infer<typeof atcCodesResponse>`. Add a brief comment block citing D-132 and pointing at the route+service. In `packages/shared/src/index.ts`, re-export `atcCodesResponse` and `AtcCodesResponse` from the `./contracts/medication.js` barrel block (insert immediately after `medicationSearchResponse` / `MedicationSearchResponse` re-export).

    2. **Service function:** In `apps/api/src/services/medication.service.ts`, add a new exported function `listGlobalAtcCodes(): Promise<string[]>` placed immediately after `searchGlobalMedications`. Implementation per D-132 exact SQL skeleton: `SELECT DISTINCT "atcCode" FROM "Medication" WHERE "atcCode" IS NOT NULL ORDER BY "atcCode" ASC`. Use `prisma.$queryRaw<Array<{ atcCode: string }>>` template-literal (no string concat, no params needed). Map to `rows.map(r => r.atcCode)`. Header docblock: cite D-132 (distinct-from-global-NPL), D-16 carve-out (no careUnitId — the global ATC list is shared across all vårdenheter; same posture as `searchGlobalMedications`), and T-08-01 disposition (mitigate via requireSession; the codes are already implicitly disclosed via /api/medications/search).

    3. **Route file:** Create `apps/api/src/routes/medications/atcCodes.ts` exporting `atcCodesRoute(app: FastifyInstance)`. Mirror the structure of `apps/api/src/routes/dashboard/lowStock.ts` (requireSession-only preHandler — T-08-01 mitigation). Path: `GET /api/medications/atc-codes`. Response schema: `{ 200: atcCodesResponse }`. Handler: `return { codes: await listGlobalAtcCodes() }`. Header docblock cites D-132 and T-08-01 disposition.

    4. **Register the route:** In `apps/api/src/routes/medications/index.ts`, import `atcCodesRoute` from `./atcCodes.js` and register it BEFORE any `:id` route via `await app.register(atcCodesRoute);`. (The medication barrel today only has search + list + create + update + delete; `update` and `delete` use `:careUnitMedicationId`. Place `atcCodesRoute` after `searchMedicationsRoute`.)

    5. **Integration test:** Create `apps/api/test/medications.atcCodes.integration.test.ts` mirroring `apps/api/test/dashboard.integration.test.ts` bootstrap (buildTestApp + ensureAllRolesSeeded + loginAs). Three assertions:
       - **Test A (shape + sort):** Login as TEST_SJUKSKOTERSKA (proves no `requirePermission` gate). GET /api/medications/atc-codes returns 200; body Zod-parses through `atcCodesResponse`; `codes.length > 100` (the NPL seed has ~3000 unique codes — assert a generous floor); the array is sorted ascending (`codes.every((c, i) => i === 0 || codes[i-1] <= c)`).
       - **Test B (unauthenticated → 401):** GET without a session cookie returns 401 with envelope code `unauthenticated` (T-08-01 enforcement).
       - **Test C (distinct):** assert `new Set(codes).size === codes.length` — no duplicates emitted (proves the `DISTINCT` clause is the source of truth).
  </action>
  <verify>
    <automated>cd C:/Projekt/MediTrack &amp;&amp; pnpm --filter @meditrack/shared exec tsc --noEmit &amp;&amp; pnpm --filter @meditrack/api exec vitest run apps/api/test/medications.atcCodes.integration.test.ts</automated>
  </verify>
  <done>
    - `atcCodesResponse` exported from `@meditrack/shared` and parses `{codes: string[]}` cleanly.
    - `GET /api/medications/atc-codes` returns 200 + ascending distinct codes when authenticated, 401 otherwise.
    - All three integration tests pass in &lt; 30s.
    - `grep -n "atcCodesRoute" apps/api/src/routes/medications/index.ts` returns the register line.
  </done>
</task>

<task type="auto">
  <name>Task 2: Shared AtcCodeCombobox component + TanStack hook + component test</name>
  <files>apps/web/src/components/AtcCodeCombobox.tsx, apps/web/src/features/medications/useAtcCodesQuery.ts, apps/web/src/components/__tests__/AtcCodeCombobox.test.tsx</files>
  <read_first>
    - apps/web/src/components/TherapeuticClassCombobox.tsx (canonical structure to mirror per D-134 — IDENTICAL Popover+Command+clear-span shell)
    - apps/web/src/routes/lakemedel/LakemedelFilter.tsx lines 175-244 (existing free-text fallback row implementation — port the `__free__${query}` + `(fri sökning)` pattern into the new component)
    - apps/web/src/features/dashboard/useLowStockQuery.ts (TanStack hook pattern — named query options export precedent if useful for testing)
    - .planning/phases/08-compose-catalog-ux/08-UI-SPEC.md §Components &amp; States §1 (locked Props interface for AtcCodeComboboxProps — value/onChange/placeholder/searchPlaceholder/ariaLabel/triggerClassName/clearable/disabled)
    - .planning/phases/08-compose-catalog-ux/08-UI-SPEC.md §Copywriting Contract §AtcCodeCombobox (locked Swedish copy table)
  </read_first>
  <action>
    Implement D-132 + D-133 + D-134 frontend foundation.

    1. **Hook:** Create `apps/web/src/features/medications/useAtcCodesQuery.ts` exporting `useAtcCodesQuery()` and a named const `ATC_CODES_QUERY_OPTIONS` (the pattern from Phase 6 `LOW_STOCK_QUERY_OPTIONS` so tests can assert the D-133 cache contract WITHOUT mounting a QueryClient — STATE.md decision precedent). Hook returns `useQuery<AtcCodesResponse>` against `/api/medications/atc-codes` with `queryKey: ['atc-codes']`, `staleTime: Infinity`, `refetchOnWindowFocus: false`, `refetchOnMount: false` (per D-133 + UI-SPEC §Data Caching). Use the existing `fetchJson` helper from `@/lib/api`.

    2. **Component:** Create `apps/web/src/components/AtcCodeCombobox.tsx` with the EXACT props interface locked in 08-UI-SPEC.md §Components &amp; States §1: `value: string`, `onChange: (next: string) => void`, optional `placeholder` (default `'ATC-kod ▾'`), optional `searchPlaceholder` (default `'Sök ATC-kod…'`), optional `ariaLabel` (default `'Välj ATC-kod'`), optional `triggerClassName`, optional `clearable` (default `true`), optional `disabled` (default `false`). MIRROR the TherapeuticClassCombobox shell structure verbatim (Popover + PopoverTrigger + PopoverContent + Command + CommandInput + CommandList + CommandGroup) with these specific differences per UI-SPEC:
       - Internal `query` state for the CommandInput value (cmdk does the live filter; we still need it to drive the free-text-fallback row's visibility).
       - Internal `open` state owned by component (consumer never sees it).
       - PopoverContent class: `w-[--radix-popover-trigger-width] min-w-[240px] p-0` (240, not 220 — per UI-SPEC §Spacing).
       - Loading branch (per UI-SPEC §Components &amp; States §1 States table): when `codesQuery.isLoading`, render `<div className="p-3 text-sm text-muted-foreground flex items-center gap-2"><Loader2 className="h-4 w-4 animate-spin" aria-hidden="true" />Laddar ATC-koder…</div>`. This case is rare in practice (staleTime Infinity) but must exist for the first-ever open.
       - Loaded branch: `<CommandEmpty>Inga träffar</CommandEmpty>` + a `CommandGroup` rendering each code as `<CommandItem key={code} value={code} onSelect={() => { onChange(code); setOpen(false); }}>` with a `<Check>` icon (opacity 1 if `value === code`, else opacity 0) and `<span className="text-xs font-semibold tabular-nums">{code}</span>`.
       - **Free-text fallback row:** after the main `.map(...)`, render an EXTRA `<CommandItem>` ONLY IF `query.trim()` is non-empty AND `query.trim().toUpperCase()` is not present in the served `codes` array. Key: `__free__${query}`. Value: `__free__${query}`. onSelect: `() => { onChange(query.trim().toUpperCase()); setOpen(false); }`. Visual: same `<Check className="...opacity-0">` then `<span className="text-xs font-semibold tabular-nums">{query.trim().toUpperCase()}</span><span className="text-xs text-muted-foreground ml-2">(fri sökning)</span>`. Mirrors LakemedelFilter.tsx lines 211-222 exactly.
       - Clear affordance: identical to TherapeuticClassCombobox span pattern but `aria-label="Rensa ATC-kod"`, and `onChange('')` (empty string) on activate — per UI-SPEC §Components.
       - `Button` props: `variant="outline"`, `role="combobox"`, `aria-expanded={open}`, `aria-label={ariaLabel}`, `className={cn('justify-between', triggerClassName)}`, `type="button"`, `disabled={disabled}`.
       - Header docblock cites D-132 / D-133 / D-134, references TherapeuticClassCombobox precedent, and explicitly notes the free-text fallback is the ONE functional difference from the therapeutic-class sibling.

    3. **Component test:** Create `apps/web/src/components/__tests__/AtcCodeCombobox.test.tsx` using `@testing-library/react` + `@testing-library/user-event` (the patterns already used in `apps/web/src/routes/dashboard/__tests__/DashboardLowStockCard.test.tsx` — read that file for the QueryClient bootstrap idiom). Tests:
       - **Render idle + click opens dropdown:** value=''; assert placeholder text is visible; click trigger; assert PopoverContent shows the loading Laddar… text OR the loaded code list (use a mocked fetch response with 3 codes like `['A01AA01','N02BE01','R03AC02']`).
       - **Pick a list code:** click `N02BE01` row; assert `onChange` called with `'N02BE01'`; popover closes.
       - **Free-text fallback appears for unknown query:** type `XYZ`; assert `(fri sökning)` text appears in the dropdown.
       - **Free-text uppercased on select:** type `abc`; click the `ABC (fri sökning)` row; assert `onChange` called with `'ABC'`.
       - **Clear button fires onChange('')**: render with value='N02BE01'; click the clear `×` (find by aria-label="Rensa ATC-kod"); assert `onChange('')` called and popover did NOT open (stopPropagation worked).
       - **Cache contract:** assert `ATC_CODES_QUERY_OPTIONS.staleTime === Infinity` and `refetchOnWindowFocus === false` (per the D-133 named-export precedent).
       Mock fetch via `vi.spyOn(global, 'fetch')` returning `{codes: [...]}` — see DashboardLowStockCard.test.tsx for the harness.
  </action>
  <verify>
    <automated>cd C:/Projekt/MediTrack &amp;&amp; pnpm --filter @meditrack/web exec vitest run src/components/__tests__/AtcCodeCombobox.test.tsx</automated>
  </verify>
  <done>
    - `apps/web/src/components/AtcCodeCombobox.tsx` exists and exports `AtcCodeCombobox` as a named export (matching the TherapeuticClassCombobox export style).
    - `apps/web/src/features/medications/useAtcCodesQuery.ts` exists and exports both `useAtcCodesQuery` and `ATC_CODES_QUERY_OPTIONS` with `staleTime: Infinity`.
    - All component tests pass; `grep -F "Rensa ATC-kod" apps/web/src/components/AtcCodeCombobox.tsx` returns &gt;= 1 match.
    - `grep -F "(fri sökning)" apps/web/src/components/AtcCodeCombobox.tsx` returns &gt;= 1 match (free-text fallback row).
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire AtcCodeCombobox into MedicationSheet + LakemedelFilter + cache invalidation</name>
  <files>apps/web/src/routes/lakemedel/MedicationSheet.tsx, apps/web/src/routes/lakemedel/LakemedelFilter.tsx, apps/web/src/routes/lakemedel/LakemedelPage.tsx, apps/web/src/features/medications/useMedicationMutations.ts</files>
  <read_first>
    - apps/web/src/routes/lakemedel/MedicationSheet.tsx lines 1140-1180 (existing user-create ATC field — the change site for D-134 wiring; also verify `Controller` import path)
    - apps/web/src/routes/lakemedel/LakemedelFilter.tsx lines 60-150 + 175-244 (props interface + rewire site; localAtc/atcOpen state to delete)
    - apps/web/src/routes/lakemedel/LakemedelPage.tsx (call site for LakemedelFilter — search for `atcSuggestions` prop to confirm it is the only consumer being deprecated per D-134)
    - apps/web/src/features/medications/useMedicationMutations.ts lines 35-57 (useCreateMedication.onSuccess — one new invalidation line)
    - .planning/phases/08-compose-catalog-ux/08-UI-SPEC.md §Components &amp; States §6 + §7 (exact wiring contract for both surfaces, including `triggerClassName` values: `'w-full mt-1'` in Sheet, `'min-w-[140px] flex-shrink-0'` in Filter)
  </read_first>
  <action>
    Wire D-134 (single shared component into BOTH consumers) + D-133 (cache invalidation in mutation hook).

    1. **MedicationSheet user-create ATC field:** In `apps/web/src/routes/lakemedel/MedicationSheet.tsx`, replace the existing `<div>...<Input id="user-atc" {...userForm.register('atcCode')} ... />` block at lines 1156-1170 with the Controller-wrapped AtcCodeCombobox per UI-SPEC §Components &amp; States §6:
       - Import `AtcCodeCombobox` from `@/components/AtcCodeCombobox` and ensure `Controller` from `react-hook-form` is imported (the file already uses `useForm`; check for an existing `Controller` import before adding).
       - The Controller renders `AtcCodeCombobox` with `value={field.value ?? ''}`, `onChange={field.onChange}`, `placeholder="Välj ATC-kod"`, `searchPlaceholder="Sök ATC-kod…"`, `ariaLabel="Välj ATC-kod"`, `triggerClassName="w-full mt-1"`, `clearable`, `disabled={isPending}`.
       - Keep the `<Label>ATC-kod</Label>` (drop the `htmlFor="user-atc"` since the combobox owns its trigger button id internally — the orphan-Label pattern is documented in UI-SPEC §6 with the Phase 6 TherapeuticClassField precedent).
       - Keep the existing `userForm.formState.errors.atcCode` error block below unchanged.
       - DO NOT touch lines 1034-1048 (the empty-state branch — Plan 02's territory).
       - DO NOT touch lines 1037-1047 inline link copy (Plan 02 lowercases it). Per `<scope_fence>`, this plan leaves the current "Skapa nytt läkemedel" text alone.

    2. **LakemedelFilter rewire:** In `apps/web/src/routes/lakemedel/LakemedelFilter.tsx`:
       - Import `AtcCodeCombobox` from `@/components/AtcCodeCombobox`.
       - Remove the existing Popover/Command/CommandInput/CommandList/CommandGroup/CommandItem/CommandEmpty/PopoverContent/PopoverTrigger imports IF they become unused after the rewire (verify the TherapeuticClassCombobox sibling still imports these internally; the LakemedelFilter file itself no longer needs them once the inline ATC block is gone).
       - Delete: `atcOpen` useState (line 123), `localAtc` useState (line 124), the `useEffect([atc])` sync (lines 127-129), the `filteredSuggestions` derived array (lines 140-148), and the entire `<div className="flex items-center gap-1">...</div>` ATC block (lines 176-244 inclusive of the clear `×` Button sibling).
       - Insert in place of the deleted block: `<AtcCodeCombobox value={atc} onChange={(next) => onChange({ atc: next, page: 1 })} placeholder="ATC-kod ▾" searchPlaceholder="Sök ATC-kod…" ariaLabel="Filtrera på ATC-kod" triggerClassName="min-w-[140px] flex-shrink-0" clearable />` per UI-SPEC §Components §7 exact-copy contract.
       - Drop the `atcSuggestions?: string[]` field (lines 69-72) from `LakemedelFilterProps`. Drop `atcSuggestions` from the destructured props in the function signature.

    3. **LakemedelPage call site:** In `apps/web/src/routes/lakemedel/LakemedelPage.tsx`, find the `<LakemedelFilter ... />` invocation and remove the `atcSuggestions={...}` prop pass-through (search for the literal string `atcSuggestions` to locate it). If the page computes a value for that prop (e.g. derived from page rows), delete that computation as dead code too. The other props (q, atc, form, belowThreshold, therapeuticClass, onChange) stay.

    4. **Mutation invalidation:** In `apps/web/src/features/medications/useMedicationMutations.ts`, add ONE line inside `useCreateMedication`'s `onSuccess` callback (the function defined around line 44-50) immediately after the existing `void queryClient.invalidateQueries({ queryKey: ['dashboard', 'low-stock'] });` line: `void queryClient.invalidateQueries({ queryKey: ['atc-codes'] });` Add a brief inline comment `// Phase 8 D-133: a freshly user-created med can introduce a brand-new ATC code.` Do NOT touch useUpdateMedication, useDeleteMedication, or useUpdateThresholdOptimistic — only create can introduce a new code (D-133 rationale).
  </action>
  <verify>
    <automated>cd C:/Projekt/MediTrack &amp;&amp; pnpm --filter @meditrack/web exec tsc --noEmit &amp;&amp; pnpm --filter @meditrack/web exec vitest run src/components/__tests__/AtcCodeCombobox.test.tsx</automated>
  </verify>
  <done>
    - `grep -F "atcSuggestions" apps/web/src/routes/lakemedel/LakemedelFilter.tsx apps/web/src/routes/lakemedel/LakemedelPage.tsx` returns ZERO matches (deprecation complete per D-134).
    - `grep -F "AtcCodeCombobox" apps/web/src/routes/lakemedel/LakemedelFilter.tsx apps/web/src/routes/lakemedel/MedicationSheet.tsx` returns &gt;= 1 match each.
    - `grep -F "['atc-codes']" apps/web/src/features/medications/useMedicationMutations.ts` returns exactly 1 match (only in useCreateMedication).
    - `pnpm --filter @meditrack/web exec tsc --noEmit` exits 0 (no breakage from the dropped atcSuggestions prop in LakemedelPage).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /api/medications/atc-codes | Authenticated session cookie crosses; returns a global list of distinct ATC codes. |
| User-create form (MedicationSheet) → POST /api/medications | The combobox writes into the `atcCode` form field; existing `medicationCreateUserRequest` Zod validates the value at the API boundary unchanged. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Information Disclosure | GET /api/medications/atc-codes | mitigate | `requireSession` preHandler. The DISTINCT global ATC list is already implicitly disclosed through the existing `/api/medications/search` typeahead (same posture as the global NPL catalog itself); no new disclosure beyond an authenticated user. No `requirePermission` gate — all three roles legitimately consume the list (read-only catalog browsing surface). |
| T-08-05 | Tampering | User-created Medication.atcCode (combobox free-text fallback path) | accept | Input validation already enforced upstream by `medicationCreateUserRequest` Zod schema (`atcCode: z.string().min(1)` + the existing `regex(/^[A-V][0-9A-Z]{0,6}$/)` on update). Combobox uppercases on select. No new write path introduced by Phase 8 Plan 01. |
| T-08-SC | Tampering | Package installs | mitigate | No new packages installed in this plan; uses existing `@anthropic-ai/sdk` (out of scope), `@tanstack/react-query`, `cmdk`, `lucide-react`, `@radix-ui/react-popover`, `react-hook-form` — all installed and audited in Phases 1–6. No package legitimacy gate needed. |
</threat_model>

<verification>
After all three tasks complete:
1. `pnpm verify` (workspace chain: lint + typecheck + test + build) exits 0.
2. `pnpm --filter @meditrack/api exec vitest run apps/api/test/medications.atcCodes.integration.test.ts` exits 0 (3/3 assertions pass).
3. `pnpm --filter @meditrack/web exec vitest run src/components/__tests__/AtcCodeCombobox.test.tsx` exits 0.
4. Manual smoke (optional, in dev): `docker compose up`, navigate to `/lakemedel`, click the ATC trigger → typeahead dropdown shows codes; type `N02` → list narrows; type `XYZ` → `(fri sökning)` row appears. Navigate to add-medication flow → user-create form ATC field renders the same combobox.
</verification>

<success_criteria>
- Roadmap Phase 8 SC #1 (CAT-09) satisfied for the BE + shared component + both consumer wirings.
- `GET /api/medications/atc-codes` returns `{codes: string[]}` ascending DISTINCT from the global NPL Medication table.
- `useCreateMedication.onSuccess` invalidates the new cache key in addition to its existing four invalidations.
- LakemedelFilter ATC control is the shared AtcCodeCombobox; `atcSuggestions` prop removed from the interface + call site.
- MedicationSheet user-create form ATC field is the shared AtcCodeCombobox via Controller; existing label and error block preserved.
- Plan does NOT touch the in-empty-state "Skapa nytt läkemedel" link (Plan 02 territory) and does NOT add CAT-08 surfaces (out of scope per `<scope_fence>`).
</success_criteria>

<output>
Create `.planning/phases/08-compose-catalog-ux/08-01-SUMMARY.md` when done, summarising:
- Files created (8 paths)
- Files modified (5 paths)
- Tests added (1 BE integration file, 1 FE component file)
- Verification outcomes (pnpm verify result, test counts)
- Any deviations from the plan
</output>
</content>
</invoke>