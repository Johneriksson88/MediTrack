---
phase: 01-foundation-auth
plan: 05
type: execute
wave: 3
depends_on: ["01-02", "01-03", "01-04"]
files_modified:
  - apps/api/prisma/seed.ts
  - apps/api/test/auth.flow.smoke.test.ts
  - apps/api/test/admin.ping.test.ts
  - apps/web/src/auth/RoleRoute.tsx
  - apps/web/src/routes/konto/KontoPage.tsx
  - README.md
autonomous: false
requirements: [AUTH-03, AUTH-04, AUTH-05, AUTH-06, AUTH-07, UX-01]
requirements_addressed: [AUTH-03, AUTH-04, AUTH-05, AUTH-06, AUTH-07, UX-01]
user_setup: []
tags: [seeds, smoke-test, integration, schema-push, readme, reconciliation, phase-completion]
must_haves:
  goals:
    - "Phase 1 closes cleanly: three seed users (one per role) bound to the same vårdenhet, an end-to-end smoke test exercises the full login → /me → admin:ping → logout pipeline, the Konto admin gate uses Plan 03's `<Can>` primitive, and the README seeds section documents demo credentials."
  truths:
    - "Three users exist in the seeded DB — `apotekare@example.test`, `sjukskoterska@example.test`, `admin@example.test` — all with password `demo1234`, all bound to the single `CareUnit { name: 'Avdelning 4, Karolinska' }` (Phase 1 success #5, CONTEXT.md §Specific Ideas, AUTH-04, AUTH-07)."
    - "`prisma db seed` is idempotent — running it twice on the same DB does not error and does not create duplicate rows (verified by `SELECT COUNT(*) FROM \"User\"` returning exactly 3 after a second run)."
    - "End-to-end smoke test `auth.flow.smoke.test.ts` exercises the full pipeline for each role: POST /api/auth/login (200 + cookie) → GET /api/me (200 + correct permissions array) → GET /api/admin/ping (200 for admin, 403 for apotekare/sjukskoterska) → DELETE /api/auth/session (204) → GET /api/me (401). Phase 1 success #4."
    - "The Konto page admin-ping demo uses `<Can action='admin:ping'>` (Plan 03's primitive) to gate the button — non-admin users see the `Denna åtgärd kräver adminrättigheter.` static note instead. The TODO comments left by Plan 04 are removed (UI-SPEC §403 / `<Can>` Gate Pattern)."
    - "`RoleRoute.tsx` imports `useAuth` from `@/auth/useAuth` (Plan 03), no inline fallback hook. The TODO comment from Plan 04 is removed."
    - "README.md exists with the four mandatory sections — `## How to run`, `## Demo credentials`, `## Project purpose`, `## Known gaps & what I'd do with more time` — populated with at least the Phase 1 surface. (Phase 7 will expand to the full brief-required sections; Plan 05 establishes the file + the seeds documentation so reviewers running compose can log in.)"
    - "`docker compose down -v && docker compose up --build` from a clean state brings the full stack up; the smoke test passes against the seeded DB; the Phase 1 demo flow works in the browser for all three roles."
  artifacts:
    - path: "apps/api/prisma/seed.ts"
      provides: "Idempotent seed creating CareUnit 'Avdelning 4, Karolinska' + 3 users (one per role, password 'demo1234')"
    - path: "apps/api/test/auth.flow.smoke.test.ts"
      provides: "End-to-end smoke covering login → /me → admin:ping → logout per role (Phase 1 success #4)"
    - path: "apps/web/src/auth/RoleRoute.tsx"
      provides: "Cleaned: imports useAuth from @/auth/useAuth (no fallback hook), no TODO comments"
    - path: "apps/web/src/routes/konto/KontoPage.tsx"
      provides: "Cleaned: uses <Can action='admin:ping'> for the admin button; non-admin shows the gate note"
    - path: "README.md"
      provides: "Run instructions + demo credentials for the three seeded users; placeholder sections for Phase 7 expansion"
  key_links:
    - from: "apps/web/src/auth/RoleRoute.tsx"
      to: "apps/web/src/auth/useAuth.ts (Plan 03)"
      via: "import { useAuth } from '@/auth/useAuth'"
      pattern: "useAuth"
    - from: "apps/web/src/routes/konto/KontoPage.tsx"
      to: "apps/web/src/auth/Can.tsx (Plan 03)"
      via: "<Can action=\"admin:ping\">{adminButton}</Can>"
      pattern: "<Can\\s+action=['\"]admin:ping['\"]"
    - from: "apps/api/test/auth.flow.smoke.test.ts"
      to: "all three seed users"
      via: "iterates [apotekare, sjukskoterska, admin] through the pipeline"
      pattern: "apotekare@example\\.test"
---

<objective>
Close Phase 1 by reconciling Plans 02–04 and proving every success criterion end-to-end:
1. Expand the Walking Skeleton seed to all three roles (one per role on a single vårdenhet) — completes Phase 1 success #5.
2. Add the integration smoke test covering the full pipeline for every role — completes Phase 1 success #4.
3. Replace Plan 04's Wave-2 fallback TODOs with Plan 03's real `useAuth` / `<Can>` imports — Konto admin gate now actually exercises `<Can>`.
4. Run the second-pass schema push (now non-blocking — Plan 02 already created the migration; this only confirms idempotency) and ensure compose comes up clean.
5. Ship a README with run instructions + demo credentials (Phase 7 will expand to the full brief-required sections).

Purpose: A reviewer doing `docker compose up` from a clean clone must be able to log in as any of three roles with documented credentials and observe the correct nav (admin tab visible only for admin), the correct /me permissions, and the correct 401/403/200 behavior on admin:ping. The README's credentials section is the entry point.

Output: Three-role seed, smoke test green, FE primitive reconciliation, README seeds section, and the Phase 1 demo runnable end-to-end on a fresh clone.
</objective>

<execution_context>
@C:/Projekt/MediTrack/.claude/get-shit-done/workflows/execute-plan.md
@C:/Projekt/MediTrack/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-auth/01-CONTEXT.md
@.planning/phases/01-foundation-auth/01-PATTERNS.md
@.planning/phases/01-foundation-auth/01-UI-SPEC.md
@.planning/phases/01-foundation-auth/01-SKELETON.md
@.planning/phases/01-foundation-auth/01-02-PLAN.md
@.planning/phases/01-foundation-auth/01-03-PLAN.md
@.planning/phases/01-foundation-auth/01-04-PLAN.md

<files_to_read>
- apps/api/prisma/seed.ts                                      # Plan 02's current single-user seed
- apps/api/prisma/schema.prisma                                # the User/CareUnit shape
- apps/api/src/auth/password.ts                                # hashPassword
- apps/api/test/helpers/buildTestApp.ts                        # test harness pattern
- apps/api/test/auth.login.test.ts                             # reference for app.inject + cookie capture
- apps/api/test/admin.ping.test.ts                             # Plan 03's matrix test — extend or reuse helpers
- apps/web/src/auth/RoleRoute.tsx                              # Plan 04's stub with TODO fallback
- apps/web/src/routes/konto/KontoPage.tsx                      # Plan 04's stub with TODO fallback
- apps/web/src/auth/useAuth.ts                                 # Plan 03's hook
- apps/web/src/auth/Can.tsx                                    # Plan 03's <Can>
- .planning/phases/01-foundation-auth/01-CONTEXT.md §Specific Ideas (demo seeds)
- .planning/phases/01-foundation-auth/01-PATTERNS.md (Pattern P for compose idempotency)
- .planning/REQUIREMENTS.md (AUTH-03..07, UX-01)
</files_to_read>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Expand seed to 3 roles (idempotent) + end-to-end smoke test</name>
  <read_first>
    - apps/api/prisma/seed.ts
    - apps/api/prisma/schema.prisma
    - apps/api/src/auth/password.ts
    - apps/api/test/helpers/buildTestApp.ts
    - apps/api/test/auth.login.test.ts
    - apps/api/test/admin.ping.test.ts
    - .planning/phases/01-foundation-auth/01-CONTEXT.md §Specific Ideas
  </read_first>
  <files>
    apps/api/prisma/seed.ts,
    apps/api/test/auth.flow.smoke.test.ts
  </files>
  <action>
    Expand `apps/api/prisma/seed.ts` to seed all three roles, idempotently:
    - One `CareUnit` upsert: `{ id: 'careunit-karolinska-01', name: 'Avdelning 4, Karolinska' }` (same as Plan 02 — keep the fixed id so subsequent runs match).
    - Three `User` upserts, all keyed by `email`:
      - `apotekare@example.test` — `name: 'Anna Apotekare'`, `role: 'apotekare'`, `careUnitId: <careunit>`, `passwordHash: await hashPassword('demo1234')`.
      - `sjukskoterska@example.test` — `name: 'Sara Sjuksköterska'`, `role: 'sjukskoterska'`, same careUnit, same password.
      - `admin@example.test` — `name: 'Admin Demo'`, `role: 'admin'`, same careUnit, same password (preserves Plan 02's admin).
    - **Idempotency:** use `prisma.user.upsert({ where: { email }, update: {}, create: {...} })` — running twice MUST produce exactly 3 users (no duplicates, no errors). For `update: {}` the existing row is unchanged on re-run.
    - **Optimization:** hash the password ONCE and reuse the same hash for all three users (saves ~300ms x 2 = ~600ms on every seed run; argon2 is slow by design). Document with a comment that this is a dev-only shortcut.
    - At end, `console.log` a table-like line listing the three seeded emails + the shared password `demo1234` for executor visibility:
      `console.log('Seeded users (password=demo1234): apotekare@example.test, sjukskoterska@example.test, admin@example.test');`

    Create `apps/api/test/auth.flow.smoke.test.ts` — the integration smoke test Phase 1 success #4 demands:
    - Setup: `beforeAll` calls `buildTestApp()` and ensures the seed has run (`await seed(app.db)` or call the seed script directly via `tsx`).
    - For EACH role in `[{ email: 'apotekare@example.test', role: 'apotekare', adminPing: 403 }, { email: 'sjukskoterska@example.test', role: 'sjukskoterska', adminPing: 403 }, { email: 'admin@example.test', role: 'admin', adminPing: 200 }]`:
      1. `app.inject({ method: 'POST', url: '/api/auth/login', payload: { email, password: 'demo1234' } })` → expect 200 + `set-cookie` header containing `meditrack.sid=`. Capture the cookie value.
      2. `app.inject({ method: 'GET', url: '/api/me', cookies: { 'meditrack.sid': sessionId } })` → expect 200; `body.role === role`; `body.careUnit.name === 'Avdelning 4, Karolinska'`; `body.permissions` is `['admin:ping']` for admin and `[]` for others.
      3. `app.inject({ method: 'GET', url: '/api/admin/ping', cookies: { ... } })` → expect status === `adminPing` (200 or 403). For 200: `body.pong === true`. For 403: `body.error.code === 'forbidden'` and `body.error.message === 'Du saknar behörighet att utföra denna åtgärd.'`.
      4. `app.inject({ method: 'DELETE', url: '/api/auth/session', cookies: { ... } })` → expect 204.
      5. `app.inject({ method: 'GET', url: '/api/me', cookies: { ... } })` → expect 401 + `body.error.code === 'unauthenticated'`.
    - Final assertion: count `SELECT COUNT(*) FROM "Session"` (via Prisma) returns 0 after all three logout cycles.

    Update `apps/api/test/admin.ping.test.ts` (Plan 03's test file) if it manually created two non-admin test users inline — replace with reading the seeded `apotekare@example.test` and `sjukskoterska@example.test` users to keep the test data shape consistent (this is a small cleanup, optional if Plan 03's test was self-sufficient).
  </action>
  <acceptance_criteria>
    - `apps/api/prisma/seed.ts` contains all three email literals (`apotekare@example.test`, `sjukskoterska@example.test`, `admin@example.test`) and uses `upsert` for each.
    - Running `pnpm --filter @meditrack/api exec prisma db seed` twice in succession exits 0 both times; `SELECT COUNT(*) FROM "User"` returns exactly 3 after the second run.
    - `apps/api/test/auth.flow.smoke.test.ts` exists and iterates exactly three roles in a table-driven loop or three sub-describes; contains the literal email `apotekare@example.test`, `sjukskoterska@example.test`, `admin@example.test`.
    - Each role's test asserts: login 200, /me 200 + correct permissions, admin:ping (200 for admin / 403 for others), logout 204, /me 401.
    - `pnpm --filter @meditrack/api exec vitest --run --reporter=basic` exits 0 with the new smoke test included.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs'); const seed=fs.readFileSync('apps/api/prisma/seed.ts','utf8'); for(const e of ['apotekare@example.test','sjukskoterska@example.test','admin@example.test']){if(!seed.includes(e)){console.error('seed.ts missing email',e);process.exit(1)}} if(!/upsert/.test(seed)){console.error('seed.ts not using upsert');process.exit(1)} if(!fs.existsSync('apps/api/test/auth.flow.smoke.test.ts')){console.error('auth.flow.smoke.test.ts missing');process.exit(1)} const t=fs.readFileSync('apps/api/test/auth.flow.smoke.test.ts','utf8'); for(const e of ['apotekare@example.test','sjukskoterska@example.test','admin@example.test']){if(!t.includes(e)){console.error('smoke test missing role:',e);process.exit(1)}} for(const c of ['/api/auth/login','/api/me','/api/admin/ping','/api/auth/session']){if(!t.includes(c)){console.error('smoke test missing endpoint',c);process.exit(1)}} console.log('OK')" && pnpm --filter @meditrack/api exec prisma db seed && pnpm --filter @meditrack/api exec prisma db seed && pnpm --filter @meditrack/api exec vitest --run --reporter=basic</automated>
  </verify>
  <done>
    Seed is idempotent and seeds three roles + one vårdenhet; the smoke test exercises the full pipeline for each role; Phase 1 success criteria #2 (401/403/200 matrix), #4 (smoke test passes), and #5 (3 seed users, one per role) are observably true.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Reconcile FE Wave-2 fallbacks — RoleRoute + KontoPage use Plan 03 primitives</name>
  <read_first>
    - apps/web/src/auth/RoleRoute.tsx                              # Plan 04's fallback version
    - apps/web/src/routes/konto/KontoPage.tsx                      # Plan 04's fallback version
    - apps/web/src/auth/useAuth.ts                                 # Plan 03
    - apps/web/src/auth/Can.tsx                                    # Plan 03
    - apps/web/src/lib/api.ts                                      # fetchJson for the admin ping fetch
    - .planning/phases/01-foundation-auth/01-UI-SPEC.md §403 / `<Can>` Gate Pattern
  </read_first>
  <files>
    apps/web/src/auth/RoleRoute.tsx,
    apps/web/src/routes/konto/KontoPage.tsx
  </files>
  <action>
    Reconcile the two files Plan 04 marked with `TODO: replace with useAuth from @/auth/useAuth once Plan 03 lands`. After this task, neither file contains an inline `useQuery(['me'])` fallback — both consume Plan 03's `useAuth`/`<Can>`.

    `RoleRoute.tsx`:
    - Replace the inline `useQuery(['me'])` block (if present) with `import { useAuth } from '@/auth/useAuth'; const { user, isLoading } = useAuth();`.
    - Logic stays the same: if `isLoading` → render `null` (AuthGate already showed the skeleton above this in the route tree); if `user && roles.includes(user.role)` → render `<Outlet/>` (and/or `children` if used both ways); else → `fallback ?? <Navigate to="/dashboard" replace/>`.
    - Remove the TODO comment.

    `KontoPage.tsx`:
    - Replace any inline `useQuery(['me'])` with `import { useAuth } from '@/auth/useAuth'; const { user } = useAuth();` (no isLoading needed — AuthGate gated this).
    - Wrap the admin-ping button in `<Can action="admin:ping">`. Import `Can` from `@/auth/Can` and `ActionKey` is not needed at runtime (the prop is type-checked at compile time).
    - The non-admin gate note is still rendered via the inverse condition (`!useCan('admin:ping')`) using `useCan` from `@/auth/useCan`, OR via the existing `user.role !== 'admin'` check. Use `useCan` — it's the canonical way (Pattern L).
    - Remove the TODO comment.
    - The admin button onClick should call a small async helper that POSTs `GET /api/admin/ping` via `fetchJson` and displays the response (e.g. a small text below the button: "Senaste pong: <timestamp>"). On `ApiError` with status 403, display `<Alert variant="destructive">Du saknar behörighet att utföra denna åtgärd.</Alert>` per UI-SPEC §403.

    NOTE: `useCan('admin:ping')` returns false for non-admin even when the BE has not yet been called — the `permissions` array in `/me` is authoritative (Plan 03 computes from PERMISSIONS map). So the gate note renders without any extra API call.
  </action>
  <acceptance_criteria>
    - `apps/web/src/auth/RoleRoute.tsx` imports `useAuth` from `@/auth/useAuth` (grep matches `from\s+['\"]@/auth/useAuth['\"]`) and does NOT contain `useQuery(['me'])` (Plan 03's hook handles it internally).
    - `apps/web/src/auth/RoleRoute.tsx` does NOT contain the string `TODO` (cleanup complete).
    - `apps/web/src/routes/konto/KontoPage.tsx` imports `Can` from `@/auth/Can` and `useCan` from `@/auth/useCan` (or `useAuth`); contains `<Can action="admin:ping">` literally.
    - `apps/web/src/routes/konto/KontoPage.tsx` does NOT contain the string `TODO`.
    - `KontoPage.tsx` still contains the verbatim Swedish strings `Logga ut` and `Denna åtgärd kräver adminrättigheter.` and `Du saknar behörighet att utföra denna åtgärd.` (the latter only inside the 403 Alert branch).
    - `pnpm --filter @meditrack/web build` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs'); const rr=fs.readFileSync('apps/web/src/auth/RoleRoute.tsx','utf8'); if(!/from\\s+['\"]@\\/auth\\/useAuth['\"]/.test(rr)){console.error('RoleRoute not importing useAuth');process.exit(1)} if(/TODO/.test(rr)){console.error('RoleRoute still has TODO');process.exit(1)} const kp=fs.readFileSync('apps/web/src/routes/konto/KontoPage.tsx','utf8'); if(!/<Can\\s+action=['\"]admin:ping['\"]/.test(kp)){console.error('KontoPage missing <Can action=admin:ping>');process.exit(1)} if(/TODO/.test(kp)){console.error('KontoPage still has TODO');process.exit(1)} for(const s of ['Logga ut','Denna åtgärd kräver adminrättigheter.']){if(!kp.includes(s)){console.error('KontoPage missing exact string:',s);process.exit(1)}} console.log('OK')" && pnpm --filter @meditrack/web build</automated>
  </verify>
  <done>
    `RoleRoute` and `KontoPage` both consume Plan 03's `useAuth` / `<Can>` / `useCan` primitives directly; no inline fallback hooks remain; both files build clean; the Konto page properly demonstrates the `<Can>` defense-in-depth pattern (UI-SPEC §403).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: README seeds section + run instructions</name>
  <read_first>
    - .planning/PROJECT.md
    - .planning/ROADMAP.md
    - .planning/phases/01-foundation-auth/01-CONTEXT.md §Specific Ideas
    - CLAUDE.md
    - apps/api/prisma/seed.ts
    - docker-compose.yml
  </read_first>
  <files>
    README.md
  </files>
  <action>
    Create `README.md` at the repo root. Phase 1's job is the seeds section + run instructions; Phase 7 expands to the full brief-required sections (purpose, stack rationale, §6 answers, known gaps, "what I'd do with more time"). Do NOT overpromise content that Phase 7 owns.

    Sections to include in Plan 05:
    - `# MediTrack` — single H1.
    - `## Vad är det här?` — one paragraph: internal Swedish-language tool for vårdenheter to manage medication stock + orders; Medovia interview submission. Reference PROJECT.md "What This Is" prose verbatim or paraphrased.
    - `## Snabbstart med Docker Compose` — the golden command:
      - Prerequisites: Docker Desktop ≥ 4.x, Node 20 (for local dev), pnpm 8+ (via `corepack enable && corepack prepare pnpm@latest --activate`).
      - Step 1: `cp .env.example .env` then set `COOKIE_SECRET` (instructions: `node -e "console.log(require('crypto').randomBytes(32).toString('base64url'))"`).
      - Step 2: `docker compose up --build` — first run pulls postgres, builds api + web, runs migrations + seed.
      - Step 3: Open `http://localhost:5173` and log in with one of the demo accounts below.
      - To reset: `docker compose down -v && docker compose up --build`.
    - `## Demo-konton` — the three seeded credentials in a table:
      | E-post | Lösenord | Roll | Vårdenhet |
      | apotekare@example.test | demo1234 | Apotekare | Avdelning 4, Karolinska |
      | sjukskoterska@example.test | demo1234 | Sjuksköterska | Avdelning 4, Karolinska |
      | admin@example.test | demo1234 | Admin | Avdelning 4, Karolinska |
      Below the table, a single line: "Passwords are for the demo environment only and stored in the seed script (`apps/api/prisma/seed.ts`)."
    - `## Lokal utveckling utan Docker` — brief:
      - Start postgres (e.g. `docker compose up postgres -d`).
      - `pnpm install` at repo root.
      - `pnpm --filter @meditrack/api exec prisma migrate dev` (initial) + `pnpm --filter @meditrack/api exec prisma db seed`.
      - `pnpm -r --parallel dev` (or run `apps/api dev` and `apps/web dev` in separate terminals).
    - `## Tester` — one line: `pnpm --filter @meditrack/api exec vitest run` (api integration smoke).
    - `## Status` — current phase (Phase 1 complete; Phases 2–7 pending). Link to `.planning/ROADMAP.md`.
    - `## Vad ligger var?` — one short table:
      | Sökväg | Innehåll |
      | apps/web | React + Vite + Tailwind + shadcn |
      | apps/api | Fastify + Prisma |
      | packages/shared | Zod-kontrakt delade mellan klient och server |
      | .planning | Planeringsartefakter (skapas inte i prod) |

    Future sections (Phase 7 owns): `## Stack-val och varför`, `## Arkitektur och svar på §6 (samtidighet, skalning, retrofitting av auth)`, `## Kända luckor och vad jag skulle göra med mer tid`, `## Commit-historik`.

    Tone: Swedish for user-facing sections (reviewer is Swedish-speaking); English allowed in code blocks / file paths / commands. Keep it short — under 200 lines total in Plan 05. Phase 7 expands it.
  </action>
  <acceptance_criteria>
    - `README.md` exists at repo root.
    - Contains a top-level `# MediTrack` heading.
    - Contains a section heading `## Snabbstart med Docker Compose` (or English equivalent `## Quick start with Docker Compose`).
    - Contains the literal command `docker compose up --build` somewhere.
    - Contains a `## Demo-konton` (or `## Demo accounts`) section listing all three demo emails: `apotekare@example.test`, `sjukskoterska@example.test`, `admin@example.test`, and the shared password `demo1234`.
    - Contains the literal vårdenhet name `Avdelning 4, Karolinska`.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs'); if(!fs.existsSync('README.md')){console.error('README.md missing');process.exit(1)} const r=fs.readFileSync('README.md','utf8'); if(!/^#\\s+MediTrack/m.test(r)){console.error('README missing # MediTrack heading');process.exit(1)} for(const s of ['docker compose up','apotekare@example.test','sjukskoterska@example.test','admin@example.test','demo1234','Avdelning 4, Karolinska']){if(!r.includes(s)){console.error('README missing required string:',s);process.exit(1)}} console.log('OK')"</automated>
  </verify>
  <done>
    README.md exists with the run instructions + demo credentials section so a reviewer can `git clone && docker compose up && open localhost:5173` and log in immediately. Phase 7 will expand to the full brief-required sections.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Phase 1 end-to-end verification — all 5 success criteria</name>
  <files>(checkpoint — no code modifications; pause for human verification)</files>
  <action>Pause for human verification. Execute the 16-point Phase 1 acceptance protocol detailed in <how-to-verify> below. Do not mark Phase 1 complete until the user types the resume-signal "phase 1 approved".</action>
  <verify><human-check>Human executes the 16 verification steps in <how-to-verify>; resume-signal "phase 1 approved" received.</human-check></verify>
  <done>User has typed "phase 1 approved" confirming all 16 verification points pass and all 5 ROADMAP success criteria are observably true.</done>
  <what-built>
    Phase 1 closed: three roles seeded, smoke test green, FE primitives reconciled, README ready. All five Phase 1 success criteria from ROADMAP must now be observably true.
  </what-built>
  <how-to-verify>
    From a fresh clone (or `docker compose down -v` to wipe volumes), run:

    ```
    docker compose up --build
    ```

    Wait for `postgres (healthy)`, `api (healthy)`, `web (running)`. Then verify ALL FIVE Phase 1 success criteria from ROADMAP.md §"Phase 1: Foundation & Auth":

    **Success #1: "Seeded user logs in at `/login`, lands on a dashboard scoped to their `vårdenhet`, and session survives refresh."**
    1. Open `http://localhost:5173` → redirected to `/login`.
    2. Log in as `admin@example.test` / `demo1234` → redirected to `/dashboard`; the user pill (desktop) shows `Admin Demo · Admin · Avdelning 4, Karolinska`.
    3. Refresh the browser. Still on `/dashboard`; user pill unchanged.

    **Success #2: "A backend endpoint exists that enforces role check ... calling it without a session returns 401, with wrong role returns 403, both as JSON."** (Honoring D-18: collapsed into /api/admin/ping)
    4. Open a new terminal: `curl -i http://localhost:3000/api/admin/ping` → expect `HTTP/1.1 401` + body `{"error":{"code":"unauthenticated","message":"Du måste logga in."}}`.
    5. Log in as `sjukskoterska@example.test` via the browser. On `/konto`, the admin ping button is HIDDEN and the gate note `Denna åtgärd kräver adminrättigheter.` is visible.
    6. Optional curl path: extract the session cookie from devtools → `curl -i -H "Cookie: meditrack.sid=<value>" http://localhost:3000/api/admin/ping` → expect `HTTP/1.1 403` + body `{"error":{"code":"forbidden","message":"Du saknar behörighet att utföra denna åtgärd."}}`.
    7. Log in as admin → `/konto` → admin ping button visible → click → expect `pong: true` confirmation; no 403.

    **Success #3: "App shell renders without horizontal scroll on 360 / 768 / 1024 / 1440 px breakpoints; primary nav reachable on each."**
    8. Run Plan 04 Task 4's full breakpoint matrix again (devtools responsive mode). All four widths × all four route pages → zero horizontal scroll. Nav reachable on every breakpoint (bottom tab bar <md, sidebar md+).

    **Success #4: "`docker compose up` brings up `postgres`, `api`, `web`; integration smoke test passes (login + `/me` round-trip)."**
    9. `docker compose ps` shows all three services healthy.
    10. From host: `pnpm --filter @meditrack/api exec vitest run --reporter=basic` → all tests pass including `auth.flow.smoke.test.ts` (iterates all three roles through login → /me → admin:ping → logout).
    11. The smoke test's three role iterations all green.

    **Success #5: "Three seed users exist — one per role — each bound to the same `vårdenhet`; their credentials are documented in the README seeds section."**
    12. `docker compose exec postgres psql -U meditrack -d meditrack -c "SELECT email, role, \"careUnitId\" FROM \"User\" ORDER BY role;"` returns exactly three rows with the same `careUnitId` value and the three role enum values.
    13. `docker compose exec postgres psql -U meditrack -d meditrack -c "SELECT name FROM \"CareUnit\";"` returns exactly one row: `Avdelning 4, Karolinska`.
    14. Open `README.md` — confirm the `## Demo-konton` table exists with all three credentials.
    15. Log in as each of the three roles (logout between) — all three logins succeed; each lands on `/dashboard` with the correct user pill content.

    **Final regression (re-running compose):**
    16. `docker compose down -v && docker compose up --build` (full reset). All five criteria above must still pass on the rebuild. The seed must remain idempotent — `SELECT COUNT(*) FROM "User"` is exactly 3 after the second startup (not 6).
  </how-to-verify>
  <resume-signal>Type "phase 1 approved" if all 16 verification points pass; otherwise describe failures for fix-and-retry. After approval, run `/gsd-transition` to mark Phase 1 complete and proceed to Phase 2 planning.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Seed script → DB | Inserts three demo users with weak password `demo1234` — dev/demo environment only |
| README → reviewer | Documents how to log in; no secrets that aren't already in the seed script |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | Demo password committed in README + seed script | accept | Documented as a dev/demo environment in README ("Passwords are for the demo environment only…"). All three users live in a Postgres DB that is also dev/demo only; production deployment would require regenerating users with strong passwords (Phase 7 README note). Acceptable risk for the interview submission. |
| T-05-02 | Tampering | Re-running seed creates duplicates | mitigate | `prisma.user.upsert({ where: { email }, update: {}, create: {...} })` is idempotent by design (Pattern P). Verified by the acceptance test running seed twice and asserting count === 3. |
| T-05-03 | Spoofing | Same-password reuse across roles | accept | All three demo users share `demo1234`. Acceptable for demo (the brief itself does not require per-account passwords); Phase 7 README mentions this as a known gap. |
| T-05-04 | Information Disclosure | Smoke test logs session cookies | accept | Vitest output may include cookie values during failures. Acceptable — these are local test fixture sessions against a test DB, not production secrets. |
| T-05-SC | Tampering | No new package installs | mitigate | Plan 05 adds zero npm packages. No supply-chain surface. |

</threat_model>

<verification>
  - Static: `pnpm -r build` exits 0; both apps typecheck cleanly.
  - Tests: `pnpm --filter @meditrack/api exec vitest run` exits 0 — all of Plan 02's tests + Plan 03's admin.ping matrix + Plan 05's auth.flow.smoke pass.
  - Idempotency: `pnpm --filter @meditrack/api exec prisma db seed && pnpm --filter @meditrack/api exec prisma db seed` succeeds both times; `SELECT COUNT(*) FROM "User" = 3` after.
  - Compose reset: `docker compose down -v && docker compose up --build` brings the stack to the same end-state with 3 users + 1 careunit.
  - All 16 manual verification points pass.
  - File audit: `apps/web/src/auth/RoleRoute.tsx` and `apps/web/src/routes/konto/KontoPage.tsx` contain no `TODO` strings.
</verification>

<success_criteria>
  Phase 1's 5 ROADMAP success criteria — all observably true after Plan 05:
  1. Login + dashboard + refresh survival ✓ (Plan 02 + Plan 05 Task 4)
  2. 401 / 403 / 200 matrix on /api/admin/ping ✓ (Plan 03 + Plan 05 Task 4)
  3. No horizontal scroll on 360 / 768 / 1024 / 1440 ✓ (Plan 04 + Plan 05 Task 4 re-verification)
  4. `docker compose up` + smoke test ✓ (Plan 02 + Plan 05 Task 1 smoke test)
  5. Three seed users, one per role, same vårdenhet, README-documented ✓ (Plan 05 Tasks 1 + 3)

  All 8 phase requirement IDs satisfied:
  - AUTH-01 (login + friendly error) — Plan 02
  - AUTH-02 (session survives refresh) — Plan 02
  - AUTH-03 (logout from any page) — Plan 04 (button) + Plan 02 (endpoint)
  - AUTH-04 (3-role enum) — Plan 02 (schema) + Plan 05 (seed)
  - AUTH-05 (BE 403 enforcement) — Plan 03
  - AUTH-06 (FE hides actions user can't perform) — Plan 03 + Plan 05 reconciliation
  - AUTH-07 (one vårdenhet per user, scoped data) — Plan 02 (schema + service layer) + Plan 05 (seed)
  - UX-01 (4-breakpoint mobile-first, no horizontal scroll) — Plan 04 + Plan 05 re-verification

  All 19 CONTEXT.md decisions (D-01..D-19) honored — verifiable by grepping each decision's signature across the committed code.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-auth/01-05-SUMMARY.md` when done. Include:
- Phase 1 completion attestation: every ROADMAP success criterion ticked.
- Tech-stack landed (echo the SKELETON.md decisions; this is the source for Phase 2 SUMMARY consumption).
- Patterns established A–P (the Phase 2 pattern mapper will use these as analogs).
- Demo credentials (one more time, for prosperity).
- Pointer to Phase 2 entry: `/gsd:plan-phase 2`.
</output>
