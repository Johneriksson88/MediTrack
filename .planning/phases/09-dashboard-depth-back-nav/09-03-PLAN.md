---
phase: 09-dashboard-depth-back-nav
plan: 03
type: execute
wave: 2
depends_on: [09-01, 09-02]
files_modified:
  - apps/web/src/features/dashboard/useDashboardOrdersQuery.ts
  - apps/web/src/features/orders/useOrderMutations.ts
  - apps/web/src/routes/dashboard/DashboardOrdersCard.tsx
  - apps/web/src/routes/dashboard/__tests__/DashboardOrdersCard.test.tsx
  - apps/web/src/routes/dashboard/DashboardPage.tsx
autonomous: true
requirements: [ORD-09]
must_haves:
  decisions_honored: [D-141, D-142, D-145, D-146, D-147, D-148]
  truths:
    - "`/dashboard` renders TWO cards side-by-side at md+ breakpoints: <DashboardLowStockCard /> (left/top) and <DashboardOrdersCard /> (right/bottom)."
    - "At <md the two cards stack vertically; low-stock first (D-146)."
    - "Nurse sees: 'Egna utkast' section (count + ≤5 rows) + 'Senaste beställningar' section (≤5 rows)."
    - "Apotekare/admin sees: 'Väntar på bekräftelse' section (count + ≤5 rows) + 'Väntar på leverans' section (count + ≤5 rows)."
    - "Each section header is a Link to /bestallningar?status=<status>."
    - "Each row is a Link to /bestallningar/:id?from=<row.status> — back-nav from detail returns to the matching tab via the Slice A helper hook."
    - "Empty states are celebratory + role-specific (CheckCircle2 emerald, NOT EmptyStateCard)."
    - "Three-layer refresh: refetchOnWindowFocus + refetchInterval 30s + sibling invalidations from 5 order mutations."
    - "DASHBOARD_ORDERS_QUERY_OPTIONS is a named export so the component test asserts the refresh-policy contract without mounting a QueryClient."
  artifacts:
    - path: "apps/web/src/features/dashboard/useDashboardOrdersQuery.ts"
      provides: "useDashboardOrdersQuery hook + DASHBOARD_ORDERS_QUERY_OPTIONS named export"
      contains: "DASHBOARD_ORDERS_QUERY_OPTIONS"
    - path: "apps/web/src/routes/dashboard/DashboardOrdersCard.tsx"
      provides: "Role-discriminated dashboard card component"
      contains: "useDashboardOrdersQuery"
    - path: "apps/web/src/routes/dashboard/DashboardPage.tsx"
      provides: "Responsive 2-column grid mounting both cards"
      contains: "DashboardOrdersCard"
    - path: "apps/web/src/routes/dashboard/__tests__/DashboardOrdersCard.test.tsx"
      provides: "9 component tests covering both subviews + empty states + loading + error + row/section links + query config contract"
      contains: "DASHBOARD_ORDERS_QUERY_OPTIONS"
    - path: "apps/web/src/features/orders/useOrderMutations.ts"
      provides: "5 mutations now also invalidate ['dashboard', 'orders']"
      contains: "['dashboard', 'orders']"
  key_links:
    - from: "apps/web/src/routes/dashboard/DashboardPage.tsx"
      to: "apps/web/src/routes/dashboard/DashboardOrdersCard.tsx"
      via: "grid grid-cols-1 md:grid-cols-2"
      pattern: "DashboardOrdersCard"
    - from: "apps/web/src/routes/dashboard/DashboardOrdersCard.tsx"
      to: "apps/web/src/features/dashboard/useDashboardOrdersQuery.ts"
      via: "const { data, isLoading, isError } = useDashboardOrdersQuery();"
      pattern: "useDashboardOrdersQuery"
    - from: "apps/web/src/features/dashboard/useDashboardOrdersQuery.ts"
      to: "GET /api/dashboard/orders"
      via: "fetchJson<DashboardOrdersResponse>('/api/dashboard/orders')"
      pattern: "/api/dashboard/orders"
    - from: "apps/web/src/features/orders/useOrderMutations.ts"
      to: "['dashboard', 'orders'] cache key"
      via: "queryClient.invalidateQueries({ queryKey: ['dashboard', 'orders'] })"
      pattern: "\\['dashboard', 'orders'\\]"
---

<objective>
Slice C — ORD-09 frontend.

Build `DashboardOrdersCard.tsx` (role-discriminated, two-section, mobile-first), the `useDashboardOrdersQuery` hook with three-layer refresh, the responsive 2-column DashboardPage grid, the 5 mutation invalidations against `['dashboard', 'orders']`, and the 9 component tests. Depends on Slice A (rows use `?from=<row.status>` so the Slice-A helper resolves correctly on back-nav from the detail page) and Slice B (the endpoint + Zod contract this slice consumes).

Purpose: SC#1, #2, #4. The card renders alongside the existing low-stock card; each row/section link pre-selects the correct `/bestallningar` tab; the back-link from `/bestallningar/:id` honors `?from=` set by these row links via the Slice A hook.

Output: One new query hook + one new card component + one new component test file + 5 one-line additions to `useOrderMutations.ts` + DashboardPage body widened to a grid.
</objective>

<execution_context>
@C:/Projekt/MediTrack/.claude/get-shit-done/workflows/execute-plan.md
@C:/Projekt/MediTrack/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-dashboard-depth-back-nav/09-CONTEXT.md
@.planning/phases/09-dashboard-depth-back-nav/09-PATTERNS.md
@CLAUDE.md
@.planning/phases/09-dashboard-depth-back-nav/09-02-PLAN.md

<interfaces>
<!-- Locked contracts from CONTEXT.md `<specifics>` + PATTERNS.md. Executor uses these directly. -->

From `apps/web/src/features/dashboard/useLowStockQuery.ts` (the analog — verbatim shape to mirror):

```ts
import { useQuery } from '@tanstack/react-query';
import { fetchJson, type ApiError } from '@/lib/api';

export const LOW_STOCK_QUERY_OPTIONS = {
  queryKey: ['dashboard', 'low-stock'] as const,
  refetchOnWindowFocus: true as const,
  refetchInterval: 30_000 as const,
};
export function useLowStockQuery() { /* useQuery using LOW_STOCK_QUERY_OPTIONS */ }
```

Phase 9 transformation (locked):
- Hook: `useDashboardOrdersQuery()` → `useQuery<DashboardOrdersResponse, ApiError>`.
- Named const: `DASHBOARD_ORDERS_QUERY_OPTIONS = { queryKey: ['dashboard', 'orders'] as const, refetchOnWindowFocus: true as const, refetchInterval: 30_000 as const }`.
- Endpoint URL: `/api/dashboard/orders`.
- Import `DashboardOrdersResponse` from `@meditrack/shared` (Slice B exports it).

Section header → URL map (from CONTEXT.md `<specifics>` line 334):

| Role | Section 1 header (link) | Section 2 header (link) | Row link target |
|---|---|---|---|
| sjukskoterska | "Egna utkast" → `/bestallningar?status=utkast` | "Senaste beställningar" → `/bestallningar?status=alla` | `/bestallningar/:id?from=<row.status>` |
| apotekare/admin | "Väntar på bekräftelse" → `/bestallningar?status=skickad` | "Väntar på leverans" → `/bestallningar?status=bekraftad` | `/bestallningar/:id?from=<section.status>` |

Celebratory empty state copy (D-147, CONTEXT.md `<specifics>` lines 33–36):
- Nurse: heading `Inga aktiva beställningar.` / sub `Skapa en ny beställning när ni behöver fylla på.`.
- Apotekare/admin: heading `Inga beställningar väntar på åtgärd.` / sub `Allt hängt klart — inget att bekräfta eller leverera just nu.`.

Error copy: `Kunde inte hämta beställningar — försök igen om en stund.` (mirrors the low-stock error copy with the noun swapped).

CardTitle / section header copy (Claude's discretion, recommended in CONTEXT.md):
- Nurse: section 1 = `Egna utkast`; section 2 = `Senaste beställningar`.
- Apotekare/admin: section 1 = `Väntar på bekräftelse`; section 2 = `Väntar på leverans`.
- Count rendered as CardDescription beneath header: `totalt {count}` (mirrors low-stock card pattern).

Row content (mobile-first, 44px touch target):
- Top line: relative time (`formatRelative(row.createdAt)`) + ChevronRight icon.
- Middle line: `Skapad av {row.createdBy.name}` (text-xs text-muted-foreground).
- Bottom line: `{row.lineCount} rader · totalt {row.totalQuantity}` (or `1 rad` for singular).

Grid container (D-145 / D-146, CONTEXT.md `<specifics>` lines 322–329):
```tsx
<div className="grid grid-cols-1 md:grid-cols-2 gap-4 max-w-5xl mx-auto p-4 md:p-6 lg:p-8">
  <DashboardLowStockCard />
  <DashboardOrdersCard />
</div>
```

Five mutation invalidation sites in `useOrderMutations.ts` (D-148; PATTERNS.md exact line numbers):
- `useCreateDraftOrder.onSuccess` (~line 55–57) — after the `['orders', { status: 'utkast' }]` invalidation.
- `useSubmitOrder.onSuccess` (~line 251–256) — after the `['orders', { status: 'utkast' }]` invalidation.
- `useConfirmOrder.onSuccess` (~line 294–301) — after the `['orders', { status: 'bekraftad' }]` invalidation, before the `toast.success('Bekräftad')`.
- `useDeliverOrder.onSuccess` (~line 357–367) — after the existing `['dashboard', 'low-stock']` line at 366. (Adjacent to the existing dashboard invalidation.)
- `useDiscardOrder.onSuccess` (~line 424–429) — after the `['orders', { status: 'utkast' }]` invalidation, before `queryClient.removeQueries`.

Each addition is exactly one line: `void queryClient.invalidateQueries({ queryKey: ['dashboard', 'orders'] });` plus a one-line comment `// Phase 9 D-148: dashboard orders card uses its own dedicated cache key (D-141).`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create useDashboardOrdersQuery hook + add 5 mutation invalidations against ['dashboard', 'orders']</name>
  <files>apps/web/src/features/dashboard/useDashboardOrdersQuery.ts, apps/web/src/features/orders/useOrderMutations.ts</files>
  <read_first>
    - apps/web/src/features/dashboard/useLowStockQuery.ts (full file — verbatim shape to mirror; named-export pattern; testable-without-mount rationale at lines 35–50)
    - apps/web/src/features/orders/useOrderMutations.ts (full file — 5 onSuccess sites at lines ~55–57, ~251–256, ~294–301, ~357–367, ~424–429; note the existing `['dashboard', 'low-stock']` invalidation in useDeliverOrder at line 366 — Phase 9 adds the orders key adjacent to that, NOT replacing)
    - packages/shared/src/contracts/dashboard.ts (Slice B — exports `dashboardOrdersResponse` + `DashboardOrdersResponse` type)
    - apps/web/src/lib/api.ts (re-confirm `fetchJson<T>` signature + `ApiError` type — already used by useLowStockQuery)
    - .planning/phases/09-dashboard-depth-back-nav/09-CONTEXT.md (D-148 — three-layer refresh; D-141 — dedicated cache key)
    - .planning/phases/09-dashboard-depth-back-nav/09-PATTERNS.md (useDashboardOrdersQuery.ts entry locks the named-const + hook shape; useOrderMutations.ts entry lists the 5 exact insertion sites)
  </read_first>
  <behavior>
    - `useDashboardOrdersQuery()` returns a TanStack `UseQueryResult<DashboardOrdersResponse, ApiError>` for `GET /api/dashboard/orders`.
    - `DASHBOARD_ORDERS_QUERY_OPTIONS.queryKey` is `['dashboard', 'orders']` (frozen literal tuple).
    - `DASHBOARD_ORDERS_QUERY_OPTIONS.refetchOnWindowFocus` is `true` (literal).
    - `DASHBOARD_ORDERS_QUERY_OPTIONS.refetchInterval` is `30_000` (literal).
    - After a successful `useCreateDraftOrder` mutation, the `['dashboard', 'orders']` query is invalidated (any active observer refetches).
    - Same invalidation fires after a successful `useSubmitOrder`, `useConfirmOrder`, `useDeliverOrder`, and `useDiscardOrder`.
  </behavior>
  <action>
    Create `apps/web/src/features/dashboard/useDashboardOrdersQuery.ts`:
    - Mirror `useLowStockQuery.ts` line-for-line:
      - Imports: `import { useQuery } from '@tanstack/react-query';` / `import type { DashboardOrdersResponse } from '@meditrack/shared';` / `import { fetchJson, type ApiError } from '@/lib/api';`.
      - Docblock citing D-141 (dedicated endpoint + cache key), D-142 (role-discriminated payload), D-148 (three-layer refresh — refetchOnWindowFocus + refetchInterval + sibling invalidations from useOrderMutations). Mirror the prose density of useLowStockQuery's docblock at lines 4–33; explicitly name the 5 mutation sites Phase 9 wires.
      - Export `const DASHBOARD_ORDERS_QUERY_OPTIONS = { queryKey: ['dashboard', 'orders'] as const, refetchOnWindowFocus: true as const, refetchInterval: 30_000 as const };`. The `as const` matters — it keeps the literal types narrow so the component test can assert the literal `true` / `30_000` / `['dashboard', 'orders']` (mirror useLowStockQuery's pattern verbatim).
      - Export `function useDashboardOrdersQuery()` returning `useQuery<DashboardOrdersResponse, ApiError>({ queryKey: DASHBOARD_ORDERS_QUERY_OPTIONS.queryKey, queryFn: () => fetchJson<DashboardOrdersResponse>('/api/dashboard/orders'), refetchOnWindowFocus: DASHBOARD_ORDERS_QUERY_OPTIONS.refetchOnWindowFocus, refetchInterval: DASHBOARD_ORDERS_QUERY_OPTIONS.refetchInterval })`.

    Edit `apps/web/src/features/orders/useOrderMutations.ts` — add ONE line + ONE comment to each of the 5 `onSuccess` callbacks (do NOT replace any existing line; ADD adjacent):
    - `useCreateDraftOrder.onSuccess` (after the existing `void queryClient.invalidateQueries({ queryKey: ['orders', { status: 'utkast' }] });` line):
      Add:
      `// Phase 9 D-148: dashboard orders card uses its own dedicated cache key (D-141).`
      `void queryClient.invalidateQueries({ queryKey: ['dashboard', 'orders'] });`
    - `useSubmitOrder.onSuccess` (after the existing `['orders', { status: 'utkast' }]` invalidation): same two lines.
    - `useConfirmOrder.onSuccess` (after the existing `['orders', { status: 'bekraftad' }]` invalidation, BEFORE the `toast.success('Bekräftad');` call): same two lines.
    - `useDeliverOrder.onSuccess` (after the existing `['dashboard', 'low-stock']` invalidation at line ~366, BEFORE the `toast.success('Levererad — lagret uppdaterat');` call): same two lines. Result: both `['dashboard', 'low-stock']` AND `['dashboard', 'orders']` are invalidated on deliver — both present in the final code.
    - `useDiscardOrder.onSuccess` (after the existing `['orders', { status: 'utkast' }]` invalidation, BEFORE the `queryClient.removeQueries({ queryKey: ['order', vars.orderId] });` call): same two lines.

    Update the doc-block at the top of each of the 5 mutation function comments (where Phase 6 NTF-02 lines exist or Phase 3 D-* lines exist): add a one-line note `Phase 9 D-148: also invalidates ['dashboard', 'orders']` mirroring how useDeliverOrder's docblock at lines 327–348 currently lists D-119 / D-120 / NTF-02.

    Do NOT modify any existing invalidation; do NOT change any onError, mutationFn, or toast logic.
  </action>
  <verify>
    <automated>pnpm --filter @meditrack/web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Source: `apps/web/src/features/dashboard/useDashboardOrdersQuery.ts` exports both `DASHBOARD_ORDERS_QUERY_OPTIONS` (named const) AND `useDashboardOrdersQuery` (function). Verify: `grep -c "export const DASHBOARD_ORDERS_QUERY_OPTIONS" apps/web/src/features/dashboard/useDashboardOrdersQuery.ts` == 1 AND `grep -c "export function useDashboardOrdersQuery" apps/web/src/features/dashboard/useDashboardOrdersQuery.ts` == 1.
    - Source: `apps/web/src/features/dashboard/useDashboardOrdersQuery.ts` contains `'/api/dashboard/orders'`.
    - Source: `apps/web/src/features/dashboard/useDashboardOrdersQuery.ts` contains `queryKey: ['dashboard', 'orders'] as const`.
    - Source: `apps/web/src/features/dashboard/useDashboardOrdersQuery.ts` contains `refetchInterval: 30_000`.
    - Source: `apps/web/src/features/orders/useOrderMutations.ts` contains `['dashboard', 'orders']` invalidation at exactly 5 sites. Verify (excluding comments): `grep -v '^\s*//' apps/web/src/features/orders/useOrderMutations.ts | grep -c "queryKey: \\['dashboard', 'orders'\\]"` == 5.
    - Source: `apps/web/src/features/orders/useOrderMutations.ts` STILL contains exactly 1 occurrence of `['dashboard', 'low-stock']` (Phase 6 line not removed). Verify: `grep -v '^\s*//' apps/web/src/features/orders/useOrderMutations.ts | grep -c "queryKey: \\['dashboard', 'low-stock'\\]"` == 1.
    - Typecheck: `pnpm --filter @meditrack/web typecheck` exits 0.
  </acceptance_criteria>
  <done>Hook ships; 5 mutation invalidations added; typecheck green; no regression to existing low-stock invalidations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build DashboardOrdersCard.tsx + DashboardOrdersCard.test.tsx (9 tests)</name>
  <files>apps/web/src/routes/dashboard/DashboardOrdersCard.tsx, apps/web/src/routes/dashboard/__tests__/DashboardOrdersCard.test.tsx</files>
  <read_first>
    - apps/web/src/routes/dashboard/DashboardLowStockCard.tsx (full file — analog for loading/error/empty/list states; CheckCircle2 emerald empty-state pattern at lines 84–104; row layout at lines 117–133; CardHeader/CardTitle/CardDescription at lines 108–111)
    - apps/web/src/routes/dashboard/__tests__/DashboardLowStockCard.test.tsx (full file — analog for vi.mock + named-const preserve pattern at lines 33–48; mockQuery helper at lines 53–58; 5 tests including the query-config contract assertion at lines 194–199)
    - apps/web/src/routes/bestallningar/DraftCard.tsx (exports `formatRelative` — reuse for row timestamps; same import path the test analog at line 5 uses)
    - apps/web/test/helpers/renderWithProviders.tsx (MemoryRouter wrapper; `<Link>` works without extra setup)
    - apps/web/src/features/dashboard/useDashboardOrdersQuery.ts (NEW — from Task 1; the hook + named const this component mocks against)
    - packages/shared/src/contracts/dashboard.ts (Slice B — `DashboardOrdersResponse` discriminated union)
    - .planning/phases/09-dashboard-depth-back-nav/09-CONTEXT.md (D-145, D-146, D-147 — layout + empty states; <specifics> lines 33–36 + 247–260 row content + 322–329 grid + 334 section header table)
    - .planning/phases/09-dashboard-depth-back-nav/09-PATTERNS.md (DashboardOrdersCard.tsx entry — full structural mirror; loading skeleton denser per CONTEXT.md `<specifics>` line 140)
  </read_first>
  <behavior>
    - Test 1 — Nurse subview rendering: mock `{ role: 'sjukskoterska', egnaUtkast: { count: 2, rows: [<2 utkast rows>] }, recentHistory: [<3 non-utkast rows>] }` → screen contains "Egna utkast" heading, "Senaste beställningar" heading, "totalt 2" (CardDescription), 2 utkast row links, 3 history row links.
    - Test 2 — Apotekare subview rendering: mock `{ role: 'apotekare', skickad: { count: 3, rows: [<3 skickad rows>] }, bekraftad: { count: 1, rows: [<1 bekraftad row>] } }` → screen contains "Väntar på bekräftelse" heading + "totalt 3" + 3 row links AND "Väntar på leverans" heading + "totalt 1" + 1 row link.
    - Test 3 — Admin subview rendering: mock with `role: 'admin'` and same shape as apotekare → renders identically (no role-specific branching beyond the discriminator).
    - Test 4 — Nurse empty state: mock `{ role: 'sjukskoterska', egnaUtkast: { count: 0, rows: [] }, recentHistory: [] }` → screen contains "Inga aktiva beställningar." heading + "Skapa en ny beställning när ni behöver fylla på." sub; Card has `role="status"`; CheckCircle2 with class `text-emerald-600`.
    - Test 5 — Apotekare empty state: mock `{ role: 'apotekare', skickad: { count: 0, rows: [] }, bekraftad: { count: 0, rows: [] } }` → screen contains "Inga beställningar väntar på åtgärd." heading + "Allt hängt klart — inget att bekräfta eller leverera just nu." sub.
    - Test 6 — Row links carry `?from=<row.status>`: mock a row with `status: 'skickad'` → its anchor's `href` ends with `?from=skickad`.
    - Test 7 — Section header links: mock nurse data → "Egna utkast" anchor `href` ends `?status=utkast`; "Senaste beställningar" anchor `href` ends `?status=alla`. Switch to apotekare data → "Väntar på bekräftelse" `?status=skickad`; "Väntar på leverans" `?status=bekraftad`.
    - Test 8 — Loading state: mock `{ isLoading: true }` → screen contains at least 3 elements with the `animate-pulse` class (Skeleton primitive); no `Beställningar` content rendered.
    - Test 9 — Error state: mock `{ isError: true, error: <ApiError> }` → screen has a `role="alert"` element containing "Kunde inte hämta beställningar — försök igen om en stund."
    - Test 10 — Query config contract (mirror DashboardLowStockCard.test.tsx Test 5 at lines 194–199):
      `expect(DASHBOARD_ORDERS_QUERY_OPTIONS.refetchOnWindowFocus).toBe(true);`
      `expect(DASHBOARD_ORDERS_QUERY_OPTIONS.refetchInterval).toBe(30_000);`
      `expect(DASHBOARD_ORDERS_QUERY_OPTIONS.queryKey).toEqual(['dashboard', 'orders']);`
    - (9 functional tests + 1 contract assertion = 10 `it(` blocks total; CONTEXT.md says "9 tests"; lump tests 2+3 if you prefer to keep exactly 9. Use 9: combine apotekare + admin rendering into one parameterized test via `it.each([...])`.)
  </behavior>
  <action>
    Create `apps/web/src/routes/dashboard/DashboardOrdersCard.tsx`:
    - Imports (mirror DashboardLowStockCard.tsx lines 1–13, swap to the orders domain):
      `import { CheckCircle2, ChevronRight } from 'lucide-react';`
      `import { Link } from 'react-router-dom';`
      `import { useDashboardOrdersQuery } from '@/features/dashboard/useDashboardOrdersQuery';`
      `import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card';`
      `import { Alert, AlertDescription } from '@/components/ui/alert';`
      `import { Skeleton } from '@/components/ui/skeleton';`
      `import { formatRelative } from '@/routes/bestallningar/DraftCard';`
      Do NOT import `LowStockBadge`.

    - Add a docblock citing D-141 / D-142 / D-145 / D-146 / D-147 / D-148, listing the 4 render states (loading / error / data-with-content / data-empty) and the 2 subview structures (nurse / apotekare-or-admin). Mirror the prose density of DashboardLowStockCard.tsx lines 15–49.

    - Export `function DashboardOrdersCard()`:
      - Call `const { data, isLoading, isError } = useDashboardOrdersQuery();`.
      - **Loading branch** — return a Card with TWO stacked sections, each with a header skeleton + 3 row skeletons (denser than the low-stock card because there are two sections — per CONTEXT.md `<specifics>` line 140). Structure: `<Card className="w-full max-w-2xl"><CardContent className="p-4 space-y-4">` containing 2 `<div className="space-y-2">` sections each with `<Skeleton className="h-5 w-32" />` + 3× `<Skeleton className="h-10 w-full" />`.
      - **Error branch** — return `<Card className="w-full max-w-2xl"><CardContent className="p-4"><Alert variant="destructive"><AlertDescription>Kunde inte hämta beställningar — försök igen om en stund.</AlertDescription></Alert></CardContent></Card>`. (Mirrors low-stock error structure verbatim; copy noun swapped to "beställningar".)
      - **Empty branch** (data exists and the role-relevant totals are 0):
        - Define `const isNurseEmpty = data.role === 'sjukskoterska' && data.egnaUtkast.count === 0 && data.recentHistory.length === 0;`
        - Define `const isPharmEmpty = (data.role === 'apotekare' || data.role === 'admin') && data.skickad.count === 0 && data.bekraftad.count === 0;`
        - When either is true, return the celebratory empty Card (mirror DashboardLowStockCard.tsx lines 84–104 verbatim) with heading + sub strings:
          - Nurse: `Inga aktiva beställningar.` / `Skapa en ny beställning när ni behöver fylla på.`
          - Apotekare/admin: `Inga beställningar väntar på åtgärd.` / `Allt hängt klart — inget att bekräfta eller leverera just nu.`
        - Keep `<Card role="status">` + `<CheckCircle2 className="h-12 w-12 text-emerald-600 mx-auto mb-4" aria-hidden="true" />`. Do NOT use `EmptyStateCard` (CONTEXT.md `<code_context>` line 209 — wrong icon color).

      - **Data branch** — render `<Card className="w-full max-w-2xl">` containing two stacked sections. Define a small inline helper or pure render function `function Section({ title, count, statusHref, rows })` that emits:
        - CardHeader containing `<Link to={statusHref}><CardTitle>{title}</CardTitle></Link>` and (when `count !== undefined`) `<CardDescription>totalt {count}</CardDescription>`.
        - CardContent with `role="list"` and `aria-label={title}` containing `rows.map(row => <Link key={row.id} to={\`/bestallningar/${row.id}?from=${row.status}\`} role="listitem" className="flex items-center justify-between py-2 min-h-[44px] hover:bg-muted/50 transition-colors">...</Link>)`. (Each row is a Link with `role="listitem"`; container has `role="list"`. This matches the analog's accessibility semantics.)
        - Row content per CONTEXT.md `<specifics>` lines 247–260 + locked above:
          - Left: a column with `<span className="text-sm font-semibold">{formatRelative(row.createdAt)}</span>` on top, `<span className="text-xs text-muted-foreground">Skapad av {row.createdBy.name}</span>` below, `<span className="text-xs text-muted-foreground">{row.lineCount} {row.lineCount === 1 ? 'rad' : 'rader'} · totalt {row.totalQuantity}</span>` below that.
          - Right: `<ChevronRight className="h-4 w-4 text-muted-foreground" aria-hidden="true" />`.
        - When `rows.length === 0` for a non-empty other section (so neither overall empty branch fires), render a tiny `<p className="text-xs text-muted-foreground px-2 py-2">Inga rader.</p>` placeholder so the section keeps its header but doesn't crash on empty array iteration. (Edge case: nurse has 2 utkast but 0 history — the history section needs SOMETHING. Use this placeholder.)
      - Discriminate via `if (data.role === 'sjukskoterska')` returning a Card with `<Section title="Egna utkast" count={data.egnaUtkast.count} statusHref="/bestallningar?status=utkast" rows={data.egnaUtkast.rows} />` and `<Section title="Senaste beställningar" statusHref="/bestallningar?status=alla" rows={data.recentHistory} />` (no count on the second — D-144 says recent history is the row list itself; recentHistory has no count field in the contract).
      - Else (apotekare/admin via the discriminated union narrowing): render `<Section title="Väntar på bekräftelse" count={data.skickad.count} statusHref="/bestallningar?status=skickad" rows={data.skickad.rows} />` and `<Section title="Väntar på leverans" count={data.bekraftad.count} statusHref="/bestallningar?status=bekraftad" rows={data.bekraftad.rows} />`.

    Create `apps/web/src/routes/dashboard/__tests__/DashboardOrdersCard.test.tsx`:
    - Mock module pattern verbatim from `DashboardLowStockCard.test.tsx` lines 33–48 (the `vi.importActual` + spread + `vi.fn()` preserves the named const for the contract test):
      ```ts
      vi.mock('@/features/dashboard/useDashboardOrdersQuery', async () => {
        const actual = await vi.importActual<typeof import('@/features/dashboard/useDashboardOrdersQuery')>(
          '@/features/dashboard/useDashboardOrdersQuery',
        );
        return { ...actual, useDashboardOrdersQuery: vi.fn() };
      });
      import { useDashboardOrdersQuery, DASHBOARD_ORDERS_QUERY_OPTIONS } from '@/features/dashboard/useDashboardOrdersQuery';
      const mockUseDashboardOrdersQuery = vi.mocked(useDashboardOrdersQuery);
      ```
    - `mockQuery(state)` helper analogous to the analog at lines 53–58, parameterized on `DashboardOrdersResponse`.
    - 9 tests (combine the apotekare + admin subview test using `it.each`):
      - Test 1: Nurse subview rendering — 2 utkast rows + 3 history rows; assert both headings + "totalt 2".
      - Test 2: it.each(['apotekare', 'admin']) Pharmacist/Admin subview rendering — assert both headings (`Väntar på bekräftelse` / `Väntar på leverans`) + count CardDescriptions; same shape rendered for both roles.
      - Test 3: Nurse empty state — assert "Inga aktiva beställningar." heading + sub + emerald CheckCircle2 (use `container.querySelector('.text-emerald-600')`).
      - Test 4: Apotekare empty state — assert "Inga beställningar väntar på åtgärd." heading + sub.
      - Test 5: Row link includes `?from=<row.status>` — mock a single skickad row in pharmacist's `skickad.rows`; query the anchor by role + accessible-name; assert `getAttribute('href')` matches `/^\/bestallningar\/[^?]+\?from=skickad$/`.
      - Test 6: Section header links — assert all 4 mapping rows in the section header → URL table from `<interfaces>` above (run as 2 sub-tests: one for nurse, one for apotekare).
      - Test 7: Loading — mock `{ isLoading: true }`; assert at least 3 `[data-slot="skeleton"]` or `.animate-pulse` matches.
      - Test 8: Error — mock `{ isError: true, error: { envelope: { error: { code: 'internal_error', message: 'x' } } } }`; assert `screen.getByRole('alert')` contains the Swedish error copy.
      - Test 9: Query config contract — assert the 3 properties of `DASHBOARD_ORDERS_QUERY_OPTIONS` exactly as the analog's Test 5 does.
    - Use `renderWithProviders` (from `../../../../test/helpers/renderWithProviders`) so `<Link>` works inside MemoryRouter without extra wiring.
    - Type-assert mock data against `DashboardOrdersResponse` so a contract drift in Slice B's discriminated union breaks the test in addition to the runtime parse.
  </action>
  <verify>
    <automated>pnpm --filter @meditrack/web test -- DashboardOrdersCard</automated>
  </verify>
  <acceptance_criteria>
    - Source: `apps/web/src/routes/dashboard/DashboardOrdersCard.tsx` contains all of: `useDashboardOrdersQuery`, `formatRelative`, `?from=${row.status}`, `text-emerald-600`, `role="list"`, `role="listitem"`, `Inga aktiva beställningar.`, `Inga beställningar väntar på åtgärd.`, `Väntar på bekräftelse`, `Väntar på leverans`, `Egna utkast`, `Senaste beställningar`.
    - Source: `apps/web/src/routes/dashboard/DashboardOrdersCard.tsx` does NOT import `LowStockBadge` and does NOT import `EmptyStateCard`. Verify: `grep -c "LowStockBadge\\|EmptyStateCard" apps/web/src/routes/dashboard/DashboardOrdersCard.tsx` == 0.
    - Source: `apps/web/src/routes/dashboard/DashboardOrdersCard.tsx` contains at least 4 `<Link to=` references (2 section headers per subview × 2 subviews = 4 distinct destinations; rows add more). Verify: `grep -c "<Link to=" apps/web/src/routes/dashboard/DashboardOrdersCard.tsx` >= 4.
    - Source: `apps/web/src/routes/dashboard/__tests__/DashboardOrdersCard.test.tsx` contains exactly 9 `it(` blocks (counting `it.each` as one). Verify: `grep -c "^  it(\\|^  it\\.each(" apps/web/src/routes/dashboard/__tests__/DashboardOrdersCard.test.tsx` == 9 (allow tolerance ±1 if it.each idiom expands counted block weirdly).
    - Source: test file contains `DASHBOARD_ORDERS_QUERY_OPTIONS.refetchOnWindowFocus` and `DASHBOARD_ORDERS_QUERY_OPTIONS.refetchInterval` and `DASHBOARD_ORDERS_QUERY_OPTIONS.queryKey`.
    - Test: `pnpm --filter @meditrack/web test -- DashboardOrdersCard` exits 0 with all tests passing.
    - Behavior: With a mocked skickad row, the rendered row anchor's `href` ends `?from=skickad`.
    - Behavior: With nurse data, section header anchors are `/bestallningar?status=utkast` and `/bestallningar?status=alla`.
  </acceptance_criteria>
  <done>Component renders correctly for all 4 states across both subviews + admin role; row links + section header links honor the locked table; 9 tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Widen DashboardPage to the 2-column grid mounting both cards</name>
  <files>apps/web/src/routes/dashboard/DashboardPage.tsx</files>
  <read_first>
    - apps/web/src/routes/dashboard/DashboardPage.tsx (current 18-line file — replace body)
    - apps/web/src/routes/dashboard/DashboardLowStockCard.tsx (existing — confirm no chrome changes needed; the card already declares its own `max-w-2xl`)
    - apps/web/src/routes/dashboard/DashboardOrdersCard.tsx (NEW — from Task 2)
    - .planning/phases/09-dashboard-depth-back-nav/09-CONTEXT.md (D-145, D-146; <specifics> lines 322–329 — locked grid markup)
    - .planning/phases/09-dashboard-depth-back-nav/09-PATTERNS.md (DashboardPage.tsx entry — locked final shape)
  </read_first>
  <behavior>
    - `/dashboard` renders both cards side-by-side at viewports ≥ 768px (md+) and stacks them vertically at < 768px.
    - At every breakpoint (360 / 768 / 1024 / 1440), the container does not overflow the viewport horizontally (`scrollWidth <= innerWidth` — same SC#4 constraint that Phase 7's sc04 harness checks).
    - Low-stock card is first in DOM order; on `<md` it appears above the orders card; on `md+` it appears to the left.
    - No `<h1>` is added — CardTitles inside the cards remain the page's primary headings (D-118 preserved).
  </behavior>
  <action>
    Edit `apps/web/src/routes/dashboard/DashboardPage.tsx`:
    - Add import: `import { DashboardOrdersCard } from './DashboardOrdersCard';` next to the existing `import { DashboardLowStockCard } from './DashboardLowStockCard';`.
    - Replace the body of `DashboardPage()` (the current `return <DashboardLowStockCard />;`) with the locked grid markup from CONTEXT.md `<specifics>` lines 322–329:
      `<div className="grid grid-cols-1 md:grid-cols-2 gap-4 max-w-5xl mx-auto p-4 md:p-6 lg:p-8">`
      `  <DashboardLowStockCard />`
      `  <DashboardOrdersCard />`
      `</div>`
    - Update the docblock above `DashboardPage` to cite D-145 (responsive 2-column grid) and D-146 (low-stock left/top, orders right/bottom). Keep the existing D-118 statement (no h1, no AppShell chrome change) and broaden it: "Phase 9 widens this to a sibling grid of two components — same principle (no chrome change beyond the route body)."
    - Do NOT add an `<h1>`. Do NOT change AppShell. Do NOT introduce a `lg:grid-cols-3` breakpoint (CONTEXT.md `<discretion>` line 141 — no third card today).
  </action>
  <verify>
    <automated>pnpm --filter @meditrack/web typecheck &amp;&amp; pnpm --filter @meditrack/web test -- DashboardPage</automated>
  </verify>
  <acceptance_criteria>
    - Source: `apps/web/src/routes/dashboard/DashboardPage.tsx` imports BOTH `DashboardLowStockCard` and `DashboardOrdersCard`.
    - Source: `apps/web/src/routes/dashboard/DashboardPage.tsx` contains the exact className `grid grid-cols-1 md:grid-cols-2 gap-4 max-w-5xl mx-auto p-4 md:p-6 lg:p-8`. Verify: `grep -c "grid-cols-1 md:grid-cols-2" apps/web/src/routes/dashboard/DashboardPage.tsx` >= 1.
    - Source: `apps/web/src/routes/dashboard/DashboardPage.tsx` does NOT contain an `<h1>` element. Verify: `grep -c "<h1" apps/web/src/routes/dashboard/DashboardPage.tsx` == 0.
    - Source: DOM order — `<DashboardLowStockCard />` appears before `<DashboardOrdersCard />` in the JSX.
    - Typecheck: `pnpm --filter @meditrack/web typecheck` exits 0.
    - Test: existing `DashboardPage`-related tests (if any) still pass; no new test required at the page level — the two child components have their own tests.
    - Manual: at `/dashboard` after `docker compose up`, low-stock card is left of orders card on a 1024px viewport; cards stack vertically (low-stock on top) at 360px.
  </acceptance_criteria>
  <done>DashboardPage hosts the 2-column grid with both cards; typecheck green; ready for the SC#4 sc04 harness re-capture (post-phase chore, not a Phase 9 commit per CONTEXT.md `<specifics>` line 351).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → `GET /api/dashboard/orders` | Read of role-discriminated dashboard payload (Slice B). Session-gated. |
| Client → `<Link to="/bestallningar/:id?from=<row.status>" />` | URL navigation only; back-link helper validates `?from=` against the closed StatusTab union (Slice A). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-09 | Information Disclosure | Stale dashboard counts shown after another user's mutation | mitigate | Three-layer refresh per D-148: refetchOnWindowFocus + refetchInterval (30s) + sibling invalidations from 5 order mutations. An apotekare's confirmation drops the Skickad count on every other apotekare's dashboard within 30s (or instantly on window focus / their own mutation). |
| T-09-10 | Tampering | Row link's `?from=<row.status>` is read from server payload; if the server were compromised it could inject a bogus value | accept | `?from=` is a decorative back-nav hint. The Slice A helper validates against the closed StatusTab union (D-156); invalid values silently fall back to `order.status` or bare `/bestallningar`. Severity: trivial. |
| T-09-11 | Denial of Service | 30s polling load | accept | TanStack Query pauses interval polling on hidden tabs by default. The endpoint is cheap (4 simple `where: { careUnitId, ... }` queries with `take: 5` + `count`). Same disposition as Phase 6 D-119 for the low-stock dashboard. |
| T-09-12 | Spoofing | Component renders apotekare data for a nurse session due to a mock leak | mitigate | The card discriminates on `data.role` (Zod-validated server-side per Slice B). The mock-fixture pattern in the test file uses `DashboardOrdersResponse` as the static type so a contract drift in Slice B fails the test before runtime. |

No `medium` or `high` severities. The FE changes do not introduce new trust boundaries beyond URL-state plumbing (covered by the Slice A validation) and a new GET (covered by Slice B's careUnitId-first service).
</threat_model>

<verification>
- `pnpm --filter @meditrack/web typecheck` exits 0.
- `pnpm --filter @meditrack/web test -- DashboardOrdersCard` exits 0 (all 9 tests pass).
- `pnpm --filter @meditrack/web test -- useDashboardOrdersQuery` if a hook-level test file exists (none planned — the component test asserts the named-const contract).
- `pnpm --filter @meditrack/web build` exits 0.
- Manual demo path: log in as sjukskoterska → `/dashboard` → see both cards side-by-side at ≥md; nurse subview shows Egna utkast + Senaste beställningar. Click "Egna utkast" header → land on `/bestallningar?status=utkast`. Click a Senaste beställningar row → land on the order detail with `?from=<status>`; click "Tillbaka till beställningar" → return to the matching status tab (back-link uses Slice A's helper). Log out, log in as apotekare → see Väntar på bekräftelse + Väntar på leverans; deliver one order → both `['dashboard', 'low-stock']` and `['dashboard', 'orders']` refresh.
- Post-phase chore (NOT this slice): re-run `apps/web/scripts/captureSc04Screenshots.ts` to refresh `docs/screenshots/sc04-360-dashboard.png`. CONTEXT.md `<specifics>` line 351–353 captures this as a follow-on, not a Phase 9 commit per se.
</verification>

<success_criteria>
- ORD-09 SC#1 fully delivered: dashboard renders the role-scoped Beställningar card alongside the low-stock card.
- ORD-09 SC#2 fully delivered: each card item links to the corresponding `/bestallningar` tab and pre-selects it (via the URL `?status=<tab>` query string already wired in Phase 4).
- ORD-09 SC#4 fully delivered: dashboard card rows include `?from=<row.status>` so the Slice A back-link helper resolves correctly when the user returns from the detail view.
- D-145 / D-146 / D-147 / D-148 fully reified in the layout, copy, and refresh strategy.
- DASHBOARD_ORDERS_QUERY_OPTIONS named export makes the refresh-policy contract testable without mounting a QueryClient.
- Five mutation invalidations land alongside (NOT replacing) the existing `['dashboard', 'low-stock']` invalidations; both keys refresh independently as designed.
</success_criteria>

<output>
Create `.planning/phases/09-dashboard-depth-back-nav/09-03-SUMMARY.md` when done, citing D-141..D-148, listing the new files + the 5 mutation invalidation sites, and noting the SC#4 sc04 harness re-capture as a follow-on chore (NOT a Phase 9 commit per CONTEXT.md guidance).
</output>
