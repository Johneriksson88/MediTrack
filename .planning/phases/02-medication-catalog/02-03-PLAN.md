---
phase: 02-medication-catalog
plan: 03
type: execute
wave: 3
depends_on: [02-01, 02-02]
files_modified:
  - apps/api/src/routes/medications/update.ts
  - apps/api/src/routes/medications/index.ts
  - apps/api/src/services/medication.service.ts
  - apps/web/src/features/medications/useMedicationMutations.ts
  - apps/web/src/components/InlineEditThreshold.tsx
  - apps/web/src/routes/lakemedel/MedicationSheet.tsx
  - apps/web/src/routes/lakemedel/MedicationTable.tsx
  - apps/web/src/routes/lakemedel/MedicationCard.tsx
  - apps/web/src/routes/lakemedel/LakemedelPage.tsx
autonomous: true
requirements: [CAT-06]

must_haves:
  truths:
    - "Per D-20 Slice 3 + CAT-06: an apotekare/admin can click a row/card, see the edit Sheet pre-filled, change Lager and/or Tröskel, click Spara → row updates in the list within ~200 ms"
    - "Per D-32 + CAT-06: for `Medication.source = 'npl'`, only `currentStock` and `lowStockThreshold` are editable; name/atc/form/strength render as read-only labels with a `<NplBadge>Från NPL · namn / form / styrka är låsta</NplBadge>`"
    - "Per D-32: for `Medication.source = 'user'`, ALL of name/atcCode/form/strength/currentStock/lowStockThreshold are editable in the Sheet"
    - "Per D-32 + security: the BE service `updateCareUnitMedication` REJECTS name/atc/form/strength updates when the underlying Medication.source === 'npl' even if the request body includes them — strips them server-side before the Prisma update (defense-in-depth against tampering)"
    - "Per D-36: a sjukskoterska clicking a row opens the Sheet in `mode='view'` — fields are read-only, the footer shows only `Stäng`, no `Spara`, no `Ta bort`; the sheet title appends ` · Visning`"
    - "Per D-41 + D-42 + UI-SPEC §5: tröskel is also editable INLINE on the table row + mobile card — click the number → input → Enter/blur saves → row updates optimistically; on error rollback + sonner toast `Kunde inte spara — försök igen.`"
    - "Per D-42: Sheet-based saves are PESSIMISTIC (await PATCH, invalidate `['medications']`, then close Sheet + show `Sparat` toast). Only inline-edit threshold is optimistic. The mixed strategy is intentional and scoped per surface."
    - "Per D-16 + tenancy: PATCH /api/medications/:careUnitMedicationId returns 404 when the id belongs to another vårdenhet (not 403 — to prevent existence-probing); same response shape as truly-not-found rows"
    - "Per D-19 + UI-SPEC §\"Interaction State Matrix\": Sheet save errors keep the Sheet open + show toast `Kunde inte spara — försök igen.`; success closes the Sheet + invalidates `['medications']` + shows `Sparat`"
  artifacts:
    - path: "apps/api/src/routes/medications/update.ts"
      provides: "PATCH /api/medications/:careUnitMedicationId with requireSession + requirePermission('medication:update')"
    - path: "apps/api/src/services/medication.service.ts"
      provides: "updateCareUnitMedication(careUnitId, careUnitMedicationId, payload) — strips NPL-locked fields, 404s on cross-tenant access"
      contains: "updateCareUnitMedication"
    - path: "apps/web/src/features/medications/useMedicationMutations.ts"
      provides: "useUpdateMedication() — pessimistic PATCH for Sheet saves; useUpdateThresholdOptimistic() for inline edits"
      contains: "useUpdateMedication"
    - path: "apps/web/src/components/InlineEditThreshold.tsx"
      provides: "Click-to-edit number input with optimistic onMutate + onError rollback"
    - path: "apps/web/src/routes/lakemedel/MedicationSheet.tsx"
      provides: "Full Sheet with create/edit/view modes; mode='edit' supports NPL-locked + user-source variants; mode='view' for sjukskoterska"
  key_links:
    - from: "apps/web/src/routes/lakemedel/MedicationSheet.tsx"
      to: "/api/medications/:id"
      via: "useUpdateMedication -> fetchJson PATCH"
      pattern: "useUpdateMedication"
    - from: "apps/web/src/components/InlineEditThreshold.tsx"
      to: "/api/medications/:id"
      via: "useUpdateThresholdOptimistic -> onMutate cache snapshot + PATCH"
      pattern: "onMutate.*cancelQueries"
    - from: "apps/api/src/services/medication.service.ts"
      to: "prisma.careUnitMedication.update"
      via: "scope-validated update with NPL-field stripping"
      pattern: "source === 'npl'"
---

<objective>
Ship Slice 3 of Phase 2 per D-20 — the edit surface (RBAC-aware Sheet + inline-edit threshold). Wave 3; depends on Slice 1 for the schema + service skeleton + route barrel + badge components + Sheet primitive, AND on Slice 2 having landed first because both slices touch LakemedelPage.tsx (filter wiring → then edit-mode Sheet expansion).

Purpose: Without this, the catalog is read-only past creation. Slice 3 lights up CAT-06 (edit existing medication) AND validates the two architectural patterns the reviewer will probe: ownership-based field locking (NPL meds vs user-created), and the mixed optimistic/pessimistic mutation strategy (per-surface) per D-42.

Output: PATCH /api/medications/:id BE route + service function, new `useUpdateMedication` + `useUpdateThresholdOptimistic` hooks, full `MedicationSheet` (create+edit+view), new `InlineEditThreshold` component, wired into both `MedicationTable` and `MedicationCard`.
</objective>

<execution_context>
@C:/Projekt/MediTrack/.claude/get-shit-done/workflows/execute-plan.md
@C:/Projekt/MediTrack/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-medication-catalog/02-CONTEXT.md
@.planning/phases/02-medication-catalog/02-PATTERNS.md
@.planning/phases/02-medication-catalog/02-UI-SPEC.md
@.planning/phases/02-medication-catalog/02-01-PLAN.md

<interfaces>
<!-- From Plan 01 -->

From packages/shared (Plan 01 produces):
- medicationUpdateRequest (NOTE: Plan 01 ships create only; this plan ADDS medicationUpdateRequest to the same medication.ts file — see Task 1 below)
- MedicationListItem, MedicationListResponse types
- NplBadge component
- LowStockBadge component

From apps/api/src/services/medication.service.ts (Plan 01):
- listMedicationsForUnit, searchGlobalMedications, createCareUnitMedication exported
- toListItem helper (the row → MedicationListItem mapper)
- This plan EXTENDS the file with updateCareUnitMedication

From apps/api/src/routes/medications/index.ts (Plan 01):
- medicationRoutes(app) currently registers list, search, create
- This plan adds the update route to the barrel

From apps/api/src/plugins/errorHandler.ts (Plan 01):
- NotFoundError, ForbiddenScopeError available

From apps/web/src/features/medications/useMedicationMutations.ts (Plan 01):
- useCreateMedication exported
- This plan adds useUpdateMedication (pessimistic) + useUpdateThresholdOptimistic (optimistic)

From apps/web/src/routes/lakemedel/MedicationSheet.tsx (Plan 01):
- Stub edit/view modes ship in Plan 01; this plan fully implements them.

From apps/web/src/routes/lakemedel/MedicationTable.tsx and MedicationCard.tsx (Plan 01):
- Tröskel cell currently renders the bare number with a `// TODO Plan 03` comment; this plan replaces it with `<InlineEditThreshold>`.
- Row/Card click currently has placeholder edit-mode wiring; this plan completes it.

From apps/web/src/auth/useCan.ts:
- useCan('medication:update') => boolean (used to drive Sheet mode='view' for sjukskoterska)

</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add medicationUpdateRequest Zod schema + update.ts route + extend service with updateCareUnitMedication (NPL-field-stripping, scope-safe)</name>
  <read_first>
    - packages/shared/src/contracts/medication.ts (Plan 01 — extend, do NOT rewrite)
    - packages/shared/src/index.ts (barrel — add re-exports)
    - apps/api/src/services/medication.service.ts (Plan 01 — extend with update function)
    - apps/api/src/routes/medications/create.ts (Plan 01 — mirror its structure for update.ts)
    - apps/api/src/routes/medications/index.ts (barrel — append the update registration)
    - .planning/phases/02-medication-catalog/02-PATTERNS.md §"apps/api/src/services/medication.service.ts" (updateCareUnitMedication block)
    - .planning/phases/02-medication-catalog/02-CONTEXT.md D-32 (NPL-field lock), D-16 (careUnitId-first), D-19 (404 vs 403)
  </read_first>
  <action>
    A. packages/shared/src/contracts/medication.ts — APPEND (do not rewrite) the `medicationUpdateRequest` schema. Use `z.object({ ... }).strict()` so unknown keys are rejected at the boundary. The field set: `currentStock` (optional `z.number().int().min(0)`), `lowStockThreshold` (optional `z.number().int().min(1)`), `name` (optional `z.string().min(1)`), `atcCode` (optional `z.string().regex(/^[A-V][0-9A-Z]{0,6}$/)` — ATC code shape; the regex enforces "first char A-V, total length 1-7" to keep junk values from landing on user-source rows), `form` (optional `z.string().min(1)`), `strength` (optional and explicitly `.nullable()` so the FE can clear an existing strength). Wrap the resulting object with `.refine((d) => Object.keys(d).length > 0, { message: 'Minst ett fält måste anges.' })` so an empty PATCH body returns 400 validation_failed instead of a no-op 200. Export the inferred type as `MedicationUpdateRequest`.

       Add a JSDoc preamble above the schema citing D-32: "name/atcCode/form/strength are accepted by the schema for user-source meds, but the service (`updateCareUnitMedication`) strips them server-side when the underlying Medication.source === 'npl' — defense in depth; the FE for NPL meds also hides those fields."

    B. packages/shared/src/index.ts — append `export { medicationUpdateRequest, type MedicationUpdateRequest } from './contracts/medication.js';` (or merge into the existing barrel re-export from Plan 01).

    C. apps/api/src/services/medication.service.ts — APPEND `updateCareUnitMedication(careUnitId: string, careUnitMedicationId: string, payload: MedicationUpdateRequest): Promise<MedicationListItem>`. Import `NotFoundError` from `'../plugins/errorHandler.js'` (already added in Plan 01) and the `Prisma` namespace type from `'@prisma/client'` for the nested update typing if needed. The function follows the analog `user.service.ts` lines from PATTERNS.md (Pattern D / D-16 careUnitId-first) and proceeds through these steps:

       1. **Scoped reload with medication joined.** Issue a single `prisma.careUnitMedication.findUnique({ where: { id: careUnitMedicationId }, include: { medication: true } })`. The include is mandatory — the source field on the joined Medication row drives the NPL-strip branch in step 3.

       2. **Existence + scope check (D-19 — return 404, never 403).** If the row is absent, OR `row.careUnitId !== careUnitId`, OR `row.deletedAt !== null`, throw `new NotFoundError('Läkemedlet hittades inte.')`. All three conditions collapse to the same envelope so cross-tenant existence-probing leaks nothing (T-02-13). Do NOT branch to ForbiddenScopeError on the cross-tenant path — the canonical posture for tenancy violations across this phase is 404.

       3. **NPL-field strip (D-32 — defense in depth).** Build a fresh `updateData` object. Always carry `currentStock` and `lowStockThreshold` from `payload` when defined. If `row.medication.source === 'user'`, additionally accept `name`, `atcCode`, `form`, `strength` from `payload` and attach them as a nested `medication: { update: { ... } }` Prisma write. If `row.medication.source === 'npl'`, those four fields are silently dropped — even when the request body includes them. (The FE for NPL meds already hides the inputs, so a body that carries them is either an out-of-date client or an explicit tamper attempt.) After stripping, if the resulting `updateData` is empty, short-circuit by mapping the already-loaded row through `toListItem` and returning it — no Prisma update needed.

       4. **Apply the update.** Call `prisma.careUnitMedication.update({ where: { id: careUnitMedicationId }, data: updateData, include: { medication: true } })`. The Prisma typed update keeps the careUnitId column untouched (Prisma cannot change relation columns via this surface), so tenancy cannot be flipped through the update path.

       5. **Defensive scope re-check.** If the returned row's `careUnitId` does not equal the input `careUnitId`, throw a generic `Error('Tenancy invariant violated')`. This is paranoia against a future Prisma bug or middleware misconfiguration — it should never fire.

       6. **Return.** Map the updated row through the shared `toListItem` helper (Plan 01 Task 5) and return — the response shape matches `medicationListItem`, identical to what the list endpoint returns for the same row.

       Cite D-32, D-19, and the analog `user.service.ts` block in PATTERNS.md as the source-of-shape in a JSDoc preamble above the function.

    D. apps/api/src/routes/medications/update.ts (NEW) — mirror create.ts structure exactly. Import `FastifyInstance` (type), `ZodTypeProvider` (type), `z` (from 'zod'), the shared schemas `medicationUpdateRequest` and `medicationListItem`, the auth preHandlers `requireSession` + `requirePermission`, and the service function `updateCareUnitMedication`. Export `async function updateMedicationRoute(app: FastifyInstance)`. Inside: bind `app.withTypeProvider<ZodTypeProvider>()` to a local `r`, then call `r.patch('/api/medications/:careUnitMedicationId', { preHandler: [requireSession, requirePermission('medication:update')], schema: { params: z.object({ careUnitMedicationId: z.string().min(1) }), body: medicationUpdateRequest, response: { 200: medicationListItem } } }, async (req) => updateCareUnitMedication(req.user!.careUnitId, req.params.careUnitMedicationId, req.body))`. preHandler order is fixed: requireSession first, requirePermission second. Header JSDoc citing D-15 preHandler order, D-32 NPL-field strip, D-19 (404 not 403 on cross-tenant), and CAT-06.

    E. apps/api/src/routes/medications/index.ts — append `await app.register(updateMedicationRoute);` after the create registration. Order in the barrel: list → search → create → update. (delete ships in Plan 04.)
  </action>
  <verify>
    <automated>pnpm --filter @meditrack/api typecheck && pnpm --filter @meditrack/api test --run --no-coverage</automated>
  </verify>
  <acceptance_criteria>
    - Source: `packages/shared/src/contracts/medication.ts` contains `export const medicationUpdateRequest = z.object({` and `.refine((d) => Object.keys(d).length > 0`.
    - Source: `packages/shared/src/index.ts` re-exports `medicationUpdateRequest` and `MedicationUpdateRequest`.
    - Source: `apps/api/src/services/medication.service.ts` exports `updateCareUnitMedication` with signature `(careUnitId: string, careUnitMedicationId: string, payload: MedicationUpdateRequest)`.
    - Source: the service function contains the literal substring `row.medication.source === 'user'` (NPL strip branch).
    - Source: the service throws `new NotFoundError('Läkemedlet hittades inte.')` on cross-tenant access (NOT a ForbiddenScopeError).
    - File exists: `apps/api/src/routes/medications/update.ts`.
    - Source: update.ts uses `preHandler: [requireSession, requirePermission('medication:update')]`.
    - Source: `apps/api/src/routes/medications/index.ts` registers `updateMedicationRoute`.
    - Behavior: with an apotekare session, `curl -X PATCH -H 'Content-Type: application/json' -b cookies.txt -d '{"lowStockThreshold":42}' http://localhost:3000/api/medications/<a-real-id-from-the-seeded-careUnit>` returns 200 with `lowStockThreshold === 42`.
    - Behavior: same PATCH against an ID from another vårdenhet returns 404 `{ error: { code: 'not_found', ... } }` (NOT 403).
    - Behavior: PATCH against an NPL-sourced row with body `{"name":"Hacked"}` returns 200 with the ORIGINAL name (stripped server-side); the diff between request and response demonstrates the strip.
    - Behavior: PATCH against a user-sourced row with body `{"name":"Renamed"}` returns 200 with `name === 'Renamed'`.
    - Behavior: PATCH from a sjukskoterska session returns 403.
  </acceptance_criteria>
  <done>BE update path is live, scope-safe, NPL-strip-safe. The Sheet save in Task 3 calls this route.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add useUpdateMedication (pessimistic) + useUpdateThresholdOptimistic + build InlineEditThreshold component</name>
  <read_first>
    - apps/web/src/features/medications/useMedicationMutations.ts (Plan 01 — currently has useCreateMedication; this task adds two more hooks)
    - apps/web/src/features/auth/useLogin.ts (useMutation + queryClient pattern)
    - apps/web/src/lib/api.ts (fetchJson + ApiError)
    - .planning/phases/02-medication-catalog/02-PATTERNS.md §"InlineEditThreshold" (optimistic onMutate/onError pattern)
    - .planning/phases/02-medication-catalog/02-UI-SPEC.md §5 "Inline-Edit Threshold" (full state matrix)
    - .planning/phases/02-medication-catalog/02-CONTEXT.md D-41, D-42
  </read_first>
  <action>
    A. Extend `apps/web/src/features/medications/useMedicationMutations.ts` with two named exports. Both hooks share the query-key shape declared by Plan 01's `must_haves.key_links` (`['medications', filters]` for the list, `['medication-search', q]` for the typeahead); invalidations target those same keys so cache freshness is consistent across slices.

       1. **`useUpdateMedication()` — pessimistic Sheet save (D-42).** Wrap a TanStack `useMutation` typed as `<MedicationListItem, ApiError, { careUnitMedicationId: string; payload: MedicationUpdateRequest }>`. The `mutationFn` calls `apiClient` / `fetchJson<MedicationListItem>(\`/api/medications/${careUnitMedicationId}\`, { method: 'PATCH', body: JSON.stringify(payload) })`. On success: `queryClient.invalidateQueries({ queryKey: ['medications'] })` then `queryClient.invalidateQueries({ queryKey: ['medication-search'] })` (search may surface the updated row's new name for user-source meds), then `toast.success('Sparat')`. On error: `toast.error('Kunde inte spara — försök igen.')`. No optimistic update — the caller (`MedicationSheet`) keeps the Sheet open until the PATCH resolves.

       2. **`useUpdateThresholdOptimistic()` — optimistic inline-edit (D-42, scoped to threshold edits only).** Wrap `useMutation<MedicationListItem, ApiError, { careUnitMedicationId: string; lowStockThreshold: number }, { snapshot: [readonly unknown[], unknown][] }>`. The `mutationFn` calls the same PATCH endpoint with just `{ lowStockThreshold }` in the body. Hook lifecycle:

          - `onMutate({ careUnitMedicationId, lowStockThreshold })`: first `await queryClient.cancelQueries({ queryKey: ['medications'] })` to keep an in-flight refetch from clobbering the optimistic write. Snapshot all matching cache entries via `const snapshot = queryClient.getQueriesData({ queryKey: ['medications'] })`. Then `queryClient.setQueriesData({ queryKey: ['medications'] }, (old: MedicationListResponse | undefined) => ...)` — for every cached page, map `rows` to flip the matching `careUnitMedicationId` row's `lowStockThreshold` to the new value, and recompute `belowThresholdTotal` locally by re-counting rows whose `currentStock < lowStockThreshold` under the new threshold (the server will replace it on the next invalidate, but the local recount keeps the LowStockBanner count consistent during the optimistic window). Return `{ snapshot }` as the mutation context.

          - `onError(_err, _vars, ctx)`: roll back by iterating `ctx?.snapshot` and calling `queryClient.setQueryData(key, val)` for each `[key, val]` entry — this restores every cache page that was mutated by `onMutate`. Then `toast.error('Kunde inte spara — försök igen.')`. No success toast on the happy path — D-42 calls inline-edit silent on success (the in-place number flip is the feedback).

          - `onSettled`: `queryClient.invalidateQueries({ queryKey: ['medications'] })` so the server-authoritative `belowThresholdTotal` replaces the locally-recomputed value, regardless of success or error.

       Add a JSDoc preamble above the optimistic hook citing D-42 + UI-SPEC §5 and reminding the reader that the Sheet uses `useUpdateMedication` (pessimistic) — the mixed strategy is intentional and scoped per surface.

    B. Create `apps/web/src/components/InlineEditThreshold.tsx` with prop shape `{ careUnitMedicationId: string; medicationName: string; value: number }`. Internal state: `editing: boolean` (default `false`) and `localValue: number` (initialized to the `value` prop; re-synced via a `useEffect` whenever the `value` prop changes so a parent re-render with a new server-authoritative value doesn't get clobbered). The component calls `const mutation = useUpdateThresholdOptimistic()` once.

       **Idle render (when `!editing`).** Render a button-styled `<span>` (NOT a real `<button>` — UI-SPEC §5 wants the visual to read as a number, not a button) that shows the current `value`. Apply Tailwind classes for "underline-dotted button affordance" plus `cursor-pointer` and `select-none`. Set `role="button"` and `tabIndex={0}` so keyboard users can tab to it. Set `aria-label={\`Redigera tröskel för ${medicationName}\`}` (Swedish — UI-SPEC §5 ARIA copy). `onClick` and `onKeyDown` (handling Enter and Space) both transition `setEditing(true)`. Critically, EVERY event handler on the idle span must call `e.stopPropagation()` to prevent the parent `<TableRow>` / `<MedicationCard>` click handler from also firing and opening the Sheet — without this, clicking the threshold opens both the inline editor AND the Sheet.

       **Editing render (when `editing`).** Render a shadcn `<Input type="number" min={1}>` (the BE schema enforces `min(1)`; the FE matches). The input is autofocused on mount (use `autoFocus` prop OR a `useRef` + `useEffect`) and its current content is selected (`onFocus={(e) => e.target.select()}`) so typing replaces the value rather than appending. `value` is bound to `localValue`, `onChange` updates `localValue` from `Number(e.target.value)`. While `mutation.isPending`, set `disabled` on the input to prevent double-submits. Commit triggers: pressing Enter OR blurring the input fires `commit()`; pressing Esc fires `cancel()`. Both handlers also `e.stopPropagation()` to keep the events from bubbling to the parent row.

       `commit()` semantics: if `localValue === value`, no PATCH — just `setEditing(false)`. If `localValue` is non-finite or `< 1`, revert `localValue` to `value` and `setEditing(false)` (silently — no toast, no PATCH). Otherwise `await mutation.mutateAsync({ careUnitMedicationId, lowStockThreshold: localValue })`. On resolve, `setEditing(false)`. On reject, stay in editing state so the user can retry or Esc-cancel; the hook's `onError` already fired the toast and rolled back the cache. `cancel()`: restore `localValue` to `value`, then `setEditing(false)`.

       Visual error state: when `mutation.isError` is true, apply the destructive border class (`border-destructive` plus `focus-visible:ring-destructive` per shadcn convention) to the input so the user sees a red outline alongside the toast.

       Integrate with RBAC: wrap the component (or its idle render) in `<Can action="medication:update">` so sjukskoterska users see the bare number with no click affordance. (UI-SPEC §5 + D-17 defense-in-depth.)

       Reference: cite D-41 (two edit surfaces — Sheet + inline) and D-42 (mixed mutation strategy) in a header JSDoc.
  </action>
  <verify>
    <automated>pnpm --filter @meditrack/web build && pnpm --filter @meditrack/web test --run --no-coverage</automated>
  </verify>
  <acceptance_criteria>
    - Source: `apps/web/src/features/medications/useMedicationMutations.ts` exports `useUpdateMedication` (pessimistic) and `useUpdateThresholdOptimistic` (with `onMutate`/`onError`/`onSettled` triple).
    - Source: `useUpdateThresholdOptimistic`'s onMutate calls `cancelQueries({ queryKey: ['medications'] })` and `setQueriesData(...)`.
    - Source: `useUpdateThresholdOptimistic`'s onError calls `setQueryData` with the snapshotted prev entries (rollback) AND `toast.error('Kunde inte spara — försök igen.')`.
    - File exists: `apps/web/src/components/InlineEditThreshold.tsx`.
    - Source: `InlineEditThreshold.tsx` contains `e.stopPropagation()` on at least the input onClick AND the idle span onClick.
    - Source: `InlineEditThreshold.tsx` `aria-label={\`Redigera tröskel för ${medicationName}\`}` on the idle span.
    - Source: `InlineEditThreshold.tsx` contains the literal `'Tröskel'` for the input aria-label.
    - Behavior: `pnpm --filter @meditrack/web build` exits 0.
    - Behavior: Mounting a table row with InlineEditThreshold, clicking the threshold number opens the input (does NOT open the Sheet — confirms stopPropagation).
    - Behavior: Typing `99` + Enter calls the PATCH and the displayed number flips to `99` BEFORE the network roundtrip completes (optimistic update visible).
    - Behavior: Forcing an API error (e.g., setting `lowStockThreshold: 0` which fails validation) shows a sonner error toast AND the number reverts to the original (rollback).
  </acceptance_criteria>
  <done>Inline-edit is fully wired with optimistic semantics + rollback. Sheet save is wired with pessimistic semantics. Both call the same PATCH route.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Complete MedicationSheet edit + view modes; wire row/card click; wire InlineEditThreshold into table + card</name>
  <read_first>
    - apps/web/src/routes/lakemedel/MedicationSheet.tsx (Plan 01 — has create mode + edit/view placeholders)
    - apps/web/src/routes/lakemedel/MedicationTable.tsx (Plan 01 — Tröskel cell has TODO Plan 03)
    - apps/web/src/routes/lakemedel/MedicationCard.tsx (Plan 01 — Tröskel area has placeholder)
    - apps/web/src/routes/lakemedel/LakemedelPage.tsx (Plan 01 — Sheet state currently 'create' only; expand to 'edit' | 'view')
    - apps/web/src/auth/useCan.ts
    - .planning/phases/02-medication-catalog/02-UI-SPEC.md §6 (full Sheet spec), §6b (edit mode RBAC-aware), §6c (view mode), §"Interaction State Matrix"
    - .planning/phases/02-medication-catalog/02-CONTEXT.md D-32, D-34, D-36, D-37 (note: D-37 delete button content lands in Plan 04, but the Sheet layout reserves the left footer slot)
  </read_first>
  <action>
    A. Modify apps/web/src/routes/lakemedel/MedicationSheet.tsx — replace the Plan 01 edit/view placeholder with full implementations.

       Update the `MedicationSheetProps` shape so the parent page can drive create, edit, and view modes through a single component. The props are:
       - `mode: 'create' | 'edit' | 'view'` — drives heading + field set + button visibility.
       - `careUnitMedication?: MedicationListItem` — required when `mode` is `'edit'` or `'view'`; omitted for `'create'`. Type the prop optional and assert non-null at the call sites that require it (or split the type with a discriminated union if cleaner).
       - `open: boolean` and `onOpenChange: (open: boolean) => void` — controlled `<Sheet>` state from the parent page; mirror the shadcn Sheet primitive contract Plan 01 already established for create mode.
       - `onSaved?: () => void` — optional callback invoked after a successful save so the parent can close the Sheet or trigger ancillary refetches; the hook's invalidation already handles the list refresh, this is purely a parent-level escape hatch.

       Heading per mode (per UI-SPEC §6 table):
       - `create` → `Lägg till läkemedel`
       - `edit` → `{careUnitMedication.name}` (truncate `max-w-[360px]`)
       - `view` → `{careUnitMedication.name} · Visning`

       For mode === 'edit' AND `careUnitMedication.source === 'npl'`: render `<NplBadge>Från NPL · namn / form / styrka är låsta</NplBadge>` below the SheetTitle inside the SheetHeader.

       Form: use react-hook-form with `zodResolver(medicationUpdateRequest)`. For the `'edit'` and `'view'` modes, derive a `defaults` object from `careUnitMedication` that covers the six locked-subset fields per D-32 — `currentStock`, `lowStockThreshold`, `name`, `atcCode`, `form`, and `strength` — pulled directly off the `careUnitMedication!` prop. For `'create'` mode, leave `defaults` as `undefined` so the existing Plan 01 create flow keeps its typeahead-driven initialization untouched. Wire that into `useForm({ resolver: zodResolver(medicationUpdateRequest), defaultValues: defaults })`. Enforce the sjukskoterska view-only contract per D-36 by using react-hook-form's `disabled` flag (set `disabled: mode === 'view'` on the form, OR render the view-mode fields as read-only `<p>` labels per UI-SPEC §6c) so the Save submit path can never fire from the view surface — the visible footer omitting `Spara` is the user-facing affordance; the form-level disabled flag is the defense-in-depth gate.

       Fields rendered by mode:

       - `mode === 'edit'` AND `source === 'npl'`:
         - `Namn`: `<p className="text-sm text-foreground">{careUnitMedication.name}</p>` (read-only label, NOT a disabled input — UI-SPEC §6b says "rendered as read-only `<p>` labels").
         - `ATC-kod`, `Form`, `Styrka`: same `<p>` pattern.
         - `Lager`: `<Input type="number" min={0} {...form.register('currentStock', { valueAsNumber: true })} />`. Show field error below if Zod fails.
         - `Tröskel`: `<Input type="number" min={1} {...form.register('lowStockThreshold', { valueAsNumber: true })} />`.

       - `mode === 'edit'` AND `source === 'user'`:
         - All six fields editable. `Form` uses `<Select>` populated from `TOP_MEDICATION_FORMS` (same control as in `LakemedelFilter` from Plan 02, but without the 'Övriga' sentinel — user-created form value must be a real form string).
         - `Strength` optional.

       - `mode === 'view'`:
         - Wrap the form in `<fieldset disabled className="space-y-4">` so all inputs propagate `disabled`. Render the same fields as edit/source-of-record, no NPL strip distinction needed (it's read-only either way).

       Footer per mode (UI-SPEC §6 + D-37):
       - `create`: right-aligned `Avbryt` (ghost) + `Spara` (default). Form submit calls `useCreateMedication.mutateAsync` (already from Plan 01).
       - `edit`: left-aligned `Ta bort` (destructive) — opens AlertDialog (Plan 04 will wire `<DeleteMedicationDialog>`; in Slice 3, render the button but its onClick can be a no-op stub `// TODO Plan 04: open DeleteMedicationDialog`. The button must visually render so Slice 3 layout is correct). Right-aligned `Avbryt` (ghost) + `Spara` (default). Spara calls `useUpdateMedication.mutateAsync({ careUnitMedicationId: careUnitMedication!.careUnitMedicationId, payload })`.
       - `view`: right-aligned only `Stäng` (secondary) — closes the Sheet.

       Save button states (UI-SPEC §"Sheet Save Button" state matrix):
       - Idle: `<Button>Spara</Button>`.
       - Submitting: `<Button disabled className="opacity-50"><Loader2 className="mr-2 h-4 w-4 animate-spin" />Sparar…</Button>`.
       - Success: hook's onSuccess fires `toast.success('Sparat')` + invalidates. Sheet closes via `onOpenChange(false)` in the onSubmit handler `.then`.
       - Error: hook's onError fires `toast.error('Kunde inte spara — försök igen.')`; Sheet stays open; button re-enables.

       Focus management (UI-SPEC §6):
       - `create`: autofocus typeahead (already Plan 01).
       - `edit` with NPL source: autofocus `Lager` input.
       - `edit` with user source: autofocus `Namn` input.
       - `view`: autofocus the `Stäng` button.

       Footer mobile padding: `pb-[calc(1rem+56px+env(safe-area-inset-bottom))]` per UI-SPEC §Spacing (already done in Plan 01 for create — verify it applies in edit/view too).

    B. Modify apps/web/src/routes/lakemedel/MedicationTable.tsx — Tröskel cell:
       - Replace the bare number with `<InlineEditThreshold careUnitMedicationId={item.careUnitMedicationId} medicationName={item.name} value={item.lowStockThreshold} />`.

    C. Modify apps/web/src/routes/lakemedel/MedicationCard.tsx — same swap in the Tröskel area, preceded by the `Tröskel: ` label literal.

    D. Modify apps/web/src/routes/lakemedel/LakemedelPage.tsx:
       - Expand the Sheet state union: `useState<{ mode: 'create' } | { mode: 'edit' | 'view'; item: MedicationListItem } | null>(null)`.
       - Row/Card onClick handler: `const canUpdate = useCan('medication:update'); function openSheetForItem(item) { setSheet({ mode: canUpdate ? 'edit' : 'view', item }); }` Per D-36, mode is driven by `useCan('medication:update')` — sjukskoterska gets `mode='view'`, apotekare/admin get `mode='edit'`.
       - Pass `onRowClick={openSheetForItem}` to `<MedicationTable>` and equivalent to `<MedicationCardList>`.
       - Pass to Sheet: `<MedicationSheet mode={sheet?.mode ?? 'create'} careUnitMedication={sheet && sheet.mode !== 'create' ? sheet.item : undefined} open={sheet !== null} onOpenChange={(o) => setSheet(o ? sheet : null)} />`.

    E. Ensure that when the Sheet save triggers on edit, `useUpdateMedication.onSuccess` triggers a `queryClient.invalidateQueries({ queryKey: ['medications'] })` (already in Task 2's hook). On success, LakemedelPage's data re-fetches and the displayed row updates.
  </action>
  <verify>
    <automated>pnpm --filter @meditrack/web build && pnpm --filter @meditrack/web test --run --no-coverage</automated>
  </verify>
  <acceptance_criteria>
    - Source: `MedicationSheet.tsx` mode-branching on `'create' | 'edit' | 'view'` is explicit (e.g., switch/if-else chain).
    - Source: `MedicationSheet.tsx` for `mode='edit' AND source==='npl'` renders `<NplBadge>Från NPL · namn / form / styrka är låsta</NplBadge>`.
    - Source: `MedicationSheet.tsx` for `mode='edit' AND source==='npl'` renders `Namn/ATC-kod/Form/Styrka` as `<p>` elements, not inputs (search source for `<p` near the `Namn` field).
    - Source: `MedicationSheet.tsx` for `mode='view'` wraps fields in `<fieldset disabled>` AND footer contains only `'Stäng'`.
    - Source: `MedicationTable.tsx` imports and renders `<InlineEditThreshold>` in the Tröskel cell.
    - Source: `MedicationCard.tsx` imports and renders `<InlineEditThreshold>`.
    - Source: `LakemedelPage.tsx` uses `useCan('medication:update')` to drive Sheet mode (edit vs view).
    - Behavior: `pnpm --filter @meditrack/web build` exits 0.
    - Behavior: As apotekare, clicking a row of an NPL-sourced medication opens the Sheet titled with the medication name and the NplBadge lock note; Namn/ATC/Form/Styrka are read-only labels; Lager/Tröskel inputs are editable; saving updates the row in the list.
    - Behavior: As apotekare, clicking a row of a user-sourced medication opens the Sheet with ALL six fields editable.
    - Behavior: As sjukskoterska, clicking a row opens the Sheet titled `{name} · Visning`; footer has only `Stäng`; all fields read-only.
    - Behavior: From the table, clicking the threshold number INSIDE a row does NOT open the Sheet (stopPropagation works); the number enters edit mode; pressing Enter saves with optimistic update (the number flips instantly, then a `Sparat` toast does NOT fire — inline edits are silent per D-42).
    - Behavior: Saving the Sheet shows toast `Sparat`; saving with an invalid value (e.g., negative stock — Zod fails) shows toast `Kunde inte spara — försök igen.` and the Sheet stays open.
  </acceptance_criteria>
  <done>End-to-end Slice 3: edit Sheet with three modes (create/edit/view) + RBAC-aware fields + NPL-strip + inline-edit threshold with optimistic mutation + per-surface mutation strategy (pessimistic vs optimistic). CAT-06 demonstrably passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → API (PATCH body) | Untrusted JSON body with arbitrary field set; Zod validates shape, service strips NPL-locked fields before persistence. |
| Sheet form → useUpdateMedication | FE form validates with Zod via zodResolver; FE is the first-stop validation, BE is canonical. |
| InlineEditThreshold → useUpdateThresholdOptimistic | Optimistic FE cache mutation precedes BE write; on error, rollback restores the snapshot — no data lost, only display state flickers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-12 | Tampering | PATCH /api/medications/:id with malicious name/atc on NPL-sourced row | mitigate | `updateCareUnitMedication` checks `row.medication.source === 'user'` BEFORE accepting any name/atcCode/form/strength field. NPL meds get those fields stripped server-side. Acceptance test: PATCH with body `{name:"Hacked"}` against NPL row returns 200 with original name unchanged. |
| T-02-13 | Information Disclosure | Cross-tenant existence probing via PATCH | mitigate | Service returns 404 (NotFoundError) — NOT 403 — when `careUnitMedicationId` belongs to another vårdenhet OR doesn't exist. Same response shape both cases. Acceptance test confirms: foreign-unit ID and non-existent ID both produce identical 404 envelope. |
| T-02-14 | Elevation of Privilege | sjukskoterska bypassing FE Sheet=view to hit PATCH directly | mitigate | `requirePermission('medication:update')` preHandler returns 403 BEFORE the service is invoked. FE Can/useCan gate is defense-in-depth only (D-17). |
| T-02-15 | Tampering | Inline-edit optimistic rollback bypass | mitigate | The onMutate snapshot is per-query-key; on onError, `setQueryData` restores. If the user double-clicks during a pending mutation, `mutation.isPending` disables the input. If the network fails mid-PATCH, the snapshot ensures no stale optimistic state leaks past the error boundary. |
| T-02-16 | Denial of Service | Mass PATCH spamming (e.g., scripted threshold edits) | accept | Rate limiting is deferred per Phase 7 "with more time" note (carry-forward from Phase 1 D-04 posture). The pg_trgm + indexed PATCH path is cheap; risk is low for a single-user demo. |

T-02-12 and T-02-13 are high-severity tenancy/integrity concerns; their acceptance tests are non-negotiable on Task 1.
</threat_model>

<verification>
1. `pnpm --filter @meditrack/api typecheck`, `pnpm --filter @meditrack/api test --run --no-coverage`, `pnpm --filter @meditrack/web build` all exit 0.
2. PATCH validation (apotekare session):
   - `PATCH /api/medications/<npl-row-id>` body `{"name":"X"}` → 200 with ORIGINAL name (strip).
   - `PATCH /api/medications/<user-row-id>` body `{"name":"X"}` → 200 with `name: 'X'`.
   - `PATCH /api/medications/<foreign-unit-id>` → 404.
   - `PATCH /api/medications/<own-id>` body `{}` → 400 validation_failed (refine on empty body).
3. RBAC: sjukskoterska PATCH → 403.
4. Sheet edit (apotekare, NPL row): titled with name, NplBadge lock note visible, Namn/ATC/Form/Styrka read-only, Lager/Tröskel editable; Spara closes the Sheet, toast `Sparat`, row updates.
5. Sheet edit (apotekare, user row): all fields editable.
6. Sheet view (sjukskoterska, any row): titled `{name} · Visning`; only `Stäng`; all inputs disabled.
7. Inline-edit threshold: click number → input → type → Enter → number flips instantly (optimistic); no toast on success; force an error → toast `Kunde inte spara — försök igen.` + number reverts.
8. Row click vs threshold click: clicking the row body opens the Sheet; clicking the threshold number opens the inline input but NOT the Sheet.
</verification>

<success_criteria>
- ROADMAP §Phase 2 success criterion #3 partially passes (the edit branch — CAT-06; create branch already covered by Slice 1; delete branch by Slice 4).
- ROADMAP §Phase 2 success criterion #4 ("Threshold is editable on each medication and the indicator updates immediately after save (TanStack Query invalidation)") demonstrably passes via the inline-edit threshold.
- CAT-06 acceptance text in REQUIREMENTS.md observably satisfied.
- D-42 mixed-mutation-strategy is visibly demoable (Sheet save = pessimistic; inline threshold = optimistic).
</success_criteria>

<output>
Create `.planning/phases/02-medication-catalog/02-03-SUMMARY.md` when done. Summary should include:
- Files created/modified.
- Manual screen-test note: NPL row edit Sheet, user row edit Sheet, sjukskoterska view Sheet, inline threshold edit success, inline threshold edit error rollback.
- Note for Plan 04: the Sheet footer's `Ta bort` button currently has a stub onClick; Plan 04 wires `<DeleteMedicationDialog>`.
</output>
</output>
