---
phase: 02-medication-catalog
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/prisma/schema.prisma
  - apps/api/prisma/migrations/{ts}_medication_catalog/migration.sql
  - apps/api/prisma/seed-data/lakemedel.csv
  - apps/api/prisma/seed.ts
  - apps/api/package.json
  - packages/shared/src/contracts/permissions.ts
  - packages/shared/src/contracts/medication.ts
  - packages/shared/src/constants/medicationForms.ts
  - packages/shared/src/constants/medicationDefaults.ts
  - packages/shared/src/index.ts
  - apps/api/src/auth/permissions.ts
  - apps/api/src/plugins/errorHandler.ts
  - apps/api/src/services/medication.service.ts
  - apps/api/src/routes/medications/list.ts
  - apps/api/src/routes/medications/search.ts
  - apps/api/src/routes/medications/create.ts
  - apps/api/src/routes/medications/index.ts
  - apps/api/src/app.ts
  - apps/web/src/lib/api.ts
  - apps/web/src/App.tsx
  - apps/web/src/components/LowStockBadge.tsx
  - apps/web/src/components/NplBadge.tsx
  - apps/web/src/features/medications/useMedicationsQuery.ts
  - apps/web/src/features/medications/useMedicationMutations.ts
  - apps/web/src/routes/lakemedel/LakemedelPage.tsx
  - apps/web/src/routes/lakemedel/MedicationTable.tsx
  - apps/web/src/routes/lakemedel/MedicationCardList.tsx
  - apps/web/src/routes/lakemedel/MedicationCard.tsx
  - apps/web/src/routes/lakemedel/MedicationSheet.tsx
  - apps/web/src/routes/lakemedel/LowStockBanner.tsx
  - apps/web/src/routes/lakemedel/PaginationFooter.tsx
  - apps/web/src/routes/lakemedel/AddMedicationButton.tsx
autonomous: true
requirements: [CAT-01, CAT-05, STK-03, STK-04]

must_haves:
  truths:
    - "Per D-20 Slice 1: a seeded user can open /lakemedel and see a paginated list of CareUnitMedication rows scoped to their vårdenhet with name, ATC, form, strength, current stock"
    - "Per D-22 + STK-04: rows whose currentStock < lowStockThreshold render <LowStockBadge> + AlertTriangle icon on the Lager cell from the very first interactive build"
    - "Per D-22 + D-39: <LowStockBanner> renders '⚠ {N} läkemedel under tröskel' above the list whenever belowThresholdTotal > 0"
    - "Per D-23, D-24, D-25, D-26: `docker compose up` on a fresh clone runs migration 0002_medication_catalog (enabling pg_trgm + GIN index) and seeds all 43 538 NPL rows plus the matching CareUnitMedication rows for careunit-karolinska-01 within ~30 s"
    - "Per D-25: ~8% of seeded CareUnitMedication rows land with currentStock < lowStockThreshold using a deterministic PRNG keyed on nplId so screenshots in README match what the interviewer sees live"
    - "Per D-27, D-28, D-29: Medication is the global NPL registry (no careUnitId) and CareUnitMedication is the per-vårdenhet join carrying currentStock + lowStockThreshold + deletedAt, with @@unique([careUnitId, medicationId])"
    - "Per D-30 + CAT-05: an apotekare/admin can open the Add Sheet, pick from the typeahead surfacing global Medication rows (excluding drugs already actively stocked at their care unit), set Lager and Tröskel, and submit — creating one CareUnitMedication"
    - "Per D-30: re-adding a soft-deleted Medication transparently restores the existing CareUnitMedication row (UPDATE deletedAt=NULL, currentStock=new, lowStockThreshold=new) rather than creating a duplicate"
    - "Per D-31 + CAT-05: a 'Skapa nytt läkemedel' fallback under empty typeahead results creates Medication{source:'user'} + CareUnitMedication in one transaction"
    - "Per D-40 + STK-03: lowStockThreshold default is computed via defaultLowStockThreshold(form) in packages/shared and is consumed by both FE prefill and BE validation fallback"
    - "Per D-43 + D-15: PERMISSIONS map exposes medication:read (all 3 roles), medication:create/update/delete (apotekare + admin); add-button is gated by <Can action='medication:create'> on FE and requirePermission on BE"
    - "Per D-44: GET /api/medications accepts q, atc, form, belowThreshold, page, pageSize and returns { rows, total, belowThresholdTotal, page, pageSize } — pagination is server-side"
    - "Per D-45: GET /api/medications/search returns top 20 global Medication rows by name/ATC, excluding drugs already actively stocked at req.user.careUnitId"
    - "Per D-21: each task in this plan commits separately (~5-8 commits expected across the 8 tasks; sub-bundles inside Task 2 may add a few more) — reviewer reads the commit history per brief §5"
    - "Per D-34: the Add surface renders as a shadcn `<Sheet>` (right-slide on desktop, bottom-sheet on mobile) with footer-pinned `Spara`/`Avbryt`; mobile bottom-sheet clears the 56 px bottom tab bar + `env(safe-area-inset-bottom)`; URL does NOT change while the Sheet is open. One `MedicationSheet` component handles create/edit/view via `mode` prop (Plan 03 extends the same component)."
    - "Per D-35: Add trigger is a desktop `<Button>` top-right next to the page heading + a mobile FAB bottom-right above the bottom tab bar; both are gated by `<Can action='medication:create'>` so `sjuksköterska` never sees them"
    - "Per D-38: `<LowStockBadge>` renders a pill (shadcn destructive `<Badge>`, red bg / white text) + `lucide-react` `AlertTriangle` icon (14 px, left of the stock number) with the tooltip label `Lågt lager`; renders identically on table rows (≥md) and mobile cards (<md); structurally parallel to `<RoleBadge>` from Phase 1, not derived from it"
  artifacts:
    - path: "apps/api/prisma/schema.prisma"
      provides: "Medication + CareUnitMedication models, MedicationSource enum"
      contains: "model Medication"
    - path: "apps/api/prisma/migrations/{ts}_medication_catalog/migration.sql"
      provides: "DDL for Medication + CareUnitMedication + pg_trgm extension + GIN index on lower(name)"
      contains: "CREATE EXTENSION IF NOT EXISTS pg_trgm"
    - path: "apps/api/prisma/seed-data/lakemedel.csv"
      provides: "Committed NPL CSV (43 538 rows) so docker compose up works on a fresh clone"
    - path: "apps/api/prisma/seed.ts"
      provides: "Extended seed: 43 538 Medications + 43 538 CareUnitMedications with deterministic stock/threshold, ~8% below threshold"
      contains: "createMany"
    - path: "packages/shared/src/contracts/medication.ts"
      provides: "Zod schemas: medicationListItem, medicationListResponse, medicationListQuery, medicationSearchQuery, medicationSearchResult, medicationCreateRequest (discriminated union 'npl'|'user')"
    - path: "packages/shared/src/constants/medicationForms.ts"
      provides: "TOP_MEDICATION_FORMS tuple + Övriga sentinel"
    - path: "packages/shared/src/constants/medicationDefaults.ts"
      provides: "defaultLowStockThreshold(form) heuristic per D-40"
    - path: "apps/api/src/services/medication.service.ts"
      provides: "listMedicationsForUnit, searchGlobalMedications, createCareUnitMedication"
      contains: "careUnitId: string"
    - path: "apps/api/src/routes/medications/list.ts"
      provides: "GET /api/medications with requireSession + requirePermission('medication:read')"
    - path: "apps/api/src/routes/medications/search.ts"
      provides: "GET /api/medications/search with requireSession + requirePermission('medication:read')"
    - path: "apps/api/src/routes/medications/create.ts"
      provides: "POST /api/medications with requireSession + requirePermission('medication:create')"
    - path: "apps/api/src/plugins/errorHandler.ts"
      provides: "ConflictDuplicateMedicationError → 409 dispatch + NotFoundError + ForbiddenScopeError"
    - path: "apps/web/src/routes/lakemedel/LakemedelPage.tsx"
      provides: "Catalog page replacing the Phase 1 stub, with URL-synced filters and Sheet orchestration"
    - path: "apps/web/src/components/LowStockBadge.tsx"
      provides: "Destructive badge with AlertTriangle icon + 'Lågt lager' label"
    - path: "apps/web/src/components/NplBadge.tsx"
      provides: "Slate badge with default 'Från NPL' children"
  key_links:
    - from: "apps/web/src/routes/lakemedel/LakemedelPage.tsx"
      to: "/api/medications"
      via: "useMedicationsQuery -> fetchJson"
      pattern: "useQuery.*\\['medications'"
    - from: "apps/web/src/routes/lakemedel/MedicationSheet.tsx"
      to: "/api/medications/search"
      via: "useMedicationSearchQuery -> fetchJson"
      pattern: "medication-search"
    - from: "apps/api/src/routes/medications/list.ts"
      to: "apps/api/src/services/medication.service.ts"
      via: "listMedicationsForUnit(req.user!.careUnitId, filters)"
      pattern: "listMedicationsForUnit\\(req\\.user"
    - from: "apps/api/src/services/medication.service.ts"
      to: "prisma.careUnitMedication"
      via: "Prisma typed client"
      pattern: "prisma\\.careUnitMedication\\.(findMany|count|create|update|upsert)"
    - from: "apps/api/src/auth/permissions.ts"
      to: "packages/shared/src/contracts/permissions.ts"
      via: "Record<ActionKey, Role[]> drift-prevention"
      pattern: "'medication:(read|create|update|delete)'"
---

<objective>
Ship Slice 1 of Phase 2 per D-20 — the foundational vertical slice that delivers: Prisma migration (Medication + CareUnitMedication + pg_trgm + GIN), seed extension reading apps/api/prisma/seed-data/lakemedel.csv (43 538 rows), GET /api/medications, GET /api/medications/search, POST /api/medications (typeahead + 'Skapa nytt läkemedel' fallback), and the /lakemedel page rendering the table/card list, low-stock pill+icon, count banner, and Add Sheet — already wired end-to-end.

Purpose: This is the single most visible slice of the phase — by the end of it, the seeded apotekare can open /lakemedel, see ~3 500 below-threshold rows out of 43 538 (red badges visible immediately), search via the API, and add a new medication through the typeahead. CAT-01 (list), CAT-05 (add), STK-03 (threshold default), STK-04 (low-stock indicator) all light up.

Output: New Prisma models + migration + committed NPL CSV + extended seed + 5 new shared schemas + extended permission map + 3 new BE routes + medication service + replaced /lakemedel page with badge, banner, table/cards, Sheet, FAB.
</objective>

<execution_context>
@C:/Projekt/MediTrack/.claude/get-shit-done/workflows/execute-plan.md
@C:/Projekt/MediTrack/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-medication-catalog/02-CONTEXT.md
@.planning/phases/02-medication-catalog/02-PATTERNS.md
@.planning/phases/02-medication-catalog/02-UI-SPEC.md
@.planning/phases/01-foundation-auth/01-CONTEXT.md
@.planning/phases/01-foundation-auth/01-UI-SPEC.md
@CLAUDE.md

<interfaces>
<!-- Phase 1 interfaces Slice 1 will consume verbatim. -->

From apps/api/prisma/schema.prisma:
- enum Role { apotekare sjukskoterska admin }
- model CareUnit { id (cuid), name, createdAt, users User[] }
- model User { id, email (unique), name, passwordHash, role Role, careUnitId, careUnit CareUnit, sessions Session[], @@index([careUnitId]) }
- model Session { id (NOT cuid — set by app), userId, careUnitId, ... }

From packages/shared/src/contracts/permissions.ts:
- export const ACTION_KEYS = ['admin:ping'] as const;  // Slice 1 appends 'medication:read'|'medication:create'|'medication:update'|'medication:delete'
- export type ActionKey = (typeof ACTION_KEYS)[number];
- export const actionKey = z.enum(ACTION_KEYS);

From apps/api/src/auth/permissions.ts:
- export const PERMISSIONS: Record<ActionKey, Role[]> = { 'admin:ping': ['admin'] };
- export function actionsForRole(role: Role): ActionKey[];

From apps/api/src/auth/requireSession.ts + requirePermission.ts:
- preHandler: [requireSession, requirePermission('medication:read')]
- decorates req.user = { id, role, careUnitId, name, email, sessionId }

From apps/api/src/plugins/errorHandler.ts:
- export class InvalidCredentialsError { code: 'invalid_credentials' }
- export class UnauthenticatedError { code: 'unauthenticated' }
- envelope translator already handles ZodError → 400 validation_failed

From apps/web/src/lib/api.ts:
- export class ApiError { status, envelope }
- export function fetchJson<T>(path, init): Promise<T>  // credentials:'include', throws ApiError on non-2xx

From apps/web/src/auth/{useAuth, Can, useCan}:
- useAuth() => { user, isLoading, can(action) }
- <Can action="medication:create">{children}</Can>
- useCan('medication:update') => boolean

From apps/web/src/components/EmptyStateCard.tsx:
- <EmptyStateCard icon={Pill} heading="Inga läkemedel ännu" body="..." cta={...}>

From apps/api/prisma/seed.ts:
- CARE_UNIT_ID = 'careunit-karolinska-01'  (carry verbatim — Slice 1 seeds CareUnitMedication rows for this exact ID)

From packages/shared exports (index.ts):
- ROLES, roleEnum, Role, ORDER_STATUSES, ORDER_STATUS_LABELS, orderStatusEnum, OrderStatus, MeResponse, LoginRequest, LoginResponse, ErrorEnvelope, ActionKey, actionKey

</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1 [BLOCKING]: Add Medication + CareUnitMedication models, generate migration, enable pg_trgm + GIN, run prisma generate</name>
  <read_first>
    - apps/api/prisma/schema.prisma (current Phase 1 schema; do not touch CareUnit/User/Session)
    - apps/api/prisma/migrations/20260520171636_init/migration.sql (DDL style reference)
    - .planning/phases/02-medication-catalog/02-PATTERNS.md §"apps/api/prisma/schema.prisma" and §"migration.sql"
    - .planning/phases/02-medication-catalog/02-CONTEXT.md D-26, D-27, D-28
  </read_first>
  <action>
    Append to apps/api/prisma/schema.prisma a JSDoc-style comment header citing D-27/D-28/D-33, then:
    1. Add `enum MedicationSource { npl user }` (lowercase variants — match Phase 1 Role enum style).
    2. Add `model Medication` with: `id String @id @default(cuid())`, `nplId String? @unique`, `name String`, `atcCode String`, `form String`, `strength String?`, `source MedicationSource`, `createdAt DateTime @default(now())`, plus relation `careUnitMedications CareUnitMedication[]` and `@@index([atcCode])`. Add a Prisma `@@map` if needed to keep the table name `Medication` (default).
    3. Add `model CareUnitMedication` with: `id String @id @default(cuid())`, `careUnitId String`, `careUnit CareUnit @relation(fields:[careUnitId], references:[id])`, `medicationId String`, `medication Medication @relation(fields:[medicationId], references:[id])`, `currentStock Int`, `lowStockThreshold Int`, `deletedAt DateTime?`, `createdAt DateTime @default(now())`, `updatedAt DateTime @updatedAt`, `@@unique([careUnitId, medicationId])`, `@@index([careUnitId])`, `@@index([deletedAt])`.
    4. Extend `model CareUnit` to add `careUnitMedications CareUnitMedication[]`.
    5. Run `pnpm --filter @meditrack/api exec prisma migrate dev --name 0002_medication_catalog --create-only` to generate but NOT apply the migration. Hand-append the following lines to the generated `migration.sql`:
       - `CREATE EXTENSION IF NOT EXISTS pg_trgm;`
       - `CREATE INDEX "Medication_name_lower_trgm_idx" ON "Medication" USING gin (lower("name") gin_trgm_ops);`
       Add a leading `-- Phase 2 D-26: enable pg_trgm + GIN for fast ILIKE search at 43k rows.` comment.
    6. Apply with `pnpm --filter @meditrack/api exec prisma migrate dev`.
    7. Regenerate the typed client: `pnpm --filter @meditrack/api exec prisma generate`.

    This task is [BLOCKING] — every downstream Slice 1 task uses the new Prisma client types. Do not write service or route code until prisma generate has succeeded.
  </action>
  <verify>
    <automated>pnpm --filter @meditrack/api exec prisma migrate status</automated>
  </verify>
  <acceptance_criteria>
    - Source: `apps/api/prisma/schema.prisma` contains `model Medication {` and `model CareUnitMedication {` and `enum MedicationSource { npl user }`.
    - Source: `apps/api/prisma/schema.prisma` `CareUnitMedication` block contains `@@unique([careUnitId, medicationId])`.
    - File exists: a new directory under `apps/api/prisma/migrations/` named `*_medication_catalog/migration.sql`.
    - Source: that migration.sql contains the literal `CREATE EXTENSION IF NOT EXISTS pg_trgm;` and `gin_trgm_ops`.
    - Behavior: `pnpm --filter @meditrack/api exec prisma migrate status` reports "Database schema is up to date" (or equivalent — no pending migrations).
    - Behavior: `psql "$DATABASE_URL" -c "\\d \"Medication\""` lists columns id, nplId, name, atcCode, form, strength, source, createdAt.
    - Behavior: `psql "$DATABASE_URL" -c "\\dx pg_trgm"` returns exactly one row.
    - Behavior: `pnpm --filter @meditrack/api exec prisma generate` exits 0 and `node -e "const c=require('@prisma/client');new c.PrismaClient(); console.log(Object.keys(new c.PrismaClient()).filter(k=>k==='medication'||k==='careUnitMedication').length)"` prints `2`.
  </acceptance_criteria>
  <done>Migration applied, pg_trgm + GIN in place, typed client regenerated, downstream tasks can import MedicationSource and the new model accessors from @prisma/client.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Widen ACTION_KEYS, extend PERMISSIONS map, add medication contracts + constants in packages/shared, re-export from barrel</name>
  <read_first>
    - packages/shared/src/contracts/permissions.ts (full file)
    - packages/shared/src/contracts/me.ts (Zod schema style)
    - packages/shared/src/contracts/login.ts (request+response paired pattern)
    - packages/shared/src/constants/orderStatus.ts (constants module pattern + labels Record)
    - packages/shared/src/index.ts (barrel re-export style)
    - apps/api/src/auth/permissions.ts (PERMISSIONS map + actionsForRole)
    - .planning/phases/02-medication-catalog/02-PATTERNS.md §packages/shared sections
    - .planning/phases/02-medication-catalog/02-CONTEXT.md D-40, D-43, D-44, D-45
  </read_first>
  <action>
    Single commit per logical sub-task per D-21 (this task = up to 5 commits acceptable; keep each diff readable).

    A. packages/shared/src/contracts/permissions.ts — append four literals to ACTION_KEYS in this exact order: 'medication:read', 'medication:create', 'medication:update', 'medication:delete'. Do not reorder 'admin:ping'.

    B. packages/shared/src/constants/medicationForms.ts (NEW) — export `TOP_MEDICATION_FORMS` as a `readonly` tuple of ~15-20 NPL form strings derived from the CSV by frequency analysis (apotekare runs a one-off script or counts manually; hardcode the top ~15-20). Export `OVRIGA_FILTER_VALUE = 'Övriga' as const`. Mirror constants/orderStatus.ts module style: JSDoc preface, `as const` tuple, `type MedicationForm = (typeof TOP_MEDICATION_FORMS)[number]`. Form frequency derivation: read seed-data/lakemedel.csv once with a small node script in the action transcript and pick the actual top-N; if running ahead of CSV copy, default to: 'Tablett','Filmdragerad tablett','Injektionsvätska, lösning','Kapsel, hård','Oral lösning','Munsönderfallande tablett','Salva','Kräm','Granulat','Infusionsvätska, lösning','Brustablett','Pulver till injektionsvätska, lösning','Resoriblett','Suppositorium','Depottablett','Inhalationspulver, avdelad dos' and refine after seeing CSV.

    C. packages/shared/src/constants/medicationDefaults.ts (NEW) — export `defaultLowStockThreshold(form: string): number` per D-40 with tiers: injection/lösning/spray → 5, salva/kräm/gel → 3, tablett/kapsel/dragerad → 20, fallback → 10. Add JSDoc header citing D-40 and STK-03.

    D. packages/shared/src/contracts/medication.ts (NEW) — define and export the following Zod schemas (one per bullet) + matching `z.infer` types per D-44/D-45 and PATTERNS.md. Add a JSDoc preamble citing D-44 (list shape + `belowThresholdTotal`), D-45 (search exclusion), and D-31 (split create paths). Schema-by-schema field shape:

       - `medicationListItem` — `z.object({ ... })` with fields: `careUnitMedicationId: z.string()`, `medicationId: z.string()`, `name: z.string()`, `atcCode: z.string()`, `form: z.string()`, `strength: z.string().nullable()`, `currentStock: z.number().int().nonnegative()`, `lowStockThreshold: z.number().int().positive()`, `source: z.enum(['npl','user'])`. Each item carries the `source` discriminator so the FE can branch NPL-locked vs user-editable in Plan 03 without an extra fetch.

       - `medicationListQuery` — `z.object({ ... })` with fields: `q: z.string().optional()`, `atc: z.string().optional()`, `form: z.string().optional()`, `belowThreshold: z.coerce.boolean().optional()`, `page: z.coerce.number().int().positive().default(1)`, `pageSize: z.coerce.number().int().min(1).max(100).default(25)`. All numeric fields use `z.coerce.*` so Fastify query strings parse cleanly.

       - `medicationListResponse` — `z.object({ ... })` with fields: `rows: z.array(medicationListItem)`, `total: z.number().int().nonnegative()`, `belowThresholdTotal: z.number().int().nonnegative()` (D-44 — required, never optional; powers the LowStockBanner count even when the filter set yields zero matching rows), `page: z.number().int().positive()`, `pageSize: z.number().int().positive()`.

       - `medicationSearchQuery` — `z.object({ q: z.string(), limit: z.coerce.number().int().min(1).max(20).default(20) })`.

       - `medicationSearchResult` — `z.object({ ... })` with fields: `id: z.string()`, `name: z.string()`, `atcCode: z.string()`, `form: z.string()`, `strength: z.string().nullable()`, `source: z.enum(['npl','user'])`.

       - `medicationSearchResponse` — `z.object({ results: z.array(medicationSearchResult) })`.

       - **Create paths — two named schemas plus a discriminated union (D-31; discriminator on `source`):**
         - `medicationCreateFromNplRequest` — `z.object({ source: z.literal('npl'), medicationId: z.string().min(1), currentStock: z.number().int().nonnegative(), lowStockThreshold: z.number().int().positive() })`. Caller has picked an existing global Medication row from the typeahead; the BE looks it up by id and creates/restores a CareUnitMedication.
         - `medicationCreateUserRequest` — `z.object({ source: z.literal('user'), name: z.string().min(1), atcCode: z.string().min(1), form: z.string().min(1), strength: z.string().nullable().optional(), currentStock: z.number().int().nonnegative(), lowStockThreshold: z.number().int().positive() })`. "Skapa nytt läkemedel" fallback (D-31) — BE inserts Medication{source:'user'} + CareUnitMedication in one transaction.
         - `medicationCreateRequest` — `z.discriminatedUnion('source', [medicationCreateFromNplRequest, medicationCreateUserRequest])`. Server rejects any body whose `source` is not one of the two literals with 400 validation_failed.

       Add `export type X = z.infer<typeof x>` for every schema above, including `MedicationCreateFromNplRequest` and `MedicationCreateUserRequest` (the named branches) so the FE can narrow with `payload.source === 'npl'` without re-deriving.

    E. packages/shared/src/index.ts — append re-exports for everything created in step D (both branch schemas AND the union) plus the constants: TOP_MEDICATION_FORMS, OVRIGA_FILTER_VALUE, MedicationForm, defaultLowStockThreshold, medicationListItem, MedicationListItem, medicationListQuery, MedicationListQuery, medicationListResponse, MedicationListResponse, medicationSearchQuery, MedicationSearchQuery, medicationSearchResult, MedicationSearchResult, medicationSearchResponse, MedicationSearchResponse, medicationCreateFromNplRequest, MedicationCreateFromNplRequest, medicationCreateUserRequest, MedicationCreateUserRequest, medicationCreateRequest, MedicationCreateRequest.

    F. apps/api/src/auth/permissions.ts — extend the PERMISSIONS map with the four new keys, mapping 'medication:read' to ['apotekare','sjukskoterska','admin'] and the three mutation keys to ['apotekare','admin']. Append at the end of the object literal — do NOT reorder 'admin:ping'. The Record<ActionKey, Role[]> exhaustiveness check now requires all four entries — verify TS compile.

    Build shared once: `pnpm --filter @meditrack/shared build` before tasks 3+ run.
  </action>
  <verify>
    <automated>pnpm --filter @meditrack/shared build && pnpm --filter @meditrack/api typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Source: `packages/shared/src/contracts/permissions.ts` contains the literal substring `'medication:read', 'medication:create', 'medication:update', 'medication:delete'`.
    - Source: `packages/shared/src/contracts/medication.ts` exports `medicationListResponse`, `medicationSearchResponse`, `medicationCreateRequest` and the file contains `z.discriminatedUnion('kind'`.
    - Source: `packages/shared/src/constants/medicationDefaults.ts` exports a function `defaultLowStockThreshold` whose body contains the literal numbers 5, 3, 20, 10.
    - Source: `packages/shared/src/index.ts` contains `export { medicationListResponse` (or equivalent grouped re-export).
    - Source: `apps/api/src/auth/permissions.ts` contains all four `'medication:*'` keys in the PERMISSIONS map literal.
    - Behavior: `pnpm --filter @meditrack/shared build` exits 0.
    - Behavior: `pnpm --filter @meditrack/api typecheck` exits 0 (Record<ActionKey, Role[]> exhaustiveness satisfied).
    - Behavior: `node -e "const s=require('@meditrack/shared'); console.log(s.defaultLowStockThreshold('Injektionsvätska, lösning'), s.defaultLowStockThreshold('Tablett'), s.defaultLowStockThreshold('Salva'), s.defaultLowStockThreshold('Granulat'))"` prints `5 20 3 10`.
  </acceptance_criteria>
  <done>Shared package built, action keys + permission map widened, drift-prevention type check passes, medication contracts importable from @meditrack/shared.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Copy NPL CSV into committed seed-data, extend seed.ts with streamed Medication + CareUnitMedication seeding using deterministic PRNG</name>
  <read_first>
    - apps/api/prisma/seed.ts (full file — preserve CareUnit + Users upsert flow exactly)
    - local/lakemedel.csv (verify column format: nplid;namn;atc_kod;form;form_kod;styrka)
    - apps/api/package.json (check if csv-parse already a dep; add as runtime dep if not)
    - .planning/phases/02-medication-catalog/02-CONTEXT.md D-23, D-24, D-25, D-26
    - .planning/phases/02-medication-catalog/02-PATTERNS.md §"apps/api/prisma/seed.ts"
  </read_first>
  <action>
    A. Copy `local/lakemedel.csv` to `apps/api/prisma/seed-data/lakemedel.csv` (use a file copy — content must match byte-for-byte). Commit it. README in Phase 7 will credit Läkemedelsverket NPL as the source.

    B. Add dependency: `pnpm --filter @meditrack/api add csv-parse` (NOT csv-parse/sync — we stream).

    C. Modify apps/api/prisma/seed.ts:
       1. Preserve the existing CareUnit + Users upsert block unchanged.
       2. After the user upsert loop, add a `seedMedications()` function that:
          - Opens a fs read stream on `path.resolve(__dirname, 'seed-data/lakemedel.csv')`, pipes to csv-parse with `{ columns: true, delimiter: ';', bom: true, skip_empty_lines: true, trim: true }`.
          - Buffers parsed rows into 1000-row chunks.
          - For each chunk, calls `prisma.medication.createMany({ data: chunk.map(rowToMedication), skipDuplicates: true })`. `rowToMedication(r)` returns `{ nplId: r.nplid, name: r.namn, atcCode: r.atc_kod, form: r.form, strength: r.styrka || null, source: 'npl' }`. The unique on `nplId` makes `skipDuplicates: true` an idempotent no-op on re-run.
          - After all Medication rows are inserted, executes a second pass: `prisma.medication.findMany({ where: { source: 'npl' }, select: { id: true, nplId: true, form: true } })` → for each row, generate `{ currentStock, lowStockThreshold }` via `derive(nplId, form)` (see step 3), buffer into 1000-row chunks, call `prisma.careUnitMedication.createMany({ data: chunk.map(m => ({ careUnitId: CARE_UNIT_ID, medicationId: m.id, currentStock: ..., lowStockThreshold: ... })), skipDuplicates: true })`. The `@@unique([careUnitId, medicationId])` makes this idempotent.
       3. Add a deterministic PRNG: implement `fnv1a(input: string): number` and `prngFromSeed(seed: number): () => number` (e.g., mulberry32). Then `function derive(nplId: string, form: string): { currentStock: number; lowStockThreshold: number }`:
          - seed = fnv1a(nplId)
          - rng = prngFromSeed(seed)
          - threshold = `defaultLowStockThreshold(form)` (imported from @meditrack/shared)  // baseline
          - With p ≈ 0.08, force `currentStock = Math.floor(rng() * Math.max(1, threshold))` (so stock < threshold for ~8% of rows). Otherwise `currentStock = threshold + Math.floor(rng() * 180)` (so stock > threshold for the remaining ~92%, range up to threshold+180 ≤ 200).
          - Document the PRNG choice and the 8% bias in a JSDoc above the function citing D-25.
       4. Add console.log lines: `[seed] Medications inserted (createMany skipDuplicates): {N}` and `[seed] CareUnitMedications inserted for ${CARE_UNIT_ID}: {M} (~{P}% below threshold)`.

    D. Update apps/api/package.json `prisma.seed` script if needed so `pnpm --filter @meditrack/api db:seed` runs the extended seed.

    Local timing target: re-seed completes within ~30 s on a modern laptop on first run; second run is a no-op (per D-24 + Phase 1 invariant).
  </action>
  <verify>
    <automated>pnpm --filter @meditrack/api db:seed</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `apps/api/prisma/seed-data/lakemedel.csv` with size matching `local/lakemedel.csv` (byte-for-byte equal — use `cmp` or `Get-FileHash` to confirm).
    - Source: `apps/api/prisma/seed.ts` imports `csv-parse` and `defaultLowStockThreshold` from `@meditrack/shared`.
    - Source: `apps/api/prisma/seed.ts` contains the literal string `createMany` and the literal `skipDuplicates: true`.
    - Source: `apps/api/prisma/seed.ts` defines a function whose body cites D-25 and contains a `fnv1a` or equivalent named PRNG helper.
    - Behavior: After running `pnpm --filter @meditrack/api db:seed`, `psql -c "SELECT COUNT(*) FROM \"Medication\""` returns 43538.
    - Behavior: `psql -c "SELECT COUNT(*) FROM \"CareUnitMedication\" WHERE \"careUnitId\" = 'careunit-karolinska-01'"` returns 43538.
    - Behavior: `psql -c "SELECT COUNT(*) FROM \"CareUnitMedication\" WHERE \"currentStock\" < \"lowStockThreshold\""` returns a number between roughly 3000 and 4400 (≈ 8% of 43538).
    - Behavior: Running `pnpm --filter @meditrack/api db:seed` a SECOND time exits 0 and `SELECT COUNT(*) FROM "Medication"` is still 43538 (idempotency).
    - Behavior: First-run wall-clock time (after migration is in place) is ≤ 60 s; target ≤ 30 s on a modern laptop.
  </acceptance_criteria>
  <done>43 538 NPL Medications + matching CareUnitMedications seeded, ~8% below threshold, deterministic per nplId, idempotent on re-run.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Add NotFoundError, ConflictDuplicateMedicationError, ForbiddenScopeError to errorHandler with envelope dispatch</name>
  <read_first>
    - apps/api/src/plugins/errorHandler.ts (full file — preserve dispatch order: ZodError, InvalidCredentialsError, UnauthenticatedError, internal_error fallback)
    - .planning/phases/02-medication-catalog/02-PATTERNS.md §"apps/api/src/plugins/errorHandler.ts"
    - .planning/phases/02-medication-catalog/02-CONTEXT.md D-19 (Phase 1 inheritance — error envelope codes Phase 2 uses)
  </read_first>
  <action>
    Modify apps/api/src/plugins/errorHandler.ts only:

    A. After `UnauthenticatedError` class declaration, add three new error classes (cite D-19 in the JSDoc):

    `NotFoundError` — readonly code = 'not_found' as const; constructor(message = 'Resursen hittades inte.') sets name='NotFoundError'.

    `ConflictDuplicateMedicationError` — readonly code = 'conflict_duplicate_medication' as const; constructor() sets message='Läkemedlet finns redan i registret för din vårdenhet.' and name='ConflictDuplicateMedicationError'.

    `ForbiddenScopeError` — readonly code = 'forbidden' as const; constructor(message = 'Du saknar behörighet att utföra denna åtgärd.') sets name='ForbiddenScopeError'.

    B. Inside the `setErrorHandler` callback, BEFORE the unknown-error fallback (`req.log.error({ err }, 'internal_error')`), insert three new branches in this order:
       - `if (err instanceof NotFoundError) return send(reply, 404, envelope('not_found', err.message));`
       - `if (err instanceof ConflictDuplicateMedicationError) return send(reply, 409, envelope('conflict_duplicate_medication', err.message));`
       - `if (err instanceof ForbiddenScopeError) return send(reply, 403, envelope('forbidden', err.message));`

    Do NOT alter the existing branches. Do NOT export new error codes anywhere — the contracts/error.ts already accepts arbitrary `code: string` per D-19.
  </action>
  <verify>
    <automated>pnpm --filter @meditrack/api typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Source: `apps/api/src/plugins/errorHandler.ts` contains `class NotFoundError`, `class ConflictDuplicateMedicationError`, `class ForbiddenScopeError`.
    - Source: each class declares `readonly code = ... as const` and a Swedish message string in the constructor.
    - Source: the file contains three `if (err instanceof ...)` branches dispatching to 404, 409, and 403 respectively.
    - Behavior: `pnpm --filter @meditrack/api typecheck` exits 0.
    - Behavior: a unit-test or quick `app.inject({ method: 'GET', url: '/throwsNotFound' })` style probe (if added) returns body `{ error: { code: 'not_found', message: 'Resursen hittades inte.' } }` with status 404.
  </acceptance_criteria>
  <done>Three new error classes export-routable from `apps/api/src/plugins/errorHandler.ts`, mapped to canonical envelope codes per D-19. Service layer in Task 5 throws these.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Build medication.service.ts with listMedicationsForUnit + searchGlobalMedications + createCareUnitMedication (careUnitId-first, scope-safe)</name>
  <read_first>
    - apps/api/src/services/user.service.ts (full file — Pattern D / D-16 careUnitId-first signature, scope re-check)
    - apps/api/src/db/client.ts (prisma export style)
    - apps/api/src/plugins/errorHandler.ts (NotFoundError, ConflictDuplicateMedicationError after Task 4)
    - .planning/phases/02-medication-catalog/02-PATTERNS.md §"apps/api/src/services/medication.service.ts"
    - .planning/phases/02-medication-catalog/02-CONTEXT.md D-16, D-30, D-31, D-44, D-45
  </read_first>
  <action>
    Create apps/api/src/services/medication.service.ts:

    A. Imports: `import { prisma } from '../db/client.js'`; `import { ConflictDuplicateMedicationError } from '../plugins/errorHandler.js'`; `import type { MedicationListResponse, MedicationListQuery, MedicationListItem, MedicationSearchResult, MedicationCreateRequest } from '@meditrack/shared'`; `import { defaultLowStockThreshold, OVRIGA_FILTER_VALUE, TOP_MEDICATION_FORMS } from '@meditrack/shared'`.

    B. Top-of-file JSDoc citing D-16 (careUnitId-first), D-30 (transparent restore on duplicate-add), D-44 (list query shape), D-45 (search exclusion).

    C. Export `async function listMedicationsForUnit(careUnitId: string, filters: MedicationListQuery): Promise<MedicationListResponse>` that:
       1. Builds a `where` object: `{ careUnitId, deletedAt: null }`. If filters.q is non-empty, adds `medication: { name: { contains: filters.q, mode: 'insensitive' } }` (Prisma maps to ILIKE, GIN-trgm-friendly). If filters.atc is non-empty, adds `medication: { atcCode: { startsWith: filters.atc, mode: 'insensitive' } }` — merge with q if both present using the spread pattern. If filters.form === OVRIGA_FILTER_VALUE, adds `medication: { form: { notIn: [...TOP_MEDICATION_FORMS] } }`. Else if filters.form non-empty, adds `medication: { form: filters.form }`. If filters.belowThreshold === true, also push raw `currentStock: { lt: prisma.careUnitMedication.fields.lowStockThreshold }` — NOTE: Prisma cannot compare two columns directly. Use Prisma raw via `$queryRaw` for the belowThreshold filter, OR (recommended) call findMany without the belowThreshold restriction and filter in JS for the **count only**, then for the rows query include all rows. CORRECT APPROACH: when belowThreshold is true, run a parameterized `$queryRaw<Array<{ id: string }>>` over CareUnitMedication joined to Medication with the `currentStock < lowStockThreshold` predicate + tenancy filter + other filters, then load the full join rows via `prisma.careUnitMedication.findMany({ where: { id: { in: ids } }, include: { medication: true } })`. Pagination still flows through findMany.
       2. Performs three queries in `Promise.all`: (a) `prisma.careUnitMedication.findMany({ where, include: { medication: true }, orderBy: { medication: { name: 'asc' } }, skip: (page-1)*pageSize, take: pageSize })`, (b) `prisma.careUnitMedication.count({ where })`, (c) `belowThresholdTotal` via `$queryRaw<Array<{ count: bigint }>>` running `SELECT COUNT(*) FROM "CareUnitMedication" cum JOIN "Medication" m ON cum."medicationId" = m.id WHERE cum."careUnitId" = $1 AND cum."deletedAt" IS NULL AND cum."currentStock" < cum."lowStockThreshold"` — parameterize `careUnitId`. Apply the same q/atc/form filters as the main where (add WHERE clauses + bind params). If filters.belowThreshold is true, query (a) and (b) also become $queryRaw or are pre-filtered by id list per above.
       3. Maps each row to `MedicationListItem` shape.
       4. Returns `{ rows, total, belowThresholdTotal: Number(belowThresholdTotal[0].count), page, pageSize }`.

    D. Export `async function searchGlobalMedications(careUnitId: string, filters: { q: string; limit: number }): Promise<MedicationSearchResult[]>` that:
       1. Looks up Medications matching `name ILIKE %q%` OR `atcCode ILIKE q%`, EXCLUDING any Medication.id already actively stocked at this careUnit: `where: { OR: [ { name: { contains: filters.q, mode: 'insensitive' } }, { atcCode: { startsWith: filters.q, mode: 'insensitive' } } ], careUnitMedications: { none: { careUnitId, deletedAt: null } } }`.
       2. Returns `prisma.medication.findMany({ where, take: filters.limit, orderBy: [{ name: 'asc' }], select: { id: true, name: true, atcCode: true, form: true, strength: true, source: true } })`.

    E. Export `async function createCareUnitMedication(careUnitId: string, payload: MedicationCreateRequest): Promise<MedicationListItem>` that:
       1. Wraps the body in `prisma.$transaction(async (tx) => { ... })`.
       2. Branch `payload.kind === 'npl'`:
          - Look up `tx.careUnitMedication.findUnique({ where: { careUnitId_medicationId: { careUnitId, medicationId: payload.medicationId } } })`.
          - If row exists AND `deletedAt === null` → throw `new ConflictDuplicateMedicationError()`.
          - If row exists AND `deletedAt !== null` → UPDATE: `deletedAt = null, currentStock = payload.currentStock, lowStockThreshold = payload.lowStockThreshold`. Return the row joined with Medication.
          - Else INSERT: `tx.careUnitMedication.create({ data: { careUnitId, medicationId: payload.medicationId, currentStock, lowStockThreshold }, include: { medication: true } })`. Return mapped row.
       3. Branch `payload.kind === 'user'`:
          - Insert `Medication { source: 'user', name, atcCode, form, strength?, nplId: null }`.
          - Insert `CareUnitMedication { careUnitId, medicationId: newMed.id, currentStock, lowStockThreshold }`.
          - Return mapped row.
       4. Each function performs a final scope assertion: rows returned MUST have `careUnitId === careUnitId` — if not, throw an Error (defensive; ensures no cross-tenant leak even on a Prisma bug).

    F. Helper `function toListItem(row: { medication: Medication } & CareUnitMedication): MedicationListItem` — used internally and exported for reuse by routes.
  </action>
  <verify>
    <automated>pnpm --filter @meditrack/api typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Source: `apps/api/src/services/medication.service.ts` exports `listMedicationsForUnit`, `searchGlobalMedications`, `createCareUnitMedication`.
    - Source: each exported function has `careUnitId: string` as its FIRST parameter.
    - Source: the file contains the literal substring `deletedAt: null` (tenancy + soft-delete filter on read path).
    - Source: `searchGlobalMedications` filters using `careUnitMedications: { none: { careUnitId, deletedAt: null } }`.
    - Source: `createCareUnitMedication` uses `prisma.$transaction` and throws `new ConflictDuplicateMedicationError()` on the active-duplicate branch.
    - Source: `createCareUnitMedication` checks the soft-deleted-row path BEFORE inserting (transparent restore per D-30).
    - Behavior: `pnpm --filter @meditrack/api typecheck` exits 0.
    - Behavior: an inline `app.inject({ method:'GET', url:'/api/medications?q=paracet' })` returns rows whose `name` matches `/paracet/i` and `total ≥ 1` (validate against seeded data).
  </acceptance_criteria>
  <done>Service layer ready; route handlers in Task 6 only need to translate req.body/query → service call → service result. Every Prisma access lives here.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 6: Wire 3 BE routes (list, search, create) + barrel + app.ts registration</name>
  <read_first>
    - apps/api/src/routes/me.ts (GET + requireSession + response schema pattern)
    - apps/api/src/routes/adminPing.ts (GET + requireSession + requirePermission chain — preHandler order matters)
    - apps/api/src/routes/auth.ts (POST + body schema pattern, e.g. login)
    - apps/api/src/app.ts (route registration block — lines 50-54)
    - .planning/phases/02-medication-catalog/02-PATTERNS.md §"apps/api/src/routes/me.ts → Phase 2 medication route files" and §"index.ts"
  </read_first>
  <action>
    Create the following files:

    A. apps/api/src/routes/medications/list.ts (NEW):
       - Import `requireSession`, `requirePermission`, `medicationListQuery`, `medicationListResponse` (from @meditrack/shared), and `listMedicationsForUnit` from the service.
       - Export `async function listMedicationsRoute(app: FastifyInstance)`. Inside: `const r = app.withTypeProvider<ZodTypeProvider>(); r.get('/api/medications', { preHandler: [requireSession, requirePermission('medication:read')], schema: { querystring: medicationListQuery, response: { 200: medicationListResponse } } }, async (req) => listMedicationsForUnit(req.user!.careUnitId, req.query));`
       - Header JSDoc citing D-15 preHandler ordering and D-44 list shape.

    B. apps/api/src/routes/medications/search.ts (NEW):
       - Same shape with GET '/api/medications/search', preHandler `[requireSession, requirePermission('medication:read')]`, `schema: { querystring: medicationSearchQuery, response: { 200: medicationSearchResponse } }`, handler calls `searchGlobalMedications(req.user!.careUnitId, { q: req.query.q, limit: req.query.limit })` and returns `{ results: rows }`.

    C. apps/api/src/routes/medications/create.ts (NEW):
       - POST '/api/medications', preHandler `[requireSession, requirePermission('medication:create')]`, `schema: { body: medicationCreateRequest, response: { 201: medicationListItem } }`, handler awaits `createCareUnitMedication(req.user!.careUnitId, req.body)` then `reply.status(201)` and returns the row.

    D. apps/api/src/routes/medications/index.ts (NEW) — barrel:
       - `export async function medicationRoutes(app: FastifyInstance) { await app.register(listMedicationsRoute); await app.register(searchMedicationsRoute); await app.register(createMedicationRoute); }`
       (Update/delete routes ship in Plans 03 and 04 — barrel will be extended.)

    E. Modify apps/api/src/app.ts:
       - Add `import { medicationRoutes } from './routes/medications/index.js';`
       - Add `await app.register(medicationRoutes);` after `adminPingRoutes` registration, before `healthzRoutes`.

    Defense-in-depth note (cite in JSDoc on each route file): `requireSession` first, `requirePermission` second — never reorder. `req.user!.careUnitId` is the first arg to every service call. Per D-16 the service re-validates scope.
  </action>
  <verify>
    <automated>pnpm --filter @meditrack/api test || pnpm --filter @meditrack/api typecheck</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `apps/api/src/routes/medications/list.ts`, `search.ts`, `create.ts`, `index.ts`.
    - Source: each route file contains `preHandler: [requireSession, requirePermission('medication:...')]` with matching permission key.
    - Source: each handler calls a service function with `req.user!.careUnitId` as the FIRST argument.
    - Source: `apps/api/src/app.ts` contains `await app.register(medicationRoutes);` exactly once.
    - Behavior: with a seeded apotekare session cookie, `curl -b cookies.txt 'http://localhost:3000/api/medications?pageSize=5'` returns 200 and `rows.length === 5`, `total === 43538`.
    - Behavior: with no session cookie, the same request returns 401 with envelope `{ error: { code: 'unauthenticated', ... } }`.
    - Behavior: a request signed by a sjukskoterska session to `POST /api/medications` with valid body returns 403 with envelope `{ error: { code: 'forbidden', ... } }` (requirePermission gate).
    - Behavior: `POST /api/medications` with body `{ kind:'npl', medicationId:'<id of an already-stocked med for this care unit>', currentStock:5, lowStockThreshold:10 }` returns 409 with envelope `{ error: { code: 'conflict_duplicate_medication', ... } }`.
    - Behavior: posting the same body for a medication NOT already stocked returns 201 with the joined row shape.
  </acceptance_criteria>
  <done>BE end-to-end: a session cookie → GET /api/medications returns paginated seeded data; POST /api/medications creates rows; search filters out actively-stocked drugs. All routes pass under the canonical envelope.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 7: Add LowStockBadge + NplBadge + medication query/mutation hooks + sonner Toaster wiring</name>
  <read_first>
    - apps/web/src/components/RoleBadge.tsx (parallel pattern — DO NOT extend)
    - apps/web/src/auth/useAuth.ts (useQuery shape)
    - apps/web/src/features/auth/useLogin.ts (useMutation + queryClient pattern)
    - apps/web/src/lib/api.ts (fetchJson + ApiError)
    - apps/web/src/App.tsx (root layout — Toaster mount point)
    - .planning/phases/02-medication-catalog/02-PATTERNS.md §"LowStockBadge", §"NplBadge", §"useMedicationsQuery", §"useMedicationMutations"
    - .planning/phases/02-medication-catalog/02-UI-SPEC.md §4 LowStockBadge, §"Toast Feedback"
  </read_first>
  <action>
    A. Install shadcn primitives needed across all Plan 01 tasks. Run from repo root (or `apps/web`):
       `pnpm dlx shadcn@latest add sheet table badge tooltip alert-dialog command sonner`
       (Per UI-SPEC §Registry Safety — official registry only.)

    B. apps/web/src/components/LowStockBadge.tsx (NEW) — implements D-38 (pill + lucide-react AlertTriangle icon; shadcn destructive Badge; renders identically on table rows ≥md and cards <md). Parallel to RoleBadge.tsx structurally. No props. JSDoc citing UI-SPEC §4 + STK-04. Renders `<span class="inline-flex items-center gap-1 rounded-full px-2 py-0.5 text-xs font-semibold bg-destructive text-destructive-foreground"><AlertTriangle className="h-3.5 w-3.5" aria-hidden="true" />Lågt lager</span>`.

    C. apps/web/src/components/NplBadge.tsx (NEW) — props `{ children?: React.ReactNode }` defaulting to `'Från NPL'`. Renders slate-toned badge per UI-SPEC §"Från NPL Badge Color Contract": `inline-flex items-center rounded-md px-2 py-0.5 text-xs font-semibold bg-slate-100 text-slate-600`.

    D. apps/web/src/features/medications/useMedicationsQuery.ts (NEW):
       - Export `useMedicationsQuery(filters: MedicationListQuery)` returning `useQuery<MedicationListResponse, ApiError>({ queryKey: ['medications', filters], queryFn: () => fetchJson<MedicationListResponse>(`/api/medications?${new URLSearchParams(Object.entries(filters).filter(([,v]) => v !== undefined && v !== '').map(([k,v]) => [k, String(v)])).toString()}`), placeholderData: keepPreviousData })`. Import `keepPreviousData` from `@tanstack/react-query`.
       - Export `useMedicationSearchQuery(q: string, enabled: boolean)` returning `useQuery<MedicationSearchResponse, ApiError>({ queryKey: ['medication-search', q], queryFn: () => fetchJson<MedicationSearchResponse>(`/api/medications/search?q=${encodeURIComponent(q)}&limit=20`), enabled, retry: false })`.

    E. apps/web/src/features/medications/useMedicationMutations.ts (NEW):
       - Export `useCreateMedication()` returning `useMutation<MedicationListItem, ApiError, MedicationCreateRequest>({ mutationFn: (body) => fetchJson<MedicationListItem>('/api/medications', { method: 'POST', body: JSON.stringify(body) }), onSuccess: () => { queryClient.invalidateQueries({ queryKey: ['medications'] }); queryClient.invalidateQueries({ queryKey: ['medication-search'] }); toast.success('Sparat'); }, onError: (err) => { if (err.envelope.error.code !== 'conflict_duplicate_medication') toast.error('Kunde inte spara — försök igen.'); } })`. Imports `toast` from `sonner` and `useQueryClient` from `@tanstack/react-query`.
       - Stub-export `useUpdateMedication` and `useDeleteMedication` as TODO placeholders (will be implemented in Plans 03 and 04). Mark with comment `// Implemented in Plan 03 (update) / Plan 04 (delete)`. Do NOT export empty functions that will break compilation — instead, just leave them as a `// TODO` block, no exports.

    F. Modify apps/web/src/App.tsx (or wherever the root QueryClientProvider lives) to mount `<Toaster richColors position="top-right" />` from `sonner` per UI-SPEC §"Toast Feedback". Import: `import { Toaster } from 'sonner';`. Place INSIDE the QueryClientProvider but OUTSIDE the RouterProvider tree.
  </action>
  <verify>
    <automated>pnpm --filter @meditrack/web build</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `apps/web/src/components/LowStockBadge.tsx`, `NplBadge.tsx`.
    - File exists: `apps/web/src/features/medications/useMedicationsQuery.ts`, `useMedicationMutations.ts`.
    - Source: `LowStockBadge.tsx` contains the literal `'Lågt lager'` and the class `'bg-destructive text-destructive-foreground'`.
    - Source: `NplBadge.tsx` contains the default children literal `'Från NPL'`.
    - Source: `useMedicationsQuery.ts` uses query key `['medications', filters]` and `['medication-search', q]`.
    - Source: `useMedicationMutations.ts` imports `toast` from `sonner` and calls `queryClient.invalidateQueries({ queryKey: ['medications'] })` on success.
    - Source: `apps/web/src/App.tsx` (or root layout) contains `<Toaster ... />` from `sonner`.
    - Behavior: `pnpm --filter @meditrack/web build` exits 0.
    - Behavior: shadcn primitives `sheet.tsx`, `table.tsx`, `badge.tsx`, `tooltip.tsx`, `alert-dialog.tsx`, `command.tsx`, `sonner.tsx` exist under `apps/web/src/components/ui/`.
  </acceptance_criteria>
  <done>Badges + query hooks + mutation hooks + toaster mounted. /lakemedel page in Task 8 can compose all of these without further plumbing.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 8: Build LakemedelPage + MedicationTable + MedicationCardList + MedicationCard + LowStockBanner + PaginationFooter + AddMedicationButton + MedicationSheet (create-only) + URL-synced filters</name>
  <read_first>
    - apps/web/src/routes/lakemedel/LakemedelPage.tsx (current Phase 1 stub)
    - apps/web/src/components/EmptyStateCard.tsx (reuse for zero-rows-in-DB empty state)
    - apps/web/src/features/auth/LoginForm.tsx (rhf + zodResolver pattern for MedicationSheet form)
    - apps/web/src/features/auth/useLogin.ts (useMutation + onError pattern for Sheet save)
    - apps/web/src/auth/Can.tsx + useCan.ts (RBAC gating)
    - apps/web/src/routes/shell/AppShell.tsx (responsive `hidden md:block` pattern reference)
    - apps/web/src/components/ui/sheet.tsx (after Task 7 install)
    - apps/web/src/components/ui/table.tsx (after Task 7 install)
    - apps/web/src/components/ui/tooltip.tsx (after Task 7 install)
    - apps/web/src/components/ui/command.tsx (after Task 7 install — typeahead popover)
    - .planning/phases/02-medication-catalog/02-PATTERNS.md §LakemedelPage and §MedicationSheet
    - .planning/phases/02-medication-catalog/02-UI-SPEC.md §1, §2, §3, §6, §6a, §9, §10, §11
  </read_first>
  <action>
    Build the catalog page and its component tree per UI-SPEC. Slice 1's MedicationSheet supports `mode="create"` only — edit/view modes ship in Plan 03; delete confirm ships in Plan 04. Components MUST compile and render now, even if edit/delete paths are stubs.

    A. apps/web/src/routes/lakemedel/LakemedelPage.tsx — REPLACE the stub. Owns:
       - URL-synced filter state via `useSearchParams`: `q`, `atc`, `form`, `belowThreshold`, `page` (default 1), `pageSize` (default 25). Wrap with helpers `filtersFromParams(searchParams)` and `applyFilterPatch(patch)`. URL is the source of truth.
       - One `useMedicationsQuery(filters)` invocation.
       - Sheet state: `useState<{ mode: 'create' } | null>(null)` for Slice 1; null means closed. URL does NOT change when Sheet is open (per D-34).
       - Composition (top-down): heading row → `<LowStockBanner>` (if belowThresholdTotal > 0 and not dismissed) → `<LakemedelFilter>` stub (the search input + below-threshold chip ship in Slice 1 minimally; ATC + form selects ship in Plan 02 — wire only the search input here as a basic `<Input placeholder="Sök på namn…" />` with debounced 200 ms update to `q` URL param) → conditional `<MedicationTable className="hidden md:block">` and `<MedicationCardList className="block md:hidden">` (when rows.length > 0) → `<EmptyStateCard icon={Pill} heading="Inga läkemedel ännu" body="Lägg till från NPL-registret eller skapa nytt." cta={<Can action="medication:create"><Button onClick={() => setSheet({mode:'create'})}>Lägg till läkemedel</Button></Can>} />` (when total === 0) → `<PaginationFooter>` → footer `<p class="text-xs text-muted-foreground text-center py-2">Data: Läkemedelsverket NPL</p>`.
       - Add `<AddMedicationButton onCreate={() => setSheet({mode:'create'})} />` in the heading row (desktop) + mobile FAB.
       - Render `<MedicationSheet mode={sheet?.mode ?? 'create'} open={sheet !== null} onOpenChange={(o) => setSheet(o ? sheet : null)} />`.
       - Page title via `document.title = 'Läkemedel — MediTrack'` (or React Helmet if already in stack — Phase 1 has none; just set via effect).
       - Loading state: shadcn `<Skeleton>` rows per UI-SPEC §1 (8 table rows / 4 cards of given heights).

    B. apps/web/src/routes/lakemedel/MedicationTable.tsx (NEW) — shadcn `<Table>` with the exact 7 columns from UI-SPEC §2 (Namn, ATC-kod, Form, Styrka, Lager, Tröskel, Åtgärd). Header `text-xs font-semibold text-muted-foreground uppercase tracking-wide bg-muted/50`. Each `<TableRow>` is `cursor-pointer hover:bg-muted/50 tabIndex={0} aria-label={`Öppna ${name}`}`. Lager cell: when `currentStock < lowStockThreshold`, render the `<AlertTriangle className="h-3.5 w-3.5 text-destructive mr-1" />` wrapped in a `<Tooltip>` with content `Lågt lager`, followed by `<LowStockBadge />`, then the number; else just the number. Tröskel cell renders just the number for Slice 1 (inline-edit ships in Plan 03 — leave a `// TODO Plan 03: <InlineEditThreshold>` comment). Åtgärd cell is visually empty (entire row clickable). Click handler: `onRowClick(item)` prop — wired from LakemedelPage to set sheet state to edit mode (placeholder for now: open the create Sheet or `// TODO Plan 03: edit-mode wiring`).

    C. apps/web/src/routes/lakemedel/MedicationCardList.tsx (NEW) — `<div className="grid gap-3 md:hidden">` containing one `<MedicationCard>` per row. Pass `onCardClick` upward.

    D. apps/web/src/routes/lakemedel/MedicationCard.tsx (NEW) — per UI-SPEC §3 layout. Top row: `{name}` (text-sm font-semibold) + optional `<LowStockBadge />` top-right. Body lines: `ATC: {atcCode} · Form: {form}`, `Lager: {n} · Tröskel: {n}`, optional `Styrka: {strength}`. `cursor-pointer hover:bg-muted/30 bg-card border border-border rounded-lg p-4 shadow-sm`. `role="button" tabIndex={0} aria-label={`Öppna ${name}`}`.

    E. apps/web/src/routes/lakemedel/LowStockBanner.tsx (NEW) — exactly per UI-SPEC §9: `<div role="alert" class="flex items-center justify-between rounded-lg border border-destructive/30 bg-destructive/10 px-4 py-2 mb-4 text-sm text-destructive">`. Renders only when `belowThresholdTotal > 0 && !dismissed`. Dismiss via `sessionStorage.setItem('lakemedel-banner-dismissed', 'true')` and lift state via `useState` + initial check.

    F. apps/web/src/routes/lakemedel/PaginationFooter.tsx (NEW) — per UI-SPEC §11. Buttons disabled at edges. Calls `onPageChange(newPage)`.

    G. apps/web/src/routes/lakemedel/AddMedicationButton.tsx (NEW) — implements D-35 (add trigger: desktop button top-right + mobile FAB above tab bar, both gated by <Can action="medication:create">). Wraps a desktop `<Can action="medication:create"><Button>Lägg till läkemedel</Button></Can>` + a mobile FAB `<Can action="medication:create"><button className="fixed right-4 bottom-[calc(56px+env(safe-area-inset-bottom)+16px)] z-50 h-14 w-14 rounded-full bg-primary text-primary-foreground shadow-lg md:hidden flex items-center justify-center" aria-label="Lägg till läkemedel" onClick={onCreate}><Plus className="h-6 w-6" /></button></Can>`.

    H. apps/web/src/routes/lakemedel/MedicationSheet.tsx (NEW — Slice 1 ships create mode only):
       - Props: `{ mode: 'create' | 'edit' | 'view'; open: boolean; onOpenChange: (open: boolean) => void; careUnitMedication?: MedicationListItem }` (edit/view ship in Plan 03; create is fully functional now).
       - Uses shadcn `<Sheet>`. `side="right"` on `md+`, `side="bottom"` on mobile (use a `useMediaQuery('(min-width: 768px)')` hook or `window.matchMedia`).
       - Create-mode body: typeahead `<Input placeholder="Sök läkemedel från NPL…">` with 150ms debounce wired to `useMedicationSearchQuery(debouncedQ, debouncedQ.length > 0)`. Below it, a `<Command>` popover or simple `<ul role="listbox">` of results showing `{name} — {atcCode} — {form} — {strength}`. Loading row: `Söker…`. Empty result row: `Inget läkemedel matchade.` + `<Button variant="link">Skapa nytt läkemedel</Button>`.
       - On result select: prefill read-only `<NplBadge />` + read-only `ATC-kod`, `Form`, `Styrka` labels. Editable `Lager` (number, required, min 0) + `Tröskel` (number, required, min 1, auto-populated via `defaultLowStockThreshold(result.form)`). Show `<NplBadge>Från NPL · namn / form / styrka är låsta</NplBadge>` below the title.
       - "Skapa nytt läkemedel" path expands a fuller form with `Namn`, `ATC-kod`, `Form` (select using `TOP_MEDICATION_FORMS`), `Styrka` (optional), `Lager`, `Tröskel` (auto-populated via heuristic when Form is picked). Validate via `zodResolver(medicationCreateRequest)` with the `kind: 'user'` discriminator.
       - Footer: `Avbryt` (ghost variant) + `Spara` (default variant). `Spara` button shows `<Loader2 animate-spin />` and label `Sparar…` while `useCreateMedication().isPending`.
       - On submit: call `useCreateMedication().mutateAsync(payload)`. On success: `onOpenChange(false)`; the hook already shows toast `Sparat` + invalidates `['medications']`. On `ApiError` with code `'conflict_duplicate_medication'`: inline error below typeahead `Läkemedlet är redan inlagt på din vårdenhet.` (no toast — the hook suppresses it for this code).
       - Auto-focus first input on open (typeahead for create mode).
       - `Esc` closes (Radix default). `Cmd/Ctrl+Enter` submits.
       - Edit and view modes: render a placeholder for Slice 1: `<div className="p-4 text-sm text-muted-foreground">Redigeringsläge implementeras i nästa skiva.</div>`. Footer becomes `Stäng` only.

    I. Update apps/web/src/routes/lakemedel/index.ts (NEW or modify if exists) so `LakemedelPage` is the default export and route registration is unchanged from Phase 1.
  </action>
  <verify>
    <automated>pnpm --filter @meditrack/web build && pnpm --filter @meditrack/web test --run --no-coverage</automated>
  </verify>
  <acceptance_criteria>
    - File exists: all 8 NEW files listed in Task 8 file paths.
    - Source: `LakemedelPage.tsx` imports `useMedicationsQuery` and reads filters from `useSearchParams`.
    - Source: `MedicationTable.tsx` renders 7 column headers in the order: Namn, ATC-kod, Form, Styrka, Lager, Tröskel, Åtgärd.
    - Source: `MedicationTable.tsx` conditional renders `<LowStockBadge />` only when `currentStock < lowStockThreshold`.
    - Source: `LowStockBanner.tsx` contains `role="alert"` and the literal Swedish text `'läkemedel under tröskel'`.
    - Source: `AddMedicationButton.tsx` uses `<Can action="medication:create">` AND mobile FAB has `aria-label="Lägg till läkemedel"`.
    - Source: `MedicationSheet.tsx` calls `useCreateMedication()` and `useMedicationSearchQuery` and validates with `zodResolver(medicationCreateRequest)`.
    - Behavior: `pnpm --filter @meditrack/web build` exits 0.
    - Behavior: Manually (or via Playwright smoke), logging in as apotekare@example.test/demo1234 and visiting `/lakemedel` shows: page heading `Läkemedel`, the count banner with `~3500` (≈8% of 43538), the desktop `Lägg till läkemedel` button, the table with 25 rows, low-stock badges on ~2 of those 25 rows.
    - Behavior: Logging in as sjukskoterska@example.test, the `Lägg till läkemedel` button + FAB are absent (Can gate).
    - Behavior: Clicking `Lägg till läkemedel` opens the Sheet; typing `paracet` shows typeahead results within 150 ms; selecting one prefills read-only fields and an editable Lager/Tröskel; clicking Spara closes the Sheet and the new row appears in the list (after invalidation).
    - Behavior: Attempting to add a medication already stocked surfaces an inline error in the Sheet (no toast).
  </acceptance_criteria>
  <done>End-to-end Slice 1 demoable: a fresh `docker compose up` + login = a fully populated catalog with low-stock indicators and a working add flow. CAT-01, CAT-05, STK-03, STK-04 all light up.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → API | Untrusted request bodies + query strings cross here; signed session cookie is the only auth proof. |
| API handler → Service | preHandler-decorated `req.user.careUnitId` flows in; service layer is the LAST chance to assert scope. |
| Service → Postgres | All queries pass through Prisma typed client; pg_trgm queries use Prisma's `contains/insensitive` (no string interpolation). |
| Seed → Filesystem CSV | Committed CSV in `apps/api/prisma/seed-data/lakemedel.csv` is trusted (publicly redistributable NPL data, repo-controlled). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | `GET /api/medications` / service `listMedicationsForUnit` | mitigate | `careUnitId` is the FIRST arg (D-16); every Prisma `where` includes `careUnitId: req.user.careUnitId` AND `deletedAt: null`. Acceptance test asserts a sjukskoterska from another vårdenhet sees no rows. |
| T-02-02 | Information Disclosure | `GET /api/medications/search` | mitigate | `careUnitMedications: { none: { careUnitId, deletedAt: null } }` filter EXCLUDES drugs already actively stocked at the caller's unit — global Medication catalog is read-only and not vårdenhet-sensitive (D-27, D-45). |
| T-02-03 | Elevation of Privilege | `POST /api/medications` | mitigate | `requirePermission('medication:create')` preHandler enforces apotekare/admin (D-43). Defense-in-depth: FE `<Can action="medication:create">` hides the trigger but the BE is the boundary (D-17). |
| T-02-04 | Tampering | `POST /api/medications` request body | mitigate | Zod `medicationCreateRequest` is a `z.discriminatedUnion('kind', [...])` — server rejects unknown discriminants with 400 validation_failed. `kind: 'npl'` ignores any name/atc/form sent (only uses medicationId); `kind: 'user'` requires the full set. |
| T-02-05 | Denial of Service | `GET /api/medications?pageSize=*` | mitigate | `pageSize: z.coerce.number().int().min(1).max(100).default(25)` caps at 100 (D-44). pg_trgm + GIN index makes worst-case `q=` searches index-backed. |
| T-02-06 | Tampering | `POST /api/medications` duplicate-add race | mitigate | Two concurrent POSTs for `(careUnitId, medicationId)`: the `@@unique([careUnitId, medicationId])` constraint is the canonical guard. Service catches Prisma's P2002 violation and re-throws as `ConflictDuplicateMedicationError` → 409 envelope (D-19). |
| T-02-07 | Tampering | Seed data CSV | accept | NPL CSV is publicly redistributable, committed to the repo, reviewed at commit time. Risk is negligible (no executable code, no PII). |
| T-02-08 | Spoofing | Session cookie | mitigate | Inherited from Phase 1 D-01: signed httpOnly+Secure+SameSite=Lax cookie with server-side session row lookup. No changes in Phase 2. |
| T-02-SC | Tampering | npm install `csv-parse` | mitigate | Package legitimacy: csv-parse is a top-tier npm package by `@adaltas` author (Sébastien Loisel), 100M+ weekly downloads, used by `node-csv` ecosystem. Verified at https://www.npmjs.com/package/csv-parse. Not [SLOP]; legitimacy gate auto-passes by widespread adoption. |

`security_enforcement` is enabled — block-on `high` severity. T-02-01 (cross-tenant leak) and T-02-03 (RBAC bypass) are high; their mitigations are non-negotiable acceptance criteria on Tasks 5 and 6.
</threat_model>

<verification>
**Phase-level Slice 1 verification (manual + automated):**

1. Migration: `pnpm --filter @meditrack/api exec prisma migrate status` returns no pending migrations; `psql -c "\\dx pg_trgm"` returns one row.
2. Seed: `pnpm --filter @meditrack/api db:seed` (first run) creates 43 538 Medication rows + 43 538 CareUnitMedication rows for `careunit-karolinska-01` in ≤ 60 s; second run is a no-op.
3. API contract: with an apotekare session, `GET /api/medications?pageSize=5` returns 200 + 5 rows + `total: 43538` + `belowThresholdTotal: ~3500`. `GET /api/medications/search?q=paracet&limit=20` returns 200 + up to 20 results, none of which are already in the apotekare's vårdenhet stock.
4. RBAC: sjukskoterska gets 403 on `POST /api/medications`. No-session gets 401 on all medication endpoints. Wrong-vårdenhet user sees zero rows for the seeded unit's data.
5. Frontend: `/lakemedel` after login renders the page heading, count banner with the ~3500 number, the table at desktop / card list at mobile, low-stock badges visible on ~7-8 % of visible rows. Add button gated by role.
6. Add flow (apotekare): typeahead shows results within 150 ms; selecting a result + entering Lager + Spara closes the Sheet and the new row appears in the list. Attempting to add a duplicate surfaces an inline error.
7. Sjuksköterska view: visiting `/lakemedel` works; the Add button and FAB are absent.
</verification>

<success_criteria>
- All 8 tasks completed and committed (one logical commit per task per D-21 — extras allowed for the Task 2 sub-bundles).
- ROADMAP §Phase 2 success criteria #1 (catalog list with low-stock indicator) and #5 (mobile cards <md / table ≥md) demonstrably pass on the seeded apotekare account.
- CAT-01, CAT-05, STK-03, STK-04 acceptance text in REQUIREMENTS.md is observably satisfied.
- `pnpm --filter @meditrack/api test`, `pnpm --filter @meditrack/web build`, and `pnpm --filter @meditrack/api typecheck` all exit 0.
- The seeded CareUnitMedication count + below-threshold count match the deterministic PRNG (43 538 total, ~3500 below threshold).
</success_criteria>

<output>
Create `.planning/phases/02-medication-catalog/02-01-SUMMARY.md` when done. Summary should include:
- Files created (count by directory) + files modified
- Final Medication / CareUnitMedication row counts after seed
- Any deviations from D-20..D-45 (should be zero — flag the orchestrator if any arose)
- Decisions for Plans 02/03/04 to inherit (e.g., final list of TOP_MEDICATION_FORMS from CSV analysis, PRNG function name)
- Note on remaining Slice 1 placeholders: edit/view Sheet mode (Plan 03), inline-edit threshold (Plan 03), ATC/form filter selects (Plan 02), delete confirm (Plan 04)
</output>
