---
phase: 02-medication-catalog
plan: 04
type: execute
wave: 4
depends_on: [02-01, 02-03]
files_modified:
  - apps/api/src/routes/medications/delete.ts
  - apps/api/src/routes/medications/index.ts
  - apps/api/src/services/medication.service.ts
  - apps/web/src/features/medications/useMedicationMutations.ts
  - apps/web/src/routes/lakemedel/DeleteMedicationDialog.tsx
  - apps/web/src/routes/lakemedel/MedicationSheet.tsx
autonomous: true
requirements: [CAT-07]

must_haves:
  truths:
    - "Per D-20 Slice 4 + CAT-07: an apotekare/admin clicks `Ta bort` in the edit Sheet footer → AlertDialog confirms `Ta bort {name} från {careUnit.name}?` → on confirm, soft-deletes (UPDATE CareUnitMedication SET deletedAt = now()) → dialog + Sheet close → toast `Borttaget från {careUnit.name}` → row vanishes from the list"
    - "Per D-33: delete is ALWAYS soft-delete on CareUnitMedication only (SET deletedAt = now()) — the global Medication row is NEVER deleted by app code. CAT-07 ('soft-delete if it has historical refs') is satisfied by being strictly stricter: always soft-delete"
    - "Per D-30 + restore continuity: re-adding the same Medication via the typeahead in a future session TRANSPARENTLY RESTORES the existing CareUnitMedication row (UPDATE deletedAt=NULL, currentStock=new, lowStockThreshold=new) — already implemented in Plan 01 Task 5's `createCareUnitMedication`; verified end-to-end now that delete actually creates soft-deleted rows"
    - "Per D-37 + UI-SPEC §7: dialog title is `Ta bort {medicationName} från {careUnitName}?`, body is `Läkemedlet finns kvar i NPL-registret och kan läggas till igen.`, Cancel is default-focused, Confirm is destructive variant"
    - "Per security: a sjukskoterska clicking the Sheet's `Ta bort` button never sees it (it's gated by `<Can action='medication:delete'>` AND the edit Sheet itself opens in `mode='view'` for sjukskoterska in Plan 03 — `Ta bort` is rendered only in `mode='edit'`). Defense-in-depth: BE requirePermission returns 403."
    - "Per D-16 + D-19: DELETE /api/medications/:id returns 404 for cross-tenant IDs (existence-probing protection); 404 for already-soft-deleted rows (idempotency); 204 on first successful soft-delete"
    - "Per success criterion #3 ('apotekare or admin can create, edit, and soft-delete'): Slice 4 completes the trio — Slice 1 add, Slice 3 edit, Slice 4 delete. The full CRUD-with-RBAC story is demoable."
  artifacts:
    - path: "apps/api/src/routes/medications/delete.ts"
      provides: "DELETE /api/medications/:careUnitMedicationId with requireSession + requirePermission('medication:delete')"
    - path: "apps/api/src/services/medication.service.ts"
      provides: "softDeleteCareUnitMedication(careUnitId, careUnitMedicationId) — scope-validated, idempotent NotFoundError"
      contains: "softDeleteCareUnitMedication"
    - path: "apps/web/src/features/medications/useMedicationMutations.ts"
      provides: "useDeleteMedication() — DELETE call + invalidate + toast `Borttaget från {careUnit.name}`"
      contains: "useDeleteMedication"
    - path: "apps/web/src/routes/lakemedel/DeleteMedicationDialog.tsx"
      provides: "shadcn AlertDialog confirm with Swedish copy + isDeleting Loader2 state"
    - path: "apps/web/src/routes/lakemedel/MedicationSheet.tsx"
      provides: "Ta bort button in edit-mode footer wired to open DeleteMedicationDialog"
  key_links:
    - from: "apps/web/src/routes/lakemedel/MedicationSheet.tsx"
      to: "apps/web/src/routes/lakemedel/DeleteMedicationDialog.tsx"
      via: "isDeleteDialogOpen state + onConfirm → useDeleteMedication"
      pattern: "<DeleteMedicationDialog"
    - from: "apps/web/src/routes/lakemedel/DeleteMedicationDialog.tsx"
      to: "/api/medications/:id"
      via: "useDeleteMedication -> fetchJson DELETE"
      pattern: "useDeleteMedication"
    - from: "apps/api/src/services/medication.service.ts"
      to: "prisma.careUnitMedication.update"
      via: "SET deletedAt = now() (never DELETE)"
      pattern: "deletedAt: new Date\\(\\)"
---

<objective>
Ship Slice 4 of Phase 2 per D-20 — the delete surface (soft-delete via `deletedAt` + AlertDialog confirm). Wave 4; depends on Slice 1 for the service skeleton + route barrel + errorHandler classes + Sheet primitive + shadcn alert-dialog install, AND on Slice 3 having fully restructured MedicationSheet (edit mode owns the Ta-bort button slot) before Slice 4 wires DeleteMedicationDialog into it.

Purpose: Closes the CRUD trio (Slice 1 add, Slice 3 edit, Slice 4 delete) so the apotekare/admin can manage the catalog end-to-end. Locks the soft-delete-only semantics that future phases inherit (Phase 4 hard-delete-on-no-references is explicitly OUT of scope per D-33). Validates the transparent restore on re-add (already implemented in Slice 1) now that real soft-deleted rows exist.

Output: DELETE /api/medications/:id BE route + service function + envelope dispatch (NotFoundError reuse from Slice 1), `useDeleteMedication` hook, `DeleteMedicationDialog` component, wiring from the edit Sheet's `Ta bort` button.
</objective>

<execution_context>
@C:/Projekt/MediTrack/.claude/get-shit-done/workflows/execute-plan.md
@C:/Projekt/MediTrack/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-medication-catalog/02-CONTEXT.md
@.planning/phases/02-medication-catalog/02-PATTERNS.md
@.planning/phases/02-medication-catalog/02-UI-SPEC.md
@.planning/phases/02-medication-catalog/02-01-PLAN.md
@.planning/phases/02-medication-catalog/02-03-PLAN.md

<interfaces>
<!-- From Plan 01 -->

From apps/api/src/services/medication.service.ts (Plan 01):
- listMedicationsForUnit, searchGlobalMedications, createCareUnitMedication, toListItem helper
- Read queries already filter `deletedAt: null`, so soft-deleted rows are invisible to list/search without further changes.
- This plan APPENDS softDeleteCareUnitMedication.

From apps/api/src/routes/medications/index.ts (after Plan 01 + Plan 03):
- medicationRoutes registers list, search, create, update
- This plan adds delete to the barrel.

From apps/api/src/plugins/errorHandler.ts (Plan 01):
- NotFoundError → 404 dispatch already in place.

From apps/web/src/features/medications/useMedicationMutations.ts (Plan 01 + Plan 03):
- useCreateMedication, useUpdateMedication, useUpdateThresholdOptimistic exported.
- This plan ADDS useDeleteMedication.

From apps/web/src/routes/lakemedel/MedicationSheet.tsx (Plan 03):
- Edit-mode footer has a `Ta bort` button with a stub onClick `// TODO Plan 04`. This plan wires it.

From apps/web/src/components/ui/alert-dialog.tsx:
- shadcn AlertDialog primitive (installed in Plan 01 Task 7).

From apps/web/src/auth/useAuth.ts:
- useAuth().user.careUnit.name → used in the dialog title + toast.

</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend service with softDeleteCareUnitMedication + add DELETE route + wire barrel</name>
  <read_first>
    - apps/api/src/services/medication.service.ts (Plans 01 + 03 — extend, do NOT rewrite the file)
    - apps/api/src/routes/medications/update.ts (Plan 03 — closest analog: PATCH on `:careUnitMedicationId` with NotFoundError on cross-tenant)
    - apps/api/src/routes/medications/index.ts (barrel — append register)
    - apps/api/src/plugins/errorHandler.ts (Plan 01 — NotFoundError class)
    - .planning/phases/02-medication-catalog/02-PATTERNS.md §"softDeleteCareUnitMedication"
    - .planning/phases/02-medication-catalog/02-CONTEXT.md D-33 (always soft-delete), D-30 (restore on re-add)
  </read_first>
  <action>
    A. apps/api/src/services/medication.service.ts — APPEND `softDeleteCareUnitMedication(careUnitId: string, careUnitMedicationId: string): Promise<{ id: string; deletedAt: Date }>`. Verify `NotFoundError` is already imported (Plan 03 added it). The function follows D-33 (always soft-delete; never `prisma.careUnitMedication.delete`) and the same scope-and-existence posture as `updateCareUnitMedication`:

       1. **Scoped reload.** `prisma.careUnitMedication.findUnique({ where: { id: careUnitMedicationId }, select: { id: true, careUnitId: true, deletedAt: true } })`. Selecting just the three fields needed keeps the query cheap — no medication join required for delete.

       2. **Existence + scope + idempotency check.** If the row is absent, OR `row.careUnitId !== careUnitId`, OR `row.deletedAt !== null`, throw `new NotFoundError('Läkemedlet hittades inte.')`. All three conditions share the same envelope per D-19 — cross-tenant access cannot be distinguished from "doesn't exist" or "already soft-deleted," which makes a second DELETE on the same id return 404 (idempotent from the caller's perspective; transparent restore on re-add is handled by `createCareUnitMedication` in Plan 01 per D-30 and is OUT of scope here).

       3. **Soft-delete write.** `prisma.careUnitMedication.update({ where: { id: careUnitMedicationId }, data: { deletedAt: new Date() } })`. Use `update`, NEVER `delete` — D-33 is non-negotiable and the verification grep checks for `prisma.careUnitMedication.delete(` absence.

       4. **Return.** Return `{ id: careUnitMedicationId, deletedAt: <the timestamp just written> }`. The route handler discards the body and replies with 204; the return shape is documentary for any future caller (e.g., a Phase 5 audit middleware that wants to log the timestamp).

       Cite D-33 (always soft-delete; global Medication row never deleted by app code) and D-30 (transparent restore handled by Slice 1's `createCareUnitMedication`) in the JSDoc preamble. Reference PATTERNS.md §"softDeleteCareUnitMedication" as the source-of-shape.

    B. apps/api/src/routes/medications/delete.ts (NEW). Mirror update.ts structure from Plan 03. Imports: `FastifyInstance` (type), `ZodTypeProvider` (type), `z` from `'zod'`, `requireSession` + `requirePermission` from the auth modules, `softDeleteCareUnitMedication` from the service. Export `async function deleteMedicationRoute(app: FastifyInstance)`. Inside, bind `app.withTypeProvider<ZodTypeProvider>()` to `r`, then declare the route as `r.delete('/api/medications/:careUnitMedicationId', { preHandler: [requireSession, requirePermission('medication:delete')], schema: { params: z.object({ careUnitMedicationId: z.string().min(1) }), response: { 204: z.null() } } }, handler)`. The handler awaits `softDeleteCareUnitMedication(req.user!.careUnitId, req.params.careUnitMedicationId)`, calls `reply.status(204)`, and `return null` so Fastify serializes a true empty body. preHandler order is fixed: requireSession first, requirePermission second. The `NotFoundError` thrown by the service surfaces as a 404 with the canonical error envelope via `errorHandler.ts` dispatch (D-19) — no per-route try/catch needed. Header JSDoc citing CAT-07, D-33, D-15 (preHandler order), and D-19 (envelope).

    C. apps/api/src/routes/medications/index.ts — append `await app.register(deleteMedicationRoute);` after the update registration. Final barrel order: list → search → create → update → delete.
  </action>
  <verify>
    <automated>pnpm --filter @meditrack/api typecheck && pnpm --filter @meditrack/api test --run --no-coverage</automated>
  </verify>
  <acceptance_criteria>
    - Source: `apps/api/src/services/medication.service.ts` exports `softDeleteCareUnitMedication` with signature `(careUnitId: string, careUnitMedicationId: string): Promise<void>`.
    - Source: the function contains the literal `deletedAt: new Date()` AND `throw new NotFoundError(`.
    - Source: the function performs `prisma.careUnitMedication.update`, NOT `delete`.
    - File exists: `apps/api/src/routes/medications/delete.ts`.
    - Source: delete.ts uses `preHandler: [requireSession, requirePermission('medication:delete')]` AND `reply.status(204)`.
    - Source: `apps/api/src/routes/medications/index.ts` registers `deleteMedicationRoute`.
    - Behavior: with an apotekare session, `curl -X DELETE -b cookies.txt http://localhost:3000/api/medications/<a-real-id-from-the-seeded-careUnit>` returns 204 with empty body.
    - Behavior: `psql -c "SELECT \"deletedAt\" FROM \"CareUnitMedication\" WHERE id = '<that-id>'"` returns a non-null timestamp.
    - Behavior: a SECOND DELETE on the same id returns 404 `{ error: { code: 'not_found', ... } }` (idempotency — already-soft-deleted rows are invisible to delete).
    - Behavior: DELETE against a foreign-vårdenhet id returns 404 (identical envelope, no existence leak).
    - Behavior: DELETE from a sjukskoterska session returns 403.
    - Behavior: After soft-delete, `GET /api/medications` does NOT return the row (read path filters `deletedAt: null` per Plan 01).
    - Behavior: After soft-delete, the same Medication appears in `GET /api/medications/search?q=...` (because it's no longer "actively stocked" per the `none` filter in `searchGlobalMedications`). Adding it via POST then transparently restores the original row (D-30) — verify the same `careUnitMedicationId` is reused.
  </acceptance_criteria>
  <done>BE soft-delete path is live, scope-safe, idempotent. Transparent restore-on-re-add (D-30) verified end-to-end.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add useDeleteMedication hook + build DeleteMedicationDialog + wire into MedicationSheet's Ta bort button</name>
  <read_first>
    - apps/web/src/features/medications/useMedicationMutations.ts (Plans 01 + 03 — extend)
    - apps/web/src/features/auth/useLogout.ts (DELETE call + queryClient.removeQueries pattern)
    - apps/web/src/components/ui/alert-dialog.tsx (shadcn primitive)
    - apps/web/src/routes/lakemedel/MedicationSheet.tsx (Plan 03 — has stub `Ta bort` button onClick)
    - apps/web/src/auth/useAuth.ts (useAuth().user.careUnit.name)
    - .planning/phases/02-medication-catalog/02-UI-SPEC.md §7 (DeleteMedicationDialog full spec) + §"Delete Flow" interaction matrix
    - .planning/phases/02-medication-catalog/02-CONTEXT.md D-37
  </read_first>
  <action>
    A. Extend `apps/web/src/features/medications/useMedicationMutations.ts` with `useDeleteMedication()`. Type the mutation as `useMutation<void, ApiError, { careUnitMedicationId: string; medicationName: string; careUnitName: string; onClose?: () => void }>`. The variables type carries `medicationName` + `careUnitName` for the success-toast template literal and an optional `onClose` callback so the caller (MedicationSheet) can dismiss the open Sheet from the hook's `onSuccess` rather than racing the dialog close. The BE only needs the `careUnitMedicationId`; the other variables are caller-context.

       `mutationFn`: `({ careUnitMedicationId }) => apiClient.delete(\`/api/medications/${careUnitMedicationId}\`)` (or the equivalent `fetchJson<void>(...path..., { method: 'DELETE' })` depending on the apiClient surface in `lib/api.ts`). The BE returns 204; the helper should treat 204 as a successful empty resolve, not an error.

       `onSuccess(_data, { careUnitName, onClose })`: invalidate BOTH query keys — `queryClient.invalidateQueries({ queryKey: ['medications'] })` so the list re-fetches without the deleted row, and `queryClient.invalidateQueries({ queryKey: ['medication-search'] })` so the typeahead surfaces the now-deletable Medication again (re-add path). Then `toast.success(\`Borttaget från ${careUnitName}\`)`. Finally, call `onClose?.()` to let the caller close the Sheet — keeps the close cascade single-source-of-truth.

       `onError`: `toast.error('Kunde inte ta bort — försök igen.')`. The caller keeps the dialog open on error so the user can retry or Avbryt. The canonical error envelope (D-19) is parsed by `apiClient` and surfaced as `ApiError`; no per-code special-casing is needed in this hook.

    B. Create `apps/web/src/routes/lakemedel/DeleteMedicationDialog.tsx` as a shadcn `<AlertDialog>` wrapper with prop shape `{ open: boolean; onOpenChange: (open: boolean) => void; medicationName: string; careUnitName: string; onConfirm: () => void; isDeleting: boolean }`. Import `AlertDialog`, `AlertDialogAction`, `AlertDialogCancel`, `AlertDialogContent`, `AlertDialogDescription`, `AlertDialogFooter`, `AlertDialogHeader`, `AlertDialogTitle` from the shadcn alert-dialog primitive, plus `Loader2` from `lucide-react` and `cn` from the local utils. Reference D-37 (delete control left-aligned in Sheet footer + AlertDialog confirm) and UI-SPEC §7 in the file's header JSDoc.

       Structure: `<AlertDialog open={open} onOpenChange={onOpenChange}>` wrapping `<AlertDialogContent>` with `<AlertDialogHeader>` containing:
       - `<AlertDialogTitle>` rendering the template literal `\`Ta bort ${medicationName} från ${careUnitName}?\`` (Swedish — locked copy per UI-SPEC §7 + D-37).
       - `<AlertDialogDescription>` rendering the locked literal: `Läkemedlet finns kvar i NPL-registret och kan läggas till igen.`

       Followed by `<AlertDialogFooter>` containing, in this order (so Cancel is shadcn-default-focused — UI-SPEC §7 safer-default-focus requirement):
       - `<AlertDialogCancel disabled={isDeleting}>Avbryt</AlertDialogCancel>` — autofocused by shadcn default; do NOT override.
       - `<AlertDialogAction>` with `onClick={(e) => { e.preventDefault(); onConfirm(); }}`, `disabled={isDeleting}`, and `className={cn('bg-destructive text-destructive-foreground hover:bg-destructive/90', isDeleting && 'opacity-50')}`. Children: when `isDeleting`, render `<Loader2 className="mr-2 h-4 w-4 animate-spin" />Tar bort…`; otherwise render the Swedish literal `Ta bort`.

       Two non-obvious points worth surfacing in implementation comments: (1) `<AlertDialogAction>` from shadcn does NOT accept a `variant` prop directly — destructive styling must be applied via `className`, not `variant="destructive"`. (2) `e.preventDefault()` on the action's onClick prevents the dialog from auto-closing before the mutation runs; the caller closes the dialog manually after the mutation resolves.

    C. Modify `apps/web/src/routes/lakemedel/MedicationSheet.tsx` to wire the `Ta bort` button that Plan 03 left as a stub:

       1. Add imports: `import { DeleteMedicationDialog } from './DeleteMedicationDialog';`, `import { useDeleteMedication } from '../../features/medications/useMedicationMutations';`, `import { useAuth } from '../../auth/useAuth';`. Also ensure `useState` is imported from React.

       2. Inside the component (relevant only when `mode === 'edit'`), declare the dialog-open state with `const [isDeleteOpen, setIsDeleteOpen] = useState(false);`, the mutation hook with `const deleteMutation = useDeleteMedication();`, and pull the current user with `const { user } = useAuth();` so we have `user.careUnit.name` available for the dialog title + the toast.

       3. Replace the Plan 03 stub onClick on the `Ta bort` button with `onClick={() => setIsDeleteOpen(true)}`. Wrap the button in `<Can action="medication:delete">` per defense-in-depth (the button is already only rendered in `mode='edit'`, which sjukskoterska never enters per Plan 03 / D-36, but the explicit Can gate makes the security posture readable and survives any future regression in mode-selection).

       4. After the `<SheetContent>` closing tag, inside the same root fragment, conditionally render `<DeleteMedicationDialog>` only when `mode === 'edit' && careUnitMedication && user` are all truthy. Pass:
          - `open={isDeleteOpen}` and `onOpenChange={setIsDeleteOpen}`
          - `medicationName={careUnitMedication.name}` and `careUnitName={user.careUnit.name}` (template-literal interpolation in the dialog title)
          - `isDeleting={deleteMutation.isPending}` so the Confirm button switches to spinner + `Tar bort…`
          - `onConfirm={async () => { try { await deleteMutation.mutateAsync({ careUnitMedicationId: careUnitMedication.careUnitMedicationId, medicationName: careUnitMedication.name, careUnitName: user.careUnit.name, onClose: () => onOpenChange(false) }); setIsDeleteOpen(false); } catch { /* toast fired by hook; keep dialog open for retry */ } }`. The cascade is: hook's onSuccess fires the toast + invalidates + closes the Sheet via the `onClose` callback we pass; the local `setIsDeleteOpen(false)` after `mutateAsync` closes the AlertDialog. On error, both stay open and the user can Avbryt or retry.

    D. Verify the close cascade end-to-end: AlertDialog closes on confirm or cancel; Sheet closes on confirm (via the hook's `onClose` callback); list re-fetches via `['medications']` invalidation; the deleted row vanishes; the toast `Borttaget från {careUnitName}` appears. On error, both surfaces stay open and `Kunde inte ta bort — försök igen.` toasts.
  </action>
  <verify>
    <automated>pnpm --filter @meditrack/web build && pnpm --filter @meditrack/web test --run --no-coverage</automated>
  </verify>
  <acceptance_criteria>
    - Source: `apps/web/src/features/medications/useMedicationMutations.ts` exports `useDeleteMedication`.
    - Source: the hook's onSuccess builds the toast message via `\`Borttaget från ${careUnitName}\`` (template literal interpolation visible).
    - Source: the hook invalidates BOTH `['medications']` AND `['medication-search']` query keys.
    - File exists: `apps/web/src/routes/lakemedel/DeleteMedicationDialog.tsx`.
    - Source: the dialog title is built as `\`Ta bort ${medicationName} från ${careUnitName}?\`` (template literal).
    - Source: the dialog body contains the literal Swedish `'Läkemedlet finns kvar i NPL-registret och kan läggas till igen.'`.
    - Source: `AlertDialogCancel` is rendered BEFORE `AlertDialogAction` (per shadcn convention so Cancel is default-focused).
    - Source: `MedicationSheet.tsx` imports `DeleteMedicationDialog` and `useDeleteMedication` and renders the dialog ONLY when `mode === 'edit'`.
    - Source: the `Ta bort` button onClick is now `() => setIsDeleteOpen(true)` (no longer a TODO stub).
    - Behavior: `pnpm --filter @meditrack/web build` exits 0.
    - Behavior: As apotekare, opening the edit Sheet on any row → clicking `Ta bort` → AlertDialog appears with title `Ta bort {name} från Avdelning 4, Karolinska?` and body matching the locked copy. Cancel is initially focused (Tab key without arrow keys lands on Confirm).
    - Behavior: Clicking Confirm shows `Tar bort…` with spinner; on success the dialog AND Sheet close, the list re-fetches, the row vanishes, and a sonner toast `Borttaget från Avdelning 4, Karolinska` appears.
    - Behavior: Forcing an API error (e.g., delete an already-soft-deleted row by clicking twice quickly) shows toast `Kunde inte ta bort — försök igen.` and the dialog stays open.
    - Behavior: As sjukskoterska, the edit Sheet opens in view mode (per Plan 03) — the `Ta bort` button is NOT rendered.
    - Behavior: End-to-end restore loop: as apotekare, delete a medication → open Add Sheet → search for the same name → it appears in the typeahead (because the `none: { deletedAt: null }` filter excludes only ACTIVELY stocked rows) → select + Spara → the list shows the row again with the NEW currentStock/lowStockThreshold (D-30 transparent restore — same careUnitMedicationId reused).
  </acceptance_criteria>
  <done>End-to-end Slice 4: delete flow with AlertDialog confirm + isPending Loader2 + toast feedback + soft-delete via `deletedAt`. Transparent restore on re-add closes the loop. CAT-07 demonstrably passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → API (DELETE) | URL path carries the row id; preHandler chain validates session + permission; service validates scope. |
| AlertDialog confirm → useDeleteMedication | Confirm click triggers the mutation; no destructive action happens until confirm. Default focus on Cancel reduces accidental destructive actions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-17 | Tampering | DELETE /api/medications/:id with foreign-vårdenhet id | mitigate | `softDeleteCareUnitMedication` returns NotFoundError (404) — same envelope shape as truly-missing rows — so an attacker cannot probe existence across tenants. Same posture as PATCH (T-02-13). Acceptance test confirms identical 404 for foreign-id and non-existent-id. |
| T-02-18 | Elevation of Privilege | sjukskoterska hits DELETE directly bypassing the Sheet | mitigate | `requirePermission('medication:delete')` preHandler returns 403 before the service is invoked. The FE Sheet opens in view-mode for sjukskoterska (Plan 03) so the `Ta bort` button is never even rendered for non-permitted roles. Defense in depth (D-17). |
| T-02-19 | Repudiation | Soft-deleted rows can be silently re-added with different stock | accept | D-30 explicitly states this is the canonical flow. Phase 5 audit-log middleware will record the soft-delete + the subsequent restore (with before/after payloads), but Phase 2 ships nothing audit-related (CONTEXT.md `<deferred>` line 231). Risk accepted — Phase 5 closes the gap. |
| T-02-20 | Denial of Service | Repeated double-click on Confirm during pending delete | mitigate | `<AlertDialogAction>` is `disabled={isDeleting}`; the hook's `isPending` covers the in-flight window. Cancel is also disabled during delete to prevent dialog-close races. |
| T-02-21 | Data Loss | Soft-delete hides rows from list AND search read paths | mitigate | Soft-delete is recoverable via re-add (D-30). The global Medication row is NEVER deleted (D-33), so even if a future bug "hard-deletes" a CareUnitMedication by writing actual SQL, the Medication catalog is preserved. Phase 2 ships no hard-delete endpoint at all — minimizing the attack surface. |

T-02-17 and T-02-18 are high-severity tenancy/RBAC concerns; both mirror Slice 3's protections and have non-negotiable acceptance tests.

CONTEXT.md `<security_threat_model_required>` "Hard-delete endpoints do not exist in Phase 2. A future audit must surface any attempt to add one." — verified: this slice ships ZERO hard-delete code paths. `prisma.careUnitMedication.delete(...)` MUST NOT appear in `apps/api/src/services/medication.service.ts` or any route. Acceptance check via grep.
</threat_model>

<verification>
1. `pnpm --filter @meditrack/api typecheck`, `pnpm --filter @meditrack/api test --run --no-coverage`, `pnpm --filter @meditrack/web build` all exit 0.
2. DELETE happy path (apotekare): `DELETE /api/medications/<own-id>` → 204; the row's `deletedAt` is now a timestamp; second DELETE → 404; row no longer in `GET /api/medications`.
3. DELETE cross-tenant: foreign-unit id → 404 (identical envelope to truly-missing).
4. DELETE RBAC: sjukskoterska session → 403.
5. UI delete: as apotekare, click row → Sheet opens → click `Ta bort` (red, footer-left) → AlertDialog with locked Swedish copy → click Confirm → `Tar bort…` spinner → AlertDialog + Sheet close → row vanishes from list → toast `Borttaget från Avdelning 4, Karolinska` appears.
6. UI delete error: force-trigger an API error → toast `Kunde inte ta bort — försök igen.` → AlertDialog stays open.
7. Sjuksköterska: edit Sheet opens in view mode → `Ta bort` button absent.
8. Restore loop: delete a medication → Add Sheet → typeahead surfaces the same name → select + Spara with NEW stock + threshold → row reappears in list → confirm via `psql -c "SELECT id, currentStock, deletedAt FROM \"CareUnitMedication\" WHERE id = '<id>'"` that the SAME id was UPDATEd (deletedAt = null, new values).
9. Negative-existence grep: `rg "prisma\\.careUnitMedication\\.delete\\(" apps/api/src` returns zero results (no hard-delete code path).
</verification>

<success_criteria>
- ROADMAP §Phase 2 success criterion #3 fully passes: apotekare/admin can create, edit, AND soft-delete; sjukskoterska cannot. Confirmed end-to-end across Slices 1, 3, 4.
- CAT-07 acceptance text in REQUIREMENTS.md observably satisfied with the stricter "always soft-delete" interpretation (D-33).
- The full v1 medication catalog feature set (CAT-01..07, STK-03, STK-04) is observable in the running app and verified by manual smoke + API curls.
- Transparent restore on re-add (D-30) verified end-to-end now that soft-deleted rows actually exist.
- Zero hard-delete code paths in the BE service layer (grep gate).
</success_criteria>

<output>
Create `.planning/phases/02-medication-catalog/02-04-SUMMARY.md` when done. Summary should include:
- Files created/modified.
- The end-to-end restore loop verification (delete → re-add → same id reused — paste the psql output showing deletedAt flipping back to null).
- Grep confirmation that no `careUnitMedication.delete(` call exists in `apps/api/src/`.
- Final Phase 2 status: all 9 REQ-IDs (CAT-01..07, STK-03, STK-04) marked as observed/satisfied. Ready for `/gsd:verify-phase 02` or the phase checker.
</output>
